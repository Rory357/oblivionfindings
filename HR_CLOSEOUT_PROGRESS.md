# HR Close-out Loop Ledger

> Executes the close-out after the HR Audit & Fix loop (merged to main `c2f6bbef`, HR pest 723/0):
> deploy verify → Decisions D-1..D-11 per the approved DECISION DEFAULTS → housekeeping → END.
> One slice per run: map → implement → gates → ledger → commit → stop.
> Row status: ⬜ not started · 🔶 partial/blocked · ✅ done.

**Decision defaults (approved by launching the loop):** D-1=surface-only aggregation · D-2=block admin/external-persona assignment for non-admin actors · D-3=audit user writes via existing mechanism · D-4=archive-not-delete sweep · D-5=lock-after-submit + addendum notes · D-6=SanitizesCsvOutput on payroll exports · D-7=CLOSE no action · D-8=read-only profile surfacing · D-9=WIRE read-only · D-10=CLOSE no action · D-11=CLOSE no action.

## Slices

| # | Slice | Status | Evidence |
|---|-------|--------|----------|
| C0 | Deploy smoke-check (oblivionfindings.com) | ✅ | Deploy of `c2f6bbef` HEALTHY (checked 2026-07-10, Chrome MCP as Demo Admin — session was live, no login needed). `/hr`→`/hr/my` renders fully (correct date, balances, policy nudges); `/hr/approvals/pending` renders the honest-empty inbox (F-78 area, "PENDING 0"); `/hr/cases` renders (SLA snapshot + HR-DEMO-CASE-001); the "New HR case" 4-step wizard opens cleanly (dismissed via navigation — Escape unverifiable due to a transient tool outage, not a site issue). Zero console errors; ~300 network requests ALL 200. No hotfix needed. |
| C1 | (D-6) Payroll CSV injection guard | ✅ | D-6 premise CONFIRMED against source: both payroll CSV builders hand-assemble output in services and bypassed the app-wide `SanitizesCsvOutput` trait (which is mounted on the base Controller, so controllers inherit it but services don't) — `PayrollExportService::encodeCsvRow` (run export; employee names + free-text static profile values) and `PayrollJournalService::buildNetPayDirectCreditCsv` (net-pay bank batch; user-chosen names). Fixed by `use SanitizesCsvOutput` on both services + `sanitizeCsvCell` per cell (no method-name collisions — verified before adopting; the trait's `sanitizeCsvCell` name exists precisely to dodge the legacy `private csvCell` fatal). Numeric cells (negative amounts) deliberately untouched for bank-import fidelity. No payslip CSV exists (payslips are PDF). `PayrollCsvInjectionGuardTest` (2 tests, 8 assertions): `=cmd`/`@SUM` cells get apostrophe-prefixed in the run export; a `=HYPERLINK(...)`-named employee is neutralised in the direct-credit file while bank account + amount survive exactly. Out-of-scope note: raw `fputcsv` also lives in `ReportBuilderService`/`EmployeeImportExportService` (HR, non-payroll — audited surfaces; not D-6). **Incidental fix:** gate run exposed a pre-existing morning-only TZ flake in `ShiftPayrollBackboneIntegrationTest` (worker-local `work_date` vs UTC-built run window — fails 00:00–12:00 NZT); window now built in `app.worker_timezone`. See Run 2 log. |
| C2 | (D-2) Role-assignment guard on intake/rehire | ✅ | `EmployeeIntakeService::assertRoleAssignable` (new, called at the top of BOTH `intake()` and `rehire()` before any write): admin-grade roles (RBAC `level >= 100`, i.e. `admin` — data-driven off the roles table with a name fallback) assignable only when the ACTOR holds admin (legacy `role` string OR RBAC pivot via `hasRole`); external portal personas (`client`, `next_of_kin`) never assignable via intake, for anyone. Throws `InvalidArgumentException` — every caller surfaces it as a flash/validation error, never a 500: `EmployeeProfileController::store` + `OnboardingController::store` newly wrapped, `EmployeeProfileController::rehire` already caught it, `CandidateController::convertToEmployee` catch extended (`LogicException\|InvalidArgumentException`), respondOffer's accept branch already catches `Throwable` gracefully. `IntakeRoleGuardTest` (6 tests, 19 assertions): blocked admin-by-hr (service + HTTP flash, nothing written), allowed admin-by-admin, external personas blocked even for admin, normal support_worker intake regression-green, rehire onto admin-grade blocked for hr / allowed for admin. S12 `OfferAcceptOnboardingFlowTest` re-run green (recruitment door unaffected). |
| C3 | (D-3) User-write auditability | ⬜ | |
| C4 | (D-4) Archive-not-delete sweep | ⬜ | |
| C5 | (D-5) Exit-interview immutability | ⬜ | |
| C6 | (D-9) Case↔incident cross-link — wire read-only | ⬜ | |
| C7 | (D-8) Injury surfacing — read-only federation | ⬜ | |
| C8 | (D-1) Approvals inbox — surface real approvables | ⬜ | |
| C9 | (D-7/D-10/D-11) No-action closures | ⬜ | |
| C10 | Housekeeping + merge→main→push + END | ⬜ | |

**Work branch:** `claude/hr-closeout` (off main `c2f6bbef`). Commits stay here until C10's merge+push (C0 hotfix exception only). **Pest baseline: 731/0 (after C2).** Vitest baseline: 8 fail/171 pass (my-day×4, app-sidebar×1, behaviour-abc-tab×2, resident-tracking×1).

## Run log

### Run 1 — C0 deploy smoke-check (2026-07-10)
Bootstrapped the loop: work branch `claude/hr-closeout` off main `c2f6bbef`; this ledger created. Browser-verified yesterday's deploy on oblivionfindings.com as Demo Admin (live session): `/hr/my` ✅, `/hr/approvals/pending` honest-empty ✅ (F-78 area), `/hr/cases` ✅, New-HR-case wizard opens ✅, 0 console errors, ~300 requests all 200. Deploy HEALTHY — no hotfix. Gates: N/A (no code touched; ledger only). Baseline stays 723/0.

### Run 2 — C1 payroll CSV injection guard (2026-07-10, D-6) + incidental TZ-flake fix
Mapped first: the `SanitizesCsvOutput` trait is mounted on the base **Controller**, so every controller inherits `putCsv`/`streamSanitizedCsv`/`sanitizeCsvCell` — but both payroll CSV paths build their output inside **services**, where nothing inherited applies: `PayrollExportService::buildCsvFromRows`→`encodeCsvRow` (profile-driven run export — encloses + doubles the enclosure char but never neutralises a formula-leading cell) and `PayrollJournalService::buildNetPayDirectCreditCsv` (hand-concatenated bank batch — strips commas only). Employee names are user-chosen and static profile mapping values are free-text → real OWASP CSV-injection vectors on both. No payslip CSV exists (payslips are PDF). Fix: `use SanitizesCsvOutput;` on both services (collision-checked: neither declares `sanitizeCsvCell`/`putCsv`/`streamSanitizedCsv`), sanitise each cell in `encodeCsvRow`, and sanitise `$name`/`$bankAccount` in the direct-credit builder; purely numeric cells stay exact (negative amounts, bank imports). New `tests/Feature/Hr/PayrollCsvInjectionGuardTest.php` (2 tests, 8 assertions): isolated ✅.

**Incidental find (fixed same slice, per the fix-incidentals rule):** the first full-suite gate came back 724/1 — `ShiftPayrollBackboneIntegrationTest` "attendance generated shift timesheet flows into payroll run" failed with a null run item, and kept failing in isolation. Investigated rather than dismissed: my diff only touches CSV cell encoding (item generation untouched), and a stash-to-HEAD experiment + source read found the real cause — a **morning-only timezone flake in the test itself**. `DraftTimesheetService:86` stamps `work_date` as the WORKER-LOCAL calendar day (`app.worker_timezone` = Pacific/Auckland), while the test built its payroll window from raw UTC `now()`. Between 00:00 and ~12:00 NZT the NZ date is a day ahead of UTC, so the fresh timesheet's `work_date` fell past `periodEnd` and the item was never generated. Every prior green run happened NZ afternoon/evening; today's runs were NZ morning (~08:00–09:00 NZST). The sibling test in the same file sets `work_date` directly in UTC (self-consistent — never flaked), and `PayrollRunIntegrityTest`/`AuditFixPayrollCasesTest` likewise. Fix: build the run window from `now()->setTimezone(app.worker_timezone)` in the affected test (mirrors the product's own date derivation); repaired test passes at 09:06 NZST — inside the window where the old one reliably failed. Product code is unaffected (real payroll runs receive explicit picker dates).

**Gates:** php -l ✅ ×4 (2 services + 2 test files) · new test isolated ✅ 2/8 · repaired backbone test ✅ 2/38 at the failing hour · full HR pest ✅ **725 passed / 0 failed** = 723 + 2 new (repaired test back green), zero regressions. **New baseline: 725.** Frontend gates N/A (no .ts/.tsx).

### Run 3 — C2 role-assignment guard (2026-07-10, D-2)
Mapped first: roles carry an RBAC `level` (RbacSeeder: admin=100, execs 93–95, board 75–90, portal personas 15–20), `User::hasRole()` exists, and intake had FOUR doors — `EmployeeProfileController::store` (unwrapped), `OnboardingController::store` new-hire mode (unwrapped), `EmployeeProfileController::rehire` (already caught `InvalidArgumentException`), `RecruitmentService::convertToEmployee` (manual-convert caller caught `LogicException` only; auto-accept catches `Throwable`). The store request validated only `exists:roles,name` — any manager with `hr.employees.manage` could mint an `admin` login or an external-portal persona. Fix: `assertRoleAssignable(?string $roleName, int $actorId)` on `EmployeeIntakeService`, called first thing in `intake()` AND `rehire()` (on the role the re-hire would restore): external personas (`client`/`next_of_kin`) always blocked; admin-grade (level ≥ 100, name fallback) requires the actor to hold admin. Callers: store + onboarding wizard wrapped into flash/validation errors; manual-convert catch extended. New `IntakeRoleGuardTest` (6 tests, 19 assertions) — first run hit the F-76 pattern itself (helper omitted NOT-NULL `employment_type`; test-side fix). S12 `OfferAcceptOnboardingFlowTest` re-run green alongside. **Gates:** php -l ✅ ×5 · isolated ✅ 6/19 (+ S12 3/3) · full HR pest ✅ **731 passed / 0 failed** = 725 + 6 new, zero regressions. **New baseline: 731.** Frontend gates N/A (no .ts/.tsx).
