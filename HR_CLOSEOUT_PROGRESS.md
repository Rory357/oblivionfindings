# HR Close-out Loop Ledger

> Executes the close-out after the HR Audit & Fix loop (merged to main `c2f6bbef`, HR pest 723/0):
> deploy verify → Decisions D-1..D-11 per the approved DECISION DEFAULTS → housekeeping → END.
> One slice per run: map → implement → gates → ledger → commit → stop.
> Row status: ⬜ not started · 🔶 partial/blocked · ✅ done.

**Decision defaults (approved by launching the loop):** D-1=surface-only aggregation · D-2=block admin/external-persona assignment for non-admin actors · D-3=audit user writes via existing mechanism · D-4=archive-not-delete sweep · D-5=lock-after-submit + addendum notes · D-6=SanitizesCsvOutput on payroll exports · D-7=CLOSE no action · D-8=read-only profile surfacing · D-9=WIRE read-only · D-10=CLOSE no action · D-11=CLOSE no action.

## Slices

| # | Slice | Status | Evidence |
|---|-------|--------|----------|
| C0 | Deploy smoke-check (oblivionfindings.com) | ✅ | Deploy of `c2f6bbef` HEALTHY (checked 2026-07-10, Chrome MCP as Demo Admin — session was live, no login needed). `/hr`→`/hr/my` renders fully (correct date, balances, policy nudges); `/hr/approvals/pending` renders the honest-empty inbox (F-78 area, "PENDING 0"); `/hr/cases` renders (SLA snapshot + HR-DEMO-CASE-001); the "New HR case" 4-step wizard opens cleanly (dismissed via navigation — Escape unverifiable due to a transient tool outage, not a site issue). Zero console errors; ~300 network requests ALL 200. No hotfix needed. |
| C1 | (D-6) Payroll CSV injection guard | ✅ | D-6 premise CONFIRMED against source: both payroll CSV builders hand-assemble output in services and bypassed the app-wide `SanitizesCsvOutput` trait (which is mounted on the base Controller, so controllers inherit it but services don't) — `PayrollExportService::encodeCsvRow` (run export; employee names + free-text static profile values) and `PayrollJournalService::buildNetPayDirectCreditCsv` (net-pay bank batch; user-chosen names). Fixed by `use SanitizesCsvOutput` on both services + `sanitizeCsvCell` per cell (no method-name collisions — verified before adopting; the trait's `sanitizeCsvCell` name exists precisely to dodge the legacy `private csvCell` fatal). Numeric cells (negative amounts) deliberately untouched for bank-import fidelity. No payslip CSV exists (payslips are PDF). `PayrollCsvInjectionGuardTest` (2 tests, 8 assertions): `=cmd`/`@SUM` cells get apostrophe-prefixed in the run export; a `=HYPERLINK(...)`-named employee is neutralised in the direct-credit file while bank account + amount survive exactly. Out-of-scope note: raw `fputcsv` also lives in `ReportBuilderService`/`EmployeeImportExportService` (HR, non-payroll — audited surfaces; not D-6). **Incidental fix:** gate run exposed a pre-existing morning-only TZ flake in `ShiftPayrollBackboneIntegrationTest` (worker-local `work_date` vs UTC-built run window — fails 00:00–12:00 NZT); window now built in `app.worker_timezone`. See Run 2 log. |
| C2 | (D-2) Role-assignment guard on intake/rehire | ✅ | `EmployeeIntakeService::assertRoleAssignable` (new, called at the top of BOTH `intake()` and `rehire()` before any write): admin-grade roles (RBAC `level >= 100`, i.e. `admin` — data-driven off the roles table with a name fallback) assignable only when the ACTOR holds admin (legacy `role` string OR RBAC pivot via `hasRole`); external portal personas (`client`, `next_of_kin`) never assignable via intake, for anyone. Throws `InvalidArgumentException` — every caller surfaces it as a flash/validation error, never a 500: `EmployeeProfileController::store` + `OnboardingController::store` newly wrapped, `EmployeeProfileController::rehire` already caught it, `CandidateController::convertToEmployee` catch extended (`LogicException\|InvalidArgumentException`), respondOffer's accept branch already catches `Throwable` gracefully. `IntakeRoleGuardTest` (6 tests, 19 assertions): blocked admin-by-hr (service + HTTP flash, nothing written), allowed admin-by-admin, external personas blocked even for admin, normal support_worker intake regression-green, rehire onto admin-grade blocked for hr / allowed for admin. S12 `OfferAcceptOnboardingFlowTest` re-run green (recruitment door unaffected). |
| C3 | (D-3) User-write auditability | ✅ | Mapped every HR path that grants/revokes `User` login or role access: intake mint/link, full re-hire, completed offboarding, and lightweight profile reactivation. All four now emit explicit events through the existing canonical `AuditLogger` → `audit_logs` mechanism: `user.employee_intake`, `user.rehire_login_restored`, `user.login_revoked`, `user.login_reactivated`. Service commands retain their explicit actor outside HTTP: `AuditLogger` falls back to a positive integer `meta.actor_id` only when no request user exists, and offboarding threads `completedBy` through checklist completion to both the self-revocation guard and audit row. Re-hire resolves its role once so the RBAC grant and audit metadata cannot disagree. No migration and no dual-write. `UserWriteAuditTest`: deliberate red 2/4 (missing offboarding actor + missing reactivation event), then ✅ 4 tests / 37 assertions. Full HR Pest ✅ **735 passed / 4,463 assertions / 0 failed** = 731 + 4. Independent review: approved, no Critical/Important findings. Architecture boundary recorded in Run 4. |
| C4 | (D-4) Archive-not-delete sweep | ✅ | Swept every HR `->delete()` call and classified root records separately from permitted file, pivot, reaction, reminder, task/milestone, version and other child cleanup. Existing SoftDeletes already protect announcements, goals, development goals and staff background checks. Converted the five root workflows that already had an honest no-schema retention mechanism: onboarding checklist → `status=archived` (tasks retained); draft wellbeing survey → `status=archived` (questions retained); policy → `is_active=false` (versions, attestations and PDFs retained); succession plan → `is_active=false` (candidate assessments retained); every HR-document removal route → `folder=Archive` (row and file retained). Updated all corresponding action/confirmation copy from delete to archive. `ArchiveNotDeleteTest`: deliberate red **5 failed / 12 assertions** because each root became null, then green **5 passed / 29 assertions**. Direct changed-test regressions: `DocumentsHubTest` + `WellbeingCareWorkflowTest` ✅ **18/197**. PHP syntax ×9, TypeScript, focused ESLint and `git diff --check` green. Full HR Pest intentionally not run under crash-containment instruction; last proven module baseline remains 735/0. See Run 5. |
| C5 | (D-5) Exit-interview immutability | ✅ | The live schema has no draft/status field and both creation flows explicitly record a completed interview, so every persisted `HrExitInterview` is a submitted record. Added an explicit guarded PATCH path that refuses core-answer edits with a flash (never a 500), plus a separate manager-only addendum path that appends a dated/attributed note to the existing `additional_comments` text without replacing original answers or inventing schema. The detail UI now labels the record locked and offers only “Add addendum”. Existing `AuditableChanges` records the append. `ExitInterviewImmutabilityTest`: deliberate red **2 failed / 2 assertions** (405 edit, 404 addendum), then green **2 passed / 16 assertions**. Existing wizard regression ✅ **4/51**. PHP syntax ×3, route generation/list, TypeScript, focused ESLint and `git diff --check` green. Full HR Pest not run in this contained slice; last full baseline remains 735/0 and expected next full count is 742. See Run 6. |
| C6 | (D-9) Case↔incident cross-link — wire read-only | ✅ | Wired the existing `HrCase.linked_incident_ids` JSON reference end-to-end without a migration or H&S write: the New Case wizard receives a tenant-scoped recent-incident list and supports multi-select; create/update reject missing, duplicate and cross-organisation IDs; case detail resolves stored IDs into minimal read-only reference/client/title/severity/status/date summaries. H&S remains the sole writer. “Open in H&S” is emitted only for `incidents.viewAny` viewers, avoiding dead links for assigned-only access; HR case viewers still get the approved minimal federation. `CaseIncidentCrossReferenceSeamTest`: deliberate red **3 failed / 24 assertions** (picker/detail missing; foreign ID accepted), then green **3 passed / 38 assertions**, including incident count and `updated_at` unchanged. Existing case regressions ✅ **11/307**. PHP syntax ×2, TypeScript, focused ESLint (0 warnings/errors) and `git diff --check` green. Full HR Pest not run in this contained slice; last full baseline remains 735/0 and expected next full count is 744. See Run 7. |
| C7 | (D-8) Injury surfacing — read-only federation | ⬜ | |
| C8 | (D-1) Approvals inbox — surface real approvables | ⬜ | |
| C9 | (D-7/D-10/D-11) No-action closures | ⬜ | |
| C10 | Housekeeping + merge→main→push + END | ⬜ | |

**Work branch:** `claude/hr-closeout` (off main `c2f6bbef`). Commits stay here until C10's merge+push (C0 hotfix exception only). **Pest baseline: 735/0 (after C3).** Vitest baseline: 8 fail/171 pass (my-day×4, app-sidebar×1, behaviour-abc-tab×2, resident-tracking×1).

## Run log

### Run 1 — C0 deploy smoke-check (2026-07-10)
Bootstrapped the loop: work branch `claude/hr-closeout` off main `c2f6bbef`; this ledger created. Browser-verified yesterday's deploy on oblivionfindings.com as Demo Admin (live session): `/hr/my` ✅, `/hr/approvals/pending` honest-empty ✅ (F-78 area), `/hr/cases` ✅, New-HR-case wizard opens ✅, 0 console errors, ~300 requests all 200. Deploy HEALTHY — no hotfix. Gates: N/A (no code touched; ledger only). Baseline stays 723/0.

### Run 2 — C1 payroll CSV injection guard (2026-07-10, D-6) + incidental TZ-flake fix
Mapped first: the `SanitizesCsvOutput` trait is mounted on the base **Controller**, so every controller inherits `putCsv`/`streamSanitizedCsv`/`sanitizeCsvCell` — but both payroll CSV paths build their output inside **services**, where nothing inherited applies: `PayrollExportService::buildCsvFromRows`→`encodeCsvRow` (profile-driven run export — encloses + doubles the enclosure char but never neutralises a formula-leading cell) and `PayrollJournalService::buildNetPayDirectCreditCsv` (hand-concatenated bank batch — strips commas only). Employee names are user-chosen and static profile mapping values are free-text → real OWASP CSV-injection vectors on both. No payslip CSV exists (payslips are PDF). Fix: `use SanitizesCsvOutput;` on both services (collision-checked: neither declares `sanitizeCsvCell`/`putCsv`/`streamSanitizedCsv`), sanitise each cell in `encodeCsvRow`, and sanitise `$name`/`$bankAccount` in the direct-credit builder; purely numeric cells stay exact (negative amounts, bank imports). New `tests/Feature/Hr/PayrollCsvInjectionGuardTest.php` (2 tests, 8 assertions): isolated ✅.

**Incidental find (fixed same slice, per the fix-incidentals rule):** the first full-suite gate came back 724/1 — `ShiftPayrollBackboneIntegrationTest` "attendance generated shift timesheet flows into payroll run" failed with a null run item, and kept failing in isolation. Investigated rather than dismissed: my diff only touches CSV cell encoding (item generation untouched), and a stash-to-HEAD experiment + source read found the real cause — a **morning-only timezone flake in the test itself**. `DraftTimesheetService:86` stamps `work_date` as the WORKER-LOCAL calendar day (`app.worker_timezone` = Pacific/Auckland), while the test built its payroll window from raw UTC `now()`. Between 00:00 and ~12:00 NZT the NZ date is a day ahead of UTC, so the fresh timesheet's `work_date` fell past `periodEnd` and the item was never generated. Every prior green run happened NZ afternoon/evening; today's runs were NZ morning (~08:00–09:00 NZST). The sibling test in the same file sets `work_date` directly in UTC (self-consistent — never flaked), and `PayrollRunIntegrityTest`/`AuditFixPayrollCasesTest` likewise. Fix: build the run window from `now()->setTimezone(app.worker_timezone)` in the affected test (mirrors the product's own date derivation); repaired test passes at 09:06 NZST — inside the window where the old one reliably failed. Product code is unaffected (real payroll runs receive explicit picker dates).

**Gates:** php -l ✅ ×4 (2 services + 2 test files) · new test isolated ✅ 2/8 · repaired backbone test ✅ 2/38 at the failing hour · full HR pest ✅ **725 passed / 0 failed** = 723 + 2 new (repaired test back green), zero regressions. **New baseline: 725.** Frontend gates N/A (no .ts/.tsx).

### Run 3 — C2 role-assignment guard (2026-07-10, D-2)
Mapped first: roles carry an RBAC `level` (RbacSeeder: admin=100, execs 93–95, board 75–90, portal personas 15–20), `User::hasRole()` exists, and intake had FOUR doors — `EmployeeProfileController::store` (unwrapped), `OnboardingController::store` new-hire mode (unwrapped), `EmployeeProfileController::rehire` (already caught `InvalidArgumentException`), `RecruitmentService::convertToEmployee` (manual-convert caller caught `LogicException` only; auto-accept catches `Throwable`). The store request validated only `exists:roles,name` — any manager with `hr.employees.manage` could mint an `admin` login or an external-portal persona. Fix: `assertRoleAssignable(?string $roleName, int $actorId)` on `EmployeeIntakeService`, called first thing in `intake()` AND `rehire()` (on the role the re-hire would restore): external personas (`client`/`next_of_kin`) always blocked; admin-grade (level ≥ 100, name fallback) requires the actor to hold admin. Callers: store + onboarding wizard wrapped into flash/validation errors; manual-convert catch extended. New `IntakeRoleGuardTest` (6 tests, 19 assertions) — first run hit the F-76 pattern itself (helper omitted NOT-NULL `employment_type`; test-side fix). S12 `OfferAcceptOnboardingFlowTest` re-run green alongside. **Gates:** php -l ✅ ×5 · isolated ✅ 6/19 (+ S12 3/3) · full HR pest ✅ **731 passed / 0 failed** = 725 + 6 new, zero regressions. **New baseline: 731.** Frontend gates N/A (no .ts/.tsx).

### Run 4 — C3 user-write auditability (2026-07-11, D-3)
Mapped first: `User` deliberately does not use `AuditableChanges` (doing so would record login-token churn), so HR's four security-bearing `User` writes needed explicit events. `EmployeeIntakeService::intake()` mints/links, assigns role and approves login; `rehire()` restores approval and the RBAC pivot; `OnboardingService` withdraws approval when offboarding completes; `EmployeeProfileController::setActive()` restores approval on the lightweight Reactivate path. The first three were Claude's initial C3 stopping point, but review caught two gaps before acceptance: offboarding dropped its explicit `$completedBy` and used ambient `auth()` in the private revoke chain, while lightweight Reactivate wrote `approved_at` with no event. TDD tightened the real paths with no ambient auth for service calls. The deliberate red run was **2 failed / 2 passed**: offboarding's audit `user_id` was null and no reactivation row existed. Fix: thread the explicit actor through checklist completion/revocation (including the self-revocation guard); add the Reactivate event; let canonical `AuditLogger` use a positive integer `meta.actor_id` only when there is no request actor; and resolve the re-hire role once before guard/write/audit so metadata matches the actual grant. Final explicit events: `user.employee_intake`, `user.rehire_login_restored`, `user.login_revoked`, `user.login_reactivated`.

**Audit-store boundary (transparent, not silently papered over):** D-3 says to use the existing mechanism, so C3 writes the canonical app-wide `AuditLogger`/`audit_logs` store (also read by `/settings/audit-logs`). The older `/hr/settings/audit-log` surface is a separate pre-existing `HrAuditLog`/`hr_audit_log` implementation; its controller/service tenant inference also expects `User::tenant_id` while this app's user boundary is `organization_id`. C3 does **not** invent a migration or dual-write into that second store. Consolidating/removing the legacy HR viewer and adding any future organisation scope to the global store is a separate architecture remediation, not evidence against these durable audit rows.

**Gates:** focused red ✅ (2 precise failures) → focused green ✅ **4 tests / 37 assertions** · php -l ✅ ×5 · `git diff --check` ✅ · independent code review ✅ (no Critical/Important findings) · full HR Pest ✅ **735 passed / 4,463 assertions / 0 failed** in 1,208.81s = 731 + 4 new, zero regressions. Frontend gates N/A (no `.ts`/`.tsx`). **New baseline: 735.**

### Run 5 — C4 archive-not-delete sweep (2026-07-11, D-4)
Mapped first and kept the prompt's primary-record boundary: storage-file replacement/deletion, pivots, targets, attendees, reactions/replies, reminders, checklist tasks, PIP milestones, policy versions, succession candidates and other explicit child cleanup remain hard deletes. Root calls already protected by `SoftDeletes` (announcements, goals, development goals, staff background checks, candidate retention and time entries) remain unchanged. Settings/templates/reference artefacts and explicit feed moderation/privacy erasure are not primary HR history for D-4. No schema was invented.

Five root workflows were genuinely destructive despite already having a retention lifecycle. Fixed all of them: `OnboardingController::destroy` now calls the existing `setChecklistStatus(..., 'archived')`; the draft-survey destroy route calls `EngagementService::archiveDraftSurvey`, preserving questions and actor attribution; policy destroy deactivates the policy and keeps versions/attestations/files; succession destroy deactivates the plan and keeps candidate assessments; `HrDocumentController` single, bulk and employee-profile removal paths move records to the existing `Archive` folder and keep the private file. Six affected React surfaces now say Archive and describe retained history instead of promising permanent deletion. Operational calendar events have no status/archive/`deleted_at` mechanism and would require the schema invention this closeout explicitly forbids; they were not given a fake archive state.

**TDD evidence:** the new five-route test first failed **5/5 (12 assertions)** solely because each root record became null. After the fix it passed **5/5 (29 assertions)**, proving dependent tasks/questions/versions/candidates and document files survive. **Contained gates:** php -l ✅ ×9 · focused regression (`DocumentsHubTest` + `WellbeingCareWorkflowTest`) ✅ **18 tests / 197 assertions** · `npm run types -- --pretty false` ✅ exit 0 · focused ESLint over the six touched TSX files ✅ exit 0 / 0 errors (41 pre-existing `no-restricted-syntax` warnings in the legacy wellbeing page) · `git diff --check` ✅. Per the explicit crash-containment instruction, no full HR suite/build/vitest/browser run was started. **Last full HR baseline remains 735/0; expected next full count is 740.**

### Run 6 — C5 exit-interview immutability (2026-07-11, D-5)
Mapped the actual lifecycle before changing it: `hr_exit_interviews` has no draft/status/submission column, `ExitInterviewService::createExitInterview()` is the sole writer, and both the standalone and offboarding UI describe that POST as recording a completed interview. There was no edit route or indirect model update anywhere in `app`; consequently, every persisted row is already submitted and adding a synthetic draft state would violate the no-schema-invention boundary.

Made that contract explicit and safe. `PATCH /hr/exit-interviews/{exitInterview}` is now a manager- and tenant-guarded compatibility path that always redirects back with `Submitted exit interviews are locked. Add an addendum instead.`; it does not validate or write any submitted field. `POST /hr/exit-interviews/{exitInterview}/addenda` validates one note and delegates to `ExitInterviewService::appendAddendum()`, which appends a worker-timezone timestamp, actor name and note to `additional_comments`, preserving the existing text and all core answers. This uses the current text column and the model's existing `AuditableChanges` update hook — no migration, parallel note store or permission was introduced. The detail page now makes the immutable state visible and gives managers a standard addendum dialog; viewers see the lock explanation but no write action.

**TDD evidence:** deliberate red ✅ **2 failed / 2 assertions** — the proposed core PATCH returned 405 and addendum POST returned 404. The same focused file then passed ✅ **2 tests / 16 assertions**, proving a rejected core edit leaves departure reason, satisfaction, feedback and comments byte-for-byte unchanged, while an addendum preserves the original answers/comments and appends the timestamped actor-attributed note. **Contained gates:** existing `ExitInterviewWizardTest` ✅ **4 tests / 51 assertions** · php -l ✅ ×3 · `php artisan wayfinder:generate` ✅ · route list shows all 7 exit-interview routes including PATCH/addenda ✅ · `npm run types` ✅ exit 0 · focused ESLint ✅ 0 errors · `git diff --check` ✅. No browser/tooling outside the terminal and no broader slice was started. **Last full HR baseline remains 735/0; expected next full count is 742 (C4 +5, C5 +2).**

### Run 7 — C6 case↔incident read-only cross-link (2026-07-11, D-9)
Mapped the dormant seam first: `hr_cases.linked_incident_ids` already exists as nullable JSON, `HrCase` already casts/fills it, and case create/update already accepted integer arrays. But the New Case wizard never sent IDs, the controller never supplied incident choices or resolved links for show, and the page rendered nothing. The old validation also accepted nonexistent and cross-organisation IDs. `ClientIncident` has no tenant column, so organisation ownership must be scoped through `client.organization_id`.

Wired the existing reference instead of inventing a relation/table. The case index now sends managers up to the 100 most recent same-organisation incidents as minimal summaries; the Assignment step provides a multi-select and the Review step confirms selected references. Both create and update validate every ID as distinct, existing, and owned through a client in the current HR tenant. Case show resolves only the stored IDs that still belong to that tenant, preserves stored order, and renders a clearly labelled read-only “Linked incidents” card with reference, client, title, date, severity and status. No `ClientIncident` update/service/observer path is called. Because `incidents.viewAssigned` is client-specific and cannot guarantee every linked record will open, the H&S deep-link is shown only with organisation-wide `incidents.viewAny`; this prevents a UI affordance that would 403 while preserving the minimal approved HR federation for authorised case viewers.

**TDD evidence:** deliberate red ✅ **3 failed / 24 assertions** — same-tenant incident absent from picker props, linked detail prop null, and a foreign-organisation ID created a case. The same file then passed ✅ **3 tests / 38 assertions**, proving tenant-scoped choices, rendered stored links, rejected cross-organisation references, one incident row only, and an unchanged H&S-owned `updated_at`. **Contained gates:** `HrCasesPageContractTest` + `AuditFixPayrollCasesTest` ✅ **11 tests / 307 assertions** · php -l ✅ ×2 · `npm run types` ✅ exit 0 · focused ESLint over all three touched TSX files ✅ 0 warnings / 0 errors · `git diff --check` ✅. No route/schema/permission changes, browser tooling or broader slice. **Last full HR baseline remains 735/0; expected next full count is 744 (C4 +5, C5 +2, C6 net +2).**
