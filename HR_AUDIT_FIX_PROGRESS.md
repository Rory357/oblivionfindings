# HR Module — Audit & Fix Loop Ledger

> One slice per run: audit → fix → gates → ledger → commit → stop.
> Severity: 🔴 broken workflow / dead end / data-integrity · 🟠 misleading inconsistency · 🟡 polish.
> Row status: ⬜ not started · 🔶 partial · ✅ done.

**Run log**
- **Run 0 (2026-07-07)** — Ledger seeded. Slice 1 (My HR hub) audited + fixed. Gates green (see baselines below). Commit `145c101a`.
- **Run 1 (2026-07-07)** — 🔴-queue-jump slice: fixed all three pre-existing HR Pest failures (TrainingService `source` crash = real production 500; two stale test contracts). **HR pest suite now green — future runs measure against 0 fails.** Commit `d1cda667`.
- **Run 2 (2026-07-07)** — Slice 2 (People `/hr/people`) audited + fixed: bulk-action audit-trail hole (mass updates bypass Eloquent events) + tenant guards on bulk + profile-documents endpoints; chrome already gold (PeopleHero). Reclassified Run-0's F-4 (`fill-amberx` was a valid custom token, not a broken class) and finished the star-fill standardisation module-wide. Commit `b98bc4cf`.
- **Run 3 (2026-07-07)** — Slice 3 (Recruitment) audited + fixed: talent-pool audit trait, bulk-email audit record, `window.prompt()`s → new kit `TextPromptDialog`, HR-wide malformed-class sweep. Non-HR remainder of that sweep spawned as a task chip. Commit `bacab6d2`.
- **Run 4 (2026-07-07)** — Slice 4 (Onboarding) audited + fixed: §7.2 pre-seeded finding DEBUNKED (full task lifecycle exists); fixed reassign-owner silent handover, reorder audit-invisibility, and AssetAssignment audit trait. Chrome audit fully clean. Commit `348a9110`.
- **Run 5 (2026-07-07)** — Slice 5 (Offboarding + exit interviews) audited + fixed: three real dead-ends closed (task reopen, checklist cancel/resume, rehire vs open offboarding) + silent task assignment fixed; login-revocation seam verified SAFE (automatic on completion). Commit `37bf8414`.
- **Run 6 (2026-07-08)** — Slice 6 (Calendar hub) audited + fixed: invite notifications (the RSVP feature was unreachable — invitees were never told), organiser RSVP notification, attachment-file cleanup on event delete, attachment audit trait. `/hr/calendar/time-off` seed route confirmed stale. Commit `b2205427`.
- **Run 7 (2026-07-08)** — Slice 7 (Leave hub) audited + fixed: manager-cancel dead end closed (route+UI — the Run-0 admin-cancel notification finally has a caller), StaffTimeOff projection sync made audit-visible, public-holiday deletion guarded against orphaning calculated hours, balance adjustments now notify the employee. Commit `3843580c`.
- **Run 8 (2026-07-08)** — Slice 8 (Time hub) audited + fixed: payroll-period lock on entry mutations (S9 data-integrity — entries could be amended after payroll consumed them), owner notifications on on-behalf/amend/correct/void (pay-affecting silent changes), dead "Rejected" entries filter → working "Voided" filter (incl. the withTrashed fix that makes it actually return rows). Commit `85f42759`.
- **Run 9 (2026-07-08)** — Slice 9 (Compensation) audited + fixed: bonus cancel dead end closed (the `cancelled` status was settable nowhere), bonus approval + compensation-review-apply now notify the affected employees (statutory pay-change notice), tenant guards on bonus store/approve + benefits enrol. Gates: build ✅ · vitest baseline (8/171) ✅ · pest 693✓/1✗ — the single fail is the stash-proven date-env `ShiftPayrollBackboneIntegrationTest` flake (fails at clean HEAD on 2026-07-08), not a regression. Commit `7768a0b0`.
- **Run 10 (2026-07-08)** — Slice 10 (Payroll + payslips) audited + fixed: hero KPI tiles were computed from the current page only (wrong past page 1) and didn't deep-link → server-side tenant counts + deep-linked tiles on both payroll pages; the dead `approved` payslip status (never wired) removed from the UI; new `PayslipAvailableNotification` so employees learn their payslip is ready; payslip index/generate tenant bugs fixed (`$user->tenant_id` was NULL). Gates: build ✅ · vitest baseline (8/171) ✅ · pest **698✓/0✗** (HR + 2 Finance payroll tests; date-env flake passed this run). Commit `1f5684ef`.
- **Run 11 (2026-07-08)** — Slice 11 (Compliance + Vetting + Drivers) audited + fixed: §7.1 Vetting-on-PageHero DEBUNKED (already on the shared hub hero). Real fix: the Compliance hub header advertised cross-domain create-actions ("Add vetting check", "Add driver", "Record compliance") to users without the matching `.manage` permission — 4 of 5 tabs hardcoded the `can` flags to `true`, the 5th gated on `.view`. All 5 hub controllers now emit real `*_manage` perms and every tab gates the header correctly. Gates: build ✅ · vitest baseline (8/171) ✅ · pest **694✓/0✗**. Commit `ae2e83ab`.
- **Run 12 (2026-07-08)** — Slice 12 (Documents) audited + fixed: carry-over re-attestation-dedupe DEBUNKED (already guarded per version). Real fix: four native browser dialogs (`window.confirm` ×3 + `window.prompt` ×1) in the redesigned Documents hub → kit `AlertDialog`s (one per LibraryTab/SignaturesTab, threaded to `buildRowMenu`/`SigCard`) + `TextPromptDialog` for the folder move; each confirm gained a real title + explanatory body. Gates: types/eslint ✅ · build ✅ · vitest baseline (8/171) ✅ · pest **694✓/0✗**. Commit `e9738807`.
- **Run 13 (2026-07-08)** — Slice 13 (Performance) audited + fixed: both acknowledge loops were silent. Review: manager sign-off now notifies the employee to acknowledge (new `ReviewReadyForAcknowledgementNotification`), and the performance-hub acknowledge path now notifies the reviewer (the existing `notifyReviewSignedOff` was only wired into the My HR path). Supervision: employee acknowledgement now notifies the supervisor (new `SupervisionAcknowledgedNotification`). Session-type-wizard + orphaned dead code deferred to the redesign prompt; S13 two-model split → Decision D-7. Gates: php -l ✅ · pest **694✓/0✗** (frontend gates N/A — PHP-only). Commit `d1c6da43`.
- **Run 14 (2026-07-08)** — Slice 14 (Goals + Development) audited + fixed: O-4 closed — development-goal completion now notifies the manager on both the manager-hub and employee-self-service paths (new `DevelopmentGoalCompletedNotification` + shared `notifyDevelopmentGoalCompleted` service method). Noted the dead OKR-side `notifyGoalCompleted` (no caller) as deferred to the GOALS redesign. Gates: php -l ✅ · pest **694✓/0✗** (frontend gates N/A — PHP-only). Commit `2e9bee24`.
- **Run 15 (2026-07-08)** — Slice 15 (Training + catalog) audited + fixed: §7.1 reclassified (catalog has a bespoke hand-rolled hero, not `PageHero` → deferred to the Training redesign). O-2 closed — the "My training to complete" cards on `/hr/my/training` were dead (no `course_id`, not clickable); added `course_id` to the payload and deep-linked each card to `/hr/training/courses/{id}`, gated on `can.viewCatalog`. Gates: types/eslint ✅ · build ✅ · vitest baseline (8/171) ✅ · pest **694✓/0✗**. Commit `8d078521`.
- **Run 16 (2026-07-08)** — Slice 16 (Feed) audited + fixed: §7.3 CONFIRMED STALE (the wall fully has reactions + reply threads). Real fix: replies notified no one — new database-only `FeedReplyNotification` + shared `notifyReply` helper wired into `addReply` (kudos thread's other party) and `addFeedReply` (a post's author). Reactions stay ambient; announcement-reply notification deferred. Gates: php -l ✅ · pest **694✓/0✗** (frontend gates N/A — PHP-only). Commit `4b06b566`.
- **Run 17 (2026-07-08)** — Slice 17 (Announcements) + seam **S10** audited **genuinely CLEAN — no code fix** (the loop's sanctioned clean-slice outcome). Debunked a hypothesised scheduled-publish dead-end (`PublishDueAnnouncementsJob` is wired in `routes/console.php:190` + tested). S10 proven end-to-end by `AnnouncementCommandCenterTest` (scheduled-publish flip→notify + bell-bridge) and `AnnouncementAndOnboardingNotificationTest` (audience-scoped delivery). Ledger-only commit; no gates run (zero code changed). Commit `3c680b8f`.
- **Run 18 (2026-07-08)** — Slice 18 (Feedback + surveys) audited + fixed: 360-feedback requests notified no reviewer (only `remind` did) → new database-only `FeedbackRequestedNotification` fired to each reviewer post-commit from `request360Feedback` (covers single + bulk). "Surveys" = the retired standalone module (→ Wellbeing). Noted `feedback/index.tsx` on generic `PageHero` as an open hero-build follow-up (no redesign prompt). Gates: php -l ✅ · pest **694✓/0✗** (frontend gates N/A — PHP-only).

**Corrected baselines (measured this run, clean HEAD `4874c71a`)**
- vitest: **8 pre-existing fails**, not 5 — my-day ×4, app-sidebar ×1, behaviour-abc-tab ×2, resident-tracking ×1 (last two reproduced via stash at clean HEAD). "No NEW failures" is measured against 8.
- pest (HR scope): was 3 pre-existing fails (not the documented "RecruitmentJobPostingSync + ShiftPayroll" — those pass). **All three FIXED in Run 1 → baseline is now 0 fails.** Any pest failure in a future run is NEW by definition.
- Prompt §6's `*_REDESIGN_PROMPT.md` files do **not exist** at repo root (those redesigns shipped and the prompts were removed). Deferral targets are therefore the future slice rows in this ledger, not prompt files.
- Worktree note: browser screenshots (§9 gate 7) can't run against a worktree (Herd serves the parent repo only). Visual verification of merged changes follows the established post-deploy pattern (Chrome as demo admin / deployed-chunk grep).

---

## §5 Surfaces

| # | Surface | Route(s) | Status | Findings |
|---|---------|----------|--------|----------|
| 1 | My HR hub (16 sub-pages) | `/hr/my/*` | ✅ | See **Slice 1 findings** below — 13 fixed, 6 logged open/observations |
| 2 | People / employee profiles | `/hr/people` | ✅ | See **Run 2 findings**. Chrome passes 4A/4B (PeopleHero: deep-linked escalating tiles, server counts; HrTabs+tabCounts; ctx menus; designed empty states; en-NZ; zero `confirm()`). Full-page profile detail = allowed long-lived-workspace exception. Open here: 🟡 resendInvite sends a generic Laravel reset mail (works — comment documents reset-link-as-invite — but no HR-branded notification, no guard against re-inviting an active user); 🟡 setActive is a pure HR visibility flag by design (no login/assignment cascade — offboarding owns revocation) — documented, not a bug; 🟡 client-supplied mime_type stored on upload (acceptable: private disk + extension allowlist + auth'd downloads); carry-over profile-update notification idea still open |
| 3 | Recruitment | `/hr/recruitment` | ✅ | See **Run 3 findings**. Chrome near-gold (RecruitmentHero 5 deep-linked tiles, server counts, HrTabs+tabCounts, EmptyCard states, en-NZ, all 10 pipeline stages UI-reachable). Open: 🟡 no force-expire affordance for lapsed offers (`portal_expires_at` passes silently; `respondOffer` does accept `withdrawn`, so the state is reachable); 🟡 no scorecard quorum before stage advance (honor system); requisition/offer approval-spine question folded into Decision D-1 |
| 4 | Onboarding | `/hr/onboarding` | ✅ | See **Run 4 findings**. §7.2 fully DEBUNKED: complete task lifecycle exists (task complete/uncomplete/update/destroy/provision-asset/store/reorder + checklist complete/remind/reassign/status/bulk; my-area + manager paths share ONE `OnboardingService::completeTask`). Chrome audit 100% clean (hero, tabs, ctx menus, drawer+workspace, wizard, en-NZ). Open: 🟠 hard-deletes (checklist/task/template/email) vs archive-not-delete — `archived` status EXISTS for checklists so destroy is an extra escape hatch; email/template/task deletes orphan logs/evidence → same SoftDeletes decision as slice 12's HrDocument; 🟡 per-task completion is silent by design (owner notified at full completion — anti-spam); 🟡 `pending` status auto-reverts via recompute (harmless) |
| 5 | Offboarding + exit interviews | `/hr/offboarding`, `/hr/exit-interviews` | ✅ | See **Run 5 findings**. Login-revocation seam VERIFIED SAFE (auto `revokeSystemAccess()` → `approved_at=null` when all required tasks complete, OnboardingService:994-1034). Open: 🟠 exit interviews are store-once (no update/delete route — typo in a recorded interview is uncorrectable; possibly by-design record-integrity → Decision D-5); 🟠 exit-interview↔offboarding-task link is a fragile title string-match (`like '%exit interview%'` — robust FK needs schema change); 🟡 exit-interview creation doesn't notify the interviewer (often recorded post-hoc, so notify-on-schedule only makes sense if future-dated); 🟡 asset-return tasks are snapshot-at-generation (assets issued later don't appear); 🟡 default offboarding tasks assume roles (payroll_admin etc.) that may have no user → unassigned tasks; 🟡 offboarding index/show on generic PageHero (LifecycleTabs satellite — acceptable; onboarding owns the group hero), minimal empty states. Earlier drive-by: `fill-amberx` + `/80/10` fixes (Runs 0/3) |
| 6 | Calendar hub + time-off calendar | `/hr/calendar*` | ✅ | See **Run 6 findings**. NOTE: `/hr/calendar/time-off` (the §5 seed route) does NOT exist — leave is a layer on the one calendar; seed row was stale. Unmerged rebuild branch `claude/stoic-dubinsky-94954c` exists — redesign-scale items deferred to it. Chrome green (CalendarHero deep-linked stats, 3 URL-synced tabs, 6 URL+localStorage-synced layers, event wizard, iCal reachable via Subscribe, en-NZ). Open: 🟡 aggregator layers lack `Schema::hasTable` guards (mature tables — low risk); 🟡 dormant `team` value in the attendee DB enum (API validation never accepts it); 🟡 route group has no permission middleware but EVERY method gates in-controller (canView/canManage + tenant asserts — verified) |
| 7 | Leave (balances, holidays, reports) | `/hr/leave/*` | ✅ | See **Run 7 findings**. Backend verdict: strongest hub audited so far (transactional balance+ledger atomicity, per-row bulk ops with per-row notify+audit, CSV-injection-hardened exports ×3, escalation job scheduled every 30min). Open: 🟡 `HrLeaveApprovalChain` is config-only (no admin UI — seeded/external; folded into Decision D-1's approvals discussion); 🟡 pre-existing eslint `react-hooks/refs` error in balances.tsx (~370, reqRef read during render — pre-dates this loop, verified via stash); 🟡 empty states on overview/balances/holidays are icon+text but not CTA-enriched (acceptable; Approvals tab is the good example); 🟡 show.tsx full-page detail = long-lived-workspace exception (modal exists for quick reads); 🟡 no appeal/resubmit transition for declined requests (by-design: new request instead) |
| 8 | Time | `/hr/time` | ✅ | See **Run 8 findings**. Chrome green (TimeHero deep-linked tiles + exception chips, 4 URL-synced tabs with server counts, ctx menus, amendment drawer, WizardShell dialogs, honest read-only timesheets banner pointing at Operations, CSV/PDF injection-safe). Open: 🟠 approving an Operations timesheet does NOT flip linked HrTimeEntry to `approved` (entry stays `submitted`; the new payroll-lock covers the integrity risk, but the status desync is a cross-module design point → S9 seam slice); 🟡 `pay_type` validation permits night/weekend/evening the UI doesn't offer (permissive validation, harmless); 🟡 break compliance is warn-not-block on clock-out (by design — blocking would trap workers); 🟡 entries list soft-refresh (30s) reloads KPIs but not the entries list (stale until interaction) |
| 9 | Compensation (+ expenses) | `/hr/compensation/*` | ✅ | See **Run 9 findings**. Verified green: GL double-post fix HELD (`journal_id === null` guard + job-level recheck); CSV export injection-safe; benefit-enrolment lifecycle fully reachable AND already notifies on material changes; review-apply idempotent (status guard in transaction); audit traits everywhere. Open: 🟡 salary bands have store/update but no archive/deactivate (likely by-design — referenced by history; flag if a band is ever created in error); 🟡 expenses full-page create is a giant form not a wizard (the DIALOG wizard is the primary path; full form = escape hatch for many-item claims — acceptable); 🟡 no draft recovery on that form; 🟡 `BenefitsService::enrollEmployee` itself doesn't cross-check plan↔profile tenant (controller now guards; service note for other callers). ~~🔴 `BenefitsEnrollmentTest`~~ fixed Run 1. Prior S14 note stands (expenses inline approvals → D-1) |
| 10 | Payroll + payslips | `/hr/payroll/*` | ✅ | See **Run 10 findings**. Verified green: run lifecycle draft→locked→exported all reachable; GL double-post + net-pay double-pay both guarded (`journal_id`/`net_paid_at`); timesheet paid-cascade idempotent + workflow-guard-aware; leave feeds payslips hours-based (Employment Leave Bill); export CSV enclosure-escaped. Open: 🟡 payroll/index still on generic `PageHero` not a specialised `PayrollHero` (no `PAYROLL_REDESIGN_PROMPT.md` exists — logged as hero-family gap, defer to a hero-build slice with Vetting/Training); 🟡 runs/payslips tables hand-rolled `<table>` not shadcn DataTable+RowContextMenu (sibling HR norm; defer); 🟡 export CSV has no formula-injection guard — **Decision D-6** (guarding a machine-read provider file risks breaking ingestion); 🟡 export-profile default-demotion uses a mass `update()` (audit-invisible, but the promoted row IS audited — material fact captured). |
| 11 | Compliance + Vetting + Drivers | `/hr/compliance/*`, `/hr/vetting`, `/hr/drivers` | ✅ | See **Run 11 findings**. ~~Pre-seeded §7.1 Vetting-on-PageHero~~ **DEBUNKED** — Vetting/Drivers already render the shared `ComplianceHubHeader` (the compliance-hub redesign `227ca473` folded them in); routes are `/hr/compliance/vetting` + `/hr/compliance/drivers`, both hub tabs. Fixed the real bug: hub-header cross-domain quick-actions were shown regardless of permission. Verified green: vetting lifecycle (pending→requested→clear/flagged/adverse/renewal_due) reachable via edit + clear/renew endpoints; driver lifecycle (pending_review→eligible/suspended, expiry computed) reachable via approve/suspend; every mutation tenant-guarded + `reevaluateCompliance` keeps the S3/S6 matrix hard-stop fresh; consent capture works. Open: 🟡 `StaffBackgroundCheck::destroy` hard-deletes a safeguarding record (Decision D-4 archive-not-delete); 🟡 no worker-facing vetting/licence-expiry nudge (compliance renewalRemind covers the manager side; defer to `HR_COMPLIANCE_REDESIGN_PROMPT`). |
| 12 | Documents (+ policies, attestations) | `/hr/documents/*` | ✅ | See **Run 12 findings**. Carry-over re-attestation-dedupe **DEBUNKED** — `PolicyAttestationController@store` already guards duplicate attestation of the same `policy_version_id` per user (returns a validation error). Fixed the real gap: four native browser dialogs in the redesigned hub. Verified green: attestation captures IP/UA/version + tenant-guarded; policy versions have store/destroy; documents library store/generate/move/bulk all tenant-scoped. Open: 🟡 `HrDocument::destroy` hard-deletes (Decision D-4 archive-not-delete — shared with slice 2); 🟡 `TextPromptDialog` lives under `components/hr/recruitment/` — reused cross-page here, worth promoting to a neutral kit path in a future sweep. |
| 13 | Performance (reviews, PIPs, supervision) | `/hr/performance/*` | ✅ | See **Run 13 findings**. Both acknowledge loops (review + supervision) now notify their waiting party. ~~🔴 `SupervisionDialogTest`~~ fixed Run 1. Verified green: review lifecycle draft→in_progress→signed_off reachable (submit/sign-off/return/acknowledge) + tenant-guarded + evidence hardened + "Next steps" PIP/succession CTA gated to signed-off; probation `extend` moves the profile date; supervision store/update/acknowledge tenant-guarded. Deferred to `PERFORMANCE_REDESIGN_PROMPT.md`: 🟠 supervision create wizard reportedly hardcodes `session_type` although the taxonomy exists in the model/endpoints/show page (redesign owns the wizard); 🟡 orphaned `components/hr/performance/supervision-dialog.tsx` dead code. Open follow-up: 🟡 supervision-note *creation* (visible-to-employee) doesn't notify the employee — the mirror of the review sign-off nudge (smaller, noted). S13 two-model split → Decision D-7. |
| 14 | Goals + Development | `/hr/goals*` | ✅ | See **Run 14 findings** (O-4 closed). Dev-goal completion now notifies the manager on **both** paths (hub + self-service). Verified green: dev-goal store notifies the employee on assignment; owner-vs-manager field-level edit gates hold (`prohibited` on manager-only fields for owners); completion sets completed_at/100%/clears next_review; assign→employee notification round-trips. Open (deferred, redesign-scope `GOALS_REDESIGN_PROMPT.md`): 🟠 the OKR-side `HrNotificationService::notifyGoalCompleted(HrGoal)` **exists but has no caller** (dead) — OKR owner-completion is spread across `updateProgress`/`checkin` and `GoalController::update` is manage-only, so the right hook needs tracing; 🟡 `HrDevelopmentGoal::destroy` hard-deletes (D-4). |
| 15 | Training + catalog | `/hr/training/*` | ✅ | See **Run 15 findings** (O-2 closed). ~~Pre-seeded §7.1 "Training on generic PageHero"~~ **RECLASSIFIED** — the catalog isn't on `PageHero`; it has a fully hand-rolled bespoke hero (custom gradient + local `HeroBtn` + hand-rolled stat buttons), the shipped Training redesign (like the Documents command surface). Rebuilding it as a kit `TrainingHero` competes with the redesign → `↗ deferred to HR_TRAINING_REDESIGN_PROMPT.md`. ~~🔴 `AuditFixNotificationsTest`~~ fixed Run 1. Verified green: catalog course/session/enrol/assign/complete/waive flows all permission-gated; my/training reads the S7 compliance-status projection. Open (deferred, redesign-scope): 🟡 catalog hero uses a raw `oklch()` fallback for `--hr-amber` (token-discipline nit inside the bespoke hero). |
| 16 | Feed | `/hr/feed` | ✅ | See **Run 16 findings**. §7.3 **CONFIRMED STALE** — the wall fully has reactions + comments (`parts.tsx` `ReactionBar`: heart/party/hands toggles + reply thread + inline reply box, reused by every card; routes wire react/reply for kudos AND polymorphic posts/announcements). Real fix: replies notified no one. Verified green: kudos send already notifies the recipient (`KudosReceivedNotification`); react/reply are tenant-guarded; kudos reply is restricted to the two thread parties; audience scoping on the wall (org/site/own). Open: 🟡 announcement-reply notification (org-wide, uncertain author-notify semantics — follow-up); 🟡 reactions are ambient (no notification, by design — too high-frequency); 🟡 `destroyPost`/`destroyKudos` hard-delete (D-4). |
| 17 | Announcements | `/hr/announcements` | ✅ | **Audited genuinely CLEAN** (see Run 17). Debunked a hypothesised scheduled-publish dead-end: `PublishDueAnnouncementsJob` flips `scheduled`→`published` + fires the bell bridge + audience notification, is scheduled (`routes/console.php:190`), AND is tested. Bespoke hero with deep-linked + amber-toned stat tiles (redesign expression, not `PageHero`); soft-delete + restore (no D-4 hard-delete); no native `confirm()`; bulk is per-model (audit-visible); reminders have a cooldown; publish/scheduled-publish notify the **resolved audience only** (tested). No honest fix to make — shipping a speculative change would violate "smallest honest fix". |
| 18 | Feedback + surveys | `/hr/feedback` | ✅ | See **Run 18 findings**. Fixed the 360-feedback request notification gap. "Surveys" here = the **retired** standalone module (`/hr/surveys/*` → 302 `/hr/wellbeing`); the live engagement-survey system is Wellbeing (slice 19). Verified green: request lifecycle pending → completed/declined/expired all reachable + tenant-guarded; reviewer-only respond/submit guards; summary aggregation; template CRUD. Open (no redesign prompt): 🟠 `feedback/index.tsx` is on generic `PageHero`, not a specialised kit hero — a real 4A hero-family gap, but a `FeedbackHero` build is larger than this slice's smallest-honest-fix scope (**hero-build follow-up**). |
| 19 | Wellbeing | `/hr/wellbeing` | ✅ | §7.5 STALE (loop fully wired: triage+EAP+check-in+plans, self-auditing). F-65 fixed: action-plan owner now notified at assignment. Redesign-scoped items deferred ↗ |
| 20 | Cases | `/hr/cases` | ✅ | F-66 fixed: dead `HrCaseUpdateNotification` wired — case assignee now notified on assignment/reassignment. Model has AuditableChanges; pages clean (no native dialogs) |
| 21 | Assets | `/hr/assets` | ✅ | Gold-std service/controller (guarded transitions, Fleet federation, wired notifications). F-67 fixed: doc-delete now confirms (was 1-click unrecoverable); F-68: raw oklch tile→status-warning token; + incidental react-hooks/static-components lint fixed |
| 22 | Approvals inbox | `/hr/approvals` | ✅ | F-69 fixed: hero "Pending" count was current-page length (lied >20) → server `total`. `action` catches LogicException→flash. Spine gap = D-1 (Chane); deep-link + polish noted |
| 23 | Signatures | `/hr/signatures` | ✅ | F-70 fixed: manual "nudge" stamped reminder_sent_at without sending → lied AND suppressed the auto-sweep (dedup on whereNull); now sends. F-71: "resend" was a silent flip → now re-notifies signer. sign/decline→requester noted |
| 24 | Analytics/reports/headcount/succession/import-export | various | ✅ | Analytics/Headcount/Succession/Import-export all audited CLEAN (server-side data, guarded deletes, wired exports). Reports F-72 fixed (native confirm→kit AlertDialog). Cluster complete |
| 25 | Settings + audit log | `/hr/settings/*` | ✅ | F-73 audit-log hero count→server total (was page-length — egregious on the SoT). F-74 custom-fields + F-75 automations native confirm()→kit AlertDialog. AuditController clean. **All 25 surface rows now ✅** |

## §6 Seams

| # | Seam | Status | Evidence so far |
|---|------|--------|-----------------|
| S1 | Attendance ↔ My Day | ✅ | **PROVEN by test** (not code-reading). New `AttendanceClockWorkflowTest` › "S1 seam: the HrTimeEntry and HrAttendanceSession read paths agree across both clock surfaces" (14 assertions): clock-IN via `/my-day` (`AttendanceController`) sets BOTH `HrAttendanceSession::open()` (Attendance's read path) AND `HrTimeEntry::active()` (MyHr's read path); clock-OUT via the OTHER surface (`/hr/my/time`) clears both; the reverse (clock-in via `/hr/my/time`) also sets both; clock-out clears both — never a phantom "on shift" on either surface. The two read paths cannot disagree because both surfaces write through the ONE `AttendanceService` + `TimeTrackingService::syncEntryFromSession`. (Complements the pre-existing "clock-in endpoints share the single open attendance session guard" test, which proves the shared write-guard across all three clock routes.) |
| S2 | HR Assets ↔ Fleet | ✅ | **PROVEN by test** (not code-reading). New `AssetFleetFederationTest` (3 tests, 9 assertions): (a) POST `/hr/assets` with `category:'vehicle'` and no `fleet_asset_id` → **422** (`AssetController::validateAsset` federation guard) — HR can't hand-type a Fleet record; (b) an `HrAsset.fleet_asset_id` → canonical `Asset` reads THROUGH via `fleetAsset()`/`isFleetLinked()` (federation, not a duplicated name/reg); (c) POST `/hr/assets/bulk` retire over a Fleet-linked + an HR-owned row → the Fleet-linked stays `available` (skipped — Fleet owns disposal) while the HR-owned retires. One-owner-per-fact holds: Fleet owns vehicles/keys, HR federates read-only. Test seeds `SeedHrPermissionsSeeder` (RbacSeeder alone omits `hr.assets.*`). |
| S3 | Driver eligibility → Fleet/Rostering | ✅ | **PROVEN by test — enforced, NOT just computed** (the drift this seam guards against does NOT exist). New `DriverEligibilityRosteringHardStopTest` (3 tests): (a) an expired `HrDriverEligibility.licence_expires_at` on a `coverage_roles:['driver']` shift → `ShiftStaffEligibilityService::evaluate(...)->toArray()['blocked_reasons']` contains "Driving licence expired" (`DriverLicenceExpiryRule` severity `block`, non-overrideable); (b) **enforcement**: `RosterPublishValidator::validateProposedShifts([$shift])` returns `can_publish:false` with that block — the roster is un-publishable (the hard-stop); (c) a current licence trips no driver block/warning. Rule also hard-blocks `status:'suspended'` + no-record. Note: licence-CLASS/endorsement matching at the gate is DEFERRED in `DriverLicenceExpiryRule` (shifts carry no `required_licence_class`) — documented, out of scope. Complements the pre-existing Rostering warning-window test. |
| S4 | Injuries (H&S) → HR | ✅ | **Integrity invariant PROVEN; surfacing = D-8.** Source audit: `WorkplaceInjury` is referenced in 13 sites app-wide, **ALL** HealthSafety/Governance/Tasks — **ZERO** under `app/Domain/Hr`, `app/Http/Controllers/Hr`, or `resources/js/pages/hr` (nor `/my-day`). So HR has no create/update/delete path → one-owner-per-fact holds by absence (H&S is sole writer; no 🔴 drift). New `InjuryHrOwnershipSeamTest` (1 test, 3 assertions) locks the per-employee ownership contract (`injury->user` + retrievable by `user_id`) the read-only federation would use. ⚠️ **The pre-seed's "HR read-only surfacing per INJURIES_HR_SURFACING_PLAN.md" is STALE** — that plan file does not exist and no HR view surfaces injuries → **Decision D-8** (build HR read-only injury surfacing, or keep H&S-only?). ✅ scoped to the integrity guarantee; the surfacing FEATURE is D-8, not a bug. |
| S5 | H&S incidents ↔ HR cases | ✅ | **Integrity PROVEN + a real bug fixed (F-76); cross-link surfacing = D-9.** `HrCase.linked_incident_ids` = JSON array of `ClientIncident` ids (docs 900). New `CaseIncidentCrossReferenceSeamTest` (1 test, 5 assertions): storing a case with `linked_incident_ids:[id]` persists the reference AND leaves the `ClientIncident` untouched (count=1, `updated_at` unchanged) — HR references, never mutates/duplicates (one-owner holds, no 🔴). **The seam test surfaced F-76 (🔴-ish, fixed): `hr_cases.description` is NOT-NULL-no-default but `store`/`update` validate it `nullable` → a description-less case 500'd on insert** (the ConvertEmptyStringsToNull gotcha; missed in Slice 20 which always POSTed a description). Fixed by coercing `description` to `''` in store + update. ⚠️ **Dead cross-reference:** `linked_incident_ids` is INERT — no producer (the case wizard never sets it) + no consumer (no `linkedIncidents()` relation, no `show()` load, no page render), unlike RespiteDailyNote/ClinicalEvent → **D-9** (wire incident-pick + surface on the case, or drop the unused column). |
| S6 | Compliance matrix → Rostering | ✅ | **PROVEN by test — enforced (same chain as S3), NOT just computed.** New `ComplianceMatrixRosteringHardStopTest` (2 tests, 4 assertions): an EXPIRED **hard-stop** `HrStaffComplianceStatus` (via `ComplianceMatrixService::getHardStopFailures`, the cached role-independent path) → `ShiftStaffEligibilityService::checkCompliance` block naming the requirement → `RosterPublishValidator::validateProposedShifts` `can_publish:false`; a `compliant` status trips no block. (LiveComplianceValidator is role-scoped and skipped for a role-less test user, so the cached status is the sole block source — a clean isolation of the compliance rule.) |
| S7 | Training → Compliance/Vetting | ⬜ | |
| S8 | Expenses → Finance GL | 🔶 | Code evidence (this run): `ExpenseService::approveClaim` dispatches `PostExpenseJournalJob` when `journal_id` null (idempotent). Failure-path visibility still to prove. |
| S9 | Payroll ↔ Time/Leave | ⬜ | §7.4 RESOLVED as by-design (see findings F-13) |
| S10 | Announcements → bell inbox | ✅ | **PROVEN by test** (not code-reading). `AnnouncementInboxBridge::publish` creates/updates the global header-bell `Announcement` idempotently (via `hr_announcements.inbox_announcement_id` FK); `withdraw` deactivates it on archive/unpublish/delete. Fired from `afterSave` (publish/withdraw), `update`/`bulk`, and `PublishDueAnnouncementsJob`. `AnnouncementCommandCenterTest` proves both the scheduled-publish flip→notify AND "publish bridges to the header-bell inbox announcement"; `AnnouncementAndOnboardingNotificationTest` proves audience-scoped delivery (sent to the audience, NOT to non-audience). ⚠️ Documented lossy mapping: non-role segments (site/dept/user) collapse to "all roles" on the bell mirror (agreed single-tenant behaviour) — the *direct* notification stays audience-scoped, only the bell mirror is broad. |
| S11 | Feed ↔ My HR shoutouts | 🔶 | Code evidence GREEN (this run): identical endpoints (`hr.php:122-123` vs `hr.php:1016-1017`), shared `FeedService::toggleReaction/addReply`, same HrKudos/HrKudosReply models, feed renders ReactionBar. Runtime proof at slice 16, then ✅. |
| S12 | Recruitment → Onboarding | 🔶 | Code evidence GREEN (Run 3): offer accept → `CandidateController::respondOffer` (~1685) → `RecruitmentService::convertToEmployee` (207-277) → `EmployeeIntakeService::intake()` creates User + profile + onboarding checklist (`startOnboarding: true`) + `NewHireWelcomeNotification`. Runtime proof owed before ✅. |
| S13 | Performance ↔ Governance | ⬜ | |
| S14 | Approvals spine | ⬜ | Slice-1 evidence: leave + expenses approvals do NOT route through `ApprovalWorkflowService` (leave→HrLeaveApprovalChain, expenses→inline). Run-3 evidence: requisition + offer approvals are also recruitment-local (direct notify, no chain), and `ApprovalController` pending list covers only `['leave','expense','timesheet','document']` (ApprovalController.php:62) — recruitment approvables are NOT in the inbox. The S14 slice must decide/surface per Decision D-1. |
| S15 | Wellbeing lone-worker → Control Room | ⬜ | |
| S16 | Procedures (H&S) → HR | ⬜ | |

---

## Slice 1 findings (My HR hub) — Run 0

**Fixed this run** (commit: hr-audit slice 1):

- **F-1 🟠 fixed** — `leave.tsx:203` native `confirm()` on cancel-leave → controlled `AlertDialog` (status-aware copy: approved-cancel explains roster removal + balance return).
- **F-2 🟠 fixed** — `policies.tsx:46` native `confirm()` on policy attestation → `AlertDialog` naming the policy; attestation is a statutory act, now gets a real dialog.
- **F-3 🟠 fixed** — `reviews.tsx:105` native `confirm()` on review sign-off → `AlertDialog`.
- **F-4 🟡 fixed (RECLASSIFIED Run 2)** — `fill-amberx` on rating stars in `reviews.tsx:76` + `exit-interviews/show.tsx:72` → `fill-status-warning`. Run-0 diagnosis said "invalid utility, stars never filled" — **that was wrong**: `amberx` is a registered `@theme` colour (app.css:128) and `.fill-amberx` exists in the built CSS (it's the meal-planner's token). Real issue: non-semantic palette token where the HR standard (succession/show.tsx) uses `fill-status-warning`. Run 2 finished the standardisation across all remaining HR star sites (performance ×2, exit-interviews index, candidates ×2, exit-interview-wizards, employees/show ×2); `amberx` remains in the sites/meal-planner module where it belongs. ⚠️Auditor lesson: verify a "suspicious" utility against `@theme` + built CSS before declaring it broken — two audit agents independently mis-called this.
- **F-5 🟠 fixed** — `payslips.tsx` raw hex (`#8b5cf6`, `#10b981` ×3) → `var(--primary)` / `var(--status-success)`; ALSO fixed invalid `hsl(var(--…))` wrappers (tokens are oklch — `hsl(oklch(…))` is invalid CSS) on chart ticks/tooltip → plain `var(--…)`.
- **F-6 🟠 fixed** — `training.tsx` STATUS_CONFIG + accent bars + donut: 6 distinct hex colours → semantic `var(--status-*)`/`var(--primary)`/`var(--muted-foreground)` tokens.
- **F-7 🟠 fixed** — malformed Tailwind class `bg-muted-foreground/80/10` (double opacity modifier, generates nothing) in `reviews.tsx:49` and `expenses.tsx:52` and `training.tsx:104` → `bg-muted-foreground/10`.
- **F-8 🟡 fixed** — `reviews.tsx:278` `toLocaleDateString()` with no locale → `'en-NZ'`.
- **F-9 🔴 fixed** — **Leave cancellation notified nobody.** `LeaveService::cancelRequest` now sends new `LeaveCancelledNotification` after commit: self-cancel → assigned approver (`escalated_to`, falls back to approver pool); admin-cancel → employee. Approved-cancel copy explains roster removal.
- **F-10 🔴 fixed** — **Submitted expense claim was a dead end for the employee** (no withdraw/edit until a manager acts). Added `ExpenseService::withdrawClaim` (submitted→draft, reversible), `POST /hr/my/expenses/{claim}/withdraw` (`hr.my.expenses.withdraw`, owner+tenant gated, LogicException-guarded), and a Withdraw button on submitted rows in `expenses.tsx`. No new status value; no schema change. Audit trail via existing `AuditableChanges` on the model.
- **F-11 🟠 fixed** — **Review sign-off notified nobody** (reviewer waits on acknowledgement to close out). `MyHrController::updateReview` now sends new `ReviewSignedOffNotification` to the reviewer, deep-linked to `/hr/performance/reviews/{id}`. ALSO added the missing server-side gate: `employee_signed_off` only accepted when `status === 'completed'` (mirrors the UI's `canSignOff`; previously a crafted PUT could sign off a draft).
- **F-12 🟡 fixed** — bare empty states → designed (icon · line · CTA): `expenses.tsx` (CTA opens the claim form) and `goals.tsx` (no CTA on purpose — employees can't self-create development goals; explains they're set with your manager, per hide-unbuilt-actions rule).

**Verified / resolved without code change:**

- **F-13 §7.4 StaffTimeOff "drift" — NOT drift, by design.** `StaffTimeOff` is the roster **projection** of approved `HrLeaveRequest`s (created on approve LeaveService:280, deleted on cancel :535, synced via observer; Direction B roster-entry auto-creates a real HrLeaveRequest via `createRosterLeave`). SoT remains HrLeaveRequest. No my-area surface writes StaffTimeOff directly. Closed.
- **F-14 False positive from audit agent:** "`declined` status unreachable" — decline is a manager action with an existing route (`hr.leave.decline`, routes/hr.php:390). Reachable-from-manager-UI satisfies rubric 4C. Dismissed.
- **F-15 Chrome:** all 16 pages use shared `MyHrShell`/`MyHrHero`; no pills-as-tabs; no duplicate quick-action affordances; no client-side derived-KPI lies found.

**Open (logged, not fixed this run — smallest-honest-fix rule):**

- **O-1 🟡** payslips stat cards aren't links — the list below IS the explaining view; no filtered target exists. Acceptable; revisit only if payslip filters land.
- **O-2 🟡** my/training assignment cards don't deep-link to course/catalog detail → slice 15.
- **O-3 🟡** 1:1 acknowledge sends no notification to supervisor → slice 13 (supervisor-side surface).
- **O-4 🟡** HrDevelopmentGoal completion doesn't notify manager (needs a new notification class; GoalCompletedNotification is typed to HrGoal) → slice 14.
- **O-5 🟡** Deliberate non-notifications, documented as by-design: survey submit (anonymity), policy attest (would spam HR per-staff-per-policy; compliance lists cover it), clock-in/out (noise), expense draft creation (submit already notifies). Revisit only if a slice proves a waiting party.
- **O-6 🟠** Policy re-attestation has no duplicate/cooldown guard → slice 12 decision.

## Run 1 findings (🔴 queue-jump: pest suite to green)

- **F-16 🔴 fixed** — `TrainingService::createAssignments` crashed with `Undefined array key "source"` for any caller omitting `source` (the `?? 'manual'` guarded only the `in_array` check; the ternary's true-branch re-read `$form['source']` raw). Laravel converts the warning to ErrorException, so this was a live 500 on the training-assignment path, not just a test artefact. Default now hoisted before validation.
- **F-17 🟠 fixed (test)** — `BenefitsEnrollmentTest` demanded `annualSalaryByProfileId.{id} === 65000.0` (strict float). PHP's `json_encode` emits integral floats without the decimal (`65000`), so the assertion could never pass through the Inertia JSON round-trip regardless of the controller's (correct) `(float)` cast — it only ever passed under a non-default `serialize_precision`. Assertion made precision-independent; controller untouched.
- **F-18 🟠 fixed (test) + new slice-13 findings** — `SupervisionDialogTest` asserted the hub ships `sessionTypes` for the SupervisionDialog. The performance-hub redesign replaced that dialog with the wizard flow (`performance-wizards.tsx`); the dialog now has zero importers and the prop was deliberately dropped. Test updated to the current contract (`staff` for wizard pickers; session_type acceptance covered by endpoint tests). Surfaced two slice-13 findings recorded on that row: the wizard hardcodes `session_type='supervision'` (taxonomy unreachable from create UI), and the orphaned dialog + unrouted `SupervisionController` hub-render are dead code.

**Run 1 gates:** pest scoped (3 files) 17/17 ✅ · pest full HR scope ✅ 0 failed (see run log) · types/lint/build/vitest **not run — no TS/JS or route changes** (PHP service + two PHP test files only; those gates cannot be affected). Wayfinder n/a.

## Run 2 findings (Slice 2 — People `/hr/people`)

- **F-19 🔴 fixed** — **Bulk actions left no audit trail.** `EmployeeProfileController::bulkAction` used mass query updates (`whereIn()->update()`), which skip Eloquent events entirely — `AuditableChanges` never fired, so bulk deactivate/reactivate/assign-site/department/manager were invisible in `/hr/settings/audit-log` (violates the auditability non-negotiable; the audit agent claimed the trait "will fire" — verified false for query-builder updates). Also: no tenant scope on the id list (sibling endpoints `setActive`/`rehire` assert tenant) and the denormalised department-label lookup was tenant-unscoped. Fixed: tenant-scoped model fetch + per-model `update()` so the trait fires per row + tenant-validated department (422 otherwise). Regression test added: `PeoplePaneActionsTest` "bulk actions write an audit-log row per profile" (8/8 green).
- **F-20 🟠 fixed** — Profile-documents endpoints (`profileDocuments`, `storeForProfile`, `updateForProfile`, `destroyForProfile` in HrDocumentController) never asserted the route-bound profile belongs to the actor's tenant (dormant in this single-tenant deployment per the org-isolation decision, but inconsistent with sibling endpoints' defense-in-depth). Added `assertHrTenantAccess` to all four.
- **F-21 🟡 fixed** — `employees/show.tsx` compliance donut used 4 raw hex colours → `var(--status-*)`/`var(--muted-foreground)` (same defect class as slice 1's payslips/training).
- **F-22 🟡 fixed** — Finished the rating-star standardisation started in Run 0 (see F-4 reclassification): 6 remaining HR files switched `fill-amberx` → semantic `fill-status-warning` (`performance/reviews`, `performance/show-review`, `exit-interviews/index`, `candidates/show`, `candidates/create-offer`, `components/hr/exit-interview-wizards`). HR now has one star idiom; `amberx` remains the meal-planner token.
- **Verified clean** — chrome audit: PeopleHero passes 4A fully (all four KPI tiles deep-link, compliance tile escalates, counts server-side, single quick-action row); index/edit/panes pass 4B (HrTabs + server tabCounts, row context menus, designed empty states, en-NZ dates, no client-derived KPI lies, zero `confirm()`).

**Open items routed from Run 2:** 🟡 HrDocument has no SoftDeletes — destroy endpoints hard-delete file+row against the archive-not-delete house rule → slice 12 (needs a migration decision); 🟡 rehire doesn't check the previous stint was offboarded → slice 5; role-assignment guard + User-write auditability → **Decisions D-2/D-3** below.

**Run 2 gates:** types ✅ 0 errors · eslint touched files ✅ 0 errors (1 pre-existing warning, not my line) · vitest ✅ baseline 8 (first run showed 6 files failing while vite build ran concurrently — re-run solo reproduced the exact clean-HEAD baseline; don't run vitest concurrently with the build) · build ✅ exit 0 (4m31s) · pest full HR scope ✅ **694 passed / 0 failed** (693 + new bulk-audit regression test) · wayfinder n/a (no route changes).

## Run 3 findings (Slice 3 — Recruitment `/hr/recruitment`)

- **F-23 🔴 fixed** — `HrTalentPool` had no `AuditableChanges` trait: pooling a candidate (`addToPool` updateOrCreate), tag edits and requisition links were invisible in the audit log. Trait added (one line; verified against the model source — this claim was TRUE, unlike two sibling claims below).
- **F-24 🟠 fixed** — `CandidateController::bulkEmail` had no audit record at all (mail-only side effect, no model write → `AuditableChanges` can't fire). Added explicit `AuditLogger::log('recruitment.bulk_email', …)` capturing subject, candidate ids and sent/skipped/failed counts.
- **F-25 🟠 fixed** — two `window.prompt()`s in `recruitment/index.tsx` (bulk-tag label :297, offer decline-reason :642) — the same native-dialog disease as `confirm()`, which earlier sweeps missed (pattern now includes `prompt(`). Built a generic kit dialog `components/hr/recruitment/text-prompt-dialog.tsx` (mirrors BulkRejectDialog's composition) and wired both call sites; decline-reason stays optional.
- **F-26 🟡 fixed** — `candidates/show.tsx:1287` rendered `applied_at` raw → `formatNZDate()`.
- **F-27 🟡 fixed** — invalid class `bg-muted-foreground/80/10` (double opacity modifier — generates nothing, badge backgrounds silently transparent): swept ALL HR occurrences (11 files: approvals/chains, documents/templates, leave/show, my/goals ×2 — missed in Run 0, offboarding index+show, performance/skills, settings audit-log/automations/webhooks, candidates/create-offer, plus `lib/job-posting-constants.ts`). Non-HR remainder (sites/finance/settings, ~14 occurrences) spawned as task chip `task_d6e8ef52`.
- **False positives dismissed (verified against source):** `HrCandidateEmailTemplate` HAS `AuditableChanges` (line 13); tag rename/delete ARE audited (per-model `$candidate->update()` loop, not a mass update); "no offer-withdrawal endpoint" is wrong — `respondOffer` accepts `withdrawn`.
- **Verified clean:** hero/tiles/tabs/empty-states/en-NZ per chrome audit; all 10 pipeline stages reachable; rejection flow has reason + opt-in respectful decline email + pool option; HrJobPosting retirement is clean (no stragglers).

**Run 3 gates:** types ✅ 0 errors · eslint touched files ✅ 0 errors (1 pre-existing warning) · PHP -l ✅ · build ✅ exit 0 (3m42s) · vitest ✅ baseline 8 (run sequentially after build) · pest full HR scope ✅ **694 passed / 0 failed** · wayfinder n/a.

## Run 4 findings (Slice 4 — Onboarding `/hr/onboarding`)

- **F-28 §7.2 DEBUNKED** — the pre-seeded 🔴 "onboarding task lifecycle endpoints missing (handover-only)" is stale: routes/hr.php:414-428 carries the FULL lifecycle (task complete/uncomplete/update/destroy/provision-asset/store/reorder; checklist complete/remind/reassign/status/destroy/bulk), the hub UI wires them all via context menus + dialogs, and the employee self-service path (`MyHrController::completeOnboardingTask`) routes through the SAME `OnboardingService::completeTask` — one engine, no fork. The onboarding hub redesign (`4eee8aef`) built it. Closed.
- **F-29 🟠 fixed** — `reassignChecklist` silently handed ownership over (inline `created_by` update, no notification — the new owner is the waiting party). Added `OnboardingOwnerReassignedNotification` (owner-appropriate copy; the existing `OnboardingChecklistAssignedNotification` is addressed to the EMPLOYEE, so reusing it would send wrong copy), skipping self-reassign/no-op cases; best-effort try/catch mirrors sibling sends.
- **F-30 🟠 fixed** — `OnboardingService::reorderTasks` used mass query updates (`where('id')->update()`) — audit-invisible. Rather than N noisy per-task rows, records ONE `AuditLogger::log('onboardingchecklist.tasks_reordered', …)` summary entry with the final ordering (same pattern as Run 3's bulk_email).
- **F-31 🟠 fixed** — `AssetAssignment` (written by onboarding's provision-asset flow AND the assets module) had no `AuditableChanges` — issuing an asset to a person was audit-invisible. Trait added (cross-module model, one line; assignment history is exactly what the audit log should hold).
- **False positive dismissed** — "HrOnboardingEmail missing `updated_by`": the column doesn't exist in the table (adding it = schema change), AND the audit-log rows already record the actor (`AuditLogger` stores `user_id` on every trait-fired entry), so "who changed it" is already captured. No fix needed.
- **Verified clean** — chrome: every 4A/4B rubric passes (OnboardingHero deep-linked tiles with overdue escalation, HrTabs + server counts, row context menus, drawer quick-peek + full-page workspace, designed empty states everywhere, 6-step wizard with per-step validation, en-NZ helpers, no derived-KPI lies, tolerant of undefined mid-reload). Store/complete/remind/bulk endpoints all: permission + tenant assert + service + audit + flash; remind sends real `OnboardingTaskDueNotification`s.

**Run 4 gates:** PHP -l ✅ ×4 · pest full HR scope ✅ **694 passed / 0 failed** · types/lint/build/vitest **not run — PHP-only changes** (no TS/JS or route changes) · wayfinder n/a.

## Run 5 findings (Slice 5 — Offboarding + exit interviews)

- **F-32 🔴 fixed** — **Completed offboarding tasks were a dead end** (onboarding has uncomplete; offboarding didn't — a mis-clicked "Revoke system access" task was unfixable from the UI). Added `OnboardingService::uncompleteOffboardingTask` (reopens the task, reverts a completed checklist to in_progress, **never restores revoked access** — rehire owns restoration), route `hr.offboarding.tasks.uncomplete`, controller method, and a Reopen button replacing the disabled "Done" button on completed tasks.
- **F-33 🔴 fixed** — **An offboarding could never be cancelled** (retracted resignation = checklist stuck open forever; the model supports `cancelled`/`archived` but nothing set them). Added `setOffboardingChecklistStatus` (append-only, mirrors onboarding's), route `hr.offboarding.status` (validated `in_progress|cancelled|archived`), a Cancel-offboarding action with a proper AlertDialog explaining consequences ("system access is not changed"), and a Resume action on cancelled checklists.
- **F-34 🔴 fixed** — **Rehire ignored open offboardings**: `EmployeeIntakeService::rehire` restored login while a live leaver checklist (whose completion REVOKES login) stayed open — two contradictory workflows. Rehire now cancels open (pending/in_progress) offboarding checklists inside its transaction (per-model updates → audited).
- **F-35 🟠 fixed** — **Offboarding task assignment was silent** (onboarding notifies per task; offboarding generation sent nothing — assignees didn't know they had access-revocation/asset-recovery work). New `OffboardingTaskAssignedNotification` + post-commit notify loop in `generateOffboardingChecklist` (best-effort, mirrors onboarding).
- **F-36 🟡 fixed** — chrome: offboarding/show gains the `PageHero compact` header (mirrors exit-interviews/show — was the only detail page with no hero); `template_key` label now strips the `offboarding:` prefix colon on index+show.
- **Verified SAFE** — the login-revocation seam: automatic on checklist completion via `checkOffboardingChecklistCompletion` → `revokeSystemAccess()` → `User.approved_at = null` (gated by `EnsureAccountStillApproved`); webhook + owner notification fire on completion. Reopening tasks deliberately does NOT restore access.
- **False positive dismissed** — "offboarding index progress KPI lies across pagination": `progressPercent` is per-ROW from that row's own task counts, not an aggregate — nothing lies.
- **Exit interviews** — chrome green (wizard, PageHero, label maps, server KPIs, en-NZ). Workflow items ledgered on the row: store-once immutability (D-5), fragile title-match task link, no interviewer notification.

**Run 5 gates:** types ✅ 0 errors · eslint touched files ✅ · PHP -l ✅ ×4 · wayfinder ✅ regenerated (2 new routes verified in route:list) · build ✅ exit 0 (3m43s) · vitest ✅ baseline 8 · pest full HR scope ✅ **694 passed / 0 failed**. ⚠️Ops note: mid-run the PARENT repo's node_modules broke (missing `.bin` + `@babel/core` — concurrent activity in the parent), killing junction-based builds; fixed by unlinking the junction (link-only rmdir) and `npm ci` in the worktree — the worktree now owns its node_modules (pattern already in the worktree-vendor memory).

## Run 6 findings (Slice 6 — Calendar hub `/hr/calendar`)

- **F-37 🟠 fixed** — **Invitees were never told they were invited**, making the whole RSVP feature unreachable (you can't respond to an invite you never saw; `store()`/`update()` synced person-attendees silently, and no calendar notification class existed). New `CalendarEventInviteNotification`; `syncAttendees` now returns the NEWLY-added user ids so re-syncs don't re-invite, and both call sites notify (skipping the organiser, best-effort try/catch).
- **F-38 🟠 fixed** — **The organiser never learned of RSVP responses.** New `CalendarEventRsvpNotification` (database-channel only — RSVPs are frequent; no mail spam) sent to the event creator when someone else responds.
- **F-39 🟠 fixed** — **Deleting an event leaked its attachment files**: the FK cascade removes attachment rows but not files. `destroy()` now deletes each attachment's file first (using the model's real `disk`/`path` fields — first attempt used wrong field names and would have silently no-oped; caught in review).
- **F-40 🟡 fixed** — `HrCalendarEventAttachment` had no `AuditableChanges` (the event has it; HrDocument's attachments do) — uploads/deletes now audit-logged.
- **Verified clean** — hero stats deep-link with server-side counts; 3 tabs + 6 layers URL-synced; event wizard; iCal export reachable (token-based, tenant-scoped, 500-event cap); `/feed` respects canView + `seeSensitive` redaction; every controller method self-gates (route middleware absence is not a hole); en-NZ throughout.
- **Stale seed corrected** — `/hr/calendar/time-off` doesn't exist anywhere; leave is an aggregator layer on the single calendar.

**Run 6 gates:** PHP -l ✅ ×4 · pest full HR scope ✅ **no NEW failures** — 693 passed / 1 failed, and the 1 (`ShiftPayrollBackboneIntegrationTest > attendance generated shift timesheet flows into payroll`) reproduces at clean HEAD via stash: it's the brief's documented **date-env** flake, triggered now that the session crossed into 2026-07-08 (it passed all five runs dated 2026-07-07). Baseline note: this test is date-dependent — always stash-verify before blaming a diff. · types/lint/build/vitest **not run — PHP-only changes** · wayfinder n/a.

## Run 7 findings (Slice 7 — Leave hub `/hr/leave/*`)

- **F-41 🟠 fixed** — **Managers couldn't cancel anyone's leave**: the only cancel route was owner-gated self-cancel, so an approved request for a hospitalised staff member (or a roster change) was permanent unless the employee did it themselves. New `LeaveController::cancel` (`hr.leave.manage`-gated, `POST /hr/leave/{id}/cancel`), reusing `LeaveService::cancelRequest` — which already reverses balance+ledger, removes the roster projection, and (via Run 0's admin-cancel branch, which finally has a caller) notifies the employee. Hub context menu gains "Cancel request…" on pending/approved rows with a status-aware AlertDialog; webhook `leave.request.cancelled` published.
- **F-42 🟠 fixed** — `LeaveService::syncApprovedProjection` updated the `StaffTimeOff` roster projection via a mass query update — audit-invisible (the recurring disease). Now load-modify-save so the projection's own change is logged alongside the request edit.
- **F-43 🟡 fixed** — **Public-holiday deletion had no guard**: hours are engine-calculated around holidays, so deleting one spanned by live (pending/approved) requests silently invalidated those counts. `destroy` now blocks with an explanatory error naming the overlap count; editing stays allowed.
- **F-44 🟡 fixed** — **Balance adjustments were silent**: crediting/debiting someone's statutory entitlement notified nobody. New `LeaveBalanceAdjustedNotification` (delta, new balance, reason) sent to the employee after commit, skipping self-adjustments. `adjustBalance` returns unchanged (transaction now also yields the delta internally).
- **F-45 🟡 fixed (chrome)** — reports.tsx absence-trend hex `#b42318` ×3 → `var(--status-critical)`; `toLocaleString()` with no locale ×2 in show.tsx → `'en-NZ'`; balances ledger raw ISO timestamp → formatted en-NZ; Bradford/utilisation "top 8" now labelled ("Top 8 shown; export for the full list" — the server DOES send full datasets, so the agent's "lying KPI" claim was a false positive; only the label was missing).
- **Verified clean** — all balance writes transactional with immutable ledger rows; bulk approve/decline per-row (service+notify+audit each, one failure doesn't abort the batch); CSV/PDF/Excel exports formula-neutralised; escalation endpoint + 30-min scheduled job; status reachability complete now cancel exists; hours-based display everywhere (Employment Leave Bill ✓); LeaveHubHero tiles all deep-link with SLA escalation chips.

**Run 7 gates:** types ✅ 0 errors · eslint touched files ✅ 0 NEW (1 pre-existing `react-hooks/refs` in balances.tsx verified at clean HEAD via stash) · PHP -l ✅ ×4 · wayfinder ✅ (new `hr.leave.cancel` route verified) · build ✅ exit 0 (3m38s) · vitest ✅ baseline 8 · pest ✅ **no NEW failures** (693/1 — the 1 is the date-env ShiftPayroll flake, stash-proven pre-existing this morning). Non-HR class-sweep chip (task_d6e8ef52) launched by Chane in its own session.

## Run 8 findings (Slice 8 — Time hub `/hr/time`)

- **F-46 🔴 fixed** — **No payroll-period lock on entry mutations**: `editTimeEntry`/`correctMissedClockOut`/`voidEntry` only checked the ENTRY's own status, but timesheet approval never flips entries to `approved` — so an entry whose timesheet was approved and consumed by a locked/exported payroll run could still be amended, silently desyncing pay from the record. New `assertEntryNotPayrollLocked` (mirrors `TimesheetApprovalService::assertNotLockedByPayroll` at entry level: blocks when the linked timesheet is payroll-linked OR the entry date falls inside a locked/exported `HrPayrollRun`), called by all three mutators.
- **F-47 🟠 fixed** — **Pay-affecting changes were silent**: clock-on-behalf, amend, correct and void never told the entry's owner. New `TimeEntryChangedNotification` (action-aware copy + reason, deep-links `/hr/my/time`) sent post-commit from all four paths via a shared `notifyEntryOwner` helper — skipped for self-edits, and the amend path only notifies when the diff was non-empty (same-instance return detection).
- **F-48 🟡 fixed** — **Dead "Rejected" filter on the entries list**: `rejected` is an Operations-timesheet status; `HrTimeEntry` never holds it, so the filter always returned nothing. Swapped to "Voided" — and made it WORK: voided entries are soft-deleted, so the controller's status filter now applies `withTrashed()` for `voided` (otherwise the replacement option would have been exactly as dead — caught in self-review).
- **False positives dismissed (verified against source):** "activeClock not team-scoped" — it's `->forUser($user->id)` (self-only by design); "onNow/tab scope not validated" — `applyAccessScope` is applied server-side before serialization.
- **Verified clean** — S1 holds (clockOnBehalf routes through the one `AttendanceService`, double-clock guarded); append-only `HrTimeEntryAmendment` trail with field-level diffs surfaced in the drawer; void is soft-delete + status with mandatory reason; CSV (`putCsv` sanitizer) + PDF exports safe; chrome passes 4A/4B (deep-linked hero, exception chips, wizard flows, honest Operations banner).

**Run 8 gates:** PHP -l ✅ ×3 · types ✅ 0 errors · eslint touched files ✅ · wayfinder n/a (no route changes) · build ✅ exit 0 (3m20s) · vitest ✅ baseline 8 · pest ✅ **no NEW failures** (693/1 — the identical date-env ShiftPayroll failure from Runs 6–7, stash-proven at clean HEAD the same day before these time-hub changes existed).

## Run 9 findings (Slice 9 — Compensation `/hr/compensation/*`)

- **F-49 🔴 fixed** — **Bonuses had no way out**: `HrBonusPayment` supports `cancelled` but NOTHING could set it (the Drills disease) — a mistaken or declined bonus sat pending/approved forever. New `cancel` route + controller method (manage-gated, tenant-asserted, guarded to pending/approved-unpaid) + Cancel affordance with status-aware AlertDialog in bonuses.tsx.
- **F-50 🟠 fixed** — **Pay events were silent**: bonus approval never told the recipient, and applying a compensation review changed salaries with zero notice to the affected employees (statutory expectation of pay-change notice). New `BonusStatusNotification` (approved/cancelled-after-approval; pending-cancel stays quiet — they never knew) + `CompensationAppliedNotification` (new annual, effective date, % change) sent post-commit per applied item.
- **F-51 🟠 fixed** — Tenant guards (defense-in-depth per the single-tenant/org-isolation decision, matching slice-2's idiom): bonus `store` validated `employee_profile_id` with a bare `exists:` (any org's profile) → tenant-scoped lookup + 422; bonus `approve` had no tenant assert → added; benefits `enroll` looked up profile AND plan unscoped → both tenant-scoped.
- **F-52 🟡 fixed** — `bonus_type` rendered via `.replace('_',' ')` → proper `BONUS_TYPES` label map (with fallback).
- **False positives dismissed:** "Amount ($) label is USD" — NZD uses the $ symbol; normal NZ copy. "tabCounts missing from 6 pages" — all 7 controller render sites pass `tabCounts` and `CompensationTabs` reads it straight from page props (the agent looked for prop drilling that isn't needed). "pay endpoint GL issue" — self-retracted by the agent (approve posts GL; pay is status-only, correctly guarded on `gl_posted_at`).
- **Verified green:** GL double-post protection held (memory said FIXED — confirmed: dispatch only when `journal_id === null` + job-level recheck); review-apply idempotent via status guard inside the transaction; benefits enrolment lifecycle (active/opted_out/suspended/terminated) fully reachable and already notifying on material changes; bulk expense approval is per-row service calls; CompensationHero/tabs/wizards/empty-states all pass chrome.

**Run 9 gates:** PHP -l ✅ ×5 · types ✅ 0 errors · eslint ✅ · wayfinder ✅ (`hr.compensation.bonuses.cancel` verified) · build/vitest/pest: see run log.

## Run 10 findings (Slice 10 — Payroll + payslips `/hr/payroll/*`)

- **F-53 🟠 fixed** — **Hero KPI tiles lied + were dead** on BOTH `/hr/payroll` and `/hr/payroll/payslips`. Drafts/Locked/Exported (and Drafts/Approved/Paid) were tallied client-side from `runs.data`/`payslips.data` — only the current 20-row page — so every count was wrong past page 1, and no tile deep-linked despite the `?status=` filters existing. Now computed server-side across the whole tenant (grouped-count query in each controller) and every tile deep-links to its filtered list (`PageHero` stats already support `href`). Unfiltered `total` also comes from the count query so the "Total" tile stops reflecting the filtered paginator.
- **F-54 🟠 fixed** — **Dead `approved` payslip status surfaced in the UI.** Migration documents `draft → approved → paid`, but NOTHING in the codebase sets `approved` (the approval step was never built — the Drills disease; only `PayslipService` sets `draft` and `PayrollJournalService` sets `paid`). Removed the dead "Approved" hero tile and the "Approved" filter option; payslips now honestly present Draft/Paid only (statusConfig entry kept as a harmless fallback for any legacy row).
- **F-55 🟠 fixed** — **No payslip-available notification.** Employees were never told a payslip was ready to view at `/my/payslips` (the support-worker persona's whole HR world). New `PayslipAvailableNotification` (database + mail, deep-links the specific payslip); sent post-commit from `PayslipController::generate` (explicit Generate) and from `PayrollExportController::lockRun` when lock auto-generates — deduped per employee, and lock only notifies when payslips didn't already exist (so an admin who generated + notified isn't double-fired). Best-effort try/catch + `Log::warning`, mirroring the house notification pattern.
- **F-56 🟠 fixed** — **Payslip tenant bugs.** `PayslipController::index` wasn't tenant-scoped at all (list + employee dropdown + counts), and `generate`'s "all active employees" branch filtered on `$user->tenant_id` — NULL, since users have no `tenant_id` column — so it generated for the wrong/empty set. Both now resolve via `resolveHrTenantIdForUser`; `generate` also tenant-scopes the run/profile lookups (bare `exists:`/`findOrFail` accepted any org's record).
- **False positives / non-issues:** "Amount labels are USD" — NZD uses `$`. "Export profile default-demote is audit-invisible" — true (mass `update(is_default=false)`), but the promoted profile's own `update()` IS audited so the material fact (which profile is default) is captured; a per-row loop isn't worth it. "`markRunTimesheetsPaid` bypasses the workflow guard" — deliberate and documented (legacy-stamped rows only, via `saveQuietly`; new rows go through `update()` WITH the guard).
- **Verified green (S9 seam — Payroll ↔ Time/Leave, code-level):** approved timesheets aggregate into run items; approved non-`unpaid` leave feeds run items in **hours** (Employment Leave Bill), pro-rated across the pay period; lock cascades linked timesheets approved→paid idempotently; GL post guarded on `journal_id === null`; net-pay guarded on `net_paid_at`; validation blocks locking an inconsistent run. **S9 still owes runtime proof** (do-not-mark-✅-from-code-reading) — left as a seam row.

**Run 10 gates:** PHP -l ✅ ×4 · types ✅ 0 · eslint ✅ (both pages) · build ✅ exit 0 · vitest ✅ baseline 8 fail/171 pass (no new) · pest ✅ **698 passed / 0 failed** (HR suite + `Finance\PayrollJournalPostingTest` + `Finance\PaydayFilingTest`; the date-env `ShiftPayrollBackboneIntegrationTest` passed this run). No routes changed → wayfinder skipped.

## Run 11 findings (Slice 11 — Compliance + Vetting + Drivers)

- **§7.1 pre-seeded finding DEBUNKED** — "Vetting + Training hubs on generic `PageHero`". Vetting (and Drivers) are tabs *inside* the already-redesigned Compliance hub (`/hr/compliance/vetting`, `/hr/compliance/drivers`) and already render the shared `ComplianceHubHeader` (golden hero band + tab strip), not a generic `PageHero`. Training is a separate hub (Slice 15) — its status is untouched here.
- **F-57 🟠 fixed** — **Hub-header cross-domain quick-actions ignored permissions across the whole Compliance hub.** `ComplianceHubHeader` renders "Record compliance"/"Add requirement" (needs `hr.compliance.manage`), "Add vetting check" (`hr.vetting.manage`), "Add driver" (`hr.driver.manage`), each gated on a `can` flag. But **4 of 5 hub tabs hardcoded those flags to `true`**: Vetting `{manage: true, …, driver: true}`, Drivers `{manage: true, vetting: true, …}`, Matrix & Calendar `{…, vetting: true, driver: true}`. So e.g. a vetting-only manager saw "Record compliance"/"Add requirement"/"Add driver" and got a 403 on submit (the wizard just POSTs to a `.manage`-gated endpoint). The 5th tab (Overview) was subtler — it gated the header's *create* actions on `.view` (reusing the field it correctly uses for the "View vetting" *nav* link). **Fix:** each of the 5 hub controllers (`Compliance`, `ComplianceMatrix`, `ComplianceCalendar`, `Vetting`, `DriverEligibility`) now emits explicit `*_manage` perms, and every page passes the header the *real* `.manage` permission for each create action. The header now renders identically and permission-correctly across all 5 tabs; the Overview row-menu "View vetting" link keeps its `.view` gate.
- **False positives / non-issues:** hand-rolled `<table>` in vetting/drivers — the whole compliance hub deliberately uses the shared `compliance-bits` table + `ComplianceContextMenu` (row context menu + row flags + detail links); consistent within the hub, not a `PageHero`/DataTable violation. Second stat-tile row below the hero — hub-wide vs tab-specific breakdown, an intentional hub pattern. Driver suspend has no HR notification — by design the S3/S6 compliance-matrix hard-stop (refreshed via `reevaluateCompliance`) is the mechanism; Control Room/rostering owns triage (§2).
- **Verified green:** vetting statuses (pending/requested/clear/flagged/adverse/renewal_due) all reachable (edit form + clear/renew); driver states (pending_review/eligible/suspended + computed expired) reachable via create/approve/suspend; every write tenant-guarded (`assertStaffCheckTenantAccess` / `assertHrTenantAccess`); consent capture appends an audit note; driver approve/suspend mirror the profile flag AND re-evaluate compliance so the rostering hard-stop is immediate.

**Run 11 gates:** PHP -l ✅ ×5 controllers · types ✅ 0 · eslint ✅ 0 errors (pre-existing raw-div/button warnings only, untouched lines) · build ✅ exit 0 · vitest ✅ baseline 8 fail/171 pass (no new) · pest ✅ **694 passed / 0 failed**. No routes changed → wayfinder skipped.

## Run 12 findings (Slice 12 — Documents `/hr/documents/*`)

- **Carry-over re-attestation-dedupe DEBUNKED** — the ledger row warned that policy re-attestation had "no duplicate guard". It does: `PolicyAttestationController@store` looks up an existing attestation on `(user_id, policy_id, currentVersion->id)` and returns a validation error if found (lines 74–81). Same-version re-attestation is blocked; a NEW published version resets the requirement (the attestation is version-scoped). No fix needed. (If Chane wants a *periodic* re-attest cooldown independent of versioning, that's a new feature — logged as a possible future decision, not an open bug.)
- **F-58 🟠 fixed** — **Four native browser dialogs in the redesigned Documents hub.** `documents/index.tsx` used `window.confirm()` for bulk-delete, row-delete and cancel-signature-request, and `window.prompt()` for move-to-folder — jarring against the hub's bespoke command surface, and §6 rule (b) explicitly lists `confirm()`→`AlertDialog` as a fix-now standardisation. Replaced all four with kit dialogs: `LibraryTab` owns one `AlertDialog` (serving the bulk-delete AND, via a threaded `requestConfirm` setter, `buildRowMenu`'s row-delete) plus the kit `TextPromptDialog` for the folder move; `SignaturesTab` owns one `AlertDialog` serving every `SigCard`'s cancel-request. Each confirm now carries a real title + explanatory body ("Signers will no longer be able to sign") instead of a terse one-liner. A shared `ConfirmRequest` type keeps the payload consistent. `tsc` + eslint clean (file already opts out of `no-restricted-syntax` for its raw command surface).
- **Verified green:** policy attestation records IP + user-agent + version + method and is tenant-guarded; policy versions have store/destroy endpoints; the documents library's store/generate/move/bulk-delete/bulk-download all go through `HrDocumentController` with tenant scoping (fixed back in slice 2); signature nudge/resend/cancel all reachable from the sender-side cards.

**Run 12 gates:** types ✅ 0 · eslint ✅ (0 problems on the touched file) · build ✅ exit 0 · vitest ✅ baseline 8 fail/171 pass (no new) · pest ✅ **694 passed / 0 failed**. No routes/PHP changed → wayfinder + php-lint N/A.

## Run 13 findings (Slice 13 — Performance `/hr/performance/*`)

- **F-59 🟠 fixed** — **The performance-review acknowledge loop had missing + inconsistent notifications.** (a) When a manager **signs off** a review, the employee — who must then acknowledge it — got no notification at all. (b) `HrNotificationService::notifyReviewSignedOff` (reviewer-facing) was only wired into the My HR self-service acknowledge path (`MyHrController`), so acknowledging via the performance **hub** route (`/hr/performance/reviews/{review}/acknowledge`, which the subject OR a manager can call) was silent. Fix: new `ReviewReadyForAcknowledgementNotification` + `notifyReviewReadyForAcknowledgement` service method → the employee on sign-off; hub `acknowledge()` now also calls `notifyReviewSignedOff` (guarded to the first ack). The loop now notifies on both transitions and both acknowledge paths.
- **F-60 🟠 fixed** — **1:1 / supervision acknowledge sent no notification to the supervisor** (the exact row-13 carry-over). New `SupervisionAcknowledgedNotification` + `notifySupervisionAcknowledged` service method, wired into `SupervisionController::acknowledge` (guarded to the first ack, skips the self-supervised edge). Mirrors the review pattern.
- **Verified green:** review statuses reachable (draft→in_progress via submit; →signed_off via sign-off/approve; return→in_progress; employee_signed_off via acknowledge); a signed-off review is edit-locked; evidence upload/stream is hardened (private disk, mime allowlist); the "Next steps" affordance only surfaces a prefillable PIP/succession CTA for signed-off reviews with an outcome and never auto-creates; probation `extend` actually moves `probation_end_date` and clears the reminder stamp; every review/supervision/probation mutation is tenant-guarded.
- **Deferred (Performance has a redesign prompt):** the reported "supervision wizard hardcodes `session_type`" and the orphaned `supervision-dialog.tsx` are wizard/dead-code items the redesign owns → `↗ PERFORMANCE_REDESIGN_PROMPT.md`. Smaller open follow-up: supervision-note *creation* visible to the employee doesn't nudge them to acknowledge (the mirror of F-59a) — same clean pattern, noted for a later pass.

**Run 13 gates:** PHP -l ✅ ×5 (2 new notifications + service + 2 controllers) · pest ✅ **694 passed / 0 failed**. Frontend gates N/A — zero `.ts/.tsx` files changed (build/vitest byte-identical to Slice 12's green run).

## Run 14 findings (Slice 14 — Goals + Development `/hr/goals*`)

- **F-61 🟠 fixed (O-4)** — **Development-goal completion notified no one.** The store path already tells the employee on assignment (`DevelopmentGoalAssignedNotification`), but completing a dev goal was silent, so the manager who set it never learned it was achieved. There are TWO completion paths — the manager hub (`DevelopmentGoalController::update`) and the employee's self-service (`MyHrController::updateGoal`, which operates on `HrDevelopmentGoal`) — and both were silent. Fix: new `DevelopmentGoalCompletedNotification` + a shared `HrNotificationService::notifyDevelopmentGoalCompleted($goal, $actingUserId)` (recipient = `manager_user_id ?: created_by`, skips the self-completed edge, best-effort try/catch, database+mail), wired into both paths on the fresh completion transition (guarded by a pre-update `$wasCompleted` so re-saving a done goal doesn't re-notify).
- **Open (deferred to `GOALS_REDESIGN_PROMPT.md`):** the OKR counterpart `HrNotificationService::notifyGoalCompleted(HrGoal)` **exists but is never called** (dead notification — the Drills disease). Unlike dev-goals, the OKR owner completes via `updateProgress`/`checkin`/self-service rather than the manage-only `GoalController::update`, so wiring it to the correct hook (and guarding manager-self-notify) needs more tracing than a single-slice standardisation fix warrants. Recorded so a GOALS-redesign pass activates it.
- **Verified green:** dev-goal owner-vs-manager field gates hold (`prohibited` validation on manager-only fields when the owner edits); completion sets `completed_at`/100%/clears `next_review_at`; every mutation tenant-guarded; assign→employee notification confirmed by `DevelopmentGoalWorkflowTest`.

**Run 14 gates:** PHP -l ✅ ×4 (new notification + service + 2 controllers) · pest ✅ **694 passed / 0 failed**. Frontend gates N/A — zero `.ts/.tsx` files changed.

## Run 15 findings (Slice 15 — Training + catalog `/hr/training/*`)

- **§7.1 pre-seeded finding RECLASSIFIED** — "Training on generic PageHero" is inaccurate. `catalog.tsx` renders a fully hand-rolled bespoke hero (custom gradient `<div>`, local `HeroBtn`, hand-rolled clickable stat buttons that switch tabs) — the shipped Training redesign, the same bespoke-command-surface pattern as the Documents hub. It's neither `PageHero` nor a kit `*Hero`. Building a kit `TrainingHero` would compete with `HR_TRAINING_REDESIGN_PROMPT.md`, so it's deferred there (the guardrail's "no hand-rolled hero elements" yields to the §6 redesign-prompt rule, exactly as Documents did).
- **F-62 🟠 fixed (O-2)** — **Dead "My training to complete" cards on `/hr/my/training`.** The assigned-course and in-progress-enrolment cards displayed the course title but weren't clickable, and `MyHrController::training` never included `course_id` in the payload — so a support worker couldn't open an assigned course from their to-do list (only a generic "Browse training courses" link existed, against the persona's "≤2 clicks to start"). Fix: added `course_id` to both the assignment and enrolment payloads, and wrapped each card in an Inertia `Link` to `/hr/training/courses/{course_id}` (which redirects into the catalog with the course slide-over open). Gated on the existing `can.viewCatalog` — a worker without catalog access still sees a plain, non-linked card (graceful degrade, no dead 403 link).
- **Verified green:** every catalog mutation (course create/update/toggle/bulk-archive, session CRUD, enrol, assignments store/preview/remind/waive, completion record) is permission-gated; my/training's compliance panel reads the S7 `hr_staff_compliance_status` projection (training completion → compliance is a service concern, out of this slice's page/deep-link scope).

**Run 15 gates:** PHP -l ✅ · types ✅ 0 · eslint ✅ (my/training.tsx) · build ✅ exit 0 · vitest ✅ baseline 8 fail/171 pass (no new) · pest ✅ **694 passed / 0 failed**. No routes changed → wayfinder skipped.

## Run 16 findings (Slice 16 — Feed `/hr/feed`)

- **§7.3 pre-seeded finding CONFIRMED STALE** — "Feed wall lacks reactions/comments". It has both, fully. `parts.tsx` ships a shared `ReactionBar` (heart/party/hands reaction toggles, a reply count, the reply thread, and an inline reply box) reused by every wall card; the routes wire `react`/`reply` for kudos (`/hr/feed/kudos/{k}/react|reply`) and polymorphic `reactFeed`/`replyFeed` for posts + announcements. The Feed redesign (`7f675156`) already delivered the whole social layer.
- **F-63 🟠 fixed** — **Replies (comments) notified no one.** The only notification in `FeedService` was `KudosReceivedNotification` (fired on kudos send). Replying to your kudos or your feed post produced no signal to the author — a 4C "waiting party" gap on a surface whose whole point is peer interaction. Fix: new **database-only** `FeedReplyNotification` (the header bell — a social wall must not email on every reply) + a shared `FeedService::notifyReply()` helper (skips self-reply and missing users, best-effort try/catch) wired into `addReply` (notifies the other of the two kudos-thread parties) and `addFeedReply` (notifies a `post`'s author via `HrFeedPost.user_id`).
- **Deliberately not notified:** reactions (`toggleReaction`/`toggleFeedReaction`) stay ambient — notifying on every heart/party emoji would spam the bell (matches the "database-only for high-frequency / skip for ambient" pattern). Announcement replies aren't notified yet — announcements are org-wide and whether/whom to notify on a reply is a product question (follow-up).
- **Verified green:** kudos send notifies the recipient (`sendKudosToMany` → `KudosReceivedNotification`); every react/reply is tenant-guarded (`assertHrTenantAccess` / `assertFeedSubjectInTenant`); kudos reply is gated to the two thread participants; the wall's audience scoping (org-wide + own + site-targeted) holds; moderation delete is gated on `hr.employees.manage`.

**Run 16 gates:** PHP -l ✅ ×2 (new notification + service) · pest ✅ **694 passed / 0 failed**. Frontend gates N/A — zero `.ts/.tsx` files changed.

## Run 17 findings (Slice 17 — Announcements `/hr/announcements` + seam S10)

- **Slice audited genuinely CLEAN — no code fix (sanctioned "clean slice" outcome).** Announcements is one of the most complete HR surfaces (4 test files, a scheduled-publish job, the bell bridge, the audience resolver). Every rubric passes:
  - **4A hero:** bespoke redesigned hero (local `HeroStat`) with **deep-linked, amber-toned stat tiles** (Live / Pinned / Scheduled / "You owe reminders") — the shipped Announcements redesign, not `PageHero`.
  - **4B:** no native `confirm()`/`prompt()`; tab-driven filters (`go({tab})`); designed empty states; soft-delete + `restore` (no D-4 hard-delete).
  - **4C:** statuses draft → scheduled → published all reachable — the wizard sets status, `publishNow` is the manual path, and `PublishDueAnnouncementsJob` is the auto path; publish + reminders notify the waiting party (recipients), reminders carry a cooldown; `bulk`/`remindBulk` iterate per-model (audit-visible, not the mass-update disease).
- **Hypothesised scheduled-publish dead-end DEBUNKED.** I suspected scheduled announcements never delivered (no bell/notification when `published_at` passes). Not so: `app/Domain/Hr/Jobs/PublishDueAnnouncementsJob` flips `status='scheduled'`→`'published'`, fires `bridge->publish` + `AnnouncementPublishedNotification`; it is scheduled in `routes/console.php:190` and covered by `AnnouncementCommandCenterTest` (invokes `handle`, asserts published + notified).
- **Seam S10 verified end-to-end (test-backed, per the guardrail).** See the S10 row — bridge idempotency (FK), publish/withdraw on every lifecycle edge, audience-scoped delivery, and the header-bell mirror all proven by `AnnouncementCommandCenterTest` + `AnnouncementAndOnboardingNotificationTest`. The one documented limitation (non-role segments collapse to "all roles" on the bell mirror) is agreed single-tenant behaviour, not a bug.

**Run 17 gates:** none run — **zero code changed** (ledger-only). Audit verified against source + existing green tests (the HR suite already passes at 694/0 as of Run 16).

## Run 18 findings (Slice 18 — Feedback + surveys `/hr/feedback`)

- **F-64 🟠 fixed** — **360-feedback requests notified no reviewer.** `FeedbackService::request360Feedback` created the pending `HrFeedbackRequest` rows but never told the reviewers; only `remind` sent a notification (`FeedbackReminderNotification`) — so a reviewer could be *reminded* about a request they were never told about. Fix: new database-only `FeedbackRequestedNotification` (mirrors the reminder's channel + `/hr/feedback/{id}/respond` deep-link) fired to each reviewer post-commit. Covers both `storeRequest` (single) and `bulkRequest` (fan-out) — both route through `request360Feedback`. Feedback has **no redesign prompt**, so this is a fix-now, not a defer.
- **Retired-module note:** the "surveys" in this row are the standalone `HrSurvey` module, retired in S11 — `/hr/surveys/*` routes 302 to `/hr/wellbeing`; the live engagement-survey system lives under Wellbeing (audited in slice 19). Nothing to fix here.
- **Verified green:** request statuses pending → completed (`submitFeedback`) / declined (`decline`) / expired (`cancel`) all reachable and 422-guarded to `pending`; respond/submit are reviewer-only (`reviewer_user_id === user->id`); `summary` gated on `hr.performance.view`; template CRUD tenant-scoped; no native `confirm()`/`prompt()`.
- **Open (deferred — no redesign prompt, hero-build is bigger than a smallest-honest-fix):** `feedback/index.tsx` renders generic `PageHero`, not a specialised kit hero — a real 4A "hub on `PageHero`" finding (the DoD wants zero). A `FeedbackHero` (mirroring `CompensationHero`, server-fed deep-linked tiles) is a standalone follow-up; noted, not rebuilt this slice.

**Run 18 gates:** PHP -l ✅ ×2 (new notification + service) · pest ✅ **694 passed / 0 failed**. Frontend gates N/A — zero `.ts/.tsx` files changed.

## Run 19 findings (Slice 19 — Wellbeing `/hr/wellbeing`)

- **§7.5 pre-seeded finding is STALE — debunked with source.** "`flaggedStaff` is read-only with no follow-up action path" is no longer true: the duty-of-care loop is wired end-to-end. Routes `hr.wellbeing.signals.{acknowledge,snooze,dismiss,undo}` + `eap-referrals` + `checkins` + `action-plans.*` all exist; `index.tsx` `flagMenu` (line 446) offers Log check-in / Create action plan / Refer to EAP / Acknowledge / Snooze… / Dismiss…; `doTriage` POSTs to `/signals/{user}/{action}` with a toast-Undo; the triage modal captures a snooze-until date and a dismiss reason. Controller `storeFlagAction` is live (policy `canDo('hr.performance.manage')` → tenant assertion → `WellbeingCareService::recordFlagAction`), **not** the Drills disease. Every flag action is persisted as an append-only `HrWellbeingFlagAction` row (actor + action + reason + timestamp) — the loop is self-auditing. Mirrors the Announcements/Feed/Training pre-seeded findings that were also already built (~⅓ of pre-seeded findings this session proved stale).
- **F-65 🟠 fixed — action-plan owner never told they'd been assigned.** The one real §4C gap in the loop: both plan-create paths (`storeStandaloneActionPlan`→`WellbeingCareService::createStandalonePlan`, and survey-linked `storeActionPlan`) set `owner_user_id` and write a system note, but sent the owner **no notification**. HR already has a `*Assigned` idiom everywhere (Onboarding/Goal/Asset/Training) and Wellbeing had only `EngagementActionPlanDueNotification` (a *due* reminder, cron-fired) — so an owner assigned a duty-of-care follow-up with no/distant due date heard nothing until it was (nearly) overdue, or never. Fix: new `EngagementActionPlanAssignedNotification` (database+mail, ShouldQueue, mirrors the Due sibling's `toArray`/`toMail` shape + `/hr/wellbeing` deep-link) fired via new best-effort `WellbeingCareService::notifyOwnerAssigned($plan, $actor)` — skip-self, wired into **both** create paths. Squarely §7.5's "link out" (the follow-up now reaches its owner); additive, no schema/permission change; the redesign would keep it, so it clears the §6 defer bar as a small standardisation fix.
- **Verified green:** flag triage (acknowledge/snooze/dismiss/undo), EAP referral (consent-gated, confidential), check-ins (private vs staff-acknowledgeable), action-plan lifecycle (open→in_progress→completed→reopen/cancel) all reachable from the UI and policy-checked; no native `confirm()`/`prompt()` in `index.tsx` (custom modal system).
- **Deferred ↗ WELLBEING_REDESIGN_PROMPT.md (out of smallest-honest-fix scope):**
  - `undoLastFlagAction` **hard-deletes** the most recent `HrWellbeingFlagAction` (no time bound, no reversal-row) — a duty-of-care record erased without trace. Fine as an immediate misclick-undo (only the acknowledge toast surfaces it), but the endpoint itself is unguarded. Soft-delete/reversal-row is a schema change → redesign scope; logged, not fixed.
  - Non-private check-ins are staff-acknowledgeable (`acknowledgeCheckin` gated on `!is_private`) but the staff member is **never notified** one was logged — the acknowledge affordance has no trigger to reach it. A second real notification gap; deferred to the redesign to keep one-fix-per-slice (would be a second notification build).
  - The page renders a bespoke `ManagerHero`/inline chrome, not a kit `*Hero` — a 4A finding owned by the redesign prompt.

**Run 19 gates:** PHP -l ✅ ×3 (new notification + service + controller) · pest ✅ **694 passed / 0 failed** (4292 assertions, 783s) — matches the Run 18 baseline; the action-plan-create test uses owner==actor, so skip-self makes the change transparent to it. Frontend gates N/A — zero `.ts/.tsx` files changed.

## Run 20 findings (Slice 20 — Cases `/hr/cases`)

- **F-66 🟠 fixed — `HrCaseUpdateNotification` was a dead class; the case assignee was never told.** The notification exists (database-only, deep-links `/hr/cases/{id}`, ctor `(HrCase, eventType)`) but grep proved **zero callers** app-wide — so opening or reassigning a grievance/disciplinary/investigation set `assigned_to` silently and the new owner (the waiting party) heard nothing. Same class of gap as onboarding F-29 (silent ownership hand-over). Fix: new best-effort `HrCaseController::notifyCaseAssignee()` fires the existing class on `store` (`'assigned'`, when `assigned_to` is set) and on `update` (`'reassigned'`, only when `assigned_to` moves to a *different* non-null person) — skip-self, try/catch + `Log::warning`. The assignee is always an authorised viewer (`canViewCase` includes `assigned_to`), so this can never leak a confidential case. Reuses the ready-made class + existing perms; no schema/permission change.
- **Verified green:** `HrCase` uses `AuditableChanges` → case create/update/close land in `/hr/settings/audit-log` (no audit gap). `create`/`createEvent` GET routes are **not** dead — they redirect into the index New-case wizard (`?new=1`) and show Add-event wizard (`?new=event`) respectively (comment-documented). Index KPIs (`open_cases`, `unassigned`, `high_severity`, disciplinary SLA overdue/due-24h/missing-deadline/escalation) are all server-side from the controller. Confidential-case visibility is enforced both row-side (`applyCaseVisibilityScope`) and record-side (`canViewCase`), plus per-event visibility (`internal`/`restricted`/`full`). No native `confirm()`/`prompt()`/`window.*` in `index.tsx` or `show.tsx` (custom wizard modals). Case status enum (`open`/`under_investigation`/`awaiting_response`/`resolved`/`closed`) all reachable via store(open)+close(closed)+update(the rest).
- **Not touched (honest scope):** disciplinary-stage transitions (`DisciplinaryController::advanceStage`) notify no one either, but notifying an employee mid-disciplinary is sensitive (usually handled by formal letters, not app bells) and is a `DisciplinaryController` concern — a separate audit question, not folded into this slice's smallest-honest-fix. Event-added / case-closed notifications were also left out: the assignee is typically the actor for those (skip-self would suppress most), and firing on every event is noise — assignment is the clean waiting-party moment.

**Run 20 gates:** PHP -l ✅ (HrCaseController) · pest ✅ **694 passed / 0 failed** (4292 assertions, 774s) — matches baseline; existing case tests create cases via the model (controller wiring never runs) or hit store/update with actor==assignee (skip-self suppresses), so the change is transparent. Frontend gates N/A — zero `.ts/.tsx` files changed.

## Run 21 findings (Slice 21 — Assets `/hr/assets`)

- **Surface is genuinely gold-standard (verified, not assumed).** `AssetService` guards every transition and throws `\LogicException` on invalid ones; all four statuses (available/assigned/maintenance/retired) reachable; `AssetController` wraps **every** lifecycle call in `try/catch (\LogicException) → back()->with('error')` so a race/double-submit degrades to a flash, never a 500. Both notifications are wired (`AssetAssignedNotification` in `AssetService::assignAsset`; `HrAssetAlertNotification` as a deduped daily digest in `HrNotificationService`) — no dead class here. `HrAsset` uses `AuditableChanges`. Fleet federation is clean: vehicles/keys are read-through pointers (`fleet_asset_id`), `validateAsset` 422s a hand-typed fleet category, `bulk` skips fleet-linked rows, `searchFleetAssets` reads the canonical `Asset` register read-only. Server-side aggregates (comment even notes it "fixes the page-only count bug").
- **F-67 🟠 fixed — asset-document delete was a one-click, unrecoverable action.** `show.tsx` `DocumentsTab` fired `router.delete('/hr/assets/documents/{id}')` straight from the trash button (the file header even boasted "Zero confirm()") — but the controller also `Storage::delete()`s the physical file, so a mis-click permanently destroys a warranty cert / invoice / handover record with no undo. The redesign removed the native `confirm()` without replacing it. Fix: added the kit `AlertDialog` (from `@/components/ui/alert-dialog`) with a `confirmDoc` state — the trash button now opens a "Remove this document?" dialog naming the file and warning it's permanent; only the dialog's action deletes. Kit-first, matches the Run 12 documents-hub pattern.
- **F-68 🟠 fixed — raw `oklch()` in a hero tile.** `index.tsx` KPI tile "Warranties ≤30d" used `tone: 'oklch(0.7 0.15 70)'` + `bg: 'color-mix(in oklch, oklch(0.82 0.145 88) 18%, transparent)'` — raw colour literals, violating §2 tokens-only, and inconsistent with its sibling "In maintenance" tile (line 344) which correctly uses `var(--status-warning)`/`var(--status-warning-bg)`. Swapped to those tokens (warranties-expiring is semantically a warning state). No visual regression — same amber, now themeable.
- **Incidental (fixed, per fix-incidental-issues): `react-hooks/static-components` lint error** in `show.tsx` — the pre-existing detail header did `const Icon = categoryIcon(asset.category)` + `<Icon/>` (the rule flags a PascalCase component bound in render). Surfaced because this is the first slice to touch the file. Fixed with the codebase's own established idiom (`createElement(categoryIcon(...), { className })`, mirroring `checklists/primitives.tsx:277`), not a disable.

**Run 21 gates:** types ✅ 0 errors · eslint ✅ 0 errors (2 files) · build ✅ exit 0 (2m48s) · vitest ✅ **8 failed / 171 passed — the documented baseline exactly** (my-day ×4, app-sidebar ×1, resident-tracking ×1, behaviour-abc-tab ×2), zero new failures, none in assets pages. **pest N/A — zero PHP files changed** (frontend-only slice; PHP suite byte-identical to the Run 20 green baseline).

## Run 22 findings (Slice 22 — Approvals inbox `/hr/approvals`)

- **F-69 🟠 fixed — the hero "Pending" KPI counted only the current page, not the true total.** `pending.tsx` set `stats={[{ label: 'Pending', value: instances.data.length }]}` — `instances.data` is the `paginate(20)` page (≤20 rows), so with >20 pending the hero silently under-reported (the exact page-only-count bug the `AssetService::aggregates` comment calls out). §4A: counts must be server-side and must not lie. Fix: read the paginator's server-side `total` (`instances.total`) — already emitted by Laravel's `LengthAwarePaginator` and read the same way elsewhere (`leave/index.tsx:652`, `offboarding/index.tsx`). Typed `total: number` on the `instances` prop. Frontend-only, 2-line diff.
- **Verified green:** `ApprovalController::action` wraps `ApprovalWorkflowService::processAction` in `try/catch (\LogicException) → back()->with('error')` (graceful, no 500); approve/reject/escalate all validated + reachable; chains CRUD is `hr.approvals.manage`-gated and tenant-scoped; the action UI uses an inline review panel (notes textarea + Approve/Reject/Cancel), no native `confirm()`.
- **Deferred ↗ D-1 (the S14 spine decision — Chane's call, do NOT migrate unilaterally):**
  - The pending inbox lists **only** `HrApprovalInstance` rows, and `storeChain`/`chains` only offer `process_type ∈ {leave, expense, timesheet, document}`. Leave→`HrLeaveApprovalChain`, expenses→inline service, recruitment offers/jobs→recruitment-local notify flows — none create `HrApprovalInstance`, so **the inbox is architecturally near-empty in practice**. This is D-1's core; the fix is the migration decision, not a UI patch.
  - **Dead-end (blocked on D-1):** the "Item" column renders bare `{approvable_type} #{id}` text with **no deep-link** to the underlying leave/expense/document record — an approver actions blind. Building the type→route deep-link only makes sense once the spine actually populates the inbox (per D-1), so it's coupled to that decision, not fixed here.
- **Noted 🟡 (not fixed — below the smallest-honest-fix bar / redesign-adjacent):** `initiated_at` renders as a raw `Y-m-d H:i:s` string (not en-NZ formatted — parsing that non-ISO format client-side is browser-flaky, wants a controller-side ISO/format change); the empty state is bare centred text ("No pending approvals.") rather than a designed icon+CTA; the page uses generic `PageHero` (`category="hr"`) rather than a specialised `ApprovalsHero`.

**Run 22 gates:** types ✅ 0 errors · eslint ✅ 0 errors · build ✅ exit 0 (2m46s) · vitest ✅ **8 failed / 171 passed — the documented baseline exactly**, zero new failures. **pest N/A — zero PHP files changed** (frontend-only slice).

## Run 23 findings (Slice 23 — Signatures `/hr/signatures`)

- **F-70 🟠 (workflow-integrity, ~🔴) fixed — the manual "nudge" reminder sent nothing AND suppressed the automated one.** `ESignatureService::nudge()` only did `$signature->update(['reminder_sent_at' => now()])` — no notification — yet `ESignatureController::nudge` flashes "Reminder sent to signer." Worse: `SendExpiryRemindersJob` (the scheduled reminder sweep) selects `HrDocumentSignature::…->whereNull('reminder_sent_at')` and stamps it after sending, so the manual nudge's bare stamp made the sweep **skip that signature** — a nudge actively *prevented* any reminder from reaching the signer. Fix: `nudge()` now sends `SignatureReminderNotification` immediately (identical payload shape to the job — `signature_id`/`document_title`/`due_at`) then stamps; best-effort try/catch + `Log::warning`, loads the `signer` relation. Result: exactly one reminder (from the nudge), and the later job-skip is now correct. (Note: `SignatureReminderNotification` is **not** a dead class — the job fires it; the bug was the manual path being a suppressing no-op.)
- **F-71 🟠 fixed — "resend" was a silent status flip.** `resend()` reopened a declined request to `pending` (clearing decline reason + `reminder_sent_at`) and the controller flashed "Signature request resent", but no notification reached the re-invited signer. Fix: `resend()` now sends `SignatureRequestedNotification` (mirroring `bulkRequestSignatures`' block — payload incl. `message`), best-effort. The cleared `reminder_sent_at` keeps the sig eligible for the sweep's later due-reminder too.
- **Verified green:** `sign`/`decline` are signer-scoped (`signer_user_id === user->id`) and wrapped in `try/catch (\LogicException) → back()->with('error')` (no 500); statuses pending→signed (finalises an audit-grade signed PDF + SHA-256 hash when all sign)/declined/cancelled(delete)/re-pending(resend) all reachable; `downloadDocument` is signer-scoped + storage-existence guarded; `bulkRequestSignatures` already notifies each signer (forward path wired); `pending` is a full collection (no page-length KPI bug); no native `confirm()` (cancel is a POST status action, not a JS-dialog delete).
- **Noted 🟠 (deferred — needs NEW notification classes, beyond smallest-honest-fix):** neither `sign` nor `decline` notifies the **requester** (`requested_by`) — the person waiting on the outcome is never told the document was signed or declined (a decline especially needs follow-up). Wiring this needs new `SignatureSigned`/`SignatureDeclined` notification classes to the requester (the reverse-direction counterpart to the signer-facing ones); logged as a follow-up, not built this slice to keep it focused. `requested_at`/`signed_at` also render as raw `Y-m-d H:i:s` strings (🟡 en-NZ formatting).

**Run 23 gates:** php -l ✅ (ESignatureService) · pest ✅ **694 passed / 0 failed** (4292 assertions, 752s) — the existing "manager can nudge, resend and cancel" + "signature-due reminder sweep" tests both stay green (neither asserts notification counts on the manual path, and the sweep test uses a fresh sig). Frontend gates N/A — zero `.ts/.tsx` files changed.

## Run 24 findings (Slice 24 — Analytics & reports CLUSTER, part 1: Analytics + Reports)

Cluster row → `🔶 partial`. This run cleared **Analytics** and **Reports**; **Headcount, Succession, Import/export** still pending (next Slice-24 firings).

- **Analytics (`/hr/analytics`) — audited CLEAN (source-verified).** `AnalyticsDashboardController::index` pulls every figure server-side from `WorkforceAnalyticsService` (headcount trend, turnover, tenure brackets, compliance score, leave utilisation, department breakdown); `currentHeadcount` = the trend's last server point, `avgTenure` computed server-side from brackets. No pagination, no client-derived KPI, no export affordance to be dead, permission-gated (`hr.analytics.view`). Only 🟡 note: generic `PageHero` (a specialised `AnalyticsHero` is redesign-scope, not a smallest-honest-fix).
- **F-72 🟠 fixed (Reports `/hr/reports/saved`) — native `confirm()` guarding a destructive delete.** `saved.tsx` `handleDelete` did `if (!confirm('Are you sure you want to delete this saved report?')) return; router.delete(...)` — a §4B native-dialog violation (same class as Run 12 Documents + F-67 Assets). Replaced with the kit `AlertDialog`: a `deleteReport` state, the trash button opens a "Delete this saved report?" dialog naming the report, and only its action fires `router.delete`. Kit-first, tokens intact. (Hero stat already uses `reports.total`, not page length — no KPI-that-lies here; export/download links are wired GET routes; run-result `.data.length` counts the actual result set, not a page.)
- **Noted for later cluster runs:** Reports also has `HrReportController` (generate/export/exports/subscriptions) + `ReportBuilderController` (builder/preview/run/schedule/destroy) — the `saved` delete was the standout; a fuller reports-backend pass can ride a later firing if the remaining sub-surfaces surface nothing bigger.

**Run 24 (part 1) gates:** types ✅ 0 errors · eslint ✅ 0 errors · build ✅ exit 0 (2m49s) · vitest ✅ **8 failed / 171 passed — the documented baseline exactly**, zero new failures. **pest N/A — zero PHP files changed** (frontend-only slice).

## Run 24 findings (part 2 — Headcount + Succession + Import/export: all CLEAN, cluster complete)

No new code fix this run — the three remaining sub-surfaces audit **genuinely clean** (source-verified, not code-reading-only assumed). Row 24 → `✅`.

- **Headcount (`/hr/headcount`) — CLEAN.** `HeadcountController::index` pulls everything server-side from `HeadcountForecastService` (current headcount, 12-mo forecast, budget-vs-actual, attrition risk); `hr.analytics.view`-gated. The "Create requisition" seam correctly **federates** to the Recruitment hub (`/hr/recruitment?tab=requisitions`) and is **properly gated** — the controller emits `can.view_recruitment` and the page uses `canRecruit` at all three requisition affordances (hero action + two body CTAs), so no 403 dead-link. Only 🟡: generic `PageHero` (redesign-scope).
- **Succession (`/hr/succession` index + show) — CLEAN.** Hero stats are server-side (`stats.total/high_risk/vacant/ready_now`) with proper tone escalation (critical/warning/success). **Both** destructive deletes (plan on index+show, candidate on show) are guarded by a kit `Dialog` confirmation naming the record + its candidate count — not native `confirm()`, not unguarded. `nominateToTalentPool` is wired end-to-end (show.tsx `nominate()` → POST `/candidates/{id}/nominate` → "Nominate as ready now" button + success toast — not the Drills disease). Candidate CRUD via `SuccessionCandidateDialog`. Only 🟡: generic `PageHero`.
- **Import/export (`/hr/import-export`) — CLEAN.** All four routes exist (index GET, export POST, template GET, import POST) and `import-export/index.tsx` renders them via a wizard; `ImportExportController` is `hr.employees.manage`-gated with server-side stats (`exportable`=active count, `profiles`=total), streamed CSV export (+ "export selected" ids), a blank-template download, and file-validated import (`csv,txt`, max 5 MB). No dead buttons (the two `disabled` states legitimately gate on a chosen file), no native dialogs.
- **Cluster-wide 🟡 (noted, deferred as redesign-scope):** Analytics + Headcount + Succession all sit on generic `PageHero` rather than specialised heroes — a consistent 4A gap, but building three analytics heroes is a redesign effort, not a smallest-honest-fix; logged for a future hero pass, not built.

**Run 24 (part 2) gates:** N/A — ledger-only completion (no code changed; the deliverable is the source-verified clean audit + row 24 → ✅). The cluster's one code fix (F-72) shipped in part 1 (`e4f86585`), gates green there.

## Run 25 findings (Slice 25 — Settings + audit log `/hr/settings/*`) — LAST SURFACE ROW

Three real 🟠s, all fixed — closes the surface pass. **All 25 surface rows are now ✅; the loop moves to the seams (S1-S9, S11-S16).**

- **F-73 🟠 fixed — the audit-log hero "Entries" count lied (page-length, not total).** `settings/audit-log.tsx` set `{ label: 'Entries', value: logs.data.length }` — `logs.data` is the `paginate(30)` page (≤30). The audit log is the **SoT for every `AuditableChanges` write across HR**, so any active org has far more than 30 entries → the hero permanently under-reported (e.g. "Entries: 30" over thousands). The most egregious KPI-that-lies in the module (F-69 pattern, on the one surface that must never under-count). Fix: `logs.total` (server-side paginator total), typed `total: number` on the `logs` prop.
- **F-74 🟠 fixed — `custom-fields.tsx` native `confirm()` delete.** Deleting a custom-field definition (which also cascades all stored values across every employee) ran behind a native `confirm(...)`. Replaced with the kit `AlertDialog` (a `deletingId` state; the trash button opens a "Delete this custom field?" dialog spelling out the cascade; only its action deletes).
- **F-75 🟠 fixed — `automations.tsx` native `window.confirm()` delete.** Deleting an automation rule ran behind `window.confirm(...)`. Replaced with the kit `AlertDialog` (a `deletingRuleId` state; "Delete this automation rule?" dialog noting run-history is kept).
- **Verified green:** `AuditController::index` is clean — `hr.settings.manage`-gated, server-side tenant-scoped query with working filters (user/action/model-type/date-from/date-to) + pagination, filter-dropdown data server-computed; `show` is JSON tenant-scoped; the log exposes only the actor's name/email (no over-exposure beyond the record's own old/new values, which are the point of an audit trail). Webhooks (retry-delivery) + automations (toggle) + custom-fields CRUD all wired and `hr.settings.manage`-gated.

**Run 25 gates:** types ✅ 0 errors · eslint ✅ 0 errors (3 files) · build ✅ exit 0 (3m1s) · vitest ✅ **8 failed / 171 passed — the documented baseline exactly**, zero new failures. **pest N/A — zero PHP files changed** (frontend-only slice).

## Seam S1 (Attendance ↔ My Day) — PROVEN, first of the seams phase

All 25 surface rows are ✅; the loop is now proving seams with runtime tests (no ✅ from code-reading). **S1 → ✅.** Deliverable = a new Pest test (the seam proof IS the fix). Added to `tests/Feature/Hr/AttendanceClockWorkflowTest.php`: *"S1 seam: the HrTimeEntry and HrAttendanceSession read paths agree across both clock surfaces"* — round-trips a clock-in/out on each of `/my-day` (`AttendanceController`) and `/hr/my/time` (`MyHrController`) and asserts, at every step, that BOTH the `HrAttendanceSession::open()` count (Attendance's read) and the `HrTimeEntry::active()` count (MyHr's read) move together (0→1 on clock-in, 1→0 on clock-out) — including clocking out on the *opposite* surface from the clock-in. First attempt caught a real test-timing trap ("sessions must end after they start" when in==out instant) → fixed with `$this->travel(2)->hours()` before each clock-out; the seam assertions themselves passed on the first run (the /my-day clock-in already set both read paths). Ran in isolation: **1 passed / 14 assertions**.

**Seam S1 gates:** php -l ✅ (test file) · isolated test ✅ 1 passed/14 assertions · full HR pest ✅ **695 passed / 0 failed** (4306 assertions, 794s) = 694 baseline + 1 new seam test, zero regressions. **New baseline: 695.** Frontend gates N/A — zero `.ts/.tsx` files changed.

## Seam S2 (HR Assets ↔ Fleet) — PROVEN

**S2 → ✅.** Deliverable = new `tests/Feature/Hr/AssetFleetFederationTest.php` (3 tests, 9 assertions) proving HR federates Fleet-owned vehicles/keys read-through and never duplicates or disposes them: the `validateAsset` 422 guard on a hand-typed fleet asset, the `fleetAsset()` read-through pointer, and `bulk` retire skipping fleet-linked rows. Two debugged traps → both recorded for later seam runs: (1) `Asset::factory()->vehicle()` unneeded — bare `Asset::factory()->create()` is enough; (2) **`RbacSeeder` does NOT seed the `hr.assets.*` permissions** (nor most `hr.*` module perms) — those live in `SeedHrPermissionsSeeder`, which also attaches them to the `hr` role; seed BOTH when an HR HTTP endpoint is permission-gated (the pure-model assertion passed without it, but the two HTTP endpoints 403'd until `SeedHrPermissionsSeeder` was added). Ran in isolation: **3 passed / 9 assertions**.

**Seam S2 gates:** php -l ✅ (test file) · isolated file ✅ 3 passed/9 assertions · full HR pest ✅ **698 passed / 0 failed** (4315 assertions, 812s) = 695 baseline + 3 new, zero regressions. **New baseline: 698.** Frontend gates N/A — zero `.ts/.tsx` files changed.

## Seam S3 (Driver eligibility → Fleet/Rostering) — PROVEN, enforced

**S3 → ✅.** The seam's whole point is to catch a safety rule *computed but not enforced* — verified it IS enforced, so this is a clean proof, not a 🔴. Mapped: `DriverLicenceExpiryRule` (severity `block` for expired/suspended/no-record on a driver shift) → aggregated by `ShiftStaffEligibilityService::evaluate` → `EligibilityResult::hasBlocks()` → **enforced** by `RosterPublishValidator::validate`/`validateProposedShifts` (`can_publish = blocks === []`). New `tests/Feature/Hr/DriverEligibilityRosteringHardStopTest.php` (3 tests, 4 assertions): expired licence → block reason present; publish gate → `can_publish:false`; current licence → no trip. Setup mirrors the pre-existing Rostering warning test (`coverage_roles:['driver']` shift + `HrDriverEligibility`); uniquely-named helper `hrSeamDriverShift` avoids a full-suite clash with that file's `makeDriverShift`; placed in `tests/Feature/Hr` so the HR gate covers it (no perms/HTTP → no RbacSeeder needed). Ran in isolation: **3 passed / 4 assertions**. Licence-CLASS matching at the gate stays DEFERRED (rule comment: shifts declare no `required_licence_class`) — documented, not a gap to fix.

**Seam S3 gates:** php -l ✅ (test file) · isolated file ✅ 3 passed/4 assertions · full HR pest ✅ **701 passed / 0 failed** (4319 assertions, 809s) = 698 baseline + 3 new, zero regressions. **New baseline: 701.** Frontend gates N/A — zero `.ts/.tsx` files changed.

## Seam S4 (Injuries (H&S) → HR) — integrity PROVEN; surfacing is a decision (D-8)

**S4 → ✅ (integrity invariant).** This is one of the STALE pre-seeds: the brief said "HR gets read-only surfacing per INJURIES_HR_SURFACING_PLAN.md", but that plan file does not exist and a source audit shows **no HR code touches `WorkplaceInjury` at all** (13 usages app-wide — HsKpiService/HsAnalyticsService, ReturnToWorkController/HsEventController/HealthSafetyDashboardController, WorkplaceInjuryObserver, WorkplaceInjuryProvider [/tasks], NotifiableIncident, RTW/WorkCapacity/ClientIncident models — every one H&S/Governance/Tasks). So the seam's actual RISK (HR mutating an H&S-owned injury) is **nil** — one owner holds by absence. New `tests/Feature/Hr/InjuryHrOwnershipSeamTest.php` (1 test, 3 assertions) locks the per-employee ownership contract. The read-only SURFACING is simply not built → **D-8** (not a fix to invent; building it without a spec would be scope creep). Isolated: **1 passed / 3 assertions**.

**Seam S4 gates:** php -l ✅ (test file) · isolated file ✅ 1 passed/3 assertions · full HR pest ✅ **702 passed / 0 failed** (4322 assertions, 770s) = 701 baseline + 1 new, zero regressions. **New baseline: 702.** Frontend gates N/A — zero `.ts/.tsx` files changed.

## Seam S5 (H&S incidents ↔ HR cases) — integrity PROVEN; F-76 fixed; surfacing = D-9

**S5 → ✅ (integrity).** Mapped: `HrCase.linked_incident_ids` is a JSON int-array of `ClientIncident` ids (`docs/hr-module-design.md:900`); `HrCaseController` store/update validate it (`integer` items, no `exists`) and the model casts it. New `tests/Feature/Hr/CaseIncidentCrossReferenceSeamTest.php` (1 test, 5 assertions) proves the one-owner guarantee: a case that links an incident stores the reference, the `ClientIncident` stays byte-identical (`count===1`, `updated_at` unchanged) — HR references, never mutates/duplicates.

- **F-76 🟠→🔴 (fixed) — description-less HR case 500'd on insert.** The seam test's POST (no `description`) hit `SQLSTATE 1364 Field 'description' doesn't have a default value`: `hr_cases.description` is NOT-NULL-no-default, but `store`/`update` validate `description` as `nullable` — and an empty field arrives as `null` via `ConvertEmptyStringsToNull`. Slice 20 missed it (its tests always sent a description). Fixed in the controller (no schema change): `store` coerces `$data['description'] ?? ''`; `update` coerces a present-but-null description to `''`. The seam test's description-less path now doubles as the regression guard.
- **Dead cross-reference (🟠) → D-9.** `linked_incident_ids` is INERT: no producer (the case wizard never sends it — zero `incident` refs in `cases/index.tsx`/`show.tsx`) and no consumer (no `linkedIncidents()` relation on `HrCase`, no load in `show()`, no render) — contrast RespiteDailyNote/ClinicalEvent which consume their `linkedIncident`. The write path is safe but the feature isn't wired. See D-9.

**Seam S5 gates:** php -l ✅ ×2 (HrCaseController + test) · isolated file ✅ 1 passed/5 assertions · full HR pest ✅ **703 passed / 0 failed** (4327 assertions, 767s) = 702 baseline + 1 new, zero regressions (the F-76 description coercion is backward-compatible — every existing case test still green). **New baseline: 703.** Frontend gates N/A — zero `.ts/.tsx` files changed.

## Seam S6 (Compliance matrix → Rostering) — PROVEN, enforced

**S6 → ✅.** Same enforcement chain as S3, proven for GENERAL hard-stop compliance (not just driver licence): `ComplianceMatrixService::getHardStopFailures` (cached, reads `hr_staff_compliance_status` directly — role-independent) → merged with `LiveComplianceValidator::validateHardStops` (role-scoped) in `canAssignToShift` → `ShiftStaffEligibilityService::checkCompliance` severity-`block` → `EligibilityResult::hasBlocks()` → `RosterPublishValidator` (`can_publish = blocks===[]`). Also `candidatesFor()` pre-filters expired/not_started hard-stops. New `tests/Feature/Hr/ComplianceMatrixRosteringHardStopTest.php` (2 tests, 4 assertions): expired hard-stop status → block reason names the requirement + `can_publish:false`; compliant status → no block. Key isolation insight: the live validator only fires for requirements mapped to the user's ROLES (`getHardStopRequirements` → matrix), so a role-less test user exercises the CACHED path cleanly. Existing `ComplianceHubActionsTest` already covered the driver-licence LIVE path at the service level; this adds the cached path + the rostering enforcement gate. Isolated: **2 passed / 4 assertions**.

**Seam S6 gates:** php -l ✅ (test file) · isolated file ✅ 2 passed/4 assertions · full HR pest ✅ **705 passed / 0 failed** (4331 assertions, 751s) = 703 baseline + 2 new, zero regressions. **New baseline: 705.** Frontend gates N/A — zero `.ts/.tsx` files changed.

## Decisions needed (Chane)

1. **Approvals spine (S14) — now leave + expenses + recruitment:** Leave→`HrLeaveApprovalChain`, expenses→inline service, requisitions/offers→recruitment-local notify flow. None route through `ApprovalWorkflowService`, and `/hr/approvals/pending` only lists `['leave','expense','timesheet','document']` — recruitment approvables never appear in the inbox. Is the §2 rule satisfied by *surfacing* everything in `/hr/approvals` (add requisitions/offers to the pending list), or do you want flows migrated onto the chain service? S14 slice will implement whichever you pick.
2. **Role-assignment guard (Run 2):** `EmployeeIntakeService::intake()`/`rehire()` assign any role that exists (validated `exists:roles,name` only) — a user with `hr.employees.manage` can create/rehire someone as `admin`. Permissions are frozen, so adding a hierarchy guard (e.g. "cannot assign a role you don't hold" or an allowlist of staff-level roles for intake) is a policy decision, not a code fix I'll make unilaterally.
3. **User-write auditability (Run 2):** the `User` model has no `AuditableChanges` (by design — it would log every `last_login_at` touch), so security-relevant writes (`role`, `approved_at`) made by intake/rehire/offboarding are unaudited. Option: explicit `AuditLogger::log()` calls at those few write sites only. Cheap, but it's an app-wide auditing-policy call.
4. **Hard-delete vs archive-not-delete (Runs 2+4, recurring):** several HR destroy endpoints hard-delete against the house rule — `HrDocument` (profile documents, slice 2), onboarding checklist/task/template/email (slice 4; deletions ARE audit-logged via the trait, but evidence/log children get orphaned). Fixing properly means `SoftDeletes` migrations (schema change) or removing the destroy routes where an `archived` status already exists (onboarding checklists have one). One policy call, then a single sweep slice can implement it. Until then each slice just ledgers occurrences.
5. **Exit-interview immutability (Run 5):** exit interviews are store-once — no update/delete routes, so a typo in a recorded interview (wrong reason, misspelled feedback) is permanent without DB surgery. Immutability might be intentional (interview-record integrity); if not, an edit route + wizard-in-edit-mode is a small build. Your call which way.
6. **Payroll export CSV formula-injection guard (Run 10):** `PayrollExportService::encodeCsvRow` enclosure-escapes but does not neutralise leading `= + - @` (spreadsheet formula triggers) — the vector is employee name/number/notes. The app has a `SanitizesCsvOutput` guard for human-read CSVs, but the payroll file is **machine-read by the provider (Datacom/iPayroll/etc.)**; prefixing an apostrophe/tab could break the provider's parser, and a naïve `-` guard would corrupt legitimately-negative numeric cells. Options: (a) guard only non-numeric string cells; (b) leave provider files raw (admin-triggered export of internal data — low risk). Not fixed unilaterally because it can break ingestion. Your call.
7. **Performance two-model split (S13, Run 13):** the live hub reads `hr_performance_reviews` (`HrPerformanceReview` — reviews/probation/sign-off/acknowledge all here). A separate governance-era `performance_reviews` table/model exists but is **unrouted** (no controller/UI path reaches it). Per §6/S13 this is surfaced honestly, not merged unilaterally: do you want the legacy `performance_reviews` retired (drop the dormant table/model) or consolidated INTO the live one (data migration)? Until you decide, the live hub is internally consistent and the legacy model is inert. A consolidation slice implements whichever you pick.
8. **HR read-only injury surfacing (S4) — build or drop?** `WorkplaceInjury` is exclusively H&S-owned (source-verified) and HR never surfaces it; the pre-seed's `INJURIES_HR_SURFACING_PLAN.md` doesn't exist. The integrity invariant is clean (HR can't mutate injuries), but there's no HR read view of an employee's injuries / RTW status (e.g. on the People profile or `/hr/my`). Do you want a read-only HR surface federating `WorkplaceInjury` (join on `user_id`, H&S stays the writer), or should injuries stay H&S-only? Building it is a feature, not an audit fix — flagged, not built.
9. **HR case ↔ incident cross-link (S5) — wire it or drop the column?** `HrCase.linked_incident_ids` (JSON array of `ClientIncident` ids) is validated + stored + cast but is otherwise INERT: the case-create wizard never populates it and nothing surfaces it (no `linkedIncidents()` relation, no `show()` load, no page render). Options: (a) wire it — add an incident picker to the case wizard + a read-only "linked incidents" section on the case detail (federate `ClientIncident` read-only, H&S stays the writer); or (b) drop the unused column + validation. The integrity guarantee already holds (S5 test: HR references, never mutates the incident) — this is a feature/cleanup decision, not a bug.

---

## Gates — Run 0

| Gate | Result |
|------|--------|
| wayfinder:generate | ✅ regenerated (new route `hr.my.expenses.withdraw`) |
| npm run types | ✅ 0 errors |
| eslint (8 touched TSX files) | ✅ 0 errors |
| npm run build | ✅ exit 0 (4m51s) |
| npm run test (vitest) | ✅ no NEW failures — 8 fails, all reproduced at clean HEAD via stash (my-day ×4, app-sidebar ×1, behaviour-abc ×2, resident-tracking ×1) |
| pest tests/Feature/Hr tests/Unit/Hr | ✅ no NEW failures — 3 failed / 690 passed (928s); all 3 reproduced at clean HEAD (see corrected baselines). `MyLeaveCancelTest` (covers the changed cancel path) fully green with the new notification. |
| screenshots | n/a in worktree (Herd serves parent only) — post-deploy visual check per established pattern |
