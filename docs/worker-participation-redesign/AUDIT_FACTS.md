# Worker Participation rebuild - verified API facts (from 10-agent audit wf_5c0174c3-668)

## Health & Safety worker-participation data model (hs_representatives, hs_committees, hs_committee_meetings, hs_consultations)

- **hs_representatives — columns (create migration)** [database/migrations/2026_03_28_100001_create_worker_participation_tables.php:11-35]: id (bigint PK); user_id (unsignedBigInteger, FK users cascadeOnDelete); site_id (unsignedBigInteger nullable, FK sites nullOnDelete); work_group (string nullable) — EXISTS; election_method (string, NOT NULL); elected_at (date, NOT NULL); term_expires_at (date nullable); status (string default 'active'); training_days_completed (integer default 0); initial_training_completed_at (date nullable); notes (text nullable); created_by (unsignedBigInteger nullable, FK users nullOnDelete); updated_by (unsignedBigInteger nullable, FK users nullOnDelete); created_at/updated_at (timestamps); deleted_at (softDeletes). Indexes: status, site_id.
- **hs_representatives — model $fillable** [app/Models/HsRepresentative.php:17-30]: user_id, site_id, work_group, election_method, elected_at, term_expires_at, status, training_days_completed, initial_training_completed_at, notes, created_by, updated_by
- **hs_representatives — model $casts** [app/Models/HsRepresentative.php:32-37]: elected_at=>date, term_expires_at=>date, initial_training_completed_at=>date, training_days_completed=>integer
- **hs_representatives — relations** [app/Models/HsRepresentative.php:43-75]: user(): belongsTo(User) [implicit FK user_id]; site(): belongsTo(Site) [implicit FK site_id]; creator(): belongsTo(User,'created_by'); updater(): belongsTo(User,'updated_by'). Scopes: active (status='active'), forSite(siteId).
- **hs_committees — columns (create migration)** [database/migrations/2026_03_28_100001_create_worker_participation_tables.php:37-56]: id (bigint PK); name (string NOT NULL); site_id (unsignedBigInteger nullable, FK sites nullOnDelete); meeting_frequency (string default 'monthly'); terms_of_reference (text nullable); established_at (date, NOT NULL); status (string default 'active'); members (json nullable); created_by (unsignedBigInteger nullable, FK users); updated_by (unsignedBigInteger nullable, FK users); timestamps; softDeletes. Index: status.
- **hs_committees — model $fillable** [app/Models/HsCommittee.php:18-28]: name, site_id, meeting_frequency, terms_of_reference, established_at, status, members, created_by, updated_by
- **hs_committees — model $casts** [app/Models/HsCommittee.php:30-33]: established_at=>date, members=>array
- **hs_committees — relations** [app/Models/HsCommittee.php:39-71]: site(): belongsTo(Site); meetings(): hasMany(HsCommitteeMeeting) [implicit FK hs_committee_id — relies on Laravel deriving 'hs_committee_id' from parent class name]; creator(): belongsTo(User,'created_by'); updater(): belongsTo(User,'updated_by'). Scopes: active, forSite.
- **hs_committee_meetings — columns (create migration)** [database/migrations/2026_03_28_100001_create_worker_participation_tables.php:58-84]: id (bigint PK); hs_committee_id (unsignedBigInteger NOT NULL, FK hs_committees cascadeOnDelete) — NOTE name is hs_committee_id NOT committee_id; scheduled_at (dateTime NOT NULL); started_at (dateTime nullable); ended_at (dateTime nullable) — EXISTS; location (string nullable); status (string default 'scheduled'); attendees (json nullable) — EXISTS; agenda_items (json nullable) — EXISTS; minutes (text nullable); action_items (json nullable) — EXISTS; safety_concerns_raised (text nullable); recorded_by (unsignedBigInteger nullable, FK users); created_by; updated_by; timestamps; softDeletes. Indexes: status, scheduled_at.
- **hs_committee_meetings — columns added by ALTER migration** [database/migrations/2026_03_29_000001_enhance_worker_participation.php:12-16]: confirmed_attendees (json nullable, after attendees) — EXISTS; minutes_document_path (string nullable) — EXISTS; minutes_document_name (string nullable) — EXISTS
- **hs_committee_meetings — model $fillable** [app/Models/HsCommitteeMeeting.php:18-36]: hs_committee_id, scheduled_at, started_at, ended_at, location, status, attendees, agenda_items, minutes, action_items, safety_concerns_raised, confirmed_attendees, minutes_document_path, minutes_document_name, recorded_by, created_by, updated_by
- **hs_committee_meetings — model $casts** [app/Models/HsCommitteeMeeting.php:38-46]: scheduled_at=>datetime, started_at=>datetime, ended_at=>datetime, attendees=>array, agenda_items=>array, action_items=>array, confirmed_attendees=>array
- **hs_committee_meetings — relations** [app/Models/HsCommitteeMeeting.php:52-95]: committee(): belongsTo(HsCommittee,'hs_committee_id') — ALREADY EXISTS with correct FK hs_committee_id; recorder(): belongsTo(User,'recorded_by'); creator(): belongsTo(User,'created_by'); updater(): belongsTo(User,'updated_by'); attachments(): morphMany(HsAttachment,'attachable'). NO attendees() belongsToMany relation exists. Scopes: scheduled, completed, upcoming.
- **hs_consultations — columns (create migration)** [database/migrations/2026_03_28_100001_create_worker_participation_tables.php:86-111]: id (bigint PK); title (string NOT NULL); consultation_type (string NOT NULL); description (text NOT NULL); site_id (unsignedBigInteger nullable, FK sites nullOnDelete); initiated_by (unsignedBigInteger NOT NULL, FK users cascadeOnDelete); consultation_date (date NOT NULL); workers_consulted (json nullable); worker_feedback_summary (text nullable); outcome (text nullable); changes_made (text nullable); status (string default 'open'); created_by; updated_by; timestamps; softDeletes. Indexes: status, consultation_type.
- **hs_consultations — columns added by ALTER migration** [database/migrations/2026_03_29_000001_enhance_worker_participation.php:19-24]: document_path (string nullable, after description); document_name (string nullable); outcome_document_path (string nullable, after outcome); outcome_document_name (string nullable)
- **hs_consultations — model $fillable** [app/Models/HsConsultation.php:18-36]: title, consultation_type, description, site_id, initiated_by, consultation_date, workers_consulted, worker_feedback_summary, outcome, changes_made, document_path, document_name, outcome_document_path, outcome_document_name, status, created_by, updated_by
- **hs_consultations — model $casts** [app/Models/HsConsultation.php:38-41]: consultation_date=>date, workers_consulted=>array
- **hs_consultations — relations** [app/Models/HsConsultation.php:47-89]: site(): belongsTo(Site); initiator(): belongsTo(User,'initiated_by'); creator(): belongsTo(User,'created_by'); updater(): belongsTo(User,'updated_by'); attachments(): morphMany(HsAttachment,'attachable'). Scopes: open (status='open'), forSite, ofType.
- **hs_attachments — polymorphic table (NEW in ALTER migration, used by meetings + consultations)** [database/migrations/2026_03_29_000001_enhance_worker_participation.php:27-41]: id; attachable_type/attachable_id (morphs); uploaded_by (unsignedBigInteger nullable, FK users); original_name (string); path (string); disk (string default 'private'); mime_type (string nullable); size_bytes (unsignedBigInteger nullable); description (text nullable); timestamps; softDeletes
- **Status values are plain VARCHAR strings (NOT DB enums); validated only in controller** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:118,199,240,260,381,393]: hs_representatives.status default 'active'; controller validation in:active,inactive,resigned. hs_committees.status default 'active' (no update-validation list found). hs_committee_meetings.status default 'scheduled'; controller in:scheduled,in_progress,completed,cancelled (completeMeeting sets 'completed', cancel sets 'cancelled'). hs_consultations.status default 'open'; BUT TWO DIFFERENT vocabularies — update() validates in:open,in_progress,closed while a separate action validates in:open,feedback_received,actioned,closed.

## routes_and_permissions

- **Route block location** [routes/health-safety.php:134]: Worker-participation routes are routes/health-safety.php lines 134-169, inside Route::middleware(['auth'])->prefix('health-safety')->name('health-safety.') group, with nested ->prefix('worker-participation')->name('worker-participation.'). Full route names are prefixed `health-safety.worker-participation.*`.
- **index route + gating** [routes/health-safety.php:137-139]: GET /health-safety/worker-participation/ -> name 'health-safety.worker-participation.index' -> WorkerParticipationController@index. Gated by middleware('permission:hazards.view').
- **All writes gated by hazards.manage** [routes/health-safety.php:141]: Every write route sits inside a single Route::middleware('permission:hazards.manage')->group spanning lines 141-168. Confirms handoff claim 'all writes -> permission:hazards.manage' exactly. (Note: writes use hazards.manage ONLY — NOT the 'hazards.manage|hazards.create' OR-pattern that substances/drills/injuries/first-aid/procedures use for their store routes.)
- **representatives.store** [routes/health-safety.php:143]: POST /representatives -> 'health-safety.worker-participation.representatives.store' -> storeRepresentative (hazards.manage)
- **representatives.update** [routes/health-safety.php:144]: PUT /representatives/{representative} -> 'health-safety.worker-participation.representatives.update' -> updateRepresentative (hazards.manage)
- **committees.store** [routes/health-safety.php:147]: POST /committees -> 'health-safety.worker-participation.committees.store' -> storeCommittee (hazards.manage)
- **committees.meetings.store** [routes/health-safety.php:150]: POST /committees/{committee}/meetings -> 'health-safety.worker-participation.committees.meetings.store' -> storeMeeting (hazards.manage)
- **meetings.update** [routes/health-safety.php:151]: PUT /meetings/{meeting} -> 'health-safety.worker-participation.meetings.update' -> updateMeeting (hazards.manage)
- **consultations.store** [routes/health-safety.php:154]: POST /consultations -> 'health-safety.worker-participation.consultations.store' -> storeConsultation (hazards.manage)
- **consultations.update** [routes/health-safety.php:155]: PUT /consultations/{consultation} -> 'health-safety.worker-participation.consultations.update' -> updateConsultation (hazards.manage)
- **consultations.status** [routes/health-safety.php:158]: PUT /consultations/{consultation}/status -> 'health-safety.worker-participation.consultations.status' -> updateConsultationStatus (hazards.manage)
- **consultations.documents.upload** [routes/health-safety.php:159]: POST /consultations/{consultation}/documents -> 'health-safety.worker-participation.consultations.documents.upload' -> uploadConsultationDocument (hazards.manage)
- **consultations.documents.download** [routes/health-safety.php:160]: GET /consultations/{consultation}/documents/{type} -> 'health-safety.worker-participation.consultations.documents.download' -> downloadConsultationDocument. NOTE: this GET download sits INSIDE the hazards.manage group, so a hazards.view-only user CANNOT download consultation docs.
- **meetings.attendees** [routes/health-safety.php:163]: POST /meetings/{meeting}/attendees -> 'health-safety.worker-participation.meetings.attendees' -> addMeetingAttendees (hazards.manage)
- **meetings.complete** [routes/health-safety.php:164]: PUT /meetings/{meeting}/complete -> 'health-safety.worker-participation.meetings.complete' -> completeMeeting (hazards.manage)
- **meetings.cancel** [routes/health-safety.php:165]: PUT /meetings/{meeting}/cancel -> 'health-safety.worker-participation.meetings.cancel' -> cancelMeeting (hazards.manage)
- **meetings.minutes.upload** [routes/health-safety.php:166]: POST /meetings/{meeting}/minutes -> 'health-safety.worker-participation.meetings.minutes.upload' -> uploadMeetingMinutes (hazards.manage)
- **meetings.minutes.download** [routes/health-safety.php:167]: GET /meetings/{meeting}/minutes/download -> 'health-safety.worker-participation.meetings.minutes.download' -> downloadMeetingMinutes. NOTE: also inside hazards.manage group (a view-only user cannot download minutes). The CURRENT index.tsx links to this at index.tsx:1396.
- **No worker-participation permission exists** [database/seeders/RbacSeeder.php:446-451]: grep of database/ for 'worker-participation' / 'worker.participation' returns ZERO permission hits. RbacSeeder defines only the hazards group: hazards.view, hazards.create, hazards.assign, hazards.close, hazards.manage, hazards.manage_types. There is NO worker-participation.view or worker-participation.manage anywhere. hazards.* is definitively the only gate.
- **canDo() definition** [app/Models/User.php:333-361]: public function canDo(string $permissionKey): bool — resolves via permissionLookupKeys($permissionKey) then: (1) explicit deny override (permissionOverrides wherePivot allowed=false) returns false; (2) explicit allow override (allowed=true) returns true; (3) role permissions: roles()->whereHas('permissions', key IN $keys)->exists(). The handoff's `can.manage => user->canDo('hazards.manage')` call is valid against this signature.
- **permissionLookupKeys() alias map** [app/Models/User.php:369-390]: private function permissionLookupKeys(string): array expands a permission key to itself plus aliases. Alias map covers ONLY timesheets/hr.time and vetting keys — there is NO alias entry for hazards.*, so canDo('hazards.manage') matches the literal 'hazards.manage' key only. Relevant if anyone considers aliasing a future worker-participation.* gate to hazards.*.
- **FE assumes /export endpoint (rebuilt prototype)** [C:/Users/steph/Downloads/wp-redesign-extract/design_handoff_worker_participation/frontend/index.tsx:178]: Rebuilt FE prototype: { icon: <Download/>, label: 'Export board report', onClick: () => window.open('/health-safety/worker-participation/export') }. This is a hard-coded literal URL via window.open (NOT a wayfinder route helper), so it will 404 silently in a new tab. No matching route exists in routes/health-safety.php.
- **Current (live) index.tsx has NO export action** [resources/js/pages/health-safety/worker-participation/index.tsx:1396]: The currently-deployed resources/js/pages/health-safety/worker-participation/index.tsx references ONLY the real per-meeting minutes download (/meetings/{id}/minutes/download at line 1396) and consultation document downloads — it does NOT reference a top-level /export. The /export assumption is introduced by the rebuild, not pre-existing.
- **Sibling export pattern A — analytics CSV (same controller family)** [app/Http/Controllers/HealthSafety/HealthSafetyDashboardController.php:263-287]: GET /health-safety/analytics/export -> 'health-safety.analytics.export' -> HealthSafetyDashboardController@analyticsExport, gated permission:hazards.view. Returns response()->streamDownload writing fputcsv rows; filename hs_analytics_{name}_{Ymd_His}.csv; Content-Type text/csv; charset=UTF-8. Closest in-module template for a worker-participation board-report/CSV export, and already under hazards.view.
- **Sibling export pattern A2 — analytics records (JSON preview)** [app/Http/Controllers/HealthSafety/HealthSafetyDashboardController.php:289-304]: GET /health-safety/analytics/records -> 'health-safety.analytics.records' -> analyticsRecords returns JsonResponse (first 200 rows + total) — the pattern for an in-page preview that pairs with the CSV download.
- **Sibling export pattern B — incidents reports export** [routes/incidents.php:121-126]: GET /reports/incidents/export -> name 'incidents.reports.incidents.export' (or similar within the incidents group) -> IncidentReportController@exportCsv, individually gated by middleware('permission:incidents.export') inside an outer 'permission:reports.viewAny|incidents.export' group. Shows the convention of a DEDICATED export permission — worker-participation has no equivalent and would default to hazards.view/manage.
- **Governance board reports (JSON, separate gate)** [routes/health-safety.php:70-71]: GET /health-safety/reports/board-summary -> 'health-safety.reports.board-summary' -> HsGovernanceReportController@boardSummary returns JSON, gated permission:governance.view (NOT hazards.*). If 'Export board report' should pull governance data, note the gate differs.

## WorkerParticipationController + HrCalendarEvent integration audit

- **index() payload shape** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:24-87]: Returns Inertia 'health-safety/worker-participation/index' with keys: representatives (HsRepresentative with user:id,name + site:id,name, desc by created_at), committees (HsCommittee with site:id,name), meetings (=upcomingMeetings: HsCommitteeMeeting with committee:id,name where scheduled_at>=now, orderBy scheduled_at, limit 20), consultations (HsConsultation with site:id,name, desc created_at, limit 50), stats{active_reps,active_committees,meetings_this_month,open_consultations}, sites (active id,name), staff (all users id,name), filters(site_id), can_manage (request user canDo('hazards.manage')).
- **storeMeeting() — HrCalendarEvent write** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:155-191]: After creating committee->meetings()->create([...'status'=>'scheduled','created_by'=>user id]), loops attendeeIds = validated['attendees'] ?? committee->members ?? [] and for EACH userId does HrCalendarEvent::create(['tenant_id'=>user->tenant_id ?? 1, 'title'=>'H&S Committee Meeting: '.committee->name, 'description'=>'Committee meeting at '.(location ?? 'TBC'), 'event_type'=>'hs_meeting', 'starts_at'=>scheduledAt, 'ends_at'=>scheduledAt->copy()->addHour(), 'is_all_day'=>false, 'location'=>location ?? null, 'created_by'=>userId]). Note created_by is set to the ATTENDEE's id, not the actor — one row per attendee.
- **addMeetingAttendees() — HrCalendarEvent write** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:326-358]: Validates user_ids[] (exists:users,id). newUserIds = array_diff(user_ids, meeting->attendees ?? []). Updates meeting->attendees to the unique merge. Then for EACH newUserId: HrCalendarEvent::create(['tenant_id'=>user->tenant_id ?? 1,'title'=>'H&S Committee Meeting: '.(committee->name ?? 'Meeting'),'description'=>'Committee meeting at '.(meeting->location ?? 'TBC'),'event_type'=>'hs_meeting','starts_at'=>meeting->scheduled_at,'ends_at'=>meeting->scheduled_at->copy()->addHour(),'is_all_day'=>false,'location'=>meeting->location,'created_by'=>newUserId]).
- **cancelMeeting() — HrCalendarEvent DELETE via fragile LIKE match** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:391-405]: Sets meeting status='cancelled', then if committee && meeting->scheduled_at: HrCalendarEvent::where('event_type','hs_meeting')->where('starts_at', meeting->scheduled_at)->where('title','like','%'.committee->name.'%')->delete(). No tenant_id scope, no meeting_id linkage — matches purely on event_type + exact starts_at + committee-name substring. This is the ONLY read-side use of event_type='hs_meeting' (a delete filter, not a display read).
- **updateMeeting() — does NOT touch HrCalendarEvent** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:196-210]: Validates status in:scheduled,in_progress,completed,cancelled; minutes; action_items[] with .description/.assigned_to/.due_date required_with. Just meeting->update(validated). No calendar event create/delete here (so editing scheduled_at would orphan the hs_meeting rows; only cancelMeeting deletes them).
- **HrCalendarEvent call-site census (entire repo)** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:180,348,398]: Only 4 PHP files reference HrCalendarEvent: the model (app/Domain/Hr/Models/HrCalendarEvent.php), Hr/CalendarController.php, Hr/ICalController.php, and this WorkerParticipationController.php. Plus 1 test tests/Feature/Hr/HrCalendarEventCrudTest.php. The literal string 'hs_meeting' appears exactly 3 times repo-wide — all 3 inside WorkerParticipationController (lines 180, 348, 398). Zero frontend/.tsx, zero blade, zero notification, zero aggregator references to 'hs_meeting'.
- **READER 1 — Hr/CalendarController@index (reads ALL events, no event_type filter)** [app/Http/Controllers/Hr/CalendarController.php:29-33]: HrCalendarEvent::forTenant(tenantId)->inRange(start,end)->with(creator,site)->orderBy('starts_at')->get() — feeds Inertia 'hr/calendar/index'. NO where('event_type',...) filter, so hs_meeting rows DO appear in the HR calendar UI as generic events. Frontend resources/js/pages/hr/calendar/index.tsx colours by eventTypeColors[e.event_type] ?? '#6b7280' (unknown type just gets the grey fallback). So this READS the rows but is agnostic to the hs_meeting value.
- **READER 2 — Hr/ICalController@feed (reads ALL events, no event_type filter)** [app/Http/Controllers/Hr/ICalController.php:37-51]: HrCalendarEvent::where('starts_at','>=',now()->subMonths(3))->orderBy('starts_at')->limit(100)->get(), each emitted as a VEVENT in the .ics feed. NO event_type filter and NO per-user scope — every tenant's hs_meeting rows in range are exported to anyone with a valid HrICalToken. Reads the rows, agnostic to hs_meeting.
- **SiteCalendarAggregator does NOT read HrCalendarEvent** [app/Services/Sites/Calendar/SiteCalendarAggregator.php:55-68]: app/Services/Sites/Calendar/SiteCalendarAggregator.php uses its own CalendarObligationProvider registry (Inspection, Compliance, Checklist, Hazard, Damage, MealPlan, Vendor, Credential, AssetMaintenance, EmergencyPlan, Respite). Its 'event_type'/'event_types' references are a generic provider-occurrence concept, NOT HrCalendarEvent. No HrCalendarEvent/hr_calendar_events reference anywhere in the aggregator or its providers.
- **My-Day feed does NOT read HrCalendarEvent** [app/Http/Controllers/MyDayActionsController.php]: grep of MyDayActionsController.php (and the My-Day controller set) for HrCalendarEvent/hr_calendar_events/hs_meeting returns no matches. No My-Day surface consumes these rows.
- **HrCalendarEvent.event_type allowed values (writer mismatch)** [app/Http/Controllers/Hr/CalendarController.php:80,110; database/migrations/2026_03_22_100030_create_hr_calendar_events_table.php:15]: Migration column comment + both CalendarController@store and @update validate event_type 'in:company,team,training,social,holiday'. 'hs_meeting' is NOT in that allow-list, so hs_meeting rows can only ever be created by WorkerParticipationController (bypassing the enum) and can never be created/edited via the normal HR calendar endpoints. Column is a plain string (no DB enum constraint), so the out-of-list value is accepted at DB level.
- **Inline validate() — storeRepresentative** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:94-102]: user_id required exists:users,id; site_id required exists:sites,id; election_method required in:elected,appointed,volunteered; elected_at required date; term_expires_at nullable date after:elected_at; training_days_completed nullable integer min:0; notes nullable string max:2000. Persists with status='active', created_by. NOTE: does NOT accept work_group (a fillable column the frontend type expects).
- **Inline validate() — updateRepresentative** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:117-122]: status sometimes in:active,inactive,resigned; training_days_completed sometimes integer min:0; term_expires_at sometimes nullable date; notes sometimes nullable string max:2000.
- **Inline validate() — storeCommittee** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:134-142]: name required string max:255; site_id required exists:sites,id; meeting_frequency required in:weekly,fortnightly,monthly,quarterly; established_at required date; terms_of_reference nullable string max:5000; members required array min:1; members.* exists:users,id. Persists status='active', created_by.
- **Inline validate() — storeMeeting** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:157-163]: scheduled_at required date; location nullable string max:255; agenda_items nullable array; attendees nullable array; attendees.* exists:users,id. Persists status='scheduled', created_by.
- **Inline validate() — updateMeeting** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:198-205]: status sometimes in:scheduled,in_progress,completed,cancelled; minutes sometimes nullable string max:10000; action_items sometimes nullable array; action_items.*.description required_with:action_items string max:500; action_items.*.assigned_to required_with:action_items exists:users,id; action_items.*.due_date required_with:action_items date.
- **Inline validate() — storeConsultation** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:217-223]: title required string max:255; consultation_type required in:change_notification,hazard_review,policy_review,risk_assessment,general; description required string max:5000; site_id required exists:sites,id; consultation_date required date. Persists status='open', initiated_by, created_by.
- **Inline validate() — updateConsultation (status enum A)** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:239-243]: status sometimes in:open,in_progress,closed; worker_feedback_summary sometimes nullable string max:5000; outcome sometimes nullable string max:5000.
- **Inline validate() — updateConsultationStatus (status enum B, conflicts with A)** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:259-265]: status required in:open,feedback_received,actioned,closed; worker_feedback_summary nullable string max:5000; outcome nullable string max:5000; changes_made nullable string max:5000; workers_consulted nullable array. The status enum here (open,feedback_received,actioned,closed) is DIFFERENT from updateConsultation's (open,in_progress,closed).
- **DB schema confirms controller field names (frontend is the drifting side)** [app/Models/HsRepresentative.php:17-37; app/Models/HsCommitteeMeeting.php:18-46; app/Models/HsConsultation.php:18-41]: HsRepresentative fillable = work_group, election_method, elected_at, term_expires_at, training_days_completed (casts elected_at/term_expires_at => date). HsCommitteeMeeting fillable uses scheduled_at/started_at/ended_at (no meeting_date). HsConsultation fillable uses consultation_date + workers_consulted(array). So controller validate() names match the schema; the frontend index.tsx type names do not.
- **Other meeting workflow methods** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:363-439]: completeMeeting (lines 363-386): validates minutes/action_items/confirmed_attendees/actual_attendee_ids, normalises actual_attendee_ids→confirmed_attendees, sets status='completed', ended_at=now() — NO calendar event change. uploadMeetingMinutes/downloadMeetingMinutes (410-439) store/stream private disk. uploadConsultationDocument/downloadConsultationDocument (275-317) likewise. None touch HrCalendarEvent.

## Site Calendar ObligationProvider contract / handoff compile-readiness audit

- **Provider contract — base class** [app/Services/Sites/Calendar/Providers/ObligationProvider.php:12]: There IS an abstract base class. `abstract class ObligationProvider implements CalendarObligationProvider` in app/Services/Sites/Calendar/Providers/ObligationProvider.php. The interface is App\Services\Sites\Calendar\Contracts\CalendarObligationProvider. The handoff `extends ObligationProvider` — CORRECT.
- **obligations() exact signature (VERBATIM)** [app/Services/Sites/Calendar/Contracts/CalendarObligationProvider.php:20,28]: Interface: `public function obligations(array $siteIds, Carbon $start, Carbon $end): array;` (Carbon = Illuminate\Support\Carbon). The handoff's `public function obligations(array $siteIds, Carbon $start, Carbon $end): array` MATCHES exactly. It is NOT `obligations(?int $orgId, …)`. Also required by the interface: `public function sourceKey(): string;` — the handoff implements `sourceKey(): string { return 'participation'; }`. CORRECT.
- **Base helper: inRange()** [app/Services/Sites/Calendar/Providers/ObligationProvider.php:32]: EXISTS. `protected function inRange(?Carbon $date, Carbon $start, Carbon $end): bool` — date-granular (compares toDateString()). Handoff calls inRange(Carbon::parse(...), $start, $end) and inRange($next, ...). MATCHES.
- **Base helper: isoDate()** [app/Services/Sites/Calendar/Providers/ObligationProvider.php:24]: EXISTS. `protected function isoDate(\DateTimeInterface|Carbon|string $date): string` — returns Carbon::parse($date)->toIso8601String(). Handoff calls isoDate($meeting->scheduled_at) etc. MATCHES.
- **Base helper: siteArray()** [app/Services/Sites/Calendar/Providers/ObligationProvider.php:14]: EXISTS. `protected function siteArray($site): ?array` — returns ['id','name','type']. Handoff calls siteArray($committee->site). MATCHES.
- **Base helper: ownerArray()** [app/Services/Sites/Calendar/Providers/ObligationProvider.php:19]: EXISTS. `protected function ownerArray($user): ?array` — returns ['id','name']. Handoff calls ownerArray($rep->user). MATCHES.
- **Base helper: dueStatus()** [app/Services/Sites/Calendar/Providers/ObligationProvider.php:46]: EXISTS. `protected function dueStatus(?Carbon $due, bool $completed): string` — completed→'completed', past-due→'overdue', else 'scheduled'. Handoff calls dueStatus($due, false). MATCHES.
- **CalendarItem constructor (full, in order)** [app/Services/Sites/Calendar/CalendarItem.php:27]: public function __construct(public string $id, public string $source, public string $group, public string $title, public ?string $start, public ?string $end = null, public bool $allDay = false, public string $status = 'scheduled', public ?array $owner = null, public ?string $room = null, public ?string $ref = null, public ?array $site = null, public ?string $link = null, public bool $editable = false, public ?string $eventType = null, public ?string $approvalStatus = null, public ?string $desc = null, public ?string $priority = null, public ?array $recurrence = null, public array $reminders = [], public array $attendeeIds = [], public ?int $seriesId = null, public bool $isOccurrence = false). Required (no default): id, source, group, title, start. All others optional.
- **CalendarItem — handoff param names all valid** [app/Services/Sites/Calendar/CalendarItem.php:48]: Handoff passes (named args): id, source, group, title, start, allDay, status, room, site, link, attendeeIds (meeting); +priority (cadence); +owner (rep). EVERY name exists as a constructor parameter. attendeeIds is typed `array` (not nullable) — fine since the value passed is an array. No positional mismatch risk because all calls use named args.
- **defaultProviders() structure** [app/Services/Sites/Calendar/SiteCalendarAggregator.php:55]: `public static function defaultProviders(): array` returns a flat array of `new XxxObligationProvider()` instances (no args), e.g. `new RespiteObligationProvider(),`. To register the new one: add `new WorkerParticipationObligationProvider(),` to that array AND add a `use App\Services\Sites\Calendar\Providers\WorkerParticipationObligationProvider;` import at the top. The aggregator iterates providers and calls $provider->obligations($siteIds,$start,$end) gated by $provider->sourceKey() being enabled.
- **CalendarSources source shape** [app/Services/Sites/Calendar/CalendarSources.php:18]: Each source is an associative array with EXACTLY these keys: ['key'=>string,'label'=>string,'short'=>string,'group'=>string,'icon'=>string,'origin'=>string]. It's a plain array literal in CalendarSources::all() (NOT an enum). To add participation, append e.g. ['key'=>'participation','label'=>'Worker participation','short'=>'Participation','group'=>'auto','icon'=>'Users','origin'=>'Worker participation']. icon must be a valid lucide-react name.
- **--src-* CSS token convention (3 per source)** [resources/css/app.css:241]: In resources/css/app.css under ':root' (~line 241). Per source THREE tokens: `--src-{key}` (label/dot), `--src-{key}-bg` (chip fill), `--src-{key}-ln` (chip border). Example existing: `--src-respite: oklch(50% 0.16 190); --src-respite-bg: oklch(96% 0.04 190); --src-respite-ln: oklch(88% 0.07 190);`. The frontend builds them dynamically: colorVars() returns var(--src-${item.source}), var(--src-${item.source}-bg), var(--src-${item.source}-ln). There is NO --src-participation/-bg/-ln today — must add all three or participation events render with empty/invalid colour vars.
- **Valid status values (frontend authority)** [resources/js/lib/calendar/recur.ts:26]: recur.ts CalendarStatus union = 'scheduled' | 'overdue' | 'pending' | 'approved' | 'completed' | 'cancelled'. STATUS_TONE map keys: scheduled→info, overdue→critical, pending→warning, approved→success, completed→neutral, cancelled→neutral. Unknown statuses fall back to 'neutral' (no crash). The type is `CalendarStatus | string` so arbitrary strings compile. The handoff emits 'scheduled','completed','cancelled','overdue' (all canonical) AND 'in_progress' (NOT in the union/map → renders neutral grey, not an error).
- **Valid group values** [app/Services/Sites/Calendar/CalendarItem.php:20]: group is one of 'manual' | 'auto' | 'external' in practice. CalendarItem doc says "manual|auto"; frontend TS type is `'manual' | 'auto'`; aggregator also emits 'external' for busy blocks. The handoff uses group:'auto' throughout — CORRECT for an obligation provider.
- **Hs models/columns the handoff reads — all real** [app/Models/HsCommittee.php:39]: HsCommittee: site() BelongsTo, meetings() HasMany, columns status/meeting_frequency/established_at(cast date) — ALL exist. HsCommitteeMeeting: scheduled_at(cast datetime), status, location columns — exist. HsConsultation: site() BelongsTo, consultation_date(cast date), title, status — exist. HsRepresentative: site() BelongsTo, user() BelongsTo, term_expires_at(cast date), status — exist. Migration confirms tables hs_representatives/hs_committees/hs_committee_meetings/hs_consultations.
- **Consultation status enum confirms handoff mapping** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:240]: WorkerParticipationController validates consultation status `in:open,in_progress,closed`. Handoff maps: 'closed'→completed, 'open'→scheduled, else→'in_progress'. The else branch can only be 'in_progress' → fine. (Note: a separate feedback endpoint also allows feedback_received/actioned, which would also hit the else branch and become 'in_progress'.)
- **Committee meeting status values** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:199]: WorkerParticipationController validates meeting status `in:scheduled,in_progress,completed,cancelled` (default 'scheduled'). Handoff's match maps cancelled→cancelled, completed→completed, default→scheduled. An 'in_progress' meeting therefore becomes calendar status 'scheduled' (acceptable).

## backend-compliance-engine

- **createObligation full signature (VERBATIM)** [app/Domain/Governance/Services/ComplianceEngineService.php:17-26]: public function createObligation(string $framework, string $title, string $description, string $frequency, User $owner, ?Carbon $dueDate = null, ?string $obligationCode = null, ?array $reminderDays = null): ComplianceObligation
- **Named-arg verification — ALL 8 handoff names MATCH** [app/Domain/Governance/Services/ComplianceEngineService.php:17-26]: Handoff uses framework:, title:, description:, frequency:, owner:, dueDate:, obligationCode:, reminderDays:. Real params: $framework, $title, $description, $frequency, $owner, $dueDate, $obligationCode, $reminderDays. Exact match on every name. NOT 'code' (it is obligationCode), NOT 'dueAt' (it is dueDate). No BLOCKER.
- **owner type** [app/Domain/Governance/Services/ComplianceEngineService.php:21, 38]: Expects an App\Models\User MODEL instance (type-hinted `User $owner`), not an id. Internally reads $owner->id to set owner_id. Handoff passes $actor / $owner (User) — correct. Non-nullable: passing null is a TypeError.
- **framework type** [app/Domain/Governance/Models/ComplianceObligation.php:167-182; database/migrations/2026_02_06_100060_create_compliance_obligations_table.php:15]: Free-form string. DB column is $table->string('framework') with NO enum/check constraint. Any string accepted; 'HSWA' (handoff value) is fine at the DB level. Note: getFrameworkLabel() match() has no 'HSWA' case, so it falls through to default => returns the raw 'HSWA' string (cosmetic, not an error).
- **frequency valid values** [app/Domain/Governance/Services/ComplianceEngineService.php:47-57, 78]: Free string column (no enum). calculateNextDueDate() match() recognises 'monthly','quarterly','annual' (default branch = addMonth for anything else incl. 'event_driven','ad_hoc'). completeObligation() treats 'ad_hoc' and 'event_driven' as non-recurring (skips scheduleNextOccurrence). All three handoff values — 'event_driven','annual','quarterly' — are valid and behave sensibly.
- **createObligation does NOT call scheduleReminders or scheduleNextOccurrence** [app/Domain/Governance/Services/ComplianceEngineService.php:27-42, 78-80, 86-108]: createObligation() body only does ComplianceObligation::create([...]) and returns it. It does NOT call scheduleReminders() (that is the CALLER's job — see ComplianceController.php:88 which calls it explicitly after createObligation). scheduleNextOccurrence() is only invoked from completeObligation() for recurring frequencies, and it itself calls createObligation() (without scheduling reminders).
- **scheduleReminders signature** [app/Domain/Governance/Services/ComplianceEngineService.php:141-162]: public function scheduleReminders(ComplianceObligation $obligation): void — deletes pending reminders, then for each $obligation->reminder_days creates a ComplianceReminder at due_date->subDays($daysBefore), skipping any already in the past.
- **scheduleNextOccurrence signature** [app/Domain/Governance/Services/ComplianceEngineService.php:86-108]: protected function scheduleNextOccurrence(ComplianceObligation $completed): void — PROTECTED (not callable externally); computes next due date, dedupes on framework+obligation_code+due_date, then re-creates via createObligation.
- **ComplianceObligation columns** [app/Domain/Governance/Models/ComplianceObligation.php:17-39]: Fillable: framework, obligation_code, obligation_title, description, frequency, workforce_requirement_id, due_date, next_due_date, reminder_days, owner_id, backup_owner_id, status, completed_at, completed_by, evidence_required, evidence_provided, sign_off_required, sign_off_role, signed_off_at, signed_off_by, notes. Note title is stored as obligation_title (createObligation maps $title->obligation_title).
- **status valid values** [app/Domain/Governance/Models/ComplianceObligation.php:61-76, 150-157; database/migrations/2026_02_06_100060_create_compliance_obligations_table.php:30-31]: DB: $table->string('status')->default('not_due'). Documented values (migration comment): not_due, due_soon, overdue, complete, exempt. updateStatus() (on saving) auto-sets not_due/due_soon/overdue from due_date unless already 'complete'. Handoff's `status != 'complete'` filter is valid — 'complete' is the canonical completed value (set by markComplete()).
- **due_date / framework column types** [database/migrations/2026_02_06_100060_create_compliance_obligations_table.php:22-27, 36; app/Domain/Governance/Services/ComplianceEngineService.php:27, 35-40]: due_date: $table->date('due_date') NOT NULL, cast 'date'. next_due_date: date NOT NULL — createObligation sets BOTH to $dueDate so a null dueDate is backfilled via calculateNextDueDate(). owner_id: foreignId constrained('users') NOT NULL. evidence_required: text() NOT NULL no default — createObligation always passes true (boolean cast), so service callers are safe.
- **SyncDonorFundComplianceCommand structure (the mirrored pattern)** [app/Domain/Governance/Console/SyncDonorFundComplianceCommand.php:18-92]: signature: 'governance:sync-donor-fund-compliance {--lead-days=30}'. Guards Schema::hasTable('fin_donor_funds'). Queries funds with next_report_due inside the lead window. Dedupe: obligationCode = "DONOR-FUND-{id}-{next_report_due}" then ComplianceObligation::where('obligation_code',$code)->first() — skip if exists (no status filter). It does NOT use the service — it calls ComplianceObligation::create([...]) DIRECTLY (so owner_id can be null: 'owner_id' => $fund->owner_id ?? null) and writes status 'not_due', frequency 'event_driven'. Wraps create in try/catch and logs via GovernanceAuditService::log('compliance.auto_created',...). Does NOT schedule reminders.
- **Real donor-fund command location differs from handoff's stated path** [app/Domain/Governance/Console/SyncDonorFundComplianceCommand.php:3; handoff backend/app/Console/Commands/SyncParticipationObligationsCommand.php:3]: Handoff says it mirrors app/Console/Commands/SyncDonorFundComplianceCommand.php, but the real file is app/Domain/Governance/Console/SyncDonorFundComplianceCommand.php (namespace App\Domain\Governance\Console). The new SyncParticipationObligationsCommand is placed at app/Console/Commands (namespace App\Console\Commands) — different namespace from the command it mirrors. Both are valid Artisan command locations, but they do not share a namespace.

## H&S Hero Kit (hs-hero-kit.tsx) + WorkflowRibbon API audit for the rebuilt Worker Participation page

- **fmt helper signature** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:45]: export function fmt(value: number | null | undefined, suffix = ''): string — returns '—' for null/undefined, else `${value}${suffix}`.
- **HeroShell props (has footer)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:55]: export function HeroShell({ children, footer }: { children: ReactNode; footer?: ReactNode }). Yes — `footer` is an optional ReactNode rendered in a border-top band below the main content. WP uses it.
- **HeroStatusPill props** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:78]: export function HeroStatusPill({ children }: { children: ReactNode }). Takes only children (the eyebrow text). No `label` prop.
- **HeroMedallion props (icon)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:91]: export function HeroMedallion({ icon: Icon }: { icon: LucideIcon }). Yes — single `icon` prop typed LucideIcon.
- **HeroCluster props (title + icon)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:146]: export function HeroCluster({ title, icon: Icon, children }: { title: string; icon: LucideIcon; children: ReactNode }). Yes — `title: string` and `icon: LucideIcon`, wraps tiles in a 2/4-col grid.
- **HeroClusterTile props (href/label/value/caption/tone + delta)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:106]: export function HeroClusterTile({ href?, label: string, value: string, caption: string, tone: Tone, delta?: string, deltaTone?: Tone }). `value` and `caption` are STRINGS (use fmt() to coerce numbers, which WP does). tone: 'success'|'warning'|'critical'|'neutral'. href optional → renders as Inertia Link when present.
- **HeroComplianceBadges FULL prop interface (verbatim)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:177]: export function HeroComplianceBadges({ worksafeAwaiting, sdsExpiring, drillsDue = 0, drillsOverdue = 0, ngaPaerewaCertified = true, firstAidOk = true }: { worksafeAwaiting: number; sdsExpiring: number; /** Drills due-soon → warning. */ drillsDue?: number; /** Drills past their cadence → critical; outranks `drillsDue`. */ drillsOverdue?: number; ngaPaerewaCertified?: boolean; firstAidOk?: boolean; }). Required: worksafeAwaiting:number, sdsExpiring:number. Optional: drillsDue, drillsOverdue, ngaPaerewaCertified, firstAidOk. NO generic badge array; the five chip LABELS are hard-coded inside (WorkSafe notifiable, Ngā Paerewa NZS 8134:2021, Hazardous substances/SDS, Fire drills, First aid).
- **HeroComplianceBadges hard-coded chip labels** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:196]: Chip text is fixed: `WorkSafe notifiable · ${worksafeAwaiting} awaiting`; `Ngā Paerewa NZS 8134:2021 · Certified|Review due`; `Hazardous substances · ${sdsExpiring} SDS expiring` | `· SDS current`; `Fire · ${drillsOverdue} drills overdue` | `· ${drillsDue} drills due` | `· Drills current`; `First aid · Cover OK|Cover gaps`. None are parameterisable — passing WP numbers yields safety wording.
- **HeroSegmented props (label/variant/ariaLabel/items/value/onChange)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:271]: export function HeroSegmented({ label?: string; items: readonly HeroSegItem[]; value: string; onChange: (key: string) => void; ariaLabel: string; variant?: 'pill' | 'segmented' }). NOTE item type is HeroSegItem = { key: string; label: string; popover?: ReactNode } — keyed by `key`, NOT `id`. ariaLabel is REQUIRED. WP passes variant='pill' and items as {key,label}[] correctly.
- **HeroSegItem type** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:262]: export type HeroSegItem = { key: string; label: string; popover?: ReactNode }. value/onChange operate on `key`.
- **Tone union** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:25]: export type Tone = 'success' | 'warning' | 'critical' | 'neutral'. (BadgeTone, used internally by compliance chips, is a narrower 'success'|'warning'|'critical'.)
- **Other exports present in the kit (not imported by WP)** [resources/js/pages/health-safety/components/hs-hero-kit.tsx:356]: Also exported: DOT_CLASS, HeroSummaryMetric ({ tone: Tone; children }), HeroSummaryStrip ({ label?, children, collapsed?, onToggle?, toggleLabel? }), HeroSegItem, Tone. WP does not use the summary strip.
- **WorkflowRibbon `current` accepted values (verbatim union)** [resources/js/pages/health-safety/components/workflow-ribbon.tsx:21]: export type WorkflowStage = 'report' | 'investigate' | 'resolve' | 'analyse'; and export function WorkflowRibbon({ current }: { current: WorkflowStage }). So valid values are exactly report/investigate/resolve/analyse. 'report' IS valid. There is NO 'analyze' (NZ/UK spelling 'analyse') and NO standalone value for participation/dashboard. WP passing current="report" is correct and matches incidents/safeguarding/fleet front-doors.
- **WorkflowRibbon step targets** [resources/js/pages/health-safety/components/workflow-ribbon.tsx:23]: Steps link: report→/incidents, investigate→/health-safety/events, resolve→/health-safety/corrective-actions, analyse→/health-safety/analytics, plus a leading H&S→/health-safety crumb. WP appears in the ribbon only as a 'report'-stage sibling (highlights the 'Report & respond' step, which links to /incidents, not WP).
- **Real-world consumers of HeroComplianceBadges (for the correct data shape)** [resources/js/pages/health-safety/components/command-centre-hero.tsx:274]: command-centre-hero.tsx:274 calls it with worksafeAwaiting={notifiableAwaiting} sdsExpiring={sdsExpiring} drillsDue={drillsDue}; analytics.tsx:709 calls it with worksafeAwaiting={worksafe_notifiable.awaiting} sdsExpiring={0} drillsOverdue={drillsOverdue}. Both feed genuine safety data. incidents/index.tsx, safeguarding/index.tsx and fleet-assets/incidents/index.tsx do NOT render HeroComplianceBadges at all — they only use the WorkflowRibbon + the rest of the hero kit. This shows the established pattern: register front-doors omit the compliance badge row; only the dashboard & analytics use it.

## Shared frontend kit API audit (register-row-kit + rostering barrel) vs the rebuilt worker-participation index.tsx

- **Tone union (register-row-kit) — EXACT members** [resources/js/pages/health-safety/components/register-row-kit.tsx:16]: `export type Tone = 'success' | 'warning' | 'critical' | 'neutral';` — exactly 4 members. NOT 'info', NOT 'primary'. (The doc comment claims it 'Matches the Tone union exported by hs-hero-kit'.)
- **TONE_BG (register-row-kit)** [resources/js/pages/health-safety/components/register-row-kit.tsx:18-23]: `export const TONE_BG: Record<Tone, string>` — yes, Record<Tone,string>. Keys: success/warning/critical/neutral → e.g. 'bg-status-success-bg text-status-success', neutral='bg-muted text-muted-foreground'.
- **TONE_DOT (register-row-kit)** [resources/js/pages/health-safety/components/register-row-kit.tsx:25-30]: `export const TONE_DOT: Record<Tone, string>` — yes, Record<Tone,string>. success/warning/critical='bg-status-success/-warning/-critical', neutral='bg-muted-foreground'.
- **titleCase (register-row-kit)** [resources/js/pages/health-safety/components/register-row-kit.tsx:32-34]: `export function titleCase(s: string): string` — replaces _ and - with space, upcases each word start.
- **initials (register-row-kit)** [resources/js/pages/health-safety/components/register-row-kit.tsx:43-48]: `export function initials(label: string | null | undefined): string` — returns 'HS' when falsy; otherwise 2-letter uppercase initials.
- **entityTone (register-row-kit) — signature** [resources/js/pages/health-safety/components/register-row-kit.tsx:51-53]: `export function entityTone(id: number): string` — returns a bg/text class from a 4-entry palette keyed by id % 4. Takes a number, returns a className string (NOT a Tone).
- **FlagBadge (register-row-kit) — props + its OWN tone union** [resources/js/pages/health-safety/components/register-row-kit.tsx:57-67]: Props: `{ icon: LucideIcon; children: ReactNode; tone: 'critical' | 'warning' | 'success' | 'info' | 'neutral'; title: string }`. NOTE: `children` is REQUIRED (not optional), and FlagBadge's tone union is BROADER than `Tone` — it adds 'info' (5 members) and omits nothing. `icon` is a LucideIcon component (passed as `icon={GraduationCap}`), not a ReactNode element.
- **RegisterTableHeader (register-row-kit) — props** [resources/js/pages/health-safety/components/register-row-kit.tsx:87-99]: Props: `{ icon: LucideIcon; title: string; subtitle?: string; hint?: string; hintIcon?: LucideIcon }`. icon and hintIcon are LucideIcon components; title required; subtitle/hint/hintIcon optional.
- **rostering barrel — re-export paths** [resources/js/components/rostering/index.ts:53,102-106,114]: `export { EntityFilter, type EntityFilterOption } from './entity-filter';` (line 53); `export { ShiftContextMenu, type ShiftCtxItem, type ShiftCtxState } from './shift-context-menu';` (lines 102-106); `export { TabStrip, type RosterTabItem, type RosterTabTone } from './tab-strip';` (line 114). All six names the rebuilt file imports are exported by the barrel.
- **TabStrip (rostering) — props** [resources/js/components/rostering/tab-strip.tsx:36-48]: Props: `{ value: string; onChange: (next: string) => void; items: RosterTabItem[]; className?: string; ariaLabel?: string }`. ariaLabel defaults to 'Roster views'.
- **RosterTabItem + RosterTabTone (rostering) — EXACT fields & tone members** [resources/js/components/rostering/tab-strip.tsx:5-19]: `type RosterTabItem = { id: string; label: string; icon: ComponentType<{ className?: string }>; tone: RosterTabTone; badge?: ReactNode }`. `type RosterTabTone = 'primary' | 'warning' | 'success' | 'info' | 'violet' | 'critical'` (6 members — includes 'info' and 'violet'). tone is REQUIRED on each item.
- **ShiftContextMenu (rostering) — props** [resources/js/components/rostering/shift-context-menu.tsx:29-35]: Props: `{ ctx: ShiftCtxState; onClose: () => void }`. Both required.
- **ShiftCtxItem (rostering) — discriminated union, EXACT fields** [resources/js/components/rostering/shift-context-menu.tsx:7-17]: `type ShiftCtxItem = { sep: true } | { sep?: false; icon: ReactNode; label: string; sub?: string; kbd?: string; tone?: 'primary' | 'critical'; onClick?: () => void }`. Separator variant is `{ sep: true }`. On the item variant: icon (ReactNode, required — pass JSX like <Eye/>), label (required), sub/kbd optional, tone optional and ONLY 'primary' | 'critical', onClick optional.
- **ShiftCtxState (rostering) — EXACT fields** [resources/js/components/rostering/shift-context-menu.tsx:19-27]: `type ShiftCtxState = { x: number; y: number; tag: string; tagBg?: string; tagColor?: string; meta: string; items: ShiftCtxItem[] }`. x/y/tag/meta/items are all REQUIRED; tagBg/tagColor are optional extras. (Handoff hypothesis listed x/y/tag/meta/items — all correct; it omitted the optional tagBg/tagColor.)
- **EntityFilter + EntityFilterOption (rostering) — props** [resources/js/components/rostering/entity-filter.tsx:20-51]: Props (EntityFilterProps): `{ label: string; allLabel: string; items: EntityFilterOption[]; value: number | null; onChange: (next: number | null) => void; onDark?: boolean; className?: string; pluralLabel?: string }`. `type EntityFilterOption = { id: number; name: string; description?: string | null }`. onDark defaults to false. Rebuilt file passes items={sites} ({id,name}[]) which is assignable since description is optional.
- **index.tsx tone usages cross-check (the key risk)** [frontend/index.tsx:80-88,111-112,128-174]: 'info' appears ONLY at lines 111-112 on RosterTabItem.tone (valid — RosterTabTone includes 'info'). 'primary' appears ONLY at lines 128/134/142/156/174 on ShiftCtxItem.tone (valid — 'primary' is allowed). Every `Record<string, Tone>` (lines 80/81/82) uses only success/warning/critical/neutral. No register-kit Tone is ever given 'info' or 'primary'.

## wizard-kit-and-add-client-modal

- **shell.tsx exports** [resources/js/components/wizard/shell.tsx:20]: Named exports: `type WizardStep` (line 20), `function WizardShell` (27), `function WizardStepPane` (213), `function WizardSuccessPane` (222), `function ReviewCard` (252), `function ReviewRow` (292). No default export.
- **WizardStep type shape** [resources/js/components/wizard/shell.tsx:20-25]: `type WizardStep = { key: string; label: string; blurb: string; icon: ComponentType<{ className?: string }> }`. Note the field is `blurb` (not `description`/`sub`), and `icon` is a component, not a node.
- **WizardShell ALL props** [resources/js/components/wizard/shell.tsx:27-63]: Props object: `open: boolean`; `onClose: () => void`; `title: string` (sr-only DialogTitle); `description: string` (sr-only DialogDescription); `railIcon: ComponentType<{className?:string}>` (rail medallion icon, destructured as RailIcon); `railTitle: string`; `railSub: string`; `steps: readonly WizardStep[]`; `stepIndex: number`; `onStepClick: (index: number) => void`; `pct?: number | null` (completeness; rail meter hidden when null); `pctLabel?: string` (default 'Completeness'); `footerStart?: ReactNode` (left footer slot e.g. Back); `footerEnd?: ReactNode` (right footer slot e.g. Cancel/Continue); `success?: ReactNode` (when set, REPLACES entire rail+body); `children?: ReactNode` (current step body). There is NO `current`, `onStepChange`, `footer`, or boolean `isReview` prop — caller owns step state and footer composition.
- **WizardShell render contract** [resources/js/components/wizard/shell.tsx:64-209]: DialogContent is `className="overflow-hidden p-0 [&>button]:hidden"` with inline `style={{ maxWidth:'min(94vw, 980px)', width:'min(94vw, 980px)' }}`. Body is `flex h-[min(88vh,760px)] min-h-0 overflow-hidden`. Rail: `hidden w-[248px] shrink-0 flex-col gap-1 overflow-y-auto border-r border-sidebar-border bg-sidebar p-4 sm:flex` (collapses below sm). Header text: `Step {stepIndex+1} of {steps.length} · {label}`. A 3px progress strip (`h-[3px] bg-muted` track, `bg-primary` fill width `((stepIndex+1)/steps.length)*100%`). Scroll body: `min-h-0 flex-1 overflow-x-hidden overflow-y-auto px-6 py-6`. Footer: `flex shrink-0 items-center justify-between gap-3 border-t border-border bg-muted/30 px-5 py-3.5` with footerStart left, footerEnd right.
- **Rail step states** [resources/js/components/wizard/shell.tsx:95-146]: Each rail step is a <button onClick={()=>onStepClick(i)}>. active = i===stepIndex (bg-primary/10, primary number badge), complete = i<stepIndex (bg-status-success-bg + Check icon), else muted with the step's own Icon. Number badge: `h-[26px] w-[26px] rounded-full`.
- **WizardStepPane** [resources/js/components/wizard/shell.tsx:213-219]: `function WizardStepPane({ children }: { children: ReactNode })` — wraps body in `motion-safe:animate-in motion-safe:fade-in-0 motion-safe:slide-in-from-right-2 motion-safe:duration-300`. (Add-client instead inlines `animate-in duration-300 fade-in slide-in-from-right-2` per step without using this helper.)
- **WizardSuccessPane** [resources/js/components/wizard/shell.tsx:222-249]: `function WizardSuccessPane({ title, blurb, actions }: { title: string; blurb: ReactNode; actions: ReactNode })`. Renders centered green Check medallion (`h-[76px] w-[76px] bg-status-success-bg`) + Sparkles accent, h2 title, muted blurb (max-w-md), actions row. minHeight `min(60vh, 460px)`. Pass this as WizardShell's `success` prop to swap the whole body.
- **ReviewCard (shell export)** [resources/js/components/wizard/shell.tsx:252-289]: `function ReviewCard({ icon: Icon, title, onEdit, span, children }: { icon: ComponentType<{className?:string}>; title: string; onEdit?: () => void; span?: boolean; children: ReactNode })`. Card `rounded-xl border border-border bg-card/70 p-4`, optional `sm:col-span-2` when span. Edit button (Pencil + 'Edit', primary) renders only when onEdit provided. NOTE: in shell.tsx onEdit is OPTIONAL; in add-client's local copy it is REQUIRED.
- **ReviewRow (shell export)** [resources/js/components/wizard/shell.tsx:292-314]: `function ReviewRow({ label, value }: { label: string; value?: ReactNode })`. Label/value flex row, bottom border per row (last:border-0); empty value (null or '') renders an em-dash '—' in muted.
- **primitives.tsx exports** [resources/js/components/wizard/primitives.tsx:21-39]: `type IconType = ComponentType<{ className?: string }>`; const class strings WIZARD_RAIL_CLASS, WIZARD_PROGRESS_TRACK_CLASS, WIZARD_PROGRESS_BAR_CLASS, WIZARD_FOOTER_CLASS; functions FieldErr, Field, SubHead, StepHead, InfoCard, SelectInput, Segmented, ChipMulti, TilePicker, Ring. All 10 primitives named by the handoff exist.
- **Field / FieldErr** [resources/js/components/wizard/primitives.tsx:44-88]: `Field({ label?: string; required?: boolean; hint?: string; error?: string; span?: boolean; children: ReactNode })` — renders Label (with red `*` when required, muted hint), children, then `<FieldErr>{error}</FieldErr>`; `span` adds sm:col-span-2. `FieldErr({ children?: ReactNode })` returns null when empty, else red AlertTriangle + text in text-status-critical.
- **SelectInput** [resources/js/components/wizard/primitives.tsx:155-180]: `SelectInput({ value: string; onChange: (v: string) => void; placeholder: string; options: { value: string; label: string }[] })` — wraps shadcn Select; passes `value || undefined` so placeholder shows when empty. options is value/label objects (NOT plain strings).
- **Segmented** [resources/js/components/wizard/primitives.tsx:182-216]: `Segmented<T extends string>({ value: T; onChange: (v: T) => void; options: { value: T; label: string; icon?: IconType }[] })` — pill group `rounded-lg bg-muted p-1`, active item `bg-card text-foreground shadow-sm`, aria-pressed. Generic over the string union.
- **ChipMulti** [resources/js/components/wizard/primitives.tsx:218-253]: `ChipMulti({ values: string[]; onChange: (v: string[]) => void; options: string[] })` — multi-select rounded-full chips; toggles membership; active chip shows Check + primary styling. options is a plain string[].
- **TilePicker** [resources/js/components/wizard/primitives.tsx:255-332]: `TilePicker({ value: string; onChange: (v: string) => void; options: { key: string; label: string; description?: string; icon?: IconType; accent?: string; meta?: string }[]; cols?: 2 | 3 })`. Single-select tile grid (1→2 cols, or 2→3 cols at sm). Selected key === value gets primary border/ring; `accent` colours the icon when inactive; `meta` is an optional highlighted primary line under description.
- **StepHead / SubHead / InfoCard** [resources/js/components/wizard/primitives.tsx:90-149]: `StepHead({ icon: IconType; title: string; blurb: string })` — large step header: primary-tinted icon chip + h2 title + muted blurb. `SubHead({ icon: IconType; children: ReactNode })` — `col-span-full` uppercase 11px section label, used inside grids. `InfoCard({ icon: IconType; tone?: 'info'|'warn'|'crit'; children: ReactNode })` — `col-span-full` tinted callout (info=primary, warn=status-warning, crit=status-critical).
- **Ring** [resources/js/components/wizard/primitives.tsx:335-367]: `Ring({ pct: number; size?: number })` (default size 56) — SVG completeness ring, muted track + primary arc (strokeDashoffset = c*(1-pct/100)), centered `{pct}%` label. Used in review steps.
- **add-client steps definition** [resources/js/components/clients/add-client-dialog.tsx:319-369]: Steps are a module-level `const STEPS: { key: StepKey; label: string; icon: IconType; blurb: string }[]` of 8: basics, cultural, support, health, about, care, contacts, review. StepKey is a string-union type. The active body is dispatched via a `StepBody` switch on `cur.key`; the 'review' key renders `<ReviewStep>` instead.
- **add-client uses useForm** [resources/js/components/clients/add-client-dialog.tsx:809-837]: `const form = useForm<ClientWizardForm>(formWithInitialValues(...))`; `const { data, setData, processing } = form`. Helpers: `set(k,v)` → setData(k, v as any); `setMedical(k,v)` → setData('medical', {...data.medical,[k]:v}); `setMany(partial)` → setData(prev=>({...prev,...partial})). Errors merge local validation `errors` state with `form.errors`.
- **add-client state + nav** [resources/js/components/clients/add-client-dialog.tsx:814-859]: Local React state: `stepIndex` (useState 0), `errors` (Record<string,string>), `done` (boolean → shows SuccessPane). `next()` runs validateStep(cur.key,data); if errors, set+abort, else advance. `back()` decrements. `goToStep(key)` finds index by key. Completeness `pct = useMemo(()=>completionPct(data),[data])` drives both the rail meter and the review Ring.
- **add-client validateStep** [resources/js/components/clients/add-client-dialog.tsx:710-749]: `validateStep(key: StepKey, d: ClientWizardForm): Record<string,string>` is client-side and mirrors StoreClientRequest. basics: first_name, last_name, date_of_birth, site_id, service_context_id required; email regex; NHI regex /^[A-Z]{3}\d{4}$/i; email required if create_client_portal_user. contacts: if any contact data entered, primary (index 0) name+phone required, keyed `emergency_contacts.0.name`/`.phone`.
- **add-client submit / onError-jump / onSuccess** [resources/js/components/clients/add-client-dialog.tsx:861-902]: `submit(addAnother: boolean)`: re-runs validateStep over ALL steps into `all`; if any error, setErrors(all) + goToStep(stepForError(firstKey)) and return. Options: `{ forceFormData: true, preserveScroll: true, preserveState: true, onSuccess, onError }`. onSuccess: edit→setDone+onSaved(clientId); addAnother→resetAll(); else setDone(true). onError(errs): goToStep(stepForError(Object.keys(errs)[0])). Edit mode: form.transform adds `_method:'put'` and POSTs `/operations/clients/{clientId}`; create POSTs `/operations/clients`.
- **stepForError mapping** [resources/js/components/clients/add-client-dialog.tsx:566-600]: `STEP_FOR_PREFIX` array maps a server field-name prefix to its StepKey (e.g. medical/conditions→health, emergency_contacts→contacts, transport_/fluid_/seizure_→support, funding_→care). `stepForError(field)` returns the first matching step, defaulting to 'basics'. This is how onError jumps to the failing step.
- **add-client Save & add another** [resources/js/components/clients/add-client-dialog.tsx:1082-1096]: On the review step footer, a `variant="secondary"` 'Save & add another' button (create mode only, hidden in edit) calls submit(true); its onSuccess calls resetAll() which does form.reset()+clearErrors()+re-seed initial+setStepIndex(0)+setDone(false). The SuccessPane also offers an 'Add another' outline button (create only) wired to onAddAnother=resetAll.
- **add-client footer (sticky Back/Cancel/Next/Submit)** [resources/js/components/clients/add-client-dialog.tsx:1060-1125]: Footer band `border-t border-border bg-muted/30 px-5 py-3.5`. Left: ghost 'Back' (ChevronLeft) when stepIndex>0. Right: outline 'Cancel' always; on review → ['Save & add another' (create only) + primary 'Create {clientSingular}'/'Save profile' with Loader2 spinner when processing]; otherwise primary 'Continue' (ChevronRight) calling next().
- **add-client file upload** [resources/js/components/clients/add-client-dialog.tsx:636-704]: File upload is bespoke, not a kit primitive: profile_photo is `File | null` on the form; `PhotoField` renders a hidden `<input type="file" accept="image/png,image/jpeg">`, sets the File via set('profile_photo', file) and previews via URL.createObjectURL. Multipart enabled by `forceFormData: true` in the post options. (Repo also has components/ui/file-dropzone.tsx for richer uploads, used by Safeguarding/Incidents — not used here.)
- **add-client review + completeness** [resources/js/components/clients/add-client-dialog.tsx:418-431]: ReviewStep shows a Ring(pct) banner + a 2-col grid of ReviewCard/ReviewRow per section, each card's onEdit calling goToStep(...). Completeness: completionPct(data) counts filled COMPLETION_FIELDS + COMPLETION_MEDICAL + 1 for a named+phoned contact, rounded to %. isFilled treats '', null, false, and [] as empty.
- **add-client dialog width / wrapper** [resources/js/components/clients/add-client-dialog.tsx:769-789]: AddClientDialog wraps its own Dialog/DialogContent (NOT WizardShell): `className="overflow-hidden p-0 [&>button]:hidden"`, width `min(94vw, 1080px)` (wider than WizardShell's 980px), sr-only DialogTitle/Description, body only mounted when isOpen. Body root: `flex h-[min(92vh,860px)] min-h-0 overflow-hidden` (taller than shell's 760px).
- **AddClientDialogProps** [resources/js/components/clients/add-client-dialog.tsx:755-767]: `{ isOpen: boolean; onClose: () => void; sites: Option[]; serviceContexts: ServiceContextOption[]; keyWorkers: Option[]; geofences: Option[]; defaultServiceContextId?: number|null; clientSingular?: string; clientId?: number; initialValues?: Partial<ClientWizardForm>; onSaved?: (clientId: number) => void }`. Note the open prop is `isOpen` here, vs `open` on WizardShell.

## premium-document-upload-component-and-wiring

- **Exports of file-dropzone.tsx** [resources/js/components/ui/file-dropzone.tsx:11,22,109,152]: Exactly: formatFileSize(bytes:number):string, FileDropzone, StagedFileCard, AttachmentUploader. No default export. eslint no-restricted-syntax disabled at top (bespoke chrome, semantic tokens only).
- **FileDropzone props** [resources/js/components/ui/file-dropzone.tsx:22-103]: { onFiles:(files:File[])=>void; accept?:string; multiple?:boolean=true; title?:string='Drag & drop files here'; hint?:string='PDF, Word, images'; disabled?:boolean=false }. Renders a role=button keyboard-activatable dashed dropzone (Enter/Space + drag handlers) with an UploadCloud medallion, title, 'or browse', and the hint; plus a hidden <input type=file accept multiple>. It only emits chosen files via onFiles (resetting input.value) — it does NOT upload or store anything itself.
- **StagedFileCard props** [resources/js/components/ui/file-dropzone.tsx:109-139]: { file:File; onRemove:()=>void; children?:ReactNode }. Renders a rounded card: object-URL <img> thumbnail for image/* files else a FileText glyph, truncated file.name, formatFileSize(file.size), a Trash2 remove button (aria-label 'Remove file'), and renders children below as a per-file metadata row. Revokes the object URL on unmount. Pure presentational — caller owns the staged array + upload.
- **AttachmentUploader props** [resources/js/components/ui/file-dropzone.tsx:152-166]: { endpoint:string; noteField?:string|null=null; sensitive?:{field:string;label:string}|null=null; accept?:string; hint?:string }. Self-contained: stages files (FileDropzone -> add), renders each as a StagedFileCard with optional note Input (when noteField) and optional sensitive checkbox (when sensitive), and an 'Upload N files' button.
- **AttachmentUploader upload mechanics (the canonical post)** [resources/js/components/ui/file-dropzone.tsx:180-209]: On upload it iterates the staged queue SEQUENTIALLY; for each it builds `const fd = new FormData(); fd.append('file', it.file); if(noteField) fd.append(noteField, it.note); if(sensitive) fd.append(sensitive.field, it.sensitive?'1':'0');` then `router.post(endpoint, fd, { preserveScroll:true, preserveState:true, onSuccess: ()=>{ remove(it.id); next(i+1) }, onError: ()=> setError(...) })`. Each successful file drops out of the staged list (acts as a progress queue); the parent page's Inertia props refresh from the redirect()->back() response, so attachments re-render WITHOUT an explicit router.reload. Field name 'file' is HARDCODED and cannot be configured via props.
- **Canonical multi-file caller #1 — Incidents** [resources/js/components/incidents/incident-detail-dialog.tsx:519-573]: PhotosSection gates on `d.status==='draft' && d.can.update` then renders `<AttachmentUploader endpoint={`/incidents/${d.id}/attachments`} hint="PDF, Word, images — up to 10 MB each" />` (no noteField/sensitive). Existing files listed via d.attachments.map -> AttachmentRow; remove = `router.delete(`/incidents/${incidentId}/attachments/${a.id}`, { preserveScroll:true })`; portal toggle = router.patch(...). Download is a plain <a href={a.download_url}>.
- **Canonical multi-file caller #2 — Safeguarding (note + sensitive)** [resources/js/components/safeguarding/concern-dialog.tsx:1134-1196]: EvidenceSection: `<AttachmentUploader endpoint={`/safeguarding/${d.id}/attachments`} noteField="notes" sensitive={{ field:'is_sensitive', label:'Sensitive — restrict to cleared staff' }} hint="PDF, Word, images — up to 10 MB each" />`. Locked (need-to-know) rows render a Lock placeholder; others show image thumb/glyph, name, fmtSize, uploaded_by, created_at, notes, Download link, and Trash2 delete via router.delete(...,{preserveScroll:true}).
- **Canonical multi-file caller #3 — Fleet (accept narrowing)** [resources/js/components/fleet/fleet-incident-dialog.tsx:561-565]: `<AttachmentUploader endpoint={`/fleet-assets/incidents/${d.id}/attachments`} noteField="notes" accept="image/*,.pdf,.doc,.docx" hint="Scene/damage photos, dashcam, the TCR or insurance PDF — up to 20 MB each" />` gated on canUpload.
- **Backend contract for the multi-file pattern (validates 'file')** [app/Http/Controllers/IncidentController.php:1060; app/Http/Controllers/SafeguardingAttachmentController.php:21-42]: Incidents IncidentController@uploadAttachment validates 'file'=>['required','file','max:10240'] and reads $request->file('file'). ClientIncidentController:162 + FleetAssets IncidentController:282 identical (fleet max 20480). Safeguarding SafeguardingAttachmentController@store validates 'file' + 'notes'(nullable) + 'is_sensitive'(nullable, read via $request->boolean('is_sensitive')). This is why AttachmentUploader's hardcoded 'file' / 'notes' / 'is_sensitive' fields line up exactly.
- **In-wizard staging pattern — Add Site (the model for a wizard 'Documents' step)** [resources/js/components/sites/add-site-dialog.tsx:1306-1367, 500-504]: StepDocuments uses FileDropzone+StagedFileCard, NOT AttachmentUploader. addFiles maps File[] -> SiteDocumentDraft{file,title,category,expiry_date} and appends to form data array `set('documents',[...])`; StagedFileCard children render per-file Title/Category/Expiry inputs; the whole wizard submits via `form.post('/sites', { forceFormData:true, preserveScroll:true, preserveState:true, ... })`. Nested File array is sent as documents[i][file] thanks to forceFormData.
- **Worker-participation routes ALREADY EXIST (spec correct that names are unchanged)** [routes/health-safety.php:135-167]: POST worker-participation/consultations (consultations.store); POST consultations/{consultation}/documents (consultations.documents.upload); PUT consultations/{consultation}/status (consultations.status); POST committees/{committee}/meetings; PUT meetings/{meeting}/complete; POST meetings/{meeting}/attendees; POST meetings/{meeting}/minutes (meetings.minutes.upload); GET .../minutes/download. All under prefix 'worker-participation', controller App\Http\Controllers\HealthSafety\WorkerParticipationController.
- **Consultation document upload field contract (NOT 'file')** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:275-299]: uploadConsultationDocument validates `'document'=>['required','file','max:20480','mimes:pdf,doc,docx,xlsx,jpg,png']` and `'type'=>['required','string','in:document,outcome']`; reads $request->file('document'); writes document_path/document_name (type=document) OR outcome_document_path/outcome_document_name (type=outcome). So the multipart field MUST be named 'document' and a 'type' field is REQUIRED.
- **Meeting minutes upload field contract (NOT 'file', no type)** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:410-426]: uploadMeetingMinutes validates `'document'=>['required','file','max:20480','mimes:pdf,doc,docx']`; reads $request->file('document'); writes minutes_document_path/minutes_document_name. Field MUST be named 'document'; no 'type' field.
- **Consultation CREATE accepts NO file** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:215-232]: storeConsultation validates only title, consultation_type (in:change_notification,hazard_review,policy_review,risk_assessment,general), description (required, max 5000), site_id, consultation_date. No file/document field. Therefore the wizard 'Documents' step cannot ship a file in the create POST — upload must happen after create (POST consultations/{id}/documents) or be a placeholder.
- **Consultation status enum mismatch (handoff-relevant)** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:240,260]: updateConsultationStatus validates status in:open,feedback_received,actioned,closed (matches the spec timeline). But storeConsultation hardcodes status:'open' and updateConsultation validates status in:open,in_progress,closed — two different enums coexist on the same model. Spec's CONSULT_ORDER (Opened->Feedback received->Actioned->Closed) matches the /status route, not updateConsultation.
- **description is REQUIRED on consultation create** [app/Http/Controllers/HealthSafety/WorkerParticipationController.php:220]: storeConsultation requires description (max 5000); the spec's NewConsultationWizard 'Topic & type' step lists only title + consultation_type and never mentions description. validateStep/useForm must include description or create 422s.
- **No worker-participation frontend yet** [(glob: none found)]: No resources/js/pages/health-safety/worker-participation* and no components/health-safety/worker-participation/** and no frontend/modals/** exist — this is greenfield UI against pre-existing backend routes.

## Detail-dialog pattern (IncidentDetailDialog) blueprint for representative/meeting/consultation dialogs, vs MODALS_AND_WIZARDS_SPEC.md

- **WizardShell is the dialog chrome for ALL detail dialogs** [resources/js/components/wizard/shell.tsx:27-210]: shell.tsx exports WizardShell({open,onClose,title,description,railIcon,railTitle,railSub,steps,stepIndex,onStepClick,pct?,pctLabel?,footerStart,footerEnd,success?,children}). Renders a Radix Dialog (maxWidth min(94vw,980px); body h-[min(88vh,760px)]), a 248px left stepper rail (steps:WizardStep[] where WizardStep={key,label,blurb,icon}), a 'Step x of y · {label}' header with the only close button, a 3px progress strip, scrollable body, and a muted footer split footerStart(left)/footerEnd(right). Detail dialogs reuse steps as named SECTION tabs and drive stepIndex off the active section.
- **IncidentDetailDialog signature + section model** [resources/js/components/incidents/incident-detail-dialog.tsx:146-249]: export function IncidentDetailDialog({ detail, open, onClose }). State: useState<SectionKey>('overview'), useState<LifecycleAction|null>(null), useState(false) editing. SectionKey='overview'|'timeline'|'photos'|'followups'|'investigation'|'linked'. SECTIONS provides {key,label,blurb,icon}; stepIndex=SECTIONS.findIndex(s=>s.key===section); onStepClick={(i)=>setSection(SECTIONS[i].key)}. Body: editing->EditPane; else action->ActionPane; else active Section by key. NO initialAction prop.
- **Header split + status/severity pill lives in footerStart (not a top header)** [resources/js/components/incidents/incident-detail-dialog.tsx:119-130,208-231]: title=`Incident INC-${d.id}` + description=`${titleCase(d.type)} — ${clientName}` are sr-only; the visible rail uses railIcon/railTitle(client)/railSub(`INC-${d.id} · ${titleCase(d.type)}`). STATUS PILL + SEVERITY chip render in footerStart (bottom-left): rounded-full bg-muted pill with ${DOT[SEV_TONE[d.severity]]} dot + SEV_LABEL, then muted STATUS_LABEL. Tone maps: DOT{neutral/info/success/warning/critical/primary->bg-* tokens}; SEV_TONE{low:success,medium:warning,high:critical,critical:critical}.
- **Options footer bar (lifecycle) — Incidents shape** [resources/js/components/incidents/incident-detail-dialog.tsx:175-206]: footerEnd is set to null whenever (action||editing) so an open pane owns its own buttons. Otherwise footerEnd is a flex-wrap row: 'Open full page' Link href=`/incidents/${d.id}`, then gated Buttons — Edit (draft&&can.update->setEditing(true)), Submit for review (can.submit&&draft->router.post direct), Review (can.review&&submitted->setAction('review')), Close (can.close&&reviewed->setAction('close')), Reopen (can.reopen&&closed->setAction('reopen')). Each lifecycle button flips state to open the matching inline pane.
- **initialAction/initialSection — implemented in SafeguardingConcernDialog (the spec's required pattern)** [resources/js/components/safeguarding/concern-dialog.tsx:139-156]: export function SafeguardingConcernDialog({ detail, open, onClose, initialAction = null, initialSection = 'overview' }: {...; initialAction?: ActionKey|null; initialSection?: SectionKey }). Seeds state directly: useState<SectionKey>(initialSection), useState<ActionKey|null>(initialAction). Comment: 'keyed by concern id in the list, so it mounts fresh on each open — the right-click menu uses initialAction/initialSection to land on a specific pane.' Copy THIS signature for representative/meeting dialogs, not Incidents'.
- **Multi-action Options bar (OptionBtn) — use for consultation/meeting footers** [resources/js/components/safeguarding/concern-dialog.tsx:219-247,303-309]: Safeguarding footerEnd is a row of OptionBtn(icon,label,onClick,disabled?,reason?) (wrapper: Button size=sm variant=outline onClick disabled title={disabled?reason:undefined} -> Icon + label). Buttons map 1:1 to panes via setAction('triage'|'assign'|'risk'|'investigation'|'report'|'action'|'close'), gated by can.* + lifecycle guards (disabled w/ reason tooltip). Matches the spec's consultation footer (Record feedback · Record outcome · Upload document · Close) and meeting footer — Incidents only has linear lifecycle buttons.
- **Action-modal = inline pane, single verbatim example (ActionPane)** [resources/js/components/incidents/incident-detail-dialog.tsx:255-320]: function ActionPane({incidentId,action,onDone}){ const meta=ACTION_META[action]; const form=useForm({review_notes:'',closed_outcome:'',closed_notes:'',reopened_reason:''}); const submit=(e)=>{e.preventDefault(); const url=`/incidents/${incidentId}/${action}`; form.post(url,{preserveScroll:true,onSuccess:(page)=>{ if(!(page.props).flash?.error) onDone(); }}); }; return(<form onSubmit={submit}><StepHead icon=meta.icon title=meta.title blurb=meta.blurb/> {action==='review'? Field>Textarea review_notes :null} {action==='close'? Field Outcome(Input,required,error) + Field Closing notes(Textarea) :null} {action==='reopen'? Field Reason(Textarea,required,error) :null} <div flex justify-end gap-2><Button type=button variant=outline onClick=onDone>Cancel</Button><Button type=submit disabled=form.processing>{meta.cta}</Button></div></form>); }. KEY: NOT a nested Dialog; a <form> swapped into the WizardShell body, posts to an EXISTING route, onSuccess (only if no flash.error) calls onDone(). List/hero refresh via controller back() redirect, no explicit reload.
- **flash.error guardrail helper (reusable verbatim)** [resources/js/components/safeguarding/concern-dialog.tsx:311-317]: function onSuccessGuard(onDone){ return (page)=>{ const flash=page.props.flash as {error?:string}|undefined; if(!flash?.error) onDone(); }; } — used as form.post(url,{preserveScroll:true,onSuccess:onSuccessGuard(onDone)}). Guardrail/validation failures come back as flash.error on a 302 (Inertia onSuccess), NOT a 422, so panes must stay open on flash.error.
- **Opening detail over the list (router.get only:['detail'])** [resources/js/pages/incidents/index.tsx:244-247,488]: index.tsx: openDetail=(id)=>router.get('/incidents',{...filters,incident:id},{preserveState:true,preserveScroll:true,only:['detail']}); closeDetail=()=>router.get('/incidents',{...filters},{preserveState:true,preserveScroll:true,only:['detail']}). Dialog renders only when prop present: {detail ? IncidentDetailDialog detail={detail} open onClose={closeDetail} : null}. Param-drop on close returns detail=null. Incidents does NOT key the dialog by id; Safeguarding does (key={detail.id}) so it remounts fresh per open — required when using initialAction.
- **initialAction wiring from right-click — Safeguarding openConcern** [resources/js/pages/safeguarding/index.tsx:230,239-246,490-496]: safeguarding/index.tsx adds pendingOpen alongside the param: const [pendingOpen,setPendingOpen]=useState<{action?:ActionKey;section?:SectionKey}|null>(null); openConcern=(id,opts?)=>{ setPendingOpen(opts??null); router.get('/safeguarding',{...filters,concern:id},{preserveState:true,preserveScroll:true,only:['detail']}); }; closeDetail also setPendingOpen(null). Dialog: SafeguardingConcernDialog key={detail.id} detail open onClose=closeDetail initialAction={pendingOpen?.action ?? null} initialSection={pendingOpen?.section ?? 'overview'}. Right-click 'Edit' calls openConcern(id,{action:'...'}).
- **Refresh-after-mutation (actual repo-wide pattern)** [incident-detail-dialog.tsx:170-171,544-545; concern-dialog.tsx:219,241,245,344,609,673]: All mutations use useForm/router with {preserveScroll:true} and let the controller back() redirect re-render all props (detail,rows,hero) in place. No-body actions: router.post/patch/delete(url,body,{preserveScroll:true}) (completeFollowup, markInformed, sensitivity, status-patch). Form actions: form.post/put(url,{preserveScroll:true,onSuccess}). NEITHER dialog uses router.reload({only:[...]}). Modal updates in place because Inertia replaces the detail prop on the redirect re-render.
- **Reusable lifecycle/stage + timeline renderers** [incident-detail-dialog.tsx:119-126,487-517; concern-dialog.tsx:124,188-196]: (1) Safeguarding linear STAGE tracker: const STAGES=['Reported','Triaged','Investigating','Action plan','Monitoring','Closed'] driven by backend stage_index on detail (define CONSULT_STAGES=['Opened','Feedback received','Actioned','Closed']+a status-order map). (2) Incidents TimelineSection builds events[] {at,label,tone,icon} from per-stage timestamps, sorts by time, renders ol className='relative ml-2 border-l border-border' with a ${DOT[tone]} node + icon + label + formatDateTime(at). Reuse DOT + SEV_TONE + this ol for Opened->Feedback received->Actioned->Closed (feedback_summary under Feedback, outcome+changes_made under Actioned), tone by CONSULT_ORDER[status]. No new tone tokens.
- **Shared section building blocks** [resources/js/components/incidents/incident-detail-dialog.tsx:428-485,519-574,821-867]: Sections built from wizard/shell ReviewCard({icon,title,onEdit?,span?})+ReviewRow({label,value}) (em-dash empty) and wizard/primitives StepHead({icon,title,blurb}),Field({label,required?,error?,hint?}),SelectInput,Segmented,TilePicker,InfoCard({icon,tone:'warn'|'crit'|'info'}). LinkedRow (Link w/ circular icon + title/sub + ExternalLink) is the row primitive for 'Linked records' (incident-detail-dialog.tsx:854-867). Attachments use AttachmentUploader/file-dropzone (endpoint POST) + router.delete/patch. Only approved blocks; tones via TONE/DOT + status tokens, no raw colours.
- **Wizard primitives exist as spec lists** [resources/js/components/wizard/primitives.tsx:44-335; shell.tsx:20-314]: primitives.tsx exports FieldErr,Field,SubHead,StepHead,InfoCard,SelectInput,Segmented<T>,ChipMulti,TilePicker,Ring — matching the spec kit-import list. shell.tsx exports WizardShell,WizardStepPane,WizardSuccessPane,ReviewCard,ReviewRow,WizardStep. All spec-named imports resolve.
- **Incidents page structure — NOT split into tab files + modals/ folder** [resources/js/pages/incidents/ (index.tsx, show.tsx, templates/); resources/js/components/incidents/]: pages/incidents/ contains only index.tsx (39.7KB monolith: hero, tabs, both tables, all wiring), show.tsx (1KB deep-link shell rendering IncidentDetailDialog detail open onClose=router.visit('/incidents')), and templates/ (edit.tsx, index.tsx = report-template admin, unrelated). Detail/report dialogs live in components/incidents/. NO pages/incidents/modals/ folder and NO per-tab files. Safeguarding is the same shape (single index.tsx + components/safeguarding/concern-dialog.tsx).


