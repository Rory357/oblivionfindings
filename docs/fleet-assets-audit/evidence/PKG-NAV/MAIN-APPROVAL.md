# PKG-NAV — Main pre-integration review

Owner: MAIN ASTRA. Revision: 2. Updated: 2026-09-26. Decision: **Approved for integration — exact application/test source at a94a22e3d12d1aabe447a84b7f2178ebdf906f41; documentation whitespace cleanup required before push**.

## Final amended-candidate approval

Main reviewed the initial full candidate, the subsequent search-ID/verification-script amendment at `407126d71957a6c2ead7147f401dd609e0347537`, and the complete N01 delta to **`a94a22e3d12d1aabe447a84b7f2178ebdf906f41`**. Base/observed remote main remains **`fa7b5291988cebfb6beaa6e6e10c6c660fb2a959`**, independently reverified at this review. Branch475b was clean. N01 is resolved: the middleware projects the existing `fleet.reports.view` through User::canDo, invalidates the old cached map via v9, and frontend discovery/report predicates remain separate from operational authority. No grant, route, policy, data, schema or protected-guide change occurs. Explicit deny remains effective.

Main independently reran **27 navigation UI tests, all passing**, and the real middleware/User permission unit suite, **7 tests / 26 assertions, all passing** on PHP8.4.16. The initial relative PHPUnit command could not find the shared dependency launcher; the rerun used its actual installed absolute path and the candidate's local-source bootstrap, with no operational database. Designer separately reports66 tests/4frontend files, scoped typecheck and lint passing; do not add overlapping counts. Main read the five-role browser evidence and personally rendered the amended report-only fixture, confirmed Reports as the sole Fleet primary, and clicked Resource use → Usage by house successfully with Reports still selected. Source-linked synthetic fixture limits and baseline middleware formatting limitations remain explicit; no claim of live Laravel role/site-data end-to-end certification.

Full-range `git diff --check fa7b529..a94a22e3d` exposed two evidence-only extra EOF blank lines in `docs/fleet-assets-audit/evidence/PKG-NAV/verification/preview.tsx` and `vitest.config.ts`. Remove those two extra blank lines and verify the full-range check passes before publication. This exact non-behavioural documentation/fixture-whitespace cleanup and inclusion of this Main approval are authorised without another round of source review. The approved application/test blobs must remain identical to a94a22e3d. Any material source/conflict-resolution change still requires affected validation and renewed review.

The same navigation Designer is now authorised to reconcile latest main, satisfy normal repository gates and publish this reviewed scope without force, then verify remote source-to-integrated inclusion and actual check results. The user's existing test-server/main instruction is sufficient; no new user approval or worker is required. Keep sibling changes intact, do not contact PKG-02B, and do not claim publication/acceptance before evidence exists. Main retains independent post-publication verification. Historical correction request below is resolved by this exact amended review, not erased.

Reviewed base `fa7b5291988cebfb6beaa6e6e10c6c660fb2a959`, implementation `02381fad5e86727eba565696d9209686e2d76b3a`, evidence candidate `649be52e3e60d77e9b22c6cec93b08ba966cd881`, in Designer worktree475b. Main inspected the complete four-runtime-file change, meaningful navigation tests, route/permission sources, and exact review/fixture packet. Changes are limited to sidebar mapping/active matching/search, Fleet navigation config/component and shared-layout breadcrumb integration. No Vehicle Profile body, domain logic, route, data, guide or sibling file change is in this candidate.

Main independently ran the navigation UI test file: **22/22 passed**, 21:38NZST on26September, with the candidate's focused Vitest config. Designer separately reports four files/61tests, scoped ESLint and scoped TypeScript passing; these overlapping runs are not summed. Main also rendered `http://127.0.0.1:8876/fleet-assets/vehicles`, observed all seven primary links and the actual shell, opened Transport, clicked Medication transit and returned through browser Back. These are actual candidate shell/navigation components with explicit synthetic page bodies/auth props, not a live Laravel authorization or site-data certification. The preview's source/config ties it to worktree475b; Main changed no application code.

## N01 — existing report-only permission omitted

The candidate correctly avoids treating unrelated `reports.viewAny` as Fleet report authority, but hides analytics links from a user who actually has `fleet.reports.view` without `fleet.viewAny`. Main independently confirmed `routes/fleet-assets.php:601` permits either, while shared `auth.can.fleet` omits the reports capability. Existing asset-plus-report users would receive only the Mileage landing and lose the direct analytics navigation; a report-only user cannot discover the module via the unchanged outer gate. Preserving an unrelated Reporting link is not a complete resolution of the requested Fleet navigation.

Authorise the smallest **read-only existing-permission projection** in `app/Http/Middleware/HandleInertiaRequests.php` and the necessary shared TypeScript shape, plus focused regression evidence. This is a source-to-navigation adapter needed to complete the already authorised change, not a new permission grant, policy, schema or business workflow. Existing route checks and permissions remain unchanged. Extend only the navigation module admission/report visibility required to use that projection. Keep operational eligibility separate: report-only users must receive a permitted Reports landing and search/context links, without denied Overview/Map/Daily checks/Vehicle/work links. Do not broaden the common operational visibility helper merely to admit the module.

Required focused cases: report-only, report-plus-assigned-assets, report-plus-asset-manager, unrelated global reports permission alone, and no permission; show valid landings, report discovery/search/context and no newly visible denied operational links. Verify the middleware projects the existing authority rather than substituting or granting it. Preserve all existing seven-item/full-role and retained-destination evidence. Return one amended exact-candidate packet for delta review; do not push before Main's explicit exact-candidate approval.

No other blocking issue found in this bounded review. N01 is the first correction for this underlying issue; no correction budget is reset and no worker is introduced. Main's programme-owned documentation records this narrow file-scope reconciliation; the same verified Astra/xhigh Designer owns the fix and subsequent authorised integration after approval. PKG-02B and all sibling tasks remain uncontacted.
