# PKG-02A revision 3 — grayscale map correction

Stephan directly requested enforcement of the existing grayscale-base-map rule. The only application delta from frozen revision 2 is three CSS lines in `resources/js/components/client-location/location-workspace.css`: `grayscale(100%)` on this workspace's Leaflet tile pane. Main, historical selection and the drawing wizard use the same component. Semantic markers, boundary overlays, handles, attribution and controls remain unfiltered. No provider, geometry, request, permission, draft or command behavior changed.

Frozen source/build manifest: **BFCA350B55D12BF63CD406B0691795037E3AB5F69B8812D31A3442031D513B68**; 30 application/test files and 6,030 local build files (including previously retained hashed assets). Runtime build manifest: **042F1007FD5414282DDA3B6D5542E65E23DFD4AA38827F55108A121F85219B3E**. Same worktree `2b9f`, branch `codex/pkg-02a-client-location-design`, baseline `2302ca33a95616e442a78e957ddce82d8db98669`, sole Astra implementer. Revision 1 and 2 source manifests are preserved; revision 2's checkpoint/artifact index are additionally copied with `-r2` names before updating the current index. No protected design guide or frozen v1/v2/v3 mockup changed.

Production build passed, exit 0, 2m57s, with the existing >500 kB app chunk advisory. Built to the local staging directory and installed by hashed assets then manifest replacement. `git diff --check` passed; all current application entries match the new manifest. This is a CSS-only change, so the earlier logic suites were not redundantly rerun. Their results and limitations remain in `care-race-and-battery-r2.md` and `realtime-regression.md`.

Browser verification on `http://127.0.0.1:4335/operations/clients/1?tab=location` used the actual new build and synthetic reviewer/client. Main map had four loaded tiles and computed `grayscale(1)`, while the overlay filter was `none` and location marker fill remained `#6d4aff`. Reopened revision-2 polygon editor had eight loaded tiles with the same filter, a purple boundary and four unfiltered purple handles. Pristine Cancel returned to Edit draft; no save occurred. Two protected historical observations loaded; selecting 9:24 retained latest 9:54, with a grayscale base and unfiltered overlays. Returned to Today/current observation. No browser warning/error was recorded. Evidence: `browser/grayscale-main.png`, `grayscale-editor.png`, `grayscale-verification.json`.

## Remaining work, without overstating this slice

Implemented: protected current/history, context actions, real circle/polygon/rectangle drawing and editing, validated private saved schedules and immutable draft revisions, explicit canonical-source review, privacy-loss clearing, battery/charging presentation, and now grayscale base tiles. These are the first protected planning slice, not operational monitoring.

Technical work remaining in the wider approved scope: operational zone promotion/activation and server schedule evaluation; corresponding deduplicated alert/response integration; complete governed in-place Locate now request/status lifecycle (the current button still redirects to existing device controls); client-scoped outings/check-ins and attributable responses in Activity. New recipient-sharing functionality remains subject to its separate privacy contract.

Unsettled operating/privacy decisions include observation quality, crossing confirmation/dwell, overlap precedence, DST handling, response ownership and any new named-recipient disclosure rules. They must not be invented as live defaults. These are distinct from implementation and QA tasks.

Unperformed gates: Main exact-code acceptance of revisions 2/3 and disposition of the two original realtime privacy failures, remaining end-to-end verification, integration/publication and Stephan's final acceptance. The first-slice review has not released the next implementation slice, operational activation, live tracker dispatch or sharing grants. No commit, stage, push, merge, operational migration or deployment occurred. Local profiles, helpers, credentials, generated builds and raw logs remain excluded from publication.
