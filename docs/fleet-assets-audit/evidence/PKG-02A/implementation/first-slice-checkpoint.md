# PKG-02A protected read and draft checkpoint

Current checkpoint: revision 4, `realtime-correction-r4.md`. Main's r2/r3 review accepted the bounded care-assignment, battery and grayscale changes. This revision records only the released realtime fixture correction and passing affected family. Its revision-3 form is preserved as `first-slice-checkpoint-r3.md`; the older narrative below is historical. The concrete Locate contract is pending technical disposition and no Locate application writes have started.

Status: **first protected slice reviewed through r3; r4 test correction frozen for Main**; not whole-package completion, integration approval or operational activation. Main's revision-1 first-slice release and contract revision 2 bound this work. Sole implementer remains Astra in worktree `2b9f`, branch `codex/pkg-02a-client-location-design`, baseline `2302ca33a95616e442a78e957ddce82d8db98669`. No commit, stage, push, merge, operational migration or deployment occurred.

Revision 1 reference: `source-build-manifest-r1.json` SHA256 **E81F1842A90B57EE47C4E026E2C1F2D2A565ADE88096575BCC1C0D643AC5C911**, 27 application/test files and 1,613 built files. Its build manifest was **BC6A08E5443AC80999FAE2573FAC31496337733E48E118CB350416EE4A3B8AC2**. **The current candidate is revision 2**, incorporating the narrow care-assignment race correction and Stephan's battery presentation request: see `care-race-and-battery-r2.md` for the new exact hashes, executed results and remaining boundaries. The historical first-slice results below are retained as evidence, not a claim that the old manifest is still current.

## Delivered slice

The Client Profile Location surface uses the existing page shell and real protected endpoints. It includes real Leaflet polygon/circle drawing, adjustable and keyboard-operable handles, rectangle starter, undo/redo, right-click/Shift+F10/visible map-action parity, a shared four-step wizard, explicit purpose, Auckland weekdays/hours/overnight dates/exceptions, and persistent private draft revisions. Save failures preserve edits and retry identity. Dirty cancellation requires an explicit discard. Drafts have no monitoring/activation state and create no operational geometry, assignment, evaluator, alert, notification, job or command effects.

History has date validation, Auckland-to-UTC range conversion, collection/assignment/consent/retention bounds, loading/error/empty states and preserved failed-range retry. Historical selection does not overwrite the latest observation. Current and history responses recheck authority after reading. The current presenter accepts a bounded canonical history observation or a consistent timestamped device metadata tuple, never a bare coordinate or last-contact timestamp; it does not attach an independent latest address. Missing history accuracy remains unknown because the canonical history service does not provide it.

Privacy revalidation is cancellable and generation/fingerprint guarded. An ended context stays cleared; old successful responses cannot restore it. A fresh authorised page entry can resume. Client/site/role/tracking access and management are checked server-side; draft saves lock canonical consent, device, client/site, assignment, authorization evidence and selected geometry/rule records before immutable writes and minimal audit. Audit failure rolls back the save.

Main's interim findings were corrected: one boundary eligibility query now protects the initial profile's explicit/fallback house reference and the draft lookup against another client's same-site direct/pivot asset boundaries; changed or unavailable canonical source links require explicit review and cannot silently become custom drafts; the current-observation presenter enforces temporal provenance. A staff regression fixture now records a genuine timestamped position after collection begins instead of assuming bare coordinates are current. Portal implementation and its fixture helper remain unchanged.

## Isolation and source identity

PHP 8.4.16: `C:/Users/steph/.config/herd/bin/php84/php.exe`. Node v22.23.2: `C:/Users/steph/.hermes/node/node.exe`. Local Composer autoload resolves App and Tests into this worktree. Shared dependencies were not written; both lock files remain unchanged. The source/build manifest and frozen-preview preservation report beside this document define the reviewable candidate.

Every backend invocation was immediately preceded by the reviewed read-only `verify-isolation.php` and explicitly used `--configuration phpunit.pkg02a.xml`. Loopback MySQL `127.0.0.1:3306`, package base `oblivion_findings_pkg02a_2b9f_test`, actual numeric process suffix from the unchanged TestCase harness; no fixed PID, process-token/URL override, environment file or cached configuration. Queue null/mail array/broadcast null plus explicit fake/prevented transport in tests. Cleanup stays in the inspected process namespace.

Synthetic browser preview: `http://127.0.0.1:4335/operations/clients/1?tab=location`, built from this worktree and served by its local guarded PHP router. Separate schema `oblivion_findings_pkg02a_2b9f_browser`, synthetic reviewer/client/device only. Before fixture writes, `.pkg02a-browser-proof.json` recorded the exact root/runtime/connection, schema absent, unique loopback port/cookie, outbound settings and exact-schema cleanup boundary. No operational data or provider credentials were imported and no unit was polled. The initial seed's incorrect device-column name was repaired only in this synthetic schema; actual location timestamp belongs in device metadata. Browser setup/runtime helpers are local-only.

## Commands and results

Backend commands used the installed PHP runtime, the isolation proof, then `vendor/bin/pest --configuration phpunit.pkg02a.xml ... --compact`:

- Initial protected slice: 11 tests, 77 assertions, exit 0; 11 missing-env warnings.
- Broad affected regression: `ClientLocationZoneDraftTest`, `ClientLocationHistoryBoundaryTest`, `ClientLocationConsentDisclosureTest`, `ClientLocationAssetSecurityTest`, `DeviceAssignmentConsentEnforcementTest`, `PersonalTrackingConsentWithdrawalTest`, `FleetRealtimePrivacyTest`; 41 tests / 508 assertions, two failures. See `realtime-regression.md` for precise limitations and traces.
- Focused earlier repeat including migration test and realtime: 16 tests / 93 assertions, same two failures; 14 missing-env warnings.
- Same-site draft boundary correction: 10 zone tests / 65 assertions, exit 0; 10 missing-env warnings.
- Main's three corrections: `tests/Feature/Operations/ClientLocationZoneDraftTest.php`, `ClientLocationHistoryBoundaryTest.php`, `ClientLocationConsentDisclosureTest.php`, `ClientLocationAssetSecurityTest.php`, and `tests/Unit/Tracking/ClientLocationDraftMigrationTest.php`; 36 tests / 525 assertions, exit 0; 36 missing-env warnings.
- Final current/history suite including a retention-only case within the current collection/assignment: six tests / 126 assertions, exit 0; six missing-env warnings.

Migration verification covers additive install through the isolated bootstrap, empty-table down/up, and refusal to drop populated draft evidence. The latest 36-test run includes both rollback cases. No real database migration was performed.

Frontend command: Node `node_modules/vitest/vitest.mjs run resources/js/components/client-location/location-workspace.test.tsx resources/js/components/client-location/zone-draft-dialog.test.tsx resources/js/test/client-location-tab.test.tsx resources/js/hooks/use-personal-location-privacy.test.tsx --config vitest.pkg02a.config.ts --configLoader runner`. Final boundary-fit candidate: 20 tests / four files pass, exit 0. Full `node_modules/typescript/bin/tsc --noEmit` passed, exit 0, after correcting a test-only geometry-union annotation. Scoped `node_modules/eslint/bin/eslint.js` on the location components, privacy hook/tests and tab tests passed with `--max-warnings 0`, exit 0. New PHP files were checked/formatted with the installed Pint; `git diff --check` passed. After backend tests, only unused controller imports/helpers were removed; `php -l` and the whitespace check passed again.

Production build: Node `node_modules/vite/bin/vite.js build --config .pkg02a-vite.config.ts --configLoader runner`. Configuration mirrors repository Vite setup, with generated Wayfinder routes produced separately under the isolated browser environment and worktree-local cache. Build passed; the existing main app chunk exceeds 500 kB and Vite reports its advisory. Final manifest records exact built assets. A local browser reload during an earlier in-place rebuild briefly hit missing `public/build/manifest.json` (500); final QA pauses the preview during builds and retests afterwards. This transient was not counted as an access-loss pass.

## Browser coverage and limits

Real synthetic-browser flows: pointer-drawn four-corner polygon, shared time/date pickers, Mon/Wed/Fri 09:00–11:00, 21–30 September 2026, 25 September exception, save revision 1, full reload/reopen, exact retained schedule/exception, keyboard corner adjustment, undo/redo, keep editing, save revision 2. Circle centre/edge drawing, exact 250 m radius and keyboard centre movement preserved focus. Pristine Draw close returned focus to Draw safe zone; the final reopened editor's dirty discard returned it to its Edit draft opener. Right-click, visible Map actions and Shift+F10 expose equivalent actions. Empty history and two real synthetic stored observations were displayed; selecting an older observation left latest position/time unchanged. Final build verifies whole-boundary framing when viewing/reopening, direct pointer corner drag, undo, persisted schedule review and discard. Database evidence retains one draft and exactly two immutable versions, with zero operational geometry/assignments/states/signals/commands/jobs/notifications.

Ending only the synthetic collection automatically showed “Location access is not active”; DOM inspection found zero maps, zero history regions and zero Draw safe zone buttons. Synthetic access was restored, and a fresh final-build entry successfully restored authorised data and the saved draft. Failed-save/retry, late denied/successful responses, route changes, unmount, unrelated assignments, canonical stale-source review and restricted management are covered by focused interaction/backend tests rather than claimed as browser network-fault injection.

Desktop/laptop scope only: 1280×800, 1440×900 and compact desktop/accessibility viewport 640×400. Compact wizard rail collapses, controls remain reachable and no horizontal document overflow was observed. Resizing is not actual browser zoom; actual 200% zoom remains unverified. No phone/tablet deliverable or mobile gate is added. Final screenshots and console inspection are in the sibling `browser` directory.

## Still outside this release

Operational activation/promotion, schedule evaluation and associated quality/direction/confirmation/overlap/DST/response-owner policy; outing mutations; new client-scoped command dispatch/status workflow and its step-up/capability checks; new recipient grants. Locate now currently preserves the existing governed redirect; the approved richer request lifecycle is not implemented. Full concurrency/interleaving proof and pristine whole-HEAD reproduction of the two realtime failures are not claimed. Main exact-code review, publication verification and Stephan acceptance remain separate. This checkpoint must not be represented as all of v3 complete.

Publication excludes `phpunit.pkg02a.xml`, `vitest.pkg02a.config.ts`, `.pkg02a-*` runtime/profile/seed/cache/log files, credentials, generated dependencies, and the local test isolation proof helper. Include only the explicit reviewed application/test/document allowlist if publication is later released. All 103 frozen v1/v2/v3 artifacts were rehashed and unchanged.
