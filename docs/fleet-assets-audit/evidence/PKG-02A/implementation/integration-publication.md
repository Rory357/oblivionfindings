# Client Location — approved integration and publication

21 September 2026. This record supersedes the pending-integration status in the frozen historical handoff. MAIN release revision 1 authorizes publication of source c18f52f648e68c38c48cc9eb51d7e02a35061e0d, based on 2302ca33a95616e442a78e957ddce82d8db98669. Stephan confirmed the manual 200% zoom check and requested the push and one next design-only mockup. This is user-reported zoom evidence.

The isolated integration checkout imports exactly the frozen 152 Designer documents, 65 MAIN context documents and both manifests. All imported hashes, the 125 application/test Git blobs and Rory's three reference files were independently checked without mismatches. The structured receipt is [integration-source-verification.json](integration-source-verification.json). Frozen historical records and the archived v1 page are retained unchanged. No unrelated files from MAIN's dirty checkout are imported.

The documentation commit follows the reviewed source commit; its application/test content is identical. The accepted corrected backend run is 75 tests / 2,168 assertions, exit 0, with 75 existing missing-.env warnings. Unchanged passing families from the preserved 216-test run are reused explicitly; that historical run had one error and one failure and is not reported as green. Frontend 60 tests / 14 files, TypeScript, scoped ESLint, focused Pint and the existing production build are reused on identical code. The build retains its existing size advisory. No redundant suite run or generated build is included in the integration.

Publication will be a normal fast-forward push to main after freshly checking remote state, protections and hooks. Exact resulting commit, remote verification time and actual hosting CI status are returned to MAIN for its independent verification; this pre-push record does not claim CI success or deployment.

The delivered work covers client tracking, safe-zone drawing and monitoring, governed tracker modes, explicit fall routing to Control Room, and the reviewed UI. Outings/responsible-worker/planned-arrival/check-ins, the combined Activity stream, separately governed sharing/self-access, and operating configuration/device acceptance remain open. Publication is not whole-package acceptance.

No operational migrations, device commands or real notifications are run during publication. Deployment retains the all-writer quiescence and audit-tail backfill requirements, shared address-search cache, disabled-by-default private reverse geocoder, device profiles/permissions/approvals and physical-device verification documented in [publication-handoff-r8.md](publication-handoff-r8.md). MAIN independently verifies the push and owns the single subsequent design-only task.

Integration detail: the repository’s existing `.gitattributes` normalizes text to LF. The structured receipt maps each imported raw SHA256 to its staged Git blob and committed SHA256; verification permits only that line-ending conversion and finds no other changes. Application `git diff --check` passes. The full documentation check reports the retained 15 unified-patch blank-context markers and 11 frozen historical end-of-file blank lines (exit 2). These approved historical bytes are preserved and the full documentation check is not described as green.
