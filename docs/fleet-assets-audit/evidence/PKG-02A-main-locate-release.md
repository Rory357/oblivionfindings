# PKG-02A — Main disposition and Locate implementation release

Owner MAIN ASTRA. Revision 15, 2026-09-21. **Bounded implementation and isolated verification now include the reviewed request-serialized audit-tail remedy and necessary collector-recovery lock-order correction. Audit/portal/provider verification and final frozen review remain unfinished. The candidate is not approved for integration, publication, live tracker dispatch, operational activation or package acceptance. Typed-address approval does not authorize public-service tracker-coordinate disclosure.**

## Current address-search approval

Stephan clarified in this Main task that users may search any address, then answered **“yes”** to Main's explicit question: **“may the app send the typed address to its existing Nominatim address-search service to find it on the map?”** The question expressly excluded client identity, tracker details and location history. This direct user approval resolves revision6's automatic-review restriction for the concrete payload and destination: the user-entered address query to https://nominatim.openstreetmap.org/search. Do not ask again for this same action. Search is not restricted to saved or approved-site addresses; the existing client draft access boundary and bounded input/result validation still apply.

Same Astra Extra High Designer may enable and complete the existing-provider client address-search path, preserving the access checks, no-store responses, explicit result selection, cancellation, provider-error behavior and ordinary Site caller compatibility recorded below. Do not append client identity, tracker details or location history to the external request. Automated verification continues with fake provider responses and synthetic data; approval does not require sending real addresses during tests. No new provider, tracker command, operational zone activation, guide change or integration gate is included.

Historical approval-review event: Main's revision5 relay was rejected because explicit payload/destination consent had not yet been established, and the message was not delivered. Revision6 applied a genuine hold and communicated a fake-only safe continuation. The subsequent direct user approval above resolves that hold; it is not an indirect workaround. Earlier fake-only/pending-permission statements below are historical where they conflict with this approval.

## Reviewed inputs and completed first-slice correction

Main read the entire Designer locate-contract.md revision 2 and independently matched its initial SHA256 CE221591161E938E6AC01A2791A67B62CEE27DD3057D2571DA41075A31485C78. The reviewed current hash is **BB271B393D847332F1691BCDB3B1792092F1216E39AFBEC5F994EA08A74AA56D** after correcting only the warning-cause wording; Main independently proved this by reversing that exact phrase in memory and recovering the initial hash. Its first proposal remains preserved. Main independently inspected the existing assignment fingerprint, management authorization, command input/model/signing payload and signer, request/dispatch construction, Queclink adapter, FrameRouter claim and return path, lifecycle helper, status enums, existing legacy redirect and relevant HTTP input surfaces. The concrete gaps and earlier review questions are preserved in PKG-02A-locate-preflight-observations.md.

Main read the complete FleetRealtimePrivacyTest diff and .pkg02a-realtime-corrected.log. The affected family passes **four tests/26 assertions** with four runner warnings whose causes are not identified in the compact output; the original ownership, site, withdrawal and channel assertions remain. The positive resident-alert fixtures use the existing resident-specific consent, and explicit general Fleet Tracking denial covers both resident-alert paths. Application authorization and allowlists are unchanged. This resolves the two failures through a reviewed fixture correction; it is not a blanket baseline waiver or a claim of a pristine whole-HEAD run. Main did not independently rerun backend tests.

Frozen r4 manifest **4B497BAA08EB8E5687B40948749A95462D36E30073B917D09004E1BF6B6D9A74** contains 31 application/test files. Main independently matched every application/test and lock/harness entry; zero mismatches. Its only delta from preserved r3 BFCA350B55D12BF63CD406B0691795037E3AB5F69B8812D31A3442031D513B68 is FleetRealtimePrivacyTest, SHA256 **BC0A566B7F4F0190977262A2FC0A888BBE975FCA3DBA20F3F0F4FD86608028AB**. All 6,030 build entries are identical to the independently reviewed r3 inventory, and runtime manifest remains **042F1007FD5414282DDA3B6D5542E65E23DFD4AA38827F55108A121F85219B3E**. No rebuild was needed for this test-only correction. Preserve r1–r4 ledgers, original failures and the diagnostic.

The same active turn01a0c139-dbaa-7912-bfc8-445340f903e1 remains independently verified as gpt-6-astra/xhigh in both fields at2026-09-20T23:54:14.286Z. Previously verified Stephan approval of v3, all-Astra ownership and continuation remain effective; no repeat user approval is needed. Maintenance remains open with its application custody hold.

## Technical decision

Accept explicit nullable, strictly typed, immutable and hidden origin_context on the existing canonical command request. Add schema 8 only for a non-null signed context. Existing context-null schema 2–7 payload bytes and signatures must remain unchanged. The context binds the exact client/assignment/consent and the existing privacy fingerprint; canonical signed fields continue to bind actor, Device, Site, reason, parameters and expiry. Authorization must not depend on an idempotency prefix. No duplicate request store or command lifecycle is authorized.

Release the exact client availability, request, status, identity-confirmation and resume adapters and in-place dialog in the contract, preserving the current non-JSON redirect. Reuse real password confirmation and canonical permissions, capability policy, request lifecycle, expiry and idempotency. Reads never dispatch. Closing the dialog stops browser monitoring, not the underlying command. ACK, command completion and a newer measured location remain distinct; current history/retention/privacy checks govern any displayed observation.

Release the listed additive migration, canonical signing/intake/idempotency changes, shared verifier, client-context guard, Queclink delivery enforcement, narrow lifecycle lock-order normalization and corresponding tests. Generic command paths must preserve and verify a client context even if the request is resumed or executed outside the client adapter. All origin input is server-owned. The rollback must refuse to erase non-null context evidence. No operational database migration is released.

## Mandatory implementation and review requirements

- Apply the contract's context-null signature compatibility tests and context stripping/tampering tests. Verify the canonical signature before classifying a linked request as client or generic. Unknown/malformed context fails closed. Include explicit prohibited-origin validation on **StoreDeviceCommandBatchRequest** as well as StoreDeviceCommandRequest where its input shape exposes those fields; this narrow request-validation addition is included in the release.
- A queued row with missing or inconsistent governed linkage must not be silently treated as a legacy command. Existing governed_role/sequence and request/attempt provenance must be considered when both IDs are absent, not only when one remains. Preserve genuinely legacy command behavior and test both cases. Do not introduce a second signing format on provider payloads as an unreviewed shortcut.
- Follow the documented actual lock chains and verify them in code. Existing commands lock request before current consent/Device/client-site/assignment/RBAC/care evidence. New-key collisions restart without acquiring an existing command beneath context locks. Normalize linkedLifecycle from attempt→request to request→attempt. Keep client-evidence transactions out of provider enqueue/claim lock chains except the documented guarded delivery sequence. Exercise ACK, fulfilment, expiry and provider enqueue compatibility; a diagram or proposed order is not proof of concurrency behavior.
- On rejected unsent delivery, update provider row, request, attempt and audit atomically using legal existing Failed/Expired transitions as applicable. Accepted/Running cannot become Blocked, and the Ready/Queued precondition helper cannot close an accepted request. Audit failure returns no bytes and rolls back the attempted updates. No retransmission or rewrite of sent/ACKed provenance.
- The authorization guarantee is the atomic delivery-claim decision. Revocation that wins that ordering denies delivery. No recall or physical-socket race guarantee is asserted after authorized bytes leave the transaction. Test the real router's returned frames using synthetic records without a listener or socket.
- All backend runs retain the immediately preceding isolation proof, forced package profile, actual-PID schema and bounded cleanup. Fake queues, notifications, mail, credentials/provider execution and HTTP as required. Browser verification stays in the proved synthetic preview; do not dispatch to a real unit, start a worker/listener or weaken a production authorization check to make the preview succeed.

The contract's full scoped backend/frontend/regression cases, desktop rendering, focus, cancellation, stale responses, no-store disclosure, rollback and exact source/build evidence are required. Existing r3 map/battery and earlier protected history/draft behavior must remain intact. Record unsupported or unrun checks honestly; do not invent operating/privacy policy to complete a state.

## Execution and next checkpoint

### Audit-tail proposal approved for bounded implementation

Main read the full audit-tail-remedy-proposal.md, SHA256 **9012DD6D62EEDCBA1F9A8582C0C5D910E2047425E3857C22367B7F011ACC01FA**, the existing request/event models, canonical append service and relevant caller sources. Main also read the disposable-index probes in .pkg02a-address-guard-corrected.log, SHA256 **4080156D1546B197AFAAF04065A133B9BA7BE57360CF0C51C1A8CFA54F9664AC**: forced(request_id,id) allowed existing-tail overlap but still blocked the adjacent empty-tail first insert with1205. Reject the index-only remedy for this demonstrated case; do not publish the diagnostic index.

**Approve the proposed nullable internal audit_tail_event_id on the existing canonical request, additive migration/backfill and exact-parent-PK serialized append for implementation and isolated verification.** The pointer is storage metadata, not a new command contract or audit stream. Keep it hidden, not fillable, rejected on external generic and batch inputs, and protected against ordinary creation/update/clearing through the model. The controlled service/migration write is the only permitted application path. Preserve the event schema, id-based predecessor definition, canonical hash bytes and signed payload schemas; no event or signature rewrite.

Append must load and lock the canonical request by its actual primary key, validate identity from that row, current-read a non-null predecessor by exact event primary key and reject missing/foreign pointers. Then insert the unchanged hashed event and update only the pointer atomically. Use a genuinely narrow database-query update so neither lifecycle fields nor updated_at are touched; do not mutate or save the caller's stale/terminal Eloquent model. Existing/first appends serialize on the parent request, while different requests can proceed. No latest-range read, ordinary snapshot predecessor, skipped-lock tail or hidden online reconstruction fallback. Null means an established empty chain only under the migration/writer protocol; do not claim resilience to arbitrary privileged SQL corruption or unsupported old writers.

**Concrete lock-order prerequisite found by Main:** CollectorCommandRecoveryService::recover currently locks DeviceCommandAttempt before DeviceCommandRequest. CollectorCommandResultService::record and dispatch completion take request before attempt. Include a narrow recovery-order normalization in this same remedy: discover the parent identity without granting authority, then lock request → attempt and revalidate their association, current statuses and expiry under both locks. Preserve timeout/recovery outcomes, audit actions and retry limits. Main also inspected standalone evidence export, which must be able to append to terminal requests through the new service without loosening terminal-model immutability. Finish the actual append/writer call-site inventory and stop only for an additional concrete unresolved inverse order; do not broaden this into a collector rewrite.

Deployment remains a later gate: quiesce and drain **all** old audit writers, backfill each request's MAX(id) pointer, verify null/no-event and pointer ownership/latest-id invariants, deploy the new append service to every writer and resume only after verification. No mixed old/new writers. A code rollback retains pointer/event storage; any later re-upgrade must rebuild/reverify tails while writers are quiesced because old code cannot maintain them. Migration down refuses to remove required pointer evidence while audit events remain. Record this as an actual integration/runbook prerequisite, not permission to stop services or run a migration now. Do not auto-enable this during unrelated preview setup against an unproved database.

Required scoped evidence is the proposal's existing/empty/adjacent/non-adjacent and simultaneous first-append concurrency matrix, same-request linear chain, old-snapshot current tail, event-insert/pointer-update failure and outer rollback, invalid/foreign pointer refusal, terminal export and unchanged timestamp/signature/hash bytes, backfill/retained rollback, collector recovery/result ordering and the original independent-client queue overlap. Use the already proved isolated PID schemas/fake transports. Preserve the original diagnostic failures. The corrected seven-case run still ended with one error/36assertions in the negative-HR fixture; Designer reports a fixture created_by/updated_by cross-connection dependency correction, whose passing result is not yet claimed.

This necessary remedy is within the existing Locate engineering release; no new user approval or worker is needed. Same verified Astra/xhigh Designer owns the coherent schema/service/recovery/tests change and continues independent provider/UI checks. Return substantive results and an exact frozen candidate before Main integration review.

### Typed-search limiter: actual cache configuration requires correction

Main read the SiteGeocodingController diff (sampled SHA256 **F8D6C66B36FBDC04DAADF83022E758E295B0E91DFF3069AD16C631ACAF7C6677**) and two new fake-provider tests. The NZ and global fallback both enter a20-second cache lock, wait at most2seconds to acquire it, space starts by1.05seconds and hold the lock through the12-second HTTP timeout. Responses remain cached and ordinary unavailable lookup returns an empty result while the strict client path reports503. The proposed tests check fallback spacing and no transmission while the budget lock is held; their current run had no final result when inspected.

Main independently found **CACHE_STORE=array in Main's .env**, despite config/cache.php's database source default. No secret values were printed and no setting was changed. This is source/environment evidence, not a booted runtime claim, but it invalidates treating the default facade store as an established shared application budget. Array storage is process-local. A same-process fake/held-lock test alone does not prove cross-request exclusion or durable last-start spacing.

The existing bounded search correction includes an explicitly selected shared provider cache/lock store, defaulting to the existing database store if appropriate, with provider-scoped keys and failure closed if shared storage is unavailable. Do not change the application-wide CACHE_STORE or silently fall back to array/null. Prove the selected store/connection belongs to the isolated test schema before any test mutation and exercise separate request/connection or process contenders with HTTP fakes. Retain the configured endpoint in result-cache identity if the destination can change. The official public policy also requires switching services without a software update: a narrow server-owned search endpoint setting may replace the hardcoded URL with the same current default, without changing the deployed destination, enabling another provider or expanding approved payloads. Preserve ordinary Site response contracts and the private reverse-lookup separation.

This is a concrete correction inside the already released shared geocoder call boundary, not a new user gate or global cache/provider redesign. Do not report the provider budget verified until the actual shared-store proof and scoped tests pass. Audit remedy and portal proofs remain in progress independently.

### Actual audit lock evidence and canonical portal-role semantics

Main independently read the diagnostic at .pkg02a-address-guard-diagnostic.log, SHA256 **849B0C3EB3C4ABFF84ECAA932173D0DA84CF819E0AD7BAEF9030D752FE95EE96**. In isolated schema suffix9464, a canonical append for request1 held an X,GAP lock on device_command_audit_request_hash_unique immediately before request2's first hash fe1b…. The recorded canonical request2 candidate hash12721… falls in that gap, and its actual INSERT timed out with MySQL1205. The separately captured EXPLAIN used PRIMARY/backward scan after insertion; do not conflate that plan with the lock inventory at the earlier step. This is concrete evidence of cross-request audit contention, not a fixture-count failure.

The diagnostic command as a whole is **not green:seven tests/34assertions/one error/one failure**. The three address-service cases and actual-connection rejection are reported passing within that run; two new portal cases failed. Main inspected User::scopeStaff, which deliberately excludes both RBAC and legacy client/next_of_kin roles. Therefore an assigned-only worker gaining a portal role is not entitled to remain an eligible care worker; the prior Main request to cover this branch is not a requirement to grant that actor Locate access. Compare current-evidence decisions with ordinary canonical behavior, prove the negative HR relation executes without TypeError, and retain the intended assigned-worker denial. Configure a genuine portal fixture explicitly using its existing role/HR/membership rules before testing committed HR addition or membership removal; do not change application policy to meet a faulty fixture expectation.

The production audit service remains unmodified. An additive (request_id,id) index is still only a candidate: demonstrate existing-tail and empty/adjacent-tail behavior before selecting it. If it cannot avoid cross-request first-insert gaps while preserving a single ordered chain, return a concrete minimal request-serialized durable-tail design, backfill/rollback/deployment ordering and exact affected fields for Main technical disposition before schema edits. Never use SKIP LOCKED or an older ordinary snapshot to choose a possibly stale predecessor; retain exact hash bytes, same-request serialization and atomic rollback. Existing safe isolated diagnostic/test permission covers these proofs, not operational migration or a broader audit rewrite. Independent UI/guard work continues without a new user gate.

### Ordinary suite passes; full-width request verified

Main independently read .pkg02a-locate-feature-regression.log, SHA256 **0783FACC313C3FDE1A963938964651153D532504797111279B22398EF33B0963**: **OK181tests/1323assertions**, duration524.67seconds,181warnings with181 explicitly counted missing-.env diagnostics. Designer reports exit0. This is the separately isolated ordinary Feature suite; it supports the process-isolation diagnosis but does not resolve the independent audit overlap failure or certify the mutable whole candidate. Preserve the prior102- and132-case failed logs. Main has not independently rerun tests.

Main verified Stephan's actual message **01a0c1b3-6505-78e1-b39d-2e51184e4c34**, “it also does not fill the screen side to side?”, and inspected its screenshot C:/Users/steph/AppData/Local/Temp/codex-clipboard-89f419dd-a1b6-426f-b458-4a8ef0d45916.png. The one-line Client show change selects existing PageLayout width=full only for the Location tab. Main confirmed PageContent's wide option applies max-w-[1600px], while full retains w-full without that cap. This is within the direct user request and reuses the canonical layout without editing Rory's rules. Source hash **22496D96DE415636EB4B2DA356DF21492C68FF86EE73C4618CD2F004741D14DC** is a mutable inspection identity; final rendered width/map-resize verification remains required, not claimed by source alone.

Main also confirmed the HR relation now uses getQuery with withTrashed preserved, and CurrentAuthorizationReads now rejects mismatched actual root/nested query connections (sampled hash **38D31FCB8D24EB9B319045195E239E8834231F532E7BC782AB2B09E781F30413**). The targeted runtime proofs are still in progress. The private address adapter is disabled by default with separate configuration and no redirect/public-OSM fallback; no approved private service is operating or enabled by this checkpoint. Its complete access/observation/transport evidence awaits final review. No public coordinate submission or global provider change is released.

### Private reverse-geocoder preparation; no approved deployment located

Designer withdrew the public-coordinate proposal after consulting the provider policy. Main independently opened the [official public Nominatim usage policy](https://operations.osmfoundation.org/policies/nominatim/) on2026-09-21: its privacy section excludes personal/confidential submissions, and its stated scope distinguishes the public service from separately operated Nominatim instances. Therefore do not ask Stephan to approve sending personal tracker coordinates to the public Nominatim reverse endpoint. The single possible public-reverse approval question discussed in revision10 is superseded; none is needed for the recorded-address presentation or fake-only private preparation.

Main searched canonical programme rules/dependencies/amendments and geocoder references; no previously approved private geocoder deployment/configuration was located. Source config/fleet.php defaults reverse geocoding to disabled, provider google and the Nominatim endpoint to http://127.0.0.1:8088. Main's .env declares none of FLEET_REVERSE_GEOCODE_ENABLED, FLEET_REVERSE_GEOCODE_PROVIDER or FLEET_NOMINATIM_ENDPOINT, and no cached config file exists. These are read-only source/environment observations, not a booted effective-config or service-reachability proof. No server was probed, installed or configured.

Prepare the bounded current-observation adapter using fake responses and a separately configured, verified organisation-controlled/private geocoder only. Do not enable a global flag, select a new provider, silently fall back to the public service/default Google provider, install a service, or regard an arbitrary non-Nominatim URL as private. Keep lookup truthfully unavailable without the required private configuration. Preserve server-resolved observation binding, consent/access/retention, before-and-after disclosure checks, cancellation and the address/measurement-time distinction. A later real configuration needs a concrete deployment decision; it is not supplied by the localhost source default.

The user-approved explicit typed-address search remains a distinct path. For final review, account for the public provider's application-wide request limit and explicit-search requirement, including the existing NZ/global fallback calls; a per-user route throttle alone does not demonstrate a shared provider budget. Record the bounded treatment without silently rewriting unrelated Site workflows or expanding the coordinate-transmission approval. All automated verification remains synthetic.

### Verified address-first presentation request

Main independently read Stephan's actual Designer message **01a0c1ac-3fcd-7a32-8091-cbef1e4b4c54**: **“INSTEAD OF CO-ORDINATES CAN THE CLOSEST ADDRESS BE RATHER USED. and the co-ordinates smaller below the address”**. Same Designer may implement the address as the primary label and smaller coordinates beneath it across the current observation, selected history and Locate result. Preserve explicit separate address/coordinate fields and the exact observation's source/time; do not parse a mixed display string or borrow a residential/Site address, a different event's address or a stale device projection. Missing address remains honestly unavailable while coordinates remain usable. A reverse-geocoded nearest mapped address must not imply exact building occupancy or change the measurement timestamp.

Main sampled ClientLocationHistoryService: its metadata-tuple fallback explicitly has no address, while the exact correlated Locate event may carry its own address. Retain that distinction when extending presentation. The request is authorized directly and needs no repeat design approval.

The prior explicit external-transmission approval is for user-typed address text, not observed tracker coordinates. Designer owns preparation of a concrete, bounded current-observation reverse-lookup proposal and its synthetic/fake verification, then one clear payload/destination approval question if that external lookup is needed. Do not transmit coordinates or enable a real reverse-lookup path before that approval. Main will not ask a duplicate question. Scope the proposed adapter to server-resolved authorized observation coordinates, current access/fingerprint and retention, with cancellation, post-read disclosure checks and no appended client/tracker identity or history; no bulk history enrichment or automatic background tracking disclosure is released. Existing typed-address search approval remains effective.

The relation/connection and audit-lock findings in revision9 remain open. Designer reports the separately isolated ordinary regression run still in progress; no new passing backend result or final freeze is claimed. Same sole Astra/xhigh writer continues independent authorized work.

### Status findings addressed; expanded backend failures remain open

Main read the corrected SOS component, strict boolean controller mapping and callback-cancelled received-report refresh hook/tests. The sampled code now distinguishes active, timestamped acknowledgement and unconfirmed/default-false state, with neutral unknown icon and no inference of an empty history from an absent timestamp. The hook tracks one pending reload, cancels late tokens on pause/hidden/unmount/access-key change and requests preserved Inertia state/scroll. The hook's unit assertion about preservation proves the reload options, not an actual rendered draft/history round trip; the reported ongoing browser check must establish that behavior. Main has not yet independently sampled this new build in the browser.

Main inspected the 50-test/ten-file passing frontend log, .pkg02a-status-frontend.log SHA256 **8B9D597CCED27D7AA701ECE079763304034BAE14FCBB2FFEB04DB5C8A01903EF**. Designer reports TypeScript and scoped lint exit0/no warnings; their sampled logs are empty. The build log ends successfully with a chunk-size warning, so do not call the entire build warning-free. Whole-shape translation, exact single Undo, corner editing, synthetic address selection/explicit movement, grayscale and clean-console browser results are Designer-reported, not Main-rendered evidence. Sampled mutable SOS file hash **B1DCF14EAC041123B8C128C19526AD7233B4638CBF0CBB1A37B2FB32BF630FE6**, refresh hook **8E13018539552C38EB5D8EEFE64A7508010D926F2053802C95FF98D71F08183C**.

Main read the full bounded locate-current-evidence-inventory.md, sampled hash **C7BD93FD36D68D9600049499342F33C90E777729E8C9423AD650F28956603BAE**, and relevant policy/helper sources. This remains a mutable implementation inventory, not acceptance of every stated guarantee. One concrete current-path defect needs focused reproduction/correction: User::canAccessClientPortal passes hrEmployeeProfile()->withTrashed() directly into CurrentAuthorizationReads::query, whose accepted types are Eloquent/Query Builder. The installed Laravel Relation::__call and forwardDecoratedCallTo return the HasOne relation for that fluent call. Once the client/next_of_kin role branch is reached, this can raise TypeError instead of running the canonical negative HR test. Normalize that actual relation to its builder without losing constraints/withTrashed, and cover eligible staff with an additional portal role and current negative-HR/portal evidence. Ordinary policy behavior must remain unchanged. Also enforce the helper's stated connection boundary against each participating query's actual connection; checking only the default connection is not proof that an arbitrary supplied builder uses it. These bounded guard refinements fit the existing release.

The initial ordinary canonical run remains preserved: **102 tests/723 assertions/two failures**, .pkg02a-locate-canonical-regression.log SHA256 **C5BD7B92A0E4B61F18C94644549D50424A32FF2F8EC172232168B8046D3FB5C2**. Main inspected both failures and corrections: the original Ready/Queued validation message is restored; the healthcare positive fixture now explicitly uses active Client status, which ClientFactory otherwise randomizes, and adds an inactive denial without weakening the original assertion. This source correction is supported; its passing rerun was still pending at this checkpoint.

The later expanded run **did not pass:132 tests/1006 assertions/four errors/13 assertion failures**. Main inspected its final summary, representative contaminated-count/sole-record failures and the independent-client audit INSERT timeout. Preserve .pkg02a-locate-final-regression.log SHA256 **AB6F6FD974C507E5E0434A36FFA15AD8431D453FFD8565FCED3C066C3809E9F5**. Designer's explanation that committed concurrency fixtures contaminated ordinary RefreshDatabase families is a diagnosis awaiting a fresh-process comparison, not a blanket baseline waiver. Separating these families into fresh forced-profile/PID schemas is within the existing safe test release; do not relax assertions or change the shared harness.

The independent-client failure is MySQL1205 at a queued audit INSERT and must be diagnosed separately. Main confirmed DeviceCommandAuditService reads the previous per-request event using latest(id)->lockForUpdate, but has not established the blocking index/range or approved a particular remedy. Collect the actual competing statements/index-lock evidence and a deterministic reproduction. A necessary narrow correction within this canonical audit query/locking path may proceed within Locate once its cause and lock-order impact are established; preserve per-request chain serialization, exact hash format, atomic rollback and generic callers, and prove independent-request overlap plus same-request chain integrity. No audit redesign, relaxed integrity or claim that the timeout is merely test contamination. Escalate only a concrete wider contract/schema change.

Same sole Astra/xhigh Designer continues these corrections, isolated reruns and final browser/freeze preparation. Main stays application read-only. No candidate-wide green, integration release or additional user permission gate is recorded.

### Verified tracker-status request and mutable-source findings

Main independently read Stephan's actual Designer message **01a0c191-2962-70c0-b312-13de1a6a4770** requesting a panic display above the battery, uniform presentation and suggestions such as power saving/live tracking. Main inspected the attached screenshot C:/Users/steph/AppData/Local/Temp/codex-clipboard-30ab6a7a-3a0f-4061-981e-40dfd0044ad9.png. The bounded response may include the SOS card, uniform status cards, pausable refresh of already received reports and an honestly unavailable power-saving status. It does not release device configuration commands, a new alert/acknowledgement lifecycle, live unit polling or new monitoring policy. Same active Astra/xhigh turn remains verified; no new worker or user gate.

Main read .pkg02a-editor-frontend-final.log: **46 tests/nine files passed**, SHA256 **3DFFCDDB3ED2636121795392BE0E5AB8F940C4E25F5375CB04C3DB829814C149**. Main also read .pkg02a-locate-migration.log: **one test/11 assertions, OK**, one explicit missing-.env warning, SHA256 **E8C90851126E423117065A9BDD2E402F15C1537CEBF5C4F09086F835C0B254A2**. These are log inspections, not independent reruns or rendered acceptance. Canonical/legacy regressions and the new build are in progress. Main sampled the mutable status component, controller mapping, refresh effect, reduced-motion CSS and two canonical panic-state writers; final full-candidate review remains outstanding.

Return the following specific refinements before freeze:

- **SOS provenance/wording:** TrackerPanic currently renders “No panic events recorded” when last_safety_event_at is absent and treats any boolean false as a reported clear state. Actual FleetTelemetryIngestService defaults a missing panic_active to false on a non-safety report; BackfillDeviceMetaDefaults also synthesizes false. ClientController acknowledgement writes false and an acknowledgement time. Therefore false alone does not establish that the device reported clear, and an absent metadata timestamp is not a history search. Distinguish the recorded active flag, an evidenced acknowledgement and unknown/defaulted state; use a neutral unknown presentation and accurately labelled event/acknowledgement times. Do not rewrite those underlying workflows or run the backfill. Bound controller metadata normalization so malformed values do not become a reassuring clear state or an invented active alert through a raw bool cast. Cover the actual default-false/no-time case as well as null, active and evidenced acknowledgement.
- **Power-saving wording:** The sampled card hardcodes unknown status but says the unit has not reported a supported control. Presentation absence alone does not prove device capability. State that power-saving status/control is unavailable here until the existing canonical capability/report supplies evidence. Do not invent a setting, command or unsupported-device verdict.
- **Live-view verification:** The sampled effect reloads the location prop every30seconds while visible. Demonstrate pause/resume, hidden/unmount cleanup and late/in-flight response behavior after privacy loss or fingerprint change; retain a historical selection and unsaved editor state across ordinary refreshes. Do not initiate Locate/device commands, infer current wellbeing, or imply that auto-refresh changes tracker reporting frequency.

These are refinements within the approved presentation request. Continue other released work; report their disposition with the final source/build/browser evidence instead of opening an acknowledgement-only approval loop.

### Corrected Locate run and verified draft-editor requests

Main independently read .pkg02a-locate-current-flow.log, SHA256 **3D1B29C9B2343D46351C77E3CD6528CB5A2A39AB95CDF946438B30AE83ED1C47**. It ends OK, **27 tests/278 assertions**, and identifies PHP8.4.16, the forced package profile and MySQL8.0.44/REPEATABLE READ. It reports 25 warnings and contains 25 missing-.env diagnostics. Designer reports exit0 and full TypeScript exit0. This supersedes the pending corrected-run status below; preserve the initial two fixture failures and their correction evidence. The reported current-authority, shared-reader, delivery, HTTP and signing coverage still requires the final call-graph/source review. Main has not independently rerun the suite, accepted all code or verified the rendered Locate flow. Standalone migration verification and final regressions/build/browser/freeze remain in progress.

Main independently read Stephan's actual messages in the same Designer task: **01a0c18b-307f-70e2-82c9-81677cbf28e7**, “when trying to move the geofence i cant can you improve this please”, and **01a0c18d-b4b1-7951-b87f-1c67190f0d47**, “also need to be able to search the address alot has been missed”. Main also inspected the attached editor screenshot at C:/Users/steph/AppData/Local/Temp/codex-clipboard-25997d7b-eb0b-4950-9687-598e12ec6faa.png. These explicitly authorize the bounded editor improvements; no repeat mockup approval or master amendment is required. The active turn remains01a0c139-dbaa-7912-bfc8-445340f903e1, independently reverified gpt-6-astra/xhigh in both fields at2026-09-21T01:18:31.008Z.

Same Designer may implement whole-shape movement, a visible centre handle, live corner preview and undo in the existing Client Location draft editor, plus the address-search presentation with fake results while the restriction above applies. Main inspected the mutable proposed client search adapter/route and SiteGeocodingController: they reuse its existing Nominatim endpoint and typed query; the new optional failure distinction leaves ordinary callers' default behavior unchanged. This source observation is not browser or external-provider verification or permission to enable the proposed transmission.

Keep the following concrete acceptance boundaries in the final candidate:

- Prove real browser dragging of polygon/circle and individual handles, map-pan restoration, keyboard movement and one undo step per completed drag. Cancellation, lost pointer capture, dialog close and access loss must not leave a stale preview or an unintended saved change. Preserve grayscale and accessible focus/handles.
- Address search uses an explicit user action with bounded input/results, cancellation and late-response protection. Distinguish no matches from unavailable service; reject invalid coordinates. Selecting a result may reposition the view, but cannot silently move or save an existing boundary. Any boundary movement is an explicit edit with undo.
- Reuse the existing provider only for the typed search query; do not append client identity, tracker information or observed coordinates. Preserve the canonical draft-access/fingerprint checks before lookup and before response disclosure, no-store headers and route throttling. Retain existing Site geocoder behavior for its ordinary callers and demonstrate the opt-in error path's compatibility.
- Use fake geocoder responses and transports in automated tests and the isolated synthetic preview; no real address-provider call is needed for this verification. Cover denial before any provider call, access loss during lookup, provider failure, stale results and cancellation. These requests do not authorize zone activation, monitoring, alerts, new sharing, live tracker dispatch or edits to Rory's guides/frozen mockups.

Continue the existing implementation and final verification without an acknowledgement-only approval loop. Main remains application read-only. Freeze exact source/build identities after these changes; the final technical/integration gate remains outstanding.

### Current-authority helper disposition and second checkpoint

Main independently confirmed the reported ordinary-read paths in DeviceManagementAuthorizationService::isExplicitlyDenied, SecurityDevicesAccessService::accessibleSiteIds and ClientWorkerEligibility::isEligible. Main also traced ClientPolicy and ClientProfileSectionAccess into UserSiteAccessService::accessibleSiteIds, which memoizes by actor identity, and ClientWorkerEligibility into applyFleetRecipientEligibility, which queries roles, current employment and site membership. Hydrating an actor or passing fresh:true alone does not demonstrate that these subsequent decisions use the locked current evidence.

The existing Locate release includes narrow opt-in current-evidence predicates/plumbing in those three helpers and, where actually on this decision path, UserSiteAccessService, ClientProfileSectionAccess and ClientPolicy. Reuse canonical decisions and preserve ordinary public-call behavior, permission aliases, explicit-deny precedence, staff approval/type, allowed roles including existing legacy-role semantics, current employment dates/timezone, site membership, operational-site predicates and existing site-bypass semantics. Do not introduce a new portal/role grant, global cache mode or a copied Locate-specific authorization policy. Evidence must be transaction-bound, server-owned and complete; a merely loaded relation or caller-supplied boolean is not proof. Missing evidence must fail closed or be acquired through the approved current-read path, never silently fall back to an old ordinary snapshot.

Before final freeze, provide one bounded inventory of the actual Locate authorization call graph, including policy helpers, nested exists/subqueries, memoization, generic command re-entry and provider claim. Identify each decision's current evidence and reuse boundary. This is a coherent completion of the already released guard, not a new approval request for each adjacent helper. Limit edits to the actual path; stop only for a concrete unresolved policy/contract change or an extension beyond Locate.

Verification must establish an older snapshot and, where relevant, prime memoization before a second connection removes a role/site assignment, deactivates employment or commits an explicit deny. Prove denied enqueue/provider output as applicable, plus claim-first writer exclusion, rollback/retry recovery and ordinary/generic caller compatibility. Preserve canonical alternative grants in fixtures so each test isolates the authority it purports to remove. Do not assume a locking clause on an outer query locks every nested subquery. Prove the relevant evidence coverage on the actual database. Use compatible shared current locks for read-only evidence where justified, and demonstrate two independent client operations can overlap while relevant writers remain excluded; no global isolation change or unbounded retry.

Main read .pkg02a-locate-concurrency.log (SHA256 9AFE050F7BE1FB9E39CF08DB9159F2A206BD6C72664C0C0CC2041393FE7937E3): OK, five tests/51 assertions, MySQL 8.0.44 REPEATABLE READ, with three explicitly reported missing-.env warnings. These are the existing bounded concurrency/signing results, not proof of the newly identified staff/site/permission paths. Main also read the scoped frontend result: five files/33 tests passed, log SHA256 73477ADB48B0F350B5D202E1A7600413EF0FC79AF9B682F7E6617AC2D80D5F53. Designer reports full TypeScript and scoped ESLint success; the inspected lint log has zero errors and one raw-button warning in the dialog test. Main has not independently rerun these commands or performed rendered Locate verification.

Designer reports the HTTP adapters/dialog now exist and the first combined backend run passed 17 of 19 cases with two fixture errors, locally corrected but not yet a wholly passing run: missing required fleet_telemetry_events.asset_id and a secondary Eloquent connection retaining the primary configuration name. Preserve the initial failures and capture the corrected rerun; no whole-green claim or integration approval. Audit rollback, lifecycle, migration, deeper snapshot and final browser/source/build evidence remain in progress. Same current turn01a0c139-dbaa-7912-bfc8-445340f903e1 is independently reverified as gpt-6-astra/xhigh in both fields at2026-09-21T00:38:20.611Z. Main remains application read-only and the same Designer continues as sole writer.

### First Locate foundation checkpoint and provider provenance

Designer delivered the substantive foundation checkpoint. Main independently read the five-test/37-assertion passing log, the strict origin DTO/shared verifier, canonical intake/dispatch diff, current-evidence reader and privacy/RBAC/custody changes while source remained mutable. The five warnings in this run are explicitly identified as missing .env reads; this does not identify r4's compact warnings retroactively. Main has not run the tests independently or accepted the whole Locate candidate. HTTP/dialog/provider adapters are not yet enabled and concurrency checks are still in progress.

Captured inspection identities: ClientLocationCommandContext.php 8EE442EDCEDADF69D809561DBEBB066A7900554D5E1DBA2796DE21FA35149738; CurrentConsentEvidence.php 87B27E968D10F72CE722FE4AFE3C22B023A1641C73E868BEF084C3563C544F42; AuthorizationEvidenceLockService.php 46813B53606C0948F72FDF984560D8A0A12A32FC1F8769EE414753ACE2270D6A; local foundation log 7CFE8C04CBBB83679D5F2931036B856C606E33039522A1C18959511B2C75B414. These identify sampled mutable source, not a new frozen release manifest.

Main confirmed migration2026_08_02_000018_create_governed_device_configuration_profiles.php defaults every provider row to sequence1/role action, including genuinely legacy rows used by FrameRouterTest. Those defaults cannot prove whether a both-links-null row was once governed. The earlier linkage requirement therefore includes an approved narrow durable was_governed marker (or equivalent typed provenance) on the existing QueclinkPendingCommand, with a separate additive migration. Backfill only from actual canonical request/attempt linkage and set the marker atomically whenever linkage is created. Provenance cannot be downgraded by normal model/service updates or supplied by generic input. Guard marked rows after loss of both IDs, partial linkage and mismatches; preserve genuinely legacy behavior. This is not a second signature or lifecycle.

Required tests cover migration/backfill, creation, downgrade attempts, stripped links, rollback/evidence retention and legacy FrameRouter cases. Record the migration/code rollout gap for future deployment. No operational backfill is authorized. Previously damaged unlinked default rows cannot be reliably reclassified, and arbitrary privileged SQL erasing every provenance field is not claimed detectable. This concrete source limitation supersedes any interpretation that sequence1/action alone was sufficient evidence.

Main also returned one concurrency concern: the sampled NOWAIT implementation exclusively locks shared ConsentType/Version, Role and all Permission definitions. Independent client commands can therefore contend without an access change. Exercise independent clients/actors sharing the normal definitions; use compatible non-waiting shared reference locks or another proved bounded solution where appropriate, without weakening current reads or exclusion against policy writers. The narrow AuthorizationEvidenceLockService opt-in method is included in the current-evidence refinement; preserve its existing public signatures/ordinary behavior and affected regressions. This is a scoped implementation finding, not a broad RBAC rewrite or a pause to independent released work.

### Current-evidence resolver clarification

After the initial release, Designer identified an additional implementation constraint and Main independently confirmed it: PersonalTrackingPrivacyService::assignmentAuthorisesClientForPurpose requeries the assignment and related consent/device/client with ordinary reads; ConsentValidationService::isValidResidentLocationConsent and consumabilityDecision call resolveConsent again. In a transaction whose REPEATABLE READ snapshot predates withdrawal, passing previously locked/hydrated models is therefore insufficient evidence that these decisions use current records.

The release includes a narrow opt-in current-evidence path in **PersonalTrackingPrivacyService** and **ConsentValidationService**, plus **DeviceCustodySiteResolver** only where its requery participates in that path, and a focused typed evidence helper if needed. Reuse the canonical decision predicates for self and substitute authority; do not copy, bypass or weaken them. Require an active transaction for the locking/current-evidence API, keep ordinary callers backward-compatible and ensure the participating nested source/type/version/authority/capacity decisions cannot silently fall back to stale ordinary reads. No global connection-isolation change, broad consent rewrite, policy/default change or new grant is released.

Reconcile the actual authority writers before choosing graph-lock order: Main observed ConsentAuthorityScopeService taking an authority-scope lock before linked consent locks, so naively adding scope locks after consent can introduce an inverse order. Document the concrete bounded resolution alongside the code and verify it with deterministic concurrent tests. Establish an older snapshot before a committed withdrawal, then prove denied queue/provider claim with no bytes/effects; also exercise the claim-first ordering and relevant substitute-authority/capacity changes. Include affected canonical consent/privacy regressions. Assess the shared draft-save path when touching its resolver, preserving the already proved care-pivot behavior and clearly distinguishing demonstrated new failures from hypothetical ones.

This is an implementation refinement required to satisfy the already released current-access guarantee. It does not suspend independent work inside the Locate slice or require another user approval. Include this evidence-path correction in the substantive implementation checkpoint; stop only for a concrete unresolved lock/contract/isolation issue rather than treating a copied model as proof.

Same Designer is the sole application/test writer for this Locate slice. It may now implement the listed files and tests, with a substantive progress checkpoint when signed context and the first guarded request path are working, or within about 45 minutes if still in progress. It may continue within this released slice while reporting progress; stop for a concrete unresolved contract/isolation issue or before expanding beyond Locate. Freeze the resulting candidate for Main review before integration. Main stays application read-only.

Operational safe-zone promotion/evaluation/response, outings and new sharing grants retain their separate unfinished requirements. This release does not extend A4, resolve unknown policy, close Maintenance, open another mockup task or alter any other product requirement.
