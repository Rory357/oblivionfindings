# PKG-01 — Main replacement technical review

Owner: MAIN ASTRA. Revision: 1. Reviewed: 2026-09-20. Decision: **Approved for integration** of the exact candidate below, subject to the existing Section 14F reconciliation, repository and publication gates. This is not publication verification, operational activation or Stephan's page acceptance.

## Exact approved candidate

Application root: `C:/Users/steph/.codex/worktrees/2375/oblivionfindings`. Branch: `codex/pkg-01-maintenance`. HEAD/integration baseline: `e62b569ff42ab471300fb6713a68758b647b2c32`. Delivery is still uncommitted; no new source commit is invented. Approval binds the complete frozen file set in Designer's `docs/fleet-assets-audit/evidence/PKG-01/designer-main-review-candidate-manifest.json` in worktree8424, SHA256 **14EA5A91290AE59808C4490613973E9142EA5ABDC99428B8F9788740E5B142D8**. Designer must record its exact mapping to the prepared source commit before integration. A mechanical commit of identical approved contents does not imply a code change; changed application/test contents require renewed affected QA and Main approval.

Main independently matched all70 source/evidence files,2 unchanged local harness files,9 principal built entries, their Vite entry mappings and the full build-manifest hash. The final comparison at2026-09-20T08:32:43Z found no candidate/build mismatch. The current app is `app-DpcluT-n.js`; work detail is `show-BBjsvSZ3.js`. Main also confirmed the stated HEAD, scoped working changes and a passing candidate `git diff --check`.

All17 canonical context entries at the Main repository match manifest12; these references are rooted in Main's programme context, not assumed copied into8424. Canonical master SHA256 remains77F404510B6E5C1A270904650A22D6AD7DD0FB8A6842183D33DAB0C687D31C56. Protected DESIGN/Popup/Work Record references match the recorded approved hashes. All21 frozen-v8 preview files match their manifest; v8 identity remainsb05ea36ccf287d7a898713939dbdbc03db0fb70c38d47f26db8e033544b314eb. No reference or product requirement was changed to accommodate the implementation.

This review follows the substantive whole-package review in [Main review revision1](PKG-01-main-technical-review.md), SHA2564DBD8B8F2D5890F6448BB9C4E4AF4C14D20CA93203356DAC380BFA08BF034CC2. That rejection remains historical evidence for its outgoing CDD077 candidate. Main inspected the replacement's27 changed/new files and affected contracts; this decision is not based solely on Designer's success summary.

## Disposition of the eight blocking findings

1. **I03 — resolved.** Work detail projects an explicit permitted shape instead of serializing the full model. Custody-only access omits private problem/owner-action content; legacy notes, completion notes, cost and journal fields cannot leak through the ordinary Fleet work projection. Finance access remains separate. Main inspected the actual nonempty sensitive-field Inertia assertions and independently ran that regression.
2. **I05 — Outing booking bypass resolved.** The Outing producer authorizes and locks the canonical approved-site asset, checks active vehicle state and applies the shared maintenance booking restriction before creating outing/booking rows within the transaction. Main inspected the real concurrent lock/hold regression and zero-partial-row assertions; Designer's full-suite pass covers it.
3. **I05 — multiple-job release deadlock resolved.** Release still requires a current-policy retest covering the exact active restriction set, relevant source timing and independent authority. It clears only the releasing job's restrictions; other holds continue to deny booking. The second job's stale coverage must be refreshed after the first release. Main independently ran the two-completed-jobs regression and inspected the own-source update and explanatory UI.
4. **I12 — default configuration authority resolved.** The actual RBAC seeder excludes maintenance configuration authority alongside independent release authority. Main inspected the actual-seeder regression and denied configuration attempt; this is covered by Designer's full run. No actual person's permissions were changed in Main review.
5. **I12 — template/configuration incompatibility resolved.** Actual template creation establishes stable question IDs, canonical yes/no options and validated select options. Authoring exposes those options; unsupported text/number items are explicitly observation-only. Configuration rejects duplicate IDs and unsupported outcome mappings. Main independently ran create-template HTTP → configuration → standalone submission and inspected the controller, form and validation boundary.
6. **I02 — rollback provenance loss resolved.** All four down migrations call the shared retained-evidence/provenance guard before any destructive operation. The guard includes standalone check provenance and report corrections. Main inspected each placement, the MySQL FK/index ordering repair and the disposable rollback regression. Designer's1-test/38-assertion pass establishes the tested empty rollback and populated refusal cases. Main did not run DDL against operational or browser data.
7. **I06 — material v8 structure/draft/search mismatch resolved for this candidate.** The compact reference hero, shared section/view tabs, summary/notes/provider hierarchy and right-side next action/responsibility/release requirements are restored. Contextual reporting stays on work detail with the retained resource card and expanded estimate calendar. Closing the mounted report retains its draft; navigation/reload limitations are stated rather than promising durable autosave. Asset search distinguishes loading, failure/retry and empty results while retaining selection. Main inspected source and performed the bounded desktop verification below; Designer's broader scoped modal, upload, failure and return-path QA was also reviewed. Legitimate fixture, role, state and theme differences do not imply pixel equality.
8. **I01 — routine checks requiring an existing job resolved.** A configured check accepts an asset without a work order. The server validates private evidence, fingerprints file content for retries, establishes evidence verification itself and cleans failed writes. Downloads require current approved-site authority. Page projections remove private storage paths. Later report linkage preserves the original answers/evidence and does not rewrite the check's work-order field. Main independently ran the authored-template/passing-standalone/failed-evidence/later-link and access assertions. This does not authorize automatic work creation for every failed check or invent an operating safety matrix.

No unresolved blocking technical/design finding remains from Main's reviewed scope on this exact candidate. Existing I04/I07/I09/I10/I11 resolved histories remain; no issue or consumed attempt is reset.

## Verification and limits

Main independently ran PHP8.4.16/Pest with the forced `phpunit.pkg01.xml` profile and the three targeted cases in `Pkg01MaintenanceProtectedSliceTest.php`: work-detail projection, authored template/source evidence and two completed jobs. Result: **3 tests,146 assertions, exit0;211.31s;3 warnings**. [Exact output](PKG-01-main-replacement-focused-test.log), SHA256133A46C54FA8B2A1CF35ABC8E4F6741874750B8BB72FDD63E8DCDF5FA65AF7F8. This is not described as warning-free. Designer documents absent-local-.env warnings in the deliberately isolated profile; Main did not add an environment file to suppress them.

Before execution Main checked forced testing/mysql settings, the dedicated `oblivion_findings_pkg01_2375_test` prefix, TestCase's uniquely suffixed disposable schemas and constrained cleanup, array/null transports, fake evidence storage, unchanged harness hashes, absent local environment/cache overrides and no ambient APP_ENV/DB_/maintenance overrides. No operational or persistent browser schema was migrated, reset or seeded by Main.

Main read Designer page revision36, correction-plan revision3 and replacement verification revision1. Designer's independently executed full protected21/454, dedicated rollback1/38 and final source-projection/link1/74 runs exit successfully. These overlap and are not summed as unique cases. Full-suite timing precedes some bounded final display/source changes; the final targeted regression plus final type/lint/build/browser evidence covers those changes. Earlier failures remain accurately recorded. Main reviewed the tests and log outcomes rather than claiming to have independently rerun every case. Designer's final TypeScript, scoped ESLint, PHP syntax, diff and Vite build checks are recorded; the build's size advisory remains.

Main compared actual2375 work detail/report at8765 against unchanged v8 at4324, viewing screenshots in tool output. The implemented compact hero and two-column work-panel hierarchy correspond to v8; release requirements are present on Overview and their action opens the completion/release review flow with the current no-hold state explained. Main inspected1440×1000 and1280×900; the latter was independently read from the DOM with1265px document width and no horizontal page overflow. The report's calendar/summary and footer fit the smaller desktop. Selecting23–25September produced3calendar days. Closing/reopening retained the exact labelled unsent title/observation and date range on the same work-detail URL. No report, check, file, note, release, assignment or appointment was submitted by Main. Designer's broader browser verification covers staged uploads, report-only resume, actual standalone evidence/linking, failed appointment recovery, search failure, profiles, source calendar and All Tasks return paths.

One initial new-tab load recorded a failed dynamic import of `show-BBjsvSZ3.js` at08:24:52.395Z and a blank body. One reload recovered fully; the later explicit reload also rendered correctly with no new warning/error entry. The matching chunk exists and matches the frozen build manifest. No root cause is asserted and the run is not called error-free. Integrated smoke verification must check a fresh page load on the actual published build; a recurrence is a blocker to publication verification. Main's temporary comparison tabs were closed and viewport override reset. Designer's existing preview/session remains available.

## Ownership and next gate

Stephan's verified Designer-task instruction stops further Sol use. Designer alone made these repairs and retains sole application/integration ownership. Actual Designer turn01a0bd94-5f29-7282-b246-49c96d7b0abd verifies gpt-6-astra/xhigh in both effective metadata fields. I03/I12/I01 remain2 requested/1 delivered and I05/I02 remain1 requested/0 delivered under Sol; the additional requests were withdrawn before delivery. I06 remains2/2 exhausted under Sol and repaired by Designer; I08 remains2/2 resolved. No new worker or further Sol assignment is authorized.

The same Designer may now prepare the exact approved commit, reconcile current remote main, satisfy the actual repository/release gates, and perform Section14F integration/publication verification. Preserve unrelated work and map the approved file set to its committed/integrated identity. Any material baseline/contract change or application/test modification requires renewed affected checks and Main approval before publication. Do not treat this local review as a hosting-platform review or permission to bypass protections. No operational migration, operating policy/grant activation or production deployment is approved by this decision.

Designer must return actual source-to-integrated mapping, remote inclusion and integrated-check evidence for Main publication verification. Stephan's page acceptance follows that verification; no next package, closure or unpinning yet. Main, Designer and stopped Sol remain pinned, verified through the app. Per-task spend/usage is unavailable and is not inferred from shared allowance or test runtime. Main remains reviewer and made only programme/evidence documentation updates, with no application edit, commit, merge, push or operational mutation. Existing unrelated `public/.user.ini` remains untouched.
