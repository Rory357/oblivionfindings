# PKG-01 — test-server publication approval

Owner: MAIN ASTRA. Revision: 1. Recorded: 2026-09-20.

Stephan directly answered Main's question about whether pushing GitHub main automatically deploys the site or runs production migrations:

> this is just a test server merge and push to main is fine.

This identifies the destination as a test server and explicitly authorizes merging and pushing the reviewed PKG-01 package to the confirmed `Rory357/oblivionfindings` origin main, including the ordinary test-server automation triggered by that push. It resolves the external-deployment-consequence pause in the prior integration-preparation record. Do not request that permission again or continue presenting it as unresolved. The historical inaccessible-webhook observation remains true; the user's explicit test-server publication decision supplies the release authority rather than a fabricated claim that no hook exists.

The current substantive technical approval covers Designer integration candidate manifest **7E5A61877E5B757B386D3AF486FE405BBE0DA5CC3D72851DAE3572CF372914B4** on base **e62b569ff42ab471300fb6713a68758b647b2c32**, including the reviewed CI-prefix/current-PID rollback-test correction. Main also approved mechanical publication of its existing D1–D4 additive guide patch and the exact non-sensitive context set. See [Main integration preparation](PKG-01-main-integration-preparation.md) and the current context/publication manifests. This release decision does not change the approved source, product requirements, issue histories, model-selection policy or protected-reference boundaries.

The same Designer Astra Extra High is the sole application/integration owner. Sol remains stopped. Designer completes traceable source/context commits, reconciles the current remote, preserves Main's older dirty checkout and unrelated files, uses the legitimate integration path and pushes the explicit main destination. No force push, protection bypass, credential publication, arbitrary schema reset, operational policy/grant invention or next package is authorized. Any source/test or materially relevant baseline change requires the existing affected review/check process; permission to push is not permission to claim unrun checks passed.

Required package/integrated checks and precise baseline-failure classification remain. Designer returns exact approved-source-to-commit mapping, actual remote inclusion/time and fresh-build browser evidence. Main then verifies publication. Stephan's final page acceptance still precedes closure, unpinning and the next package. Actual operating configuration and live activation remain governed by A4. Test-server publication is not production approval or evidence that deployment has completed.
