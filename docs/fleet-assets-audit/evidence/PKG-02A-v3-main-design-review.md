# PKG-02A v3 — Main design review

Owner MAIN ASTRA. Revision 1, 2026-09-21. **Ready for Stephan's exact-version and bounded Client Location design decision. No implementation approval.**

Reviewed frozen http://127.0.0.1:4334/PKG-02A/v3/ from Designer task 01a0be31-19ef-7d10-86a9-cbe968989a76, worktree C:/Users/steph/.codex/worktrees/2b9f/oblivionfindings, branch codex/pkg-02a-client-location-design, baseline 2302ca33a95616e442a78e957ddce82d8db98669. Main previously verified its actual Astra/xhigh continuation before releasing v3. Manifest SHA-256 **1BA5E6B75D44964C2C20B6BC21FB735B79F1CE0C30BE3BD613C7FA7299F737B9**.

Main independently matched all 40 artifact hashes/lengths and all 39 baseline input hashes with zero mismatches. Main also rehashed all 29 v1 and 34 v2 artifacts and their original manifests; both remain unchanged. Git status showed only scoped untracked preview/evidence/page content, with no tracked application or guide modifications. Git emitted a nonfatal permission warning reading the user's global ignore file. Main did not alter Git settings, application source or preview files.

## Independent rendered review

Main read the Designer page, QA, source-contract delta, focus and reduced-viewport evidence. In a separate hidden tab Main exercised:

- Visible Map actions, menu Escape returning to its button, and actual right-click on Garden walks exposing draw circle/custom zone, inspect, edit proposal and Locate now actions. The menu was also visually inspected at 1440×900.
- Draw safe zone opens directly on Boundary. The keyboard starter rectangle, a corner arrow-key action, explicit Finish boundary and progression to naming/purpose worked. Linking a place is optional. Main did not independently repeat pointer dragging or complete/save a new operational draft; those broader synthetic paths remain Designer-reported.
- Dirty Cancel → Discard draft returned focus to Draw safe zone, confirmed by the actual DOM active element. This resolves the prior Main focus observation for the sampled path. Designer's additional Keep editing and pristine Escape checks are recorded separately.
- Locate now accepted a synthetic reason and preview identity confirmation. Acknowledgement-only retained the 2:32 pm observation, and reopening retained request context with dispatch disabled while pending. A separate simulated successful response displayed 2:36 pm, retained the original 2:32 pm history record and kept the battery sample at 2:32 pm.
- Activity restored From/To calendars. Applying 19–20 September showed earlier 19 September records in addition to 20 September; observations/responses and future plans are explicitly separated. Forced failure kept the range and displayed Retry history; retry restored its records.
- The reduced 640×400 profile now lays out all five metric cards without the overlapping labels reproduced in v2. Main visually inspected both rows and the final full-width card; DOM sizes matched 242×94 for four cards and 491×94 for the fifth, with document width 625 inside viewport width 640. Main also sampled the 1280×800 desktop layout and the 1440×900 map/menu. This is reflow evidence, not proof of actual 200% browser zoom.
- Switching to Collection decision withdrawn removed the location map, history and request workspace. The final sampled warning/error console was empty. Main reset the temporary viewport and closed only its own review tab.

One transient closing snapshot changed the Locate dialog's heading to the generic Recorded staff response heading before dismissal completed; the next DOM snapshot showed it closed with focus on View locate request. This is a non-blocking transition polish observation, not a stuck dialog. Preserve for frontend transition/accessibility QA; do not silently change frozen v3.

## Decision and remaining gates

The three bounded v2 findings are resolved in Main's independent sample: usable historical date range, reduced-width profile readability and dirty-discard focus recovery. The user's requested drawing entry, contextual map actions and Locate now are present and coherent in the reviewed preview. No additional scoped design blocker was found.

Main independently read ClientController::locateNow and app/Services/Queclink/LocateNowService.php. They support the reported existing governed management redirect and tracking.location_refresh capability boundary. The preview does not prove actual command dispatch, identity enforcement, backend privacy, geometry topology, schedules, DST/overlap policy or notification delivery. Real implementation must reuse the canonical workflow and preserve current authority, assignment and capability checks.

Designer reports passing typecheck/build and 40 privacy-state/desktop combinations, as well as pointer geometry, draft-save, timeout/offline and broader history-state checks. Main did not rerun that full matrix or production tests. Actual 200% zoom and full keyboard/screen-reader acceptance remain unverified. No real tracker, map service, database, consent/grant, policy or operational rule changed.

Stephan still decides whether to approve this exact v3 and its bounded Client Location scope. Such approval does not settle unknown privacy/operating policy or itself release implementation. Required PKG-01 closure, explicit frontend/backend contracts, technical prerequisites and later Main technical/publication/acceptance gates remain. Same Designer owns future frontend at Astra Extra High; any later authorized Sol work is backend only with one-writer handoffs. No new task, Sol assignment, extra mockup package, commit or push is authorized by this review.
