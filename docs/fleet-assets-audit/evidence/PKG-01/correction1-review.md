# PKG-01 correction1 independent review

Owner: DESIGNER ASTRA. Revision 1. 2026-09-20 05:20 UTC. **Return I06 and I08 for correction2; no package PASS.**

Actual review turn `01a0bd32-9f8a-7590-98fa-cb5f5893d090`, metadata timestamp 2026-09-20T05:06:38.751Z, verified `gpt-6-astra / xhigh`. Same Sol writer, source/build frozen throughout independent QA. No application edits by Designer.

## Exact candidate and independent verification

- Worktree `C:/Users/steph/.codex/worktrees/2375/oblivionfindings`, branch `codex/pkg-01-maintenance`, HEAD/base `e62b569ff42ab471300fb6713a68758b647b2c32`, uncommitted correction1.
- Worker manifest `docs/fleet-assets-audit/evidence/PKG-01/implementation/correction1-candidate-manifest.json`, SHA256 `C7635160DDCDA2B087FAD5750F597EBE8FD98C1ADE5DA98DE4497BC0303C7DEE`. Independently matched all 62 source, 5 built-asset and 2 local-harness hashes plus the build-manifest hash before and after QA.
- IAB tab 3 on isolated loopback `http://127.0.0.1:8765`, independently loaded `app-BBLaTBVv.js`. Other recorded principal assets: index-DXxebCUo.js, show-CGuGa8mo.js, create-wizard-DS7a7YAE.js, app-BUGo9VPK.css. No operational host or operating data used.
- Rechecked absent `.env`, `.env.testing`, `bootstrap/cache/config.php`, `public/hot`, and ambient APP_ENV/DB override names. Read the forced disposable profile and process-specific database create/drop/prune boundary. Ran only Herd PHP8.4 `vendor/bin/pest --configuration phpunit.pkg01.xml tests/Feature/FleetAssets/Pkg01MaintenanceProtectedSliceTest.php --compact` in2375, unique `oblivion_findings_pkg01_2375_test` prefix, null/array transports. **Exit0, 15 tests / 297 assertions, 498.47s, 15 warnings.** Compact output does not expand warning causes. Process exited after cleanup. No duplicate test run or frontend rebuild performed.
- At1280×900 and1440×1000, work detail measured clientWidth=scrollWidth1265 and1425 respectively (vertical scrollbar present). Screenshots viewed beside frozen v8 `02-hero-1440.jpg`. No horizontal overflow. Browser warning/error log returned empty. Temporary viewport reset; review tab retained for handoff.

## Independent browser journey and fixture mutations

On synthetic WO-2026-0001, as PKG-01 Manager, uploaded `designer-synthetic-evidence.png` through the documented chooser/dropzone, observed staging and saved evidence, marked repair work complete, and observed the hold remain active. Saved a new passing brake retest with a clearly synthetic observation. Searched and nominated PKG-01 Backup for custody. Final release remained disabled for the manager and before receipt.

Signed in as the report-only Backup. Exact work GET rendered without broad Fleet navigation. Release review opened directly at custody; **Acknowledge receipt** succeeded and showed “Receipt saved by PKG-01 Backup.” Private attachment/report/Finance lists were withheld by inspected source and tested HTTP projection. Signed in as PKG-01 Reviewer, a different actor with the pre-existing synthetic site/category grant. Final review showed saved repair evidence/completion, passed retest and Backup receipt. **Release asset** succeeded, modal closed, active hold count became0, and History attributed Complete to Manager, Acknowledge Custody to Backup and Release to Reviewer. No operational release occurred; automatic effects remain disabled in the synthetic browser environment.

Tab3 ends on WO1 Work detail, signed in as Reviewer. WO1 is now completed and released, with retained repair evidence, new retest and custody/release records. WO2's provider plan was not mutated. Do not assume the original open/held WO1 fixture state when resuming.

## Bounded findings resolved by correction1

- **I01:** ConfiguredQuestions is wired into the relevant check/retest entry paths with exact options, conditional applicability and saved same-work private evidence IDs. Independent tests cover conditional/missing/borrowed evidence and N/A guards. Worker reports the conditional evidence browser cases; Designer independently proved repair upload and normal retest, not every conditional screen. Remaining native upload presentation belongs to I06, not a reset of I01.
- **I03:** Selected custody participant GET/entry/receipt works independently in the browser without general Fleet access. Source and tests cover supersession, wrong site/unrelated actor, released-record denial and retry reauthorization; current-site resolution uses current HR profile state. The original inaccessible-recipient finding is resolved. Restricted-view wording defects remain under I06.
- **I09:** Restored seven-day target horizon, undated exclusion and default completed/cancelled exclusion. Context link uses canonical reference and explains ineligible work instead of presenting a misleading empty Tasks destination. Independently observed the undated explanation; provider regression passed.
- **I10:** A hold records owned affected-booking follow-ups once, projects them into work/booking detail and retains review/release attribution without altering the booking decision. Source inspected and independent tests passed, including existing-booking protection and overlapping asset-lock checks. Browser fixture had no affected booking; no booking browser success is claimed.
- **I11:** Provider appointment projection now uses a stable work-based identity and latest planned/confirmed/cancelled/completed state alongside separately identified report estimates. Site Calendar destination is labelled truthfully. Generic asset current-hold indicator is present. Source inspected and independent projection tests passed; not a complete calendar/browser acceptance.
- **I12:** Supported configuration command/service validates explicit actor/site/category, current Coordinator and separate backup, exact check-template mapping, approval reference and reviewer grant/revocation. Preserves versions/audit history and introduces no default reviewer/configuration grant. Independent configuration tests passed. No actual operating policy/person/grant was configured by Designer.
- Prior I02/I04/I05/I07 bounded resolutions remain covered by the passing regression suite. None of these bounded resolutions constitutes whole-package PASS or live activation approval.

## Correction2: I06 — approved layout and complete modal/state handling

1. **Approved v8 hierarchy is still not implemented.** Search, metrics and section controls are below the hero; metrics are Progress/Active holds/Checks/Booking follow-ups instead of Restriction/Evidence/Affected bookings/Finance. The six section controls are ordinary buttons outside the hero rather than the shared tabs treatment. Overview/Activity subviews and Work overview introduction are absent. Port the approved structure using existing shared components, preserving the user's theme and real data. Do not approximate it again with unrelated cards. Recheck both desktop sizes and keyboard tab semantics.
2. **A planned provider appointment cannot finish positively from release.** In show.tsx repair step, a latest `plan_provider` exposes cancellation only; completion appears only after a confirmation recorded elsewhere. Add permitted confirmation and then completion within the release flow. `openProvider(..., true)` returns on successful save and X-close, but the provider footer's “Keep draft and close” only hides the provider modal and drops out of release. Route every close/Cancel/Back path through consistent draft-preserving return behavior and prove the planned -> confirmed -> completed -> repair-complete journey.
3. **Stale repair policy after completion has no recovery path.** The updated attestation form is shown, but `attest_repair` permits only open/in_progress/on_hold. `assertCanRelease` requires the latest attestation's policy version to match the current repair rule. After completion followed by an approved rule change, release blocks and the user cannot record the required new attestation; no reopen transition exists. Provide a narrow attributable recovery under the active hold, with new evidence/retest/receipt as needed. Never overwrite the old attestation or weaken release freshness/independence. Add a regression for policy change after completion, not only before completion.
4. **Restricted information is presented as missing evidence.** Backup's custody-only view correctly hides repair actions/files, but derives “Repair evidence Needed” and “Record the work done” from those empty arrays even though completion and evidence are saved. Send only permitted readiness summaries or label unavailable details accurately; do not broaden attachment access. Source inspection also identifies a missing-policy message in the Retest step when `can.manage` is false despite configured rules; Designer did not separately exercise that reviewer step. Distinguish permission from missing configuration.
5. **Shared upload and status affordances need consistency.** ConfiguredQuestions uses a native file input rather than approved FileDropzone/StagedFileCard. Reuse the approved upload treatment while retaining per-question evidence binding. Hide/disable unsupported actions with clear explanations for terminal work: the current detail still offers owner handover and updated attestation that the server rejects for completed records. Keep the supported held-work recovery in item3 explicit.

These are the same I06 approved-layout/complete-modal/prerequisite-recovery finding. Count becomes **2 requested / 1 delivered**. This is Sol's last correction attempt on this underlying issue.

## Correction2: I08 — prove the newly wired automatic path

The after-commit trigger and one-minute scheduled retry now exist behind a false default. The remaining gap is verification: the suite runs `schedule:list` with the switch enabled, then in the release test performs release while disabled and enables the switch only before manually invoking `maintenance:dispatch-effects`. It never executes an enabled release through the new after-commit trigger. The passing suite therefore does not prove the worker's claimed enabled automatic execution.

Add a bounded disposable regression that commits an enabled release, observes automatic delivery without calling the dispatcher/CLI manually, and proves rollback emits no effect/notification. Exercise retry recovery without duplicate observable notification while retaining the disabled default. Do not activate operational scheduling or transports. Count becomes **2 requested / 1 delivered**, same I08; code wiring is a positive result, automatic execution evidence remains open.

## Handoff and limits

I01/I03/I09/I10/I11/I12 each have1 requested/1 delivered and the above bounded resolution. I06/I08 receive correction2 from the same Sol task. No issue IDs/counts reset, extra worker or competing application writer. Sol may resume only the scoped correction2 and directly required regressions; return a new exact frozen candidate and evidence. If either underlying issue remains unsuccessful after this second correction, Designer takes over that issue after explicitly stopping Sol edits to those files, per the agreed maximum-two-corrections rule.

Main exact-code approval, integrated verification, publication verification and user acceptance remain later gates. No commit/integration/publication/deployment/live migration/operating configuration or package PASS is authorised by this review. All three settled user decisions and A4 remain unchanged.
