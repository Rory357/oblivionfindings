# PKG-01 initial protected-slice review

Owner: DESIGNER ASTRA. Revision5. Updated 2026-09-20 02:58 UTC. **Initial implementation continues; no package PASS.**

Actual Designer continuation `01a0bc53-6e31-7543-93cc-0804e81fdb81`, timestamp2026-09-20T00:59:39.585Z, verified `gpt-6-astra / xhigh`. Sol remains the sole application writer in2375, branch `codex/pkg-01-maintenance`, basee62b569. This is an early review of a changing working tree before the full initial delivery, not a new worker or correction cycle.

Read the worker's protected-slice-checkpoint.md, new access/report/check/policy/transition/fingerprint services, changed controllers/routes/model/migration, Finance observer diff and focused tests. Sol reports one existing test/six assertions and three new tests/28 assertions passed with the unique forced test profile; Designer has not independently rerun that suite. The latest staff-routing edits were not included in that reported test pass. No UI or concurrent-transaction acceptance is claimed.

## Positive bounded observations

- Reports and transitions acquire the canonical asset before the work order; report replay binds actor and input fingerprint and preserves separately submitted reports.
- Approved site routing is explicitly checked, with current staff verification for Coordinator/backup and no fixture-derived owner.
- Missing approved rules or changed template hash cannot produce a positive check through the inspected ordinary path; corrections create a new run.
- Owner stays unchanged on proposal and moves only on acceptance in the service; the HTTP access gap below still prevents claiming that journey works.
- The source diff actually removes the completion/estimated-cost ProcessFinancialEventJob dispatch and retains H&S behavior. The existing test does not yet adequately protect that regression.

## Findings carried into the unfinished initial build

**PKG01-I01 — N/A bypasses required evidence.** MaintenancePolicyService::checkOutcome handles allowed N/A with an early continue before evidence_required. Independently reproduced with a read-only PHP class invocation, without app bootstrap or DB: one question `{id: tyres, pass_values: [pass], allow_na: true, evidence_required: true}` and answer `{tyres: {result: na}}` returned `passed`. Expected: unresolved required evidence prevents passed unless an explicit approved applicability rule waives it. Sol must cover N/A, conditional requirements, missing/borrowed evidence and a configured valid evidence path; current non-N/A evidence-required path is also still an incomplete positive workflow.

**PKG01-I02 — original question content is not fully snapshotted.** MaintenanceCheckService records the policy rules and a template hash, but rules can hold only item IDs/pass values, and an unconfigured run records no rule snapshot. A hash cannot reconstruct original question labels/options. New runs must preserve the actual presented template/question/options snapshot independently of policy approval and use it in historical display. Mutating the template afterwards must not alter either approved or needs-assessment history. Do not fabricate old snapshots for legacy runs.

**PKG01-I03 — service tests miss real report/acceptance access paths.** Report-only can POST, but the inspected asset option search remains manage-only. The handover test accepts with an actor lacking management/read permissions through the service, whereas the HTTP update route requires management and controller workOrder lookup requires read. Deliver narrow report entry/search and selected-recipient acceptance/read without granting broad management or unrelated evidence visibility. Recheck current approved staff membership and exact resource site at proposal and acceptance, and test HTTP denial/success including removed, unrelated and superseded recipients.

**PKG01-I04 — Finance regression proof is suppressed by null transport.** The row-count assertion with queue=null would pass even if the removed observer still dispatched a job. Assert ProcessFinancialEventJob is not dispatched with a controlled fake, including estimate-only/direct completion/retry, instead of treating null-queue suppression as proof. Source removal is observed; runtime regression protection is pending.

Each ID is owned by the same Sol assignment during its ongoing initial build, correction count0. No full initial implementation has been delivered or committed; these observations must remain traceable and do not reset later per-issue budgets. Designer sent all four findings and requested targeted evidence. The full remaining approved package stays required: booking restrictions/concurrency, provider/repair/retest/custody/release, private files/effects, Finance link, UI, Tasks/profile/calendar, browser and broader regression verification. A4 still gates live activation/accepted closure on actual approved configuration.

Designer performed no competing application writes and no operational DB changes. Frozen previews and guide references remain untouched. Next: review Sol's targeted fixes and full initial delivery, then appropriate independent QA before Main technical review.

## Targeted follow-up review

Designer actual continuation `01a0bc61-5dce-7023-ab0c-e419541807ba` at2026-09-20T01:14:52.812Z verifies Astra/xhigh. Sol reports5 tests/51 assertions passing after changes, PHP lint clean; Designer read the changed services, routes/controller and all five tests. No independent DB-suite rerun while the working tree continues changing.

- I01 original reproduction is independently fixed: the exact same read-only PHP probe now returns `needs_assessment`. Server normalization strips supplied evidence_verified and checks a private attachment linked to the same work order. The new test covers spoofing and a saved synthetic file. Configured conditional evidence is still incomplete: any `when` currently forces assessment. Its negative conservative test is not evidence of implemented positive conditional evaluation; full-scope acceptance remains pending.
- I02 now persists presented_template_json for all new runs, including unknown and stale runs, and tests original labels after mutation. It remains open for actual historical read/display: no inspected controller or frontend consumes that snapshot yet. Sol was asked for response/UI proof, including saved labels/options for unknown runs, before closing the item.
- I03 source now has narrow report entry/search and direct selected-recipient access plus current-staff validation. Worker HTTP tests cover report/search/submit, unrelated/foreign denial and selected acceptance. Removed and superseded recipient tests and browser rendering remain to prove; the named Inertia component returning200 is not browser evidence.
- I04 now uses Bus::fake and assertNotDispatched for the removed Finance job. Source/test weakness is addressed; independent scoped regression verification remains for the final stable delivery.

All IDs remain in the same unfinished initial implementation with correction count0; no formal full-delivery PASS or scope omission. Designer returned the targeted findings and requested a durable updated worker test checkpoint. Sol continues the full package.

## Release, projection and design review of continuing initial build

Designer current turn `01a0bc7b-0921-7bc2-bd51-27f53d853a20`, timestamp2026-09-20T01:42:55.117Z, verified Astra/xhigh. Read new release/provider/custody transitions, attachment and Finance services, restriction guard/effect dispatcher, controller projections and initial detail/report UI. This is a changing source review; no independently executed release/browser scenario or overall PASS. Sol reports a6-test expanded run had5 passes and a release timing failure, with a timestamp fix/rerun in progress. That failed run is not hidden by the prior5/51 targeted pass.

**PKG01-I05 — release lacks coverage of a new hold on the same work order.** The inspected release guard checks newer unresolved checks and active restrictions on other work orders. place_restriction allows completed work and nullable source_run_id. A new same-WO hold after an existing retest can therefore reach release, which updates all active restrictions for that WO using earlier evidence. Expected: release is bound to the exact current source/restriction set covered by fresh applicable assessment/retest, and newer holds invalidate readiness. Sol must prove this sequential case and an overlapping hold/release transaction; datetime precision alone cannot establish coverage. NULL check outcomes must remain unknown. Related delayed effect message must not assert universal booking readiness when a newer restriction or legacy asset state still blocks it. Source hypothesis requiring regression proof; open, owner Sol, initial correction count0.

**PKG01-I06 — interim frontend diverges from approved v8.** Current source reduces the five-step Resource/Problem/Evidence/Related work/Review report to3 steps with no report upload step; puts Next action/Progress in the left main column; uses a free-text waiting reason; leaves note/history rows expanded and only collapses Add note; uses native file input instead of the approved premium upload components; and changes hero primary/task-link behavior while adding duplicate source links below. No browser fidelity claim is made from source alone, but these are explicit approved-contract differences. Sol must port the exact approved hierarchy/flows and shared calendar/clock/manual entry consistently, including estimate after selected resource, then compare rendered1280×900/1440×1000 output. The five-step flow or missing functionality is not an authorised simplification. Open, owner Sol, initial correction count0.

**PKG01-I07 — linked Finance projection must revalidate current allocation.** Link creation checks bill site/asset, but projection reads historical links without verifying the bill's current site/asset allocation against the current resource and applicable access. A reassigned asset or reallocated bill can make a retained link stale and expose unrelated identity/amount/status. Expected: recheck canonical allocation and approved-site/object access on read, preserving a bounded reconciliation state without leaking stale bill facts. Add reallocation/resource-move regressions; leave Finance approval/journal ownership unchanged. Source finding, open, owner Sol, initial correction count0.

All three were sent to the existing worker with concrete scenarios and evidence requirements. No new worker, application write takeover, source-guide change or permission question. I01–I04 history and the complete remaining approved scope remain intact. Full initial delivery, independent stable-code tests/browser QA and later Main integration review are still pending.

## Subsequent release-coverage checkpoint

Designer actual turn `01a0bc88-a1eb-7360-916e-3c9e58541e66` at2026-09-20T01:57:46.175Z verified Astra/xhigh. Sol reports7 tests/113 assertions passing, including old retest rejected after a new same-WO hold and fresh retest/custody accepted, plus effect retry. Designer inspected changed source: retest stores covered restriction IDs; release compares the exact active set while holding the asset lock, checks origin chronology, explicitly treats NULL outcomes as unknown and records restriction source/version identities. I05's original sequential scenario appears addressed; independent regression and overlapping transaction proof remain pending. No Designer runtime release test occurred at this checkpoint.

I07 source now rechecks current bill site/asset and deletion against the work's canonical resource, returning only reconciliation_required when stale; its new regression had not yet rerun when Sol reported. I06 UI port remains in progress. Requested consolidated durable commands/results and a stable delivery/browser URL for independent QA; no scope deferral or new permission gate. Approved configurable advisory-versus-blocking behavior and real concurrent booking/hold/release proof remain required. All initial-build issue IDs and correction counts0 persist.

## Provisional connected UI checkpoint

At02:58 UTC Designer verified current Astra/xhigh metadata and completed a bounded read-only/client-draft browser pass on the independently identified2375 preview host8765. [Provisional browser record](provisional-application-browser-review.md) records five report steps, premium evidence input, connected calendar/clock/manual controls and minute validation, plus remaining I06 queue/detail/navigation/date/overflow gaps sent to Sol. No submissions or work-order mutations. The requested viewport override was unreliable/shared, so no required desktop-matrix PASS is claimed. Frozenv8 stays unchanged.

Since the preceding checkpoint Sol reported8 tests/117 assertions including I07, followed by a focused overlapping MySQL release regression1 test/26 assertions. The latter exposed a stale repeatable-read lookup, reportedly fixed using post-asset-lock locking reads. Designer has not independently rerun or fully reviewed that final concurrency change. Worker reports frontend typecheck/lint clean and an expanded11-case suite still running. These are worker reports on a changing initial build, not consolidated final acceptance. Stable delivery/commit, final bundle, complete scope map and independent tests/QA remain pending; all I01–I07 identities and correction counts0 persist.
