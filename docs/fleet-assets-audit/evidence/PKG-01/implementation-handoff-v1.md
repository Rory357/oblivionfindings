# PKG-01 — approved implementation brief

Owner: DESIGNER ASTRA. Revision10, 2026-09-20. **Current integration handoff: Designer alone owns the package after Stephan stopped further Sol work.** Main renewed technical approval for manifest7E5A61877E5B757B386D3AF486FE405BBE0DA5CC3D72851DAE3572CF372914B4 and verified the exact63-path source commit869edf738035e7e42890fdf902d75d914d393a55 plus three approved additive guides in c008fb0cf3ab45cc56b0b038a542f18ed4cf20ba. Stephan authorized test-server merge/push. Context-manifest14 and Main's test-server publication approval supersede the historical integration pauses below. Final integrated checks and publication verification are still being completed; acceptance and operating configuration remain separate. All earlier issue attempts are retained, with no new Sol assignment or worker.

## Revision9 and earlier handoffs — historical

Owner: DESIGNER ASTRA. Revision9, 2026-09-20. **Application implementation released to the single verified Sol High task in2375 after accepted setup/proposal and verified Main A4 context. Package not complete; actual operating rules and later integration/acceptance gates remain mandatory.**

## Current review handoff — supersedes writer-release history below

Sol is stopped after correction2. I06 exhausted2/2 and was directly repaired by Designer as sole writer in2375; I08 resolved2/2. The candidate is frozen for Main substantive technical review. Final manifest SHA256 CDD07766305CC64B64BCBA9CE02BECC8981259DC9C65CE150E17341798077D18, same e62 base and codex/pkg-01-maintenance branch, uncommitted. See [final independent review](correction2-and-designer-takeover-review.md) and package page revision32. No third correction, application-writer restart, integration/publication or live activation is authorised by this handoff.

## Historical application-writer release — superseded above

2026-09-20 00:28 UTC. Designer verified Main manifest12 SHA256 `D50DB1DE51C7A6DB5950A30B4033752181D25CC7E51BE3C7AA8B21C79C65B198`, all17 file hashes, and canonical master A4 SHA256 `77F404510B6E5C1A270904650A22D6AD7DD0FB8A6842183D33DAB0C687D31C56`. Read00rev11,02rev8,03rev6,06rev4,Main handoff10 and live05rev32. Main independently verified the answers and confirms no further launch/model/mockup question. Prior source revisions remain historical, not another gate.

Designer accepts the setup evidence and additive migration/transition proposal at `C:/Users/steph/.codex/worktrees/2375/oblivionfindings/docs/fleet-assets-audit/evidence/PKG-01/implementation/`. Actual branch `codex/pkg-01-maintenance` at approved e62 baseline; only setup evidence and local test XML were untracked at review. Local locked dependencies and App/Tests Reflection into2375 are reported verified; Designer independently read the forced test-only XML connection and unique schema prefix. No app bootstrap/tests/migrations occurred before release.

**Sol alone now owns application/schema/test writes in2375** for the approved bounded package. Designer owns8424 page/handoff/QA records and performs no competing application edits. Test-only bootstrap/migrations are authorised under the verified unique disposable boundary, rechecked immediately before use. Browser writes additionally require its separately verified synthetic schema/server/asset provenance. No operational configuration/database writes, deployment or integration/publication.

Proposal accepted with these implementation requirements (initial-implementation review, not a correction attempt):

- Use one deterministic lock order across commands and booking paths: canonical asset first, then work/source/policy records; multiple resources ordered by ID. Recheck active restrictions and policy/source versions inside the transaction. Prove concurrent hold/booking/release behavior, not only sequential calls.
- Idempotency must include authorised actor, operation and payload identity. Reusing a key with different payload/operation is a conflict; reauthorise before replay. Add small fingerprint/command fields or indexes where needed. Do not return another actor's result or permit unique-key races to create orphan work/effects.
- Original reports/checks and approved policy snapshots remain immutable. Link duplicates after submission through append-only facts rather than overwriting original report data. Never allow policy edits or later attachments to silently rewrite a prior observed outcome; new evidence/corrections retain source history.
- The additional fleet_maintenance_reviewer_grants proposal is accepted with explicit asset-category scope alongside site/user/review kind; no implicit all-category grant. Preserve approval/revocation history, and recheck current grants and reviewer independence in the locked release command. No grants or named people are seeded.
- Unconfigured rules cannot discard submitted observations or manufacture passed/ready. Preserve permitted observations as needs-assessment evidence with unknown provenance and block dependent operational actions. Missing site routes need explicit recoverable feedback; never silently select a Coordinator/person.
- Validate exactly one attachment parent and same canonical WO/asset/site ownership server-side; whitelist source types and reauthorise source reads. One financial source/journal per invoice; retain existing H&S behavior where independently required while removing maintenance completion/estimate posting.
- Use null/array transports for normal isolation, but explicitly exercise durable effect dispatch, failure and retry through controlled fakes/test delivery; an unexecuted null-queue job is not evidence of working delivery. The machine-specific PHPUnit profile must not expose credentials or be presented as portable project configuration without sanitising it.

No live rules, routes, reviewers or grants may be seeded from fixtures. The configuration mechanism and clearly synthetic positive/negative proofs are in scope; approved actual operating values still gate affected activation and accepted operational closure. Keep the agreed first protected slice or45 minutes active implementation checkpoint; continue in the same assignment with correction counts unchanged. This approves initial implementation only, not delivery QA or integration.
## Current authority, decisions and exact design

Stephan approved Sol High and the corrected v8 mockup, then explicitly asked Designer to collect remaining questions here, start the new task after the answers and tell Main afterwards. Do not repeat mockup, model or policy-timing questions.

All three answers were received in Designer task `01a0b91c-8ddd-7e83-af84-725da96ca56a` against request `call_yiJOTYzhr4fzLhRt2tMLSjcq` on2026-09-20: “Yes—start development now; require approved rules before live activation”; “The site's Coordinator, with a nominated backup”; “Yes—require an independent authorised release reviewer.” This approves the limited staged-start exception proposed in Main live register31: build configurable mechanisms and synthetic tests now, while approved actual operating rules and configuration remain prerequisites to live activation and accepted operational closure. No production database, deployment or assumed policy is authorised. Main is to record this explicit amendment after the new task starts; Designer releases application writes after that record and technical preflight. Starting the real task does not imply application code or tests already ran.

Adopted ownership: site Coordinator plus nominated backup receives/owns new reports until acknowledged allocation. Frontline access is narrow report-only, targets manual, and current owner remains accountable until the proposed recipient acknowledges. Do not choose actual users from fixtures or grant broad management to enable reporting. Use approved-site, canonical user IDs and bounded authorised search.

Adopted release: safety-related repair needs a specifically authorised final reviewer distinct from the repair attestor. Required retest, evidence and applicable custody checks remain mandatory. No blanket fleet.manage release grant or impersonated receipt. Actual release grants, approved category/rule versions, custody applicability and real site owner/backup mappings must be configured before affected live actions. Missing/unmapped rules stay Needs assessment and cannot assert Passed/Ready. Synthetic configured policy fixtures may prove the mechanism but never become production defaults. These remaining operating values no longer block building the package; they still block affected live activation and operational closure.

Exact approved v8: `C:/Users/steph/.codex/worktrees/8424/oblivionfindings/docs/fleet-assets-audit/previews/PKG-01/v8`, URL `http://127.0.0.1:4324/`, identity `b05ea36ccf287d7a898713939dbdbc03db0fb70c38d47f26db8e033544b314eb`. Evidence and manifest are in the sibling evidence/PKG-01/v8 directory. V8 includes the Back, vehicle and All Tasks links inside the actual PageHeader hero. It retains v7's shared date/time selectors throughout, collapsible notes, progress/guarded completion and consolidated release modal. Frozen v1–v8 files, screenshots, manifests and QA must not be edited.

Canonical Designer page and issue ledger: `C:/Users/steph/.codex/worktrees/8424/oblivionfindings/docs/fleet-assets-audit/pages/PKG-01.md` revision20 or later. Main task `01a0b8c5-186f-7681-83fc-40229d94ef87`. Main context is `C:/Users/steph/Herd/oblivionfindings/docs/fleet-assets-audit`: read00-project-rules,02-approved-workflows,04-design-rules,06-model-selection and the current PKG-01 Designer handoff/manifest. At receipt:00rev10,02rev7,06rev3,Designer handoff9,live05rev31; Main will update affected records with the explicit answers. Manifest11 SHA256 `6991CF13AF2C8FA4A49BF8B6AD0534EE815776F3F70C1CBA53F494219DEEAA22`, all17 entries verified. Current master is `C:/Users/steph/Downloads/oblivion-findings-fleet-assets-complete-astra-prompt-v10.md`, Revision10+A1/A2/A3, last verified hash `A61469ED0C48B0A1F179D873A43E73B1254FFB9754733C3556CE6BC88AFFD990`; reread any explicit staged-start amendment before code. Main owns global rule changes and Rory D1–D4 supplements. Read current worktree design guides and the supplements; do not overwrite newer guides with Main's older whole files.

Designer baseline is `e62b569ff42ab471300fb6713a68758b647b2c32`, verified origin/main, with no tracked application changes. This approved new task uses its own managed implementation worktree, whose exact HEAD and branch must be reconciled with that baseline before code. Designer owns the package lineage and supervises it; Sol is the sole application writer after explicit preflight release. Do not write Main's operational checkout or Designer's frozen preview. No other agents, sibling packages or competing writers. Model `gpt-5.6-sol / high` must be proved from effective turn metadata before application writes. Designer remains `gpt-6-astra / xhigh` (verified current turn `01a0bc21-a268-70c1-a3bd-a0ad634a3a6c`).

## Verified actual worker launch

Single Implementer task `01a0bc28-a386-7653-9b29-361f93478222` is active/pinned in `C:/Users/steph/.codex/worktrees/2375/oblivionfindings`. Designer independently verified actual first turn `01a0bc28-a54c-77e2-9673-8c67da096463` at2026-09-20T00:12:57.046Z: `gpt-5.6-sol / high`; clean HEAD equals approved `e62b569ff42ab471300fb6713a68758b647b2c32`. No baseline reconciliation is needed. Main was notified after creation with the exact answers. Initial read-only preflight remains in progress; application writer handoff follows its evidence and Main's amendment record. No code/test/database operation is claimed yet. Designer documentation and frozen artifacts remain in8424; application implementation will use2375.
## Settled source contracts

- Canonical FleetWorkOrder.reference_number, assignee and due target already feed All Tasks through FleetMaintenanceProvider with composite fleet_work_order-{id}. Preserve the existing seven-day horizon, undated exclusion and default completed filtering; do not claim every record is always visible in every Tasks filter or create a competing ticket identity.
- WorkOrderController and route boundaries need server-side site/resource checks including missing site IDs; single/bulk transitions must share guards, locking, versioning and effects. Existing raw bulk updates are not a permitted bypass.
- ChecklistController currently infers pass from nonempty required responses; InspectionController can auto-create templates and infer pass from no explicit fail. Replace this with exact approved version snapshots and explicit conservative outcomes, preserving uncertain legacy provenance. Never invent historic rule snapshots from current templates.
- Completion, operational release, provider confirmation and custody receipt are distinct facts even within one modal. A scheduled end or Finance approval does not clear a hold. Existing booking commands must recheck active restrictions under the canonical resource lock.
- P4 Finance is resolved in WF-09rev7: invoice-backed Maintenance actual costs link canonical FinBill approval/journal and permitted status. Finance retains finance.ap.manage, approval checks/rates/spend rules/corrections/site-asset allocations. Do not also dispatch FinancialEvent for the same invoice. Maintenance completion, release and estimates never approve or post. Remove FleetWorkOrderObserver's completion/estimated-cost posting bypass. No second invoice/payment UI, new finance grants, thresholds, tax rates or historical reposting. Non-invoice sources retain approved Finance-owned contracts, not a new expense route.
- Existing source anchors: routes/fleet-assets.php; WorkOrderController; ChecklistController; InspectionController; FleetWorkOrderObserver; Finance FinBillPolicy and AccountsPayableService; VehicleBookingController/AccessService; Tasks FleetMaintenanceProvider; Sites Calendar FleetServiceScheduleObligationProvider and AssetMaintenanceObligationProvider. Inspect actual code/tests in the implementation checkout, not just this brief.
## Concrete implementation scope after release of this brief

One package: maintenance queue → report/check → work detail/ownership/provider/evidence → repair → separately authorised release, with the minimum booking/task/profile/calendar/Finance effects. One operating organisation, approved sites/roles/record ownership/privacy. Desktop web only. Preserve domain sources, existing records, canonical references and useful services. Do not build a parallel ticket/task system, rewrite Finance, redesign all profiles/calendars, add provider self-service, or start sibling packages.

1. **Canonical commands and reads:** a common scoped read/transition service for queue counts, search/options, direct records, related checks and attachments. Re-read current actor/site/resource at mutation; reject invalid/missing scope. Single and bulk transitions use the same guards, audit, optimistic version and idempotent result. No raw status update bypass.
2. **Report/check/evidence:** preserve report author, observed/submitted times, asset/source IDs and request identity; duplicate linking keeps both reports. Submit against an approved exact check version and snapshot all rule/question/evidence provenance. Attributable corrections/new runs only. Reuse approved private evidence storage/serving patterns and FileDropzone/StagedFileCard; server validation and cleanup must match actual storage limits, not the preview's illustrative quotas. Same-tab local draft retention is the demonstrated minimum; do not silently add long-lived browser/server drafts or invent retention periods.
3. **Work detail:** implement approved PageHeader hierarchy and v8 action placement, shared WizardShell, canonical links, compact collapsible notes and searchable asset/recipient selectors backed by bounded authorised server search. Preserve status enum compatibility; represent waiting reason separately where appropriate. Completed/Closed and visible Complete/Resolve call the same guarded command. Notes cannot mutate original checks or approve/release work.
4. **Provider/release:** persist internal appointment, external response, service evidence/completion, new retest, actual custody receipt and final release as distinct attributable facts with versions. One modal can orchestrate each permitted action, skip already-recorded prerequisites and preserve drafts after failure; it cannot impersonate another actor or imply provider acceptance. Final release atomically rechecks all applicable current policy/source facts; failed/unknown/stale prerequisites block. Effects follow committed state through durable/retryable identities.
5. **Minimum dependent interfaces:** booking request/approval/change/checkout checks the same source-owned restriction under resource locking; existing bookings are flagged for owned follow-up and never silently moved/cancelled/approved. Canonical Tasks reference/owner/target preserved. Resource context shows current/history source links and source-owned estimated/internal appointment calendar projections; no duplicated event on retry, no estimate as hard booking block or automatic timed release. Full vehicle/profile redesign remains later scope.
6. **Finance bridge:** implement only the adopted P4 contract. Completion/release/estimate cannot post. Reuse canonical Finance services, expose approval/posting/failure states and stable source identities, preserve existing journals, and prove retry/correction cannot double-account. No hardcoded new accounting policy.

Likely bounded areas: `routes/fleet-assets.php`; relevant `app/Http/Controllers/FleetAssets/`, request classes and new focused `app/Services/Fleet/`/policies/models; additive `database/migrations/`; WorkOrder observer and narrowly necessary Finance bridge; Tasks and SiteCalendar source providers; maintenance/inspection frontend and minimum profile links; shared picker composition only where reuse requires it; focused tests. Main owns global programme records and protected guide edits. Frozen previews/evidence must remain unchanged. Unexpected cross-domain architecture changes return to Designer/Main before expansion.

## Migrations, compatibility and rollback boundary

Before schema writes, record exact proposed tables/columns/indexes and adopted policy references with Designer. Use additive fields/records for immutable reports/check snapshots, work version/waiting reason/next action, source-linked restrictions, handover/appointment/receipts, append-only decisions and durable effect/approval identity as needed; reuse existing canonical models wherever truthful. Schema names are an implementation decision, not fixed by this preliminary brief.

Never backfill a historical template snapshot from today's template as if it were original, reclassify legacy unknown as passed, clear a legacy hold automatically or reverse/repost historical Finance rows in a migration. Preserve WO IDs/status meanings and existing source links. Reconciliation must report unmatched/uncertain legacy records separately. Production/live schema execution or destructive repair is not authorised. Test migrations and rollback only against the verified disposable package database; post-data rollback should stop new writes and preserve evidence, not erase new audit records. Main reviews exact migration/reconciliation/rollback evidence before integration.

## Safe verification prerequisite and acceptance

Read-only preflight did not bootstrap the app, run migrations/tests or inspect operational data. Worktree vendor/node_modules are not established; Main has dependency installations. Node runtime used by the previews is verified. PHP is not on PATH, but a read-only installed-runtime lookup and `-v`/`-m` checks now verify `C:/Users/steph/.config/herd/bin/php84/php.exe`: PHP8.4.16 with pdo_mysql, bcmath, fileinfo, mbstring and openssl. Initial sandbox access to that directory was denied; approved read-only escalation resolved it. No PHP/app installation or database connection occurred. A vendor symlink into Main can resolve generated App/Tests class maps against Main: verify Reflection/autoload resolves the actual2375 implementation checkout before trusting any application test. Reuse installed dependencies safely or provision the isolated worktree without changing Main dependencies/lockfiles or upgrading packages.

`phpunit.xml` targets MySQL/testing, but `tests/TestCase.php:121` creates/drops a process-suffixed schema, loads the dump/applies migrations and at227 prunes orphan schemas sharing its base prefix. Before any such bootstrap, force a unique package-specific base such as `oblivion_findings_pkg01_2375_test`, verify the actual effective connection/schema and cleanup boundary, and suppress real mail/broadcast/queue transports. Do not run with operational .env or a cached production connection; do not use a common prefix that could prune another assignment's data. Establish a separate synthetic browser database/server rooted in2375 and verify host/assets; do not point browser writes at Main's operational app. These are technical setup checks, not new user policy questions; do not launch with an unverified destructive test boundary.

Existing focused regression anchors (inspected, **not run** in this preflight):

- `tests/Feature/Fleet/FleetWorkOrderSiteScopeTest.php`; `tests/Feature/FleetAssets/FleetPermissionBoundaryTest.php`, `FleetSiteAccessIsolationTest.php`, `FleetBoundedOptionsTest.php`.
- `tests/Feature/FleetAssets/FleetMaintenanceWiringTest.php`; `VehicleBookingSitePrivacyTest.php`, `FleetHandoverMutationIntegrityTest.php`, `FleetHandoverSiteIsolationTest.php` and key-ledger boundary tests for affected interfaces.
- `tests/Feature/Finance/ProcessFinancialEventJobDispatchTest.php` plus adopted approval bridge/Finance tests. Existing tests are baselines to preserve or change for an explicitly changed contract, not proof of correctness.
- Frontend maintenance filter/checklist tests, `fleet-modal-first-workflows.test.ts`, `fleet-modal-contract.test.ts`, `fleet-bounded-options-contract.test.ts`; focused new shared picker/transition tests and scoped lint/typecheck/build.

Required new proof: forbidden/missing-site/borrowed IDs on every read/mutation/search/count/file route; exact-version check after template changes; missing/unknown/N/A/conditional evidence derivation; immutable correction history; report retry/duplicate/conflict and file failure recovery; pending recipient acknowledgement; provider cancellation; guarded single/bulk completion; no release with failed retest, missing receipt, wrong reviewer, stale policy/source or concurrent new hold; one authorised successful release and attributable sources; booking rejection before/after concurrent hold; stable task/profile/calendar IDs; estimate-only/no-approval Finance produces no posting; approved retry once, changed-cost invalidation and correction. Exercise actual overlapping transactions against MySQL where locks matter, not just sequential mocks. Date/time tests must cover Auckland ordinary, nonexistent and ambiguous local times, exact minutes/noon/midnight, cross-month/same-day/overnight intervals, and date-only estimates.

Browser QA on real synthetic persistence at1440×1000 and1280×900: queue/populated/empty/filtered/loading/error/denied; complete report and duplicate-link journey; check/correction; searchable recipients; notes expand/draft/retry; calendars/clocks keyboard/Cancel/Apply/validation; completion and consolidated release including blocked sources; resource/history/calendar/All Tasks return paths; visible permissions, failure/unknown-save recovery and console/network errors. Designer independently reviews actual diff, persistence and browser evidence. Untested acceptance remains open.

## Launch, checkpoint and return contract

The exact-v8/model gates and explicit staged-development decision are approved. Start the one Sol High task for read-only effective-configuration, baseline and test-isolation preflight; after Designer confirms those checks and Main records the explicit timing amendment, Sol is the only application writer. Establish the package branch in that managed worktree and preserve the approved lineage. Live policy values still gate activation and accepted operational closure. Verify effective model/effort/host before code writes; requested UI settings alone are insufficient. No competing worker, model rescue reset, paid tier, benchmark or integration permission is inferred.

Practical first checkpoint: Sol confirms scope/policy source, effective configuration, worktree/autoload/disposable test boundary and minimal migration/transition plan, then returns an early evidence checkpoint after the first protected report/check/transition slice or45 minutes of active work, whichever occurs first. Designer checks access, immutable sources and absence of automatic Finance/release effects before continuing the rest of this same assignment. This is a work-review limit, not fabricated host-enforced token/cash control. Per-task tokens/cash and exact speed mode remain unknown; no account-wide percentage is presented as package cost.

Initial implementation is distinct from corrections. Canonical issue ledger stays in `pages/PKG-01.md`; all issue attempts currently zero. Preserve at most two worker corrections per underlying issue across continuations/configurations; exhausted issues transfer to Designer directly with explicit writer handoff. Independent issues may return to the same worker within their remaining allowance. No ledger reset or whole-package takeover inference.

Worker return must include exact files/diff/commit when authorised, actual commands/results, migration and legacy reconciliation/rollback evidence, acceptance-to-evidence mapping, desktop screenshots/URLs, unresolved failures/deviations and saved checkpoint under `evidence/PKG-01/implementation/`. No self-awarded PASS. Designer full QA and Main's substantive approval of exact code/base precede Designer integration/publication and integrated checks; Main publication verification and Stephan acceptance remain later gates. This brief currently grants none of those actions.
