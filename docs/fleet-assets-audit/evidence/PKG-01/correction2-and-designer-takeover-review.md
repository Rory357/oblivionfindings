# PKG-01 correction2 and Designer takeover review

Owner: DESIGNER ASTRA. Revision 1. 2026-09-20. **Designer repair verified; ready for Main substantive technical review of the exact staged candidate. No package acceptance or integration permission.**

Actual Designer turn `01a0bd5d-8e13-7452-85f8-fa162b614b51`, metadata timestamp `2026-09-20T05:50:20.3Z`, verified `gpt-6-astra / xhigh`. Main also independently verified this configuration. This review continues the same package, issue IDs and correction counts.

## Candidate and writer custody

Source is `C:/Users/steph/.codex/worktrees/2375/oblivionfindings`, branch `codex/pkg-01-maintenance`, HEAD/base `e62b569ff42ab471300fb6713a68758b647b2c32`. The candidate remains uncommitted. Sol correction2 manifest SHA256 `9A661C26F9D9B55E6DDC17972300043E6B28A89E015EF93625DBBE0A82FA2954` matched all63 source,2 harness,5 principal built files and the full build manifest before review.

I06 exhausted its second correction. Sol explicitly acknowledged that application/build/browser/evidence edits had stopped and Designer was sole writer in2375. Revision30 of the package page records that transfer; there is no third correction or new worker. Only `resources/js/pages/fleet-assets/maintenance/work-orders/show.tsx` was changed by Designer after transfer. The outgoing file is preserved as `correction2-show.frozen.tsx`, byte-for-byte verified SHA256 `851AEEF7140C08C765114669EBD9D49D1521933071445962BC5EA345F3B959A5`.

Designer initially repaired subview keyboard semantics and source labelling. The first rebuilt manifest `4EC1C55ABB30F3749B038906DC06E18745198FA49183915A43D918323E26900D` matched64 source/2 harness/5 build files and full build manifest after browser QA. Wider-screen focus testing then exposed an additional I06 hero defect: the decorative overflow-hidden header could internally scroll99px, hiding its title. That first candidate is superseded. The final source adds the existing project `overflow-clip!` header treatment locally; no shared component or design-guide change. Final show.tsx SHA256 is `562F91BE01D37D829443A9AC2795606CA12266803A2D47F62ABC11E724A9CD4C`.

Final `designer-takeover-candidate-manifest.json` SHA256 `CDD07766305CC64B64BCBA9CE02BECC8981259DC9C65CE150E17341798077D18` binds64 source files,2 local harness files,5 principal built assets and the full build manifest. All hashes were rechecked with no mismatch. Comparison to outgoing Sol manifest confirms only show.tsx changed. Final loaded app is `app-Dpo1_6O3.js`; detail is `show-CwD0uHFt.js`, queue `index--J1AHJjF.js`, wizard `create-wizard-B3A5ybm_.js`, CSS `app-BUGo9VPK.css`. Full hashes are in the manifest.

## Findings and correction history

- **I06: 2 requested / 2 delivered, Sol correction2 failed/exhausted; resolved by Designer direct repair and browser verification.** Sol corrected the approved hero/search/meters/connected six tabs, Overview/Activity structure, provider confirmation/completion within release, draft-preserving return paths, permission-aware readiness, shared conditional evidence uploads and stale completed-repair recovery. Independent browser/source/tests support these bounded corrections. Residual Overview/Activity arrow-key failure and later-check-as-original-source labelling were reproduced. Designer added roving tab focus, ArrowLeft/ArrowRight/Home/End behavior, associated tabpanel semantics, and a canonical report link distinct from later checks. Final wider-screen testing added the local hero clipping fix described above. No release or permission guard was weakened.
- **I08: 2 requested / 2 delivered, resolved.** Independent tests now execute the enabled after-commit release path outside the outer test transaction, prove rollback emits no effect, and prove recovery/retry without duplicate notification. Disabled-by-default operation remains intact.
- I01/I03/I09/I10/I11/I12 retain1 requested/1 delivered with bounded resolution recorded in correction1 review. I02/I04/I05/I07 retain their initial-implementation resolutions. No count or issue identity is reset by the Designer repair. This is staged engineering review, not actual policy approval or whole-package operational acceptance.

## Test evidence and limits

Before the independent PHP run, Designer rechecked absent `.env`, `.env.testing`, cached configuration, `public/hot`, and ambient APP_ENV/DB overrides. The inspected forced testing profile uses127.0.0.1:3306 and unique `oblivion_findings_pkg01_2375_test` prefix with PID-bounded schemas/cleanup and null/array transports. No operational database was used.

Independent current command: Herd PHP8.4 `vendor/bin/pest --configuration phpunit.pkg01.xml tests/Feature/FleetAssets/Pkg01MaintenanceProtectedSliceTest.php --filter 'custody receipt access|release requires an independent|a completed held repair' --compact`. **3 tests / 97 assertions, exit0,496.10s,3 warnings**, whose causes are not expanded by compact output. This covers custody access, independent authorised release and held completed-repair policy recovery, including the actual automatic/rollback/retry proof. Earlier attestation/history remain retained; new evidence, retest, custody and current rule checks still apply. No backend source changed during Designer's UI repair, so this suite was not needlessly repeated.

Sol reports the full correction2 suite **16 tests / 323 assertions, exit0,783.41s,16 warnings**, plus a focused1/20 regression and build/type/lint/PHP/diff checks. These are worker-reported results, not an independent full16-test run. Designer previously independently passed correction1's full15/297 and initial12/176; those distinct candidate results remain in their original reviews.

Designer final-file TypeScript (`tsc --noEmit`), scoped ESLint (`--max-warnings=0`), `git diff --check` and production Vite build all passed, exit0. Final build took3m and retained the existing greater-than500kB chunk warning; see `designer-build-final.log`. The earlier pre-hero build remains separately recorded in `designer-build.log`. `git diff --no-index` against the frozen Sol file shows only the bounded UI patch; its exit1 means differences exist, not a failed test. Git reports future CRLF-to-LF normalisation.

## Independent browser proof and synthetic mutations

The retained IAB review tab3 uses the verified2375 server on `http://127.0.0.1:8765`, schema `oblivion_findings_pkg01_2375_browser`. Existing synthetic Manager/Backup/Reviewer accounts and fixture rules only; no operating person, policy, grant or transport was created or activated.

Correction1's separately documented browser journey completed repair evidence/completion, new brake retest, distinct report-only Backup receipt, and independent Reviewer release on WO1, with the manager blocked from final release. Those records remain. Correction2 verification used WO2 as Manager:

- Planned **Designer synthetic workshop**,23 September09:00 to25 September10:00. From the completion modal, provider response opened; **Keep draft and close** returned to completion, and reopening preserved the entered source method/reference. Saved confirmation and then labelled synthetic provider completion both returned to the completion flow, removing the provider prerequisite. History/Activity retained attributed plan, confirmation and completion.
- Conditional warning=yes made the required photograph applicable. Shared upload/staged-file UI accepted `designer-synthetic-evidence.png`, saved attachment3 on WO2, and bound it to the new check. Saved check5 visibly showed Passed while original report2 remained separate. No WO2 repair attestation, work completion, custody receipt or final release was submitted by Designer.
- On the first repaired bundle, Overview ArrowRight selected/focused Activity; ArrowLeft returned Overview; End selected Activity; Home selected Overview. Selected tab had tabIndex0 and the other-1; Tab left the tablist. The panel was labelled by the selected tab. **Report #2** opened Checks & evidence with the original report and later check5/file separately visible.
- First repaired desktop screenshots were inspected at1280×900 and1440×1000, clientWidth=scrollWidth1265/1425. No horizontal overflow. The1440 hero internal-scroll defect was discovered here and is explicitly not claimed as passed until the final rebuild is checked. Browser warning/error log was empty.

Final browser verification loaded `app-Dpo1_6O3.js` and confirmed computed hero overflow=clip. Clicking Checks & evidence then Work detail at1440×1000 retained hero scrollTop0 and visible title at111px; screenshot shows the whole hero and its controls. At1280×900, hero ArrowRight/Enter and ArrowLeft/Enter navigation also retained scrollTop0, and Overview ArrowRight selected/focused Activity with the correctly labelled panel; Home returned Overview. ClientWidth equalled scrollWidth at1425 and1265 respectively. Final screenshots were visually inspected; no horizontal overflow, browser warning or error. Temporary viewport reset; tab3 retained on WO2 Overview as Manager. Screenshots were viewed in tool output, not saved as new local image artifacts.

## Required next gate

Send Main the exact repaired manifest, base, branch, source diff and this review for substantive technical approval. Sol remains stopped and the candidate is frozen for that review. Main must assess exact-code correctness, boundaries and integration risks; receipt of this document is not approval. Designer may not integrate/publish until that approval, and integrated checks, Main publication verification and Stephan acceptance remain separate later gates. A4 still requires approved actual operating rules, site Coordinator/backup and authorised independent reviewer configuration before live policy-dependent use. No commit, integration, publication, deployment, live migration or activation occurred in this review.
