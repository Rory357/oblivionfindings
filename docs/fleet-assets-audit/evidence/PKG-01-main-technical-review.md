# PKG-01 — Main technical review

Owner: MAIN ASTRA. Revision: 1. Reviewed: 2026-09-20. Decision: **Changes requested. Not approved for integration.**

## Exact candidate and authority

Reviewed the uncommitted `codex/pkg-01-maintenance` candidate in `C:/Users/steph/.codex/worktrees/2375/oblivionfindings`, HEAD/base `e62b569ff42ab471300fb6713a68758b647b2c32`. Designer's final `designer-takeover-candidate-manifest.json` in worktree8424 has SHA256 `CDD07766305CC64B64BCBA9CE02BECC8981259DC9C65CE150E17341798077D18`. Main independently matched all64 source entries,2 local harness entries,5 principal build assets and the full build-manifest hash. Final show.tsx is `562F91BE01D37D829443A9AC2795606CA12266803A2D47F62ABC11E724A9CD4C`. Git confirms the stated base and scoped uncommitted changes; no commit or integration is inferred.

Revision10+A1/A2/A3/A4, approved workflows, released implementation handoff and frozen v8 govern this review. Main rechecked the master and three protected reference hashes unchanged. All21 entries in v8's `files` collection matched; its recorded identity remains `b05ea36ccf287d7a898713939dbdbc03db0fb70c38d47f26db8e033544b314eb`. A4 permits staged mechanisms and synthetic verification, not incomplete engineering or invented operating rules. No live activation, migration or policy/grant approval is supplied here.

## Review scope and evidence

Main read the changed controllers/routes/models/observer, access/report/check/policy/transition/restriction/attachment/Finance/effect/configuration services, configuration and reconciliation commands, additive migrations, Task/calendar adapters, and the report/detail/configured-question/date-time frontend paths. Existing booking producers and template creation were inspected as integration dependencies. The protected suite's cases and relevant assertions were inspected; Main did not rerun a database suite. Designer's independent current3 tests/97 assertions, prior15/297, and Sol's reported16/323 are distinct evidence on the candidates identified in Designer's review. None establishes the missing cases below.

Main independently ran a pure PHP8.4 serialization probe against2375's actual FleetWorkOrder model with synthetic in-memory values. It loaded vendor/autoload only, with no application bootstrap or database. `toArray()` returned description, notes, completion_notes, estimated_cost, actual_cost and journal_id; synthetic actual_cost remained456.00. This verifies serialization mechanics, not an HTTP runtime reproduction.

Main compared frozen v8 at4324 with the actual8765 work-order2 detail and Report a problem modal at1440x1000. The implementation loaded `app-Dpo1_6O3.js` and `app-BUGo9VPK.css`, with1425px document width and no observed horizontal overflow. Screenshots were viewed in tool output, not saved as standalone image files. Main opened only temporary comparison tabs, did not submit reports/checks/notes or change workflow data. The two previews use different synthetic records and themes; those differences alone are not findings. The structural differences below were corroborated in source.

Designer verified that existing PHP8.4 PID21412 still serves2375 on8765 with the existing isolated browser schema; no restart/reset was needed. Main restored the unchanged static v8 server only. Designer's latest turn `01a0bd8b-8bbb-7660-b0e6-76906e7b3b54` independently verifies Astra/xhigh in both fields. Designer page revision33 reopens I06 on Stephan's actual feedback, with2 requested/2 delivered and Designer ownership. Source/build writes remained frozen during this review.

## Required corrections

The numbered findings below are Main review references, not replacement canonical issue IDs. Designer owns classification, ledger and per-issue counters. Existing issue history must not reset.

1. **[P1] Narrow custody access still exposes the whole work-order model — I03, with the Finance privacy boundary also affected.** `app/Http/Controllers/FleetAssets/WorkOrderController.php:496` passes the complete model despite separately concealing reports, attachments and Finance. `app/Models/FleetWorkOrder.php:35` retains cost fields and internal notes without a serialization whitelist. A report-only custody recipient can receive the original work description and internal/financial fields; ordinary Fleet readers without Finance authority also receive legacy costs. The detail UI itself reads the unredacted description at show.tsx:375. Project an explicit permitted shape for each view and gate financial data independently. Add actual Inertia-response assertions using nonempty sensitive fields for custody-only, Fleet-only and appropriately authorised readers; testing only empty `finance`/`attachments` arrays is insufficient. Preserve I03's1 requested/1 delivered history.

2. **[P1] Outing-created approved bookings bypass maintenance restrictions — minimum dependent booking interface.** The new guard is called in VehicleBookingController, but `app/Http/Controllers/FleetAssets/OutingController.php:331` still directly creates an **approved** booking with no canonical asset lock or maintenance guard. No booking model/observer guard was found. An outing can therefore reserve a held vehicle after the hold was created, also missing the hold-time affected-booking snapshot. This is the released handoff's explicit minimum request/approval interface, not a request to redesign Outings. Use the same atomic guard at this producer, with rollback of partial outing/booking state and an executable held/concurrent-hold regression. Assign a new issue only if no existing canonical issue already covers this underlying integration gap.

3. **[P1] Two work orders with active holds cannot reach release — I05 current restriction/release coverage.** `MaintenanceTransitionService.php:449` reads every active restriction on the asset; line470 rejects release whenever any belongs to another work order, while line338 releases only the current work order's restrictions. Both work orders can legitimately receive holds, and cancellation also rejects an active hold. Even after both repairs are complete, neither can be the first to release. No alternate supported restriction reconciliation writer was found. Provide a source-attributable way to resolve multiple holds while keeping the asset unavailable until every applicable requirement is met. Do not simply ignore the other hold. Prove two independent work orders can finish in a safe sequence, with stale/concurrent coverage still enforced. Preserve I05's initial-resolution history.

4. **[P1] Configuration authority is granted by the default administrator seeder — I12.** RbacSeeder.php:188 defines `fleet.maintenance.configure`; the administrator sync at600–603 grants every permission except the restricted list, which excludes release but omits configure. This contradicts the delivered no-default-configuration-authority contract. A qualified administrator gains the ability to record safety policies/routes/reviewer grants without an explicit configuration-authority assignment. Correct the default grant and exercise the actual RBAC seeder/role contract, not only manually constructed test users. This does not claim that release permission itself is seeded to administrators; it is correctly excluded.

5. **[P2] The supported template authoring path cannot produce a configurable approved checklist — I12.** ChecklistController.php:76 stores the form's label/type/options/required items without stable question IDs; the template form begins with checkbox items and null options. MaintenanceConfigurationService.php:200 requires a string ID and a nonempty options list for every item. Even a select template created through the current UI lacks IDs and cannot be assigned through the new configuration command. Provide a supported, validated authoring/import path compatible with exact versioned rules, preserving historical snapshots and making unsupported question types explicit. Test the real create-template → configure → submit path. Do not rely on synthetic direct database fixtures as the setup mechanism. Findings4/5 retain I12's1 requested/1 delivered history.

6. **[P1] Rollback can discard provenance before its evidence guard runs — immutable evidence/migration boundary.** `database/migrations/2026_09_20_000130_pkg01_report_corrections.php:20` unconditionally removes `corrects_report_id`; a batch rollback executes this before000100's guard notices retained reports and stops. Separately000100's guard at209–216 does not check new checklist provenance, yet line230 drops presented_template_json/outcome/source fields. A standalone Needs-assessment run can exist without any rows in the guarded maintenance tables. Preserve report-correction links and new check provenance before any destructive down step; verify empty rollback and populated refusal on disposable fixtures only. Main ran no rollback. Designer must map this to the existing immutable-evidence history where applicable, without disguising a repeated issue as new.

7. **[P2] The implementation still differs materially from approved v8 and loses a promised draft — I06, Designer-owned and exhausted under Sol.** Browser and source confirm: (a) v8's right-side Release requirements and Release vehicle/asset action are absent from the implemented Overview; show.tsx:510 mounts that card only for the separate release tab; (b) contextual Report a problem leaves work detail for the queue and replaces v8's retained resource card and expanded estimated-window/calendar section with an editable search list and collapsed date button; (c) the implemented hero repeats the long problem title instead of the approved compact work-reference hierarchy, and the Overview/Activity treatment and panel hierarchy differ; (d) report.tsx:12 navigates away on the wizard's “Keep draft and close”, while the draft/files/request key live only in the unmounted wizard; (e) create-wizard.tsx:75 turns asset-search failures into an empty result, shown at142 as “No matching asset”, with no truthful failed-search recovery. Correct the approved structure and shared interactions, preserving genuine role/state differences. Compare queue/detail/report/triage/provider/release/notes/selectors/uploads directly against v8 at both desktop sizes, including draft and failed-save recovery. Do not change v8 or the protected design references to match the implementation. No third Sol correction is permitted; retain2 requested/2 delivered and Designer ownership.

8. **[P2] Configuring a routine checklist makes an existing maintenance job mandatory — I01 check/evidence integration.** maintenance/checklists/run.tsx:99 refuses a configured check without `selectedWork`; lines250–258 hide the questions and tell the user to report a problem first. A routine check on a vehicle with no defect/job cannot be submitted through this path, even when all answers should pass. This reverses the approved checks-before-defect workflow and encourages artificial maintenance work. Support the canonical standalone check with its required evidence, then the explicit linked defect/work assessment path when relevant. Prove both a passing routine check without a work order and a failed check retaining its original answers/evidence when linked to maintenance. This does not prescribe automatic work creation for every failure or invent a safety matrix. Preserve I01's1 requested/1 delivered history.

## Return path and remaining gates

Return this single review to the same Astra/xhigh Designer. It must retain I06's direct ownership and all historical counts, assign remaining corrections without simultaneous application writers, and return a new exact candidate with focused regressions and full scoped design QA. Sol must not receive a third I06/I08 correction. Main will substantively review the replacement before any integration. Current source review findings are sufficient to withhold approval; the unexecuted HTTP/rollback/multi-work-order cases above remain required verification, not claimed observed test failures.

Stephan's checklist question is answered from the approved navigation: Fleet & Assets → Maintenance → Templates & checks, with contextual pre-use/return entry points in their appropriate sequential packages. Failed checks and evidence feed the canonical maintenance workflow; the current implementation does not automatically create a job for every failed check. No product requirement or operating policy has been changed by this clarification.

No Main application edit, stage, commit, push, deployment, migration, seed, actual grant or operational workflow action occurred. Existing unrelated public/.user.ini remains untouched. Programme manifest revision12 remains valid because this Main evidence record and05 are excluded from its delivery hash list.
