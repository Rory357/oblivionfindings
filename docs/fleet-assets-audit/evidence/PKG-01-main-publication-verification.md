# PKG-01 — Main publication verification

Owner: MAIN ASTRA. Revision: 6. Recorded: 2026-09-20. **Publication verified for the exact technically approved candidate. Main's final bounded check assessment is complete. Repository-wide CI remains failed; operating configuration and Stephan page acceptance remain open. No deployment-success or Accepted/Closed claim.**

## Final Main decision — current checkpoint

At **2026-09-20T10:19:18Z**, Main independently re-read remote main and all15 GitHub check runs. Remote main remains **2302ca33a95616e442a78e957ddce82d8db98669**. All checks are complete: database bootstrap succeeded; nine test jobs, four visual jobs and quality failed. The earlier63 application/3 guide/107 context mapping remains exact, with no subsequent source change or corrective publication. This decision verifies the published technical candidate; it does not recast those GitHub conclusions as green.

Main reviewed Designer's published-check-assessment revision3, final run-status record, test-coverage map and final lint/visual/feature/foundation comparisons in8424. Main additionally parsed both original lint logs independently and confirmed **322 identical ordered path/line/column/severity/normalized-message diagnostics**,250 errors/72 warnings, with no change. PHP/frontend formatting steps succeeded; that does not prove equal autoformat output or a successful lint job.

The final visual set has no newly failing case names or changed Error headings among retained failing cases. Ordinary Chromium is48failed/62passed/2skipped/4notrun versus baseline49/61/2/4; the removed rostering-suggestions failure is not claimed as a package-caused fix. Other projects retain12/14/14 failing names/headings. These comparisons establish emitted case/error identity only, not pixel/full-stack equivalence. Relevant Maintenance rendering is instead covered by the separately reviewed exact-build desktop evidence.

The completed feature/foundation comparisons and the three specifically investigated fixture failures are recorded below. Main accepts their evidenced pre-existing/out-of-package classifications without weakening any authorization, medication, employment or release guard. The newly reached timesheet case was never observed in baseline CI and remains explicitly distinguished. No unexplained new package failure remains in the evidence reviewed. This is a bounded package decision, not certification of the whole repository.

Both new Maintenance test files were **not executed on GitHub** because their jobs failed before batch4. Required package verification is supplied by the reviewed local exact-candidate suite and final committed-source regressions/build/browser evidence:21/454 protected suite, later1/74 source regression, final1/38 rollback and5/157 integrated regressions, with identities and disclosed warnings retained. Overlapping runs are not summed; skipped/unexecuted repository tests are not passed. Main judges the recorded scoped verification sufficient for this published technical candidate under the master's provision for evidenced unrelated pre-existing failures. No required Maintenance safety/privacy/integration check is silently waived.

**Decision: Publication verified.** The implementation may be presented to Stephan for page review, with these limitations explicit. A4's actual approved rule matrices, Coordinator/backup assignments, site/category release grants and custody applicability remain prerequisites for affected operating activation and accepted operational closure. Stephan has not yet accepted the final page. Do not close/unpin PKG-01, claim production readiness or publish a fabricated accepted-context checkpoint. After actual acceptance and applicable prerequisites, publish/verify the final context checkpoint with current remote inclusion. A5 separately permits the already-running PKG-02A mockup; its implementation remains gated.

The sections below preserve earlier observations and investigation chronology. Their pending statuses describe those earlier checkpoints and are superseded by this final decision, not rewritten as though later results were known then.

## Actual remote publication

Stephan explicitly authorized merge/push to the test-server main in [the release record](PKG-01-test-server-publication-approval.md). Designer reported a successful fast-forward push from e62b569ff42ab471300fb6713a68758b647b2c32 to **2302ca33a95616e442a78e957ddce82d8db98669**. Main independently ran an authoritative `git ls-remote origin refs/heads/main` at **2026-09-20T09:15:03Z** and observed that exact published commit.

Verified chain:

- e62b569ff42ab471300fb6713a68758b647b2c32 — reviewed integration baseline.
- 869edf738035e7e42890fdf902d75d914d393a55 —63 approved application/configuration/migration/route/test paths. Main matched every working SHA256 to the renewed7E5A618 candidate manifest and every normalized Git blob to the committed content, with zero mismatch.
- c008fb0cf3ab45cc56b0b038a542f18ed4cf20ba —3 authorized design-reference files,464 additive lines. Their three hashes match Main's approved projection and preserve newer upstream guidance.
- 2302ca33a95616e442a78e957ddce82d8db98669 —107 programme/evidence paths only. No application or guide change after c008fb. Published tree: **a2f136470ecd7a3909438418536f8180605e5ee2**.

Main independently verified the106 content entries in the published public-staging-manifest against actual Git blobs; the107th path is that manifest itself. All42 exact Main-owned snapshot files also match their approved raw hashes and published normalized blobs. The snapshot's own publication manifest is included separately. No mismatches were found. Targeted tree inspection found no local browser seed, PHPUnit profile, stored browser fixture, temporary preview sources, executable context-preparation script or unrelated public/.user.ini in the published package paths.

Published `source-commit-mapping.json` and `public-staging-manifest.json` retain the detailed identities. Main publication manifest revision2/F59BF5673552B79052E93E51A338B9DA53F4A77C0441297F37E18A02308C9703 remains the historical snapshot verified at this commit; subsequent Main status updates are later local facts and are not falsely claimed to be inside2302ca3. The final accepted-context checkpoint remains a later gate.

Designer used a separate integration clone's main to preserve Main's older dirty checkout/local ref. Main performed read-only Git/API verification and programme documentation updates, not the merge/push. No history rewrite or unrelated application change is shown in the inspected published diff.

## Integrated verification observed so far

Designer rebuilt the committed c008fb application and reported successful build, TypeScript and scoped18-file ESLint. Main independently matched the rebuilt app-DpcluT-n.js SHA256B494E227A19C63B0A1F4DFB98B1E7B4C1AD8A1F5F191219B2880BE2008D98108 to the approved build. The full9 principal asset/manifest match and fresh browser detail/calendar/clock smoke with no warning/error entries are recorded by Designer. This is approved local preview verification, not evidence of deployment to the external test server.

Main read the actual integrated focused-test log: **5 tests/157 assertions, exit0 reported,476.44s**, with5 absent-local-.env warnings. No warning-free or aggregate unique-case total is claimed. The earlier Main3/146, Designer full21/454, final source1/74 and rollback1/38 results remain candidate-specific evidence. The initial interrupted Artisan invocation is not counted; the successful direct Pest run uses the forced isolated package profile. No operational data migration/reset is claimed.

Preserved raw logs and the exact approved .patch retain their evidence bytes; Designer discloses107 trailing-whitespace lines across those artifacts. Authored source/context whitespace checks passed. This is not a source/lint failure being relabeled as green.

## Current GitHub checks and remaining gate

Main independently queried GitHub check-runs for2302ca3 at09:15:03Z. All15 returned check runs were in progress, with null conclusions:9 test jobs,4 visual jobs, database bootstrap and quality. Their workflow runs are tests35501735175, visual35501735176, database bootstrap35501735177 and quality35501735166. No green CI or completed-check claim is made.

Designer is collecting the final results and classifying them against the pre-existing e62 failures. Existing failed baseline checks do not justify an unexamined new failure or a blanket waiver. Pending/failed mandatory checks keep the package open. Main will append the actual assessment after evidence arrives; subsequent remote advances must be identified separately rather than silently inheriting this commit's evidence.

### Subsequent CI investigation — not a completed assessment

Designer subsequently reported database-bootstrap35501735177 successful; broad checks are no longer all pending. Foundation failures and multiple feature-shard failures match the e62 baseline, with remaining comparisons still being classified. The completed Chromium desktop visual job has the same12 failing app-shell screenshot cases as baseline; matching case labels do not establish pixel equivalence or successful visual checks. The original failed statuses remain failures.

Two different failures in feature shard0 receive specific investigation rather than a blanket baseline exemption:

- `AssetControllerTest::test_asset_store_infers_site_from_client` expected a redirect and received404. The unchanged Client factory can generate inactive clients, while unchanged assignment authorization requires an active client. Designer's private deterministic diagnostic reproduced active-client success and inactive-client404/no creation:1 test/6 assertions,217.22s, confirmed exit0, one missing-.env warning. Main read the diagnostic report and inspected the controller's package diff: only the detail-page maintenance restriction count changed, not store/placement. This supports classifying the failure as an existing nondeterministic fixture outside PKG-01, without loosening assignment authorization. The original CI client's status was not logged; an inactive draw remains an inference, not an observed CI database fact.
- `ComplianceDashboardSiteScopeTest::test_reports_view_any_is_the_explicit_application_reporting_bypass` expected2 open alerts and received1 at line216. Main independently traced `controlRoomSummary` through `applyControlledMedicationContentScope`: the test creates two random alert types, and the unchanged factory includes `Medication Error`. Unmarked medication alerts require `medications.controlled.view` even when site reporting is bypassed. The test seeds roles before creating its viewer; `makeViewer` sets a legacy role string and four explicit overrides, without attaching an RBAC role. Although the seeded coordinator role includes the controlled-medication capability, `User::canDo` consults role relationships/overrides, not that legacy string. A randomly selected unmarked medication alert can therefore be filtered from the count. This is a source-supported fixture vulnerability; the exact original CI alert types remain unknown. Designer was asked to verify the actual viewer capability and ordinary/medication count outcomes in its private isolated diagnostic. No runtime reproduction is claimed yet.

Main confirmed zero e62..2302 differences in the Compliance test, alert factory/model, metrics service and controlled-medication access service. The RBAC diff adds three maintenance capability registrations and two separately controlled administrator exclusions; it does not change the medication capability or coordinator mapping. No privacy boundary, application source or tracked test was weakened or rewritten for this investigation. Final failure classification and remaining integrated-check evidence are still required.

The separately user-authorized finance task notified Main that it may inspect/integrate missing finance work in an isolated checkout. Main asked it to coordinate remote advances with Designer and preserve the shared dirty checkout. It subsequently reported a fresh fetch confirming finance tip e62b569ff already an ancestor of2302ca3, with zero missing finance-branch commits; no additional finance integration is needed on that evidence. Main requested that the older shared local checkout remain untouched. Current maintenance evidence remains pinned to2302ca3; any later head must be recorded separately with ancestry and relevant checks, not silently substituted.

### Completed bounded diagnostics and later sequencing decision

The earlier pending Compliance diagnostic is now complete: **1 test/22 assertions, exit0,216.73s**, one absent-.env warning. Main read the actual log tail and Designer report. The fixture explicitly confirms no controlled-medication viewing permission; two ordinary alerts return2, changing one to unmarked Medication Error returns1 and omits its ID, both HTTP200. Combined with the unchanged source this supports Main's classification as an existing nondeterministic test fixture outside PKG-01. The original CI random draw remains unknown. The Asset diagnostic likewise has confirmed exit0; neither investigation changes published application or tracked tests. Failed GitHub statuses remain failures.

Main independently inspected normalized feature2/3/6/7 differences: the assertion/cause differences were limited to generated fixture text, test PID/date/time or residual ANSI formatting. Main also read foundation and feature4/5 comparison records. These comparisons cover executed batches only. Designer's latest status still has feature1, quality and three visual jobs pending; the unusually long feature1 run is not classified as passed or baseline-equivalent. Remaining checks and their impact still need assessment.

### Newly reached batch — Main classification

Feature1 subsequently finished its first batch successfully104/865, unlike the baseline that stopped at two Control Room failures, and therefore reached a second batch previously unexecuted by that baseline job. The second batch failed1/passed85 with495 assertions in1255.25s. The newly surfaced failure is `DraftTimesheetServiceReconciliationCallTest` normal attendance clock-out reconciliation: `The requested medication record was not found`, at MedicationGovernanceScopeService1671/1236. Main read the actual bounded log block. This is not an identical observed baseline-CI failure, and later package-test batches were not reached by this job.

Main independently inspected the test and call path: the fixture creates worker/role, site/client, shift/task and attendance session but no HrEmployeeProfile. Line1236 enforces that every locked user has a current staff profile. AttendanceService clock-out enters the existing handover/current-staff lock boundary before the timesheet expectation. The test, AttendanceService, MedicationGovernanceScopeService, HrEmployeeProfile and UserFactory all have zero e62..2302 diff. No application guard or tracked test has been changed to conceal the failure.

Designer then ran that exact unmodified test on published2302 under the forced isolated package profile: **exit2,1 test/1 assertion/1 error,216.74s**. Main read the actual error/stack in published-timesheet-existing-test.log and additionally verified unchanged ShiftHandoverService. The same current-staff-profile predicate fails through ShiftHandoverService1692/246/126 and AttendanceService1968/284 before reconciliation. Main accepts this as a newly reached, source-evidenced pre-existing missing-fixture incompatibility outside PKG-01. This is neither a passing test nor an executed e62 reproduction; original baseline CI did not reach it. No further broad rerun is justified absent contradictory evidence, and no employment/privacy guard is weakened. The unexecuted later CI batches remain explicitly unexecuted; scoped package evidence must be mapped separately.

Main also read the completed visual comparison: Chromium visual has the same12 failed names/error headings; IT-security desktop1440 has the same14 failed names/error headings and12 passes (23.7m versus baseline29.5m). This is case/error-heading equivalence only, not pixel or full-trace equivalence. Other visual and quality checks remain under Designer assessment.

Publication to Git main is accomplished. Full publication/check verification and Stephan's final page acceptance are still pending. Designer remains Astra Extra High and sole remaining PKG-01 writer; its Sol stays stopped with all issue counts/history preserved. Stephan subsequently approved A5 to start one separate PKG-02A mockup during this wait and assigned its future frontend to Astra Extra High/backend to Sol. That design-only exception does not close this package, waive its checks or allow concurrent application implementation. No PKG-01 closure/unpinning yet. Actual operating configuration/live activation remains governed by A4; no production approval is inferred from test-server publication.
