# PKG-02A v1 — Main design review

Owner MAIN ASTRA, revision1,2026-09-20. **Ready for Stephan's exact-version/bounded-scope mockup decision; not implementation approval or completed backend verification.**

Reviewed Designer task01a0be31-19ef-7d10-86a9-cbe968989a76, branchcodex/pkg-02a-client-location-design, worktree C:/Users/steph/.codex/worktrees/2b9f/oblivionfindings, baseline2302ca33a95616e442a78e957ddce82d8db98669. Exact candidate is the frozen synthetic preview http://127.0.0.1:4332/PKG-02A/v1/. Manifest SHA256 **657A34B88376594534309E87F083C8F23C05B57502E20053D504B70BF9398154**.

Main independently hashed all29 manifest entries and all33 baseline-input source/reference entries: zero mismatches. Tracked application/guide diff is empty; only isolated preview/evidence/page records are untracked. Existing PKG-01 preview and shared source are untouched. Main previously verified actual Designer Astra/xhigh metadata before releasing design writes.

Main read the Designer-owned page record, source-contract, QA record and manifest. The scoped design reuses Client Snapshot→Location with contextual Consents/assignment references; it does not release the whole profile, Fleet device register, resident overview, vehicle calendar or another package. Existing collection/privacy/no-store/withdrawal/export/realtime boundaries are preservation requirements, not newly claimed implemented features. Independent named-recipient disclosure remains a later backend contract with explicit policy decisions.

Main independently opened a separate hidden browser tab on the frozen static build, visually inspected the page and sharing wizard, exercised the searchable recipient picker and switched to the staff-denied state. Collection, staff-view and recipient-sharing decisions are distinct; observation time is separate from refresh/device registry status and no wellbeing assurance is inferred. The denied state removes map/history and sensitive decision/recipient details. Closing the wizard returned focus to Review sharing. Main's captured warning/error log was empty. The review tab was closed; the Designer's deliverable and existing user tabs were preserved.

Search nuance: a short `Mara` query left all three options visible in Main's review, whereas exact `Jo Ellis` narrowed to that one account and `zzznomatchzz` showed the explicit no-match state. Thus filtering is functional; short-query relevance is a non-blocking usability observation, not proof of an input-event failure or permission defect. Preserve this observation for realistic directory/search acceptance before implementation. Do not alter frozen v1 silently to remove evidence of the observation.

Designer reports40 state/viewport combinations at1280x800 and1440x900 and detailed history/date/time/retry/withdrawal/focus checks; Main sampled the flows above rather than claiming to rerun all40. Actual200% browser zoom remains unverified;640x400 reduced desktop reflow is only an approximation. Preserve that limitation for later required frontend verification. No production API, real data disclosure, grant, migration, permission proof, concurrency, DST or live device action was performed by this review.

No scoped design blocker was found in Main's sample. Stephan still decides whether to approve this exact v1 and bounded scope. Approval would not settle outstanding privacy-owner purposes/audiences/authority, durations/review/retention, client self-access or freshness rules, and would not itself release implementation. PKG-01 required closure and applicable technical/policy prerequisites remain. Under A5 the same Designer owns future frontend at Astra Extra High and may later supervise Sol backend only with explicit contracts/file ownership and one-writer handoffs. No Sol task or additional mockup is authorized by this review.
