# PKG-02B v12 — complete the action where it starts

Open http://127.0.0.1:4347/PKG-02B/v12/#/fleet-assets/vehicles/14/compliance

Expiry is entered beside the document renewal reminder, with editable date/time, searchable owner and backup. Conditional required fields and errors remain on that step. The editable 30-day suggestion is an example, not organisational policy.

Contextual uploads retain their category, reference and known cover date. Edit details & expiry changes metadata without uploading again. Files in an upload form a document set with one reminder. Replacing that set retains the originals and transfers its existing reminder. Unchecking renewal pauses the task; archive offers an explicit pause option. Documents are searchable reminder sources, with calendar links in their library entries.

Compliance rows expose Update dates & evidence; the attachment action opens the same source workflow. Required dates/references depend on applicability and outcome. Not applicable needs its basis and creates no due reminder. Optional dates can be cleared. Work completion accepts completion/retest reports directly; attachment IDs can supply the evidence reference. Work completion does not independently release a restriction.

The v11 List/Cards layouts, specialised maps/calendar/charts and shared wizard controls remain. Related document fields sit side by side on desktop and stack on narrow screens. A clearly labelled synthetic PNG sample lets reviewers exercise the workflow without supplying private files.

Synthetic isolated mockup only. Main has not been notified. Earlier candidates remain frozen. No production implementation, real notifications, payments or tracker/provider changes.

## Restart

`C:/Users/steph/.hermes/node/node.exe --use-system-ca docs/fleet-assets-audit/previews/PKG-02B/v12/serve.mjs`

Localhost port 4347. GET/HEAD only; no application APIs. Operational records/files reset on reload. Catalogue choices and layouts persist locally. Existing map-image proxy unchanged.

## Verification

TypeScript, Vite and focused document-workflow regressions pass. Browser verification covers uploads, date validation, owners, calendar linking, metadata edit, replacement history, reminder reuse/pause, source discovery, compliance updates, work-completion attachment handoff, interrupted-save retry, discard protection, view-only permissions and a 390px form. Console errors: zero. Existing Leaflet/bundle warnings remain.

Files were created through the visible synthetic-sample button. Native OS file-picker transfer, external delivery and unchanged export engines were not rerun. See the v12 evidence AUDIT.md and screenshots.
