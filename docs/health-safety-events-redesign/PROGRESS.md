# Health & Safety Events register redesign — PROGRESS tracker

> Self-paced `/loop` from the **"Events Inestigations Page" design drop** (`.design-drops/health-safety-events-redesign/`).
> **NZ-only** (WorkSafe NZ / HSWA 2015 s23/s24/death; notify ASAP / preserve site / keep ≥5 yrs; Ngā Paerewa NZS 8134:2021; ACC; 5-whys/fishbone/bow-tie/ICAM/TapRooT — no RIDDOR/HSE/CQC). **Web-only.**
> Branch `feat/health-safety-events-redesign` (off `main`, in the **MAIN repo** — not a worktree).
> Single source of truth for the A–H checklist: `docs/HEALTH_SAFETY_EVENTS_GAP_ANALYSIS.md`. Spec: `.design-drops/.../HANDOFF.md`.
> Scope: **expose + gate + standardise** the existing strong, gated governance engine. **No rebuild.** No new schema except the one small additive WorkSafe-notification migration in Step 3 (flagged).

## Audit baseline (verified against live code, 18 Jun 2026)
- **Engine is strong + gated (reuse, don't rebuild):** `HsInvestigationService` (draft→in_progress→findings_recorded→under_review→completed, all guarded; `complete()` already auto-advances the event to `corrective_action`), `HsCorrectiveActionService` (open→in_progress→completed→verified→closed; **verify ≠ completer enforced**; closing the last action **auto-advances the event to monitoring**; `createFromRecommendation`/`createStandalone`), `HsRiskAssessment` (5×5 bands), `NotifiableEventClassifier` (HSWA s23/s24/death → `{notifiable,category,authority,reason}`).
- **HsEvent columns present:** `closed_at`/`closed_by`/`closure_summary` (→ close needs **no schema**), `worksafe_notifiable`/`worksafe_status`/`worksafe_reference`. **Absent:** `worksafe_notified_at` / `worksafe_method` / `worksafe_acknowledged_at` (→ Step 3 needs a tiny additive migration — flagged).
- **Missing (the work):** `HsEventService::closeEvent/recordWorksafeNotification/acknowledgeWorksafe`; `HsInvestigationController` + `HsCorrectiveActionController` + all write routes; `source()` `{label,url}` resolver; seed-from-recommendations affordance; 3 orphan categories (`exposure`/`inspection_failure`/`equipment_fault`).
- **UI is off-standard + read-only:** `events/index.tsx` (`PageHero`, shadcn `Select`, no `TabStrip`/right-click/source column, navigate-away); `events/show.tsx` (full page, shadcn `Tabs`, read-only).

## Templates being mirrored (gold standard, just shipped)
- List: `resources/js/pages/safeguarding/index.tsx` (hs-hero-kit footer band, `TabStrip`, detail-over-list via `router.get(…,{only:['detail']})`, `openRowCtx` mirroring the Options bar 1:1).
- Detail-as-modal: `resources/js/components/incidents/incident-detail-dialog.tsx` (`WizardShell` read-only chrome, `section`+`action` panes, **flash.error guardrail**).
- Chrome: `@/pages/health-safety/components/hs-hero-kit`, `@/components/rostering` (`TabStrip`/`EntityFilter`/`ShiftContextMenu`), `@/components/wizard/{shell,primitives}`, `@/components/health-safety/{risk-matrix,event-timeline}`.

## Step plan (each: backend → route → modal → permission `hazards.manage` → acceptance; tick the gap analysis on completion)

- [x] **Step 1 — Foundation: list + detail-modal shell (read-only, gold standard).** [no schema] ✅ verified (tsc 0 · eslint 0 · php-lint 0 · 5 feature tests / 63 assertions green)
  - 1a Backend: `HsEventController::buildEventDetail()`; `index()` upgraded (hero counts, `tabCounts`, filters incl. category/site/severity/worksafe + search, rows payload w/ source + governance flags, `detail` when `?event=`); `show()` → thin shell rendering the dialog. (Source EntityFilter deferred to Step 5 with its resolver.)
  - 1b Frontend: new `@/components/health-safety/event-detail-dialog.tsx` (`HsEventDialog` — 6 read-only sections + stage tracker + WorkSafe banner + SoD note + Options bar) · rewrote `events/index.tsx` (hs-hero-kit + `TabStrip` + footer filters + standardised rows + `ShiftContextMenu` + open-over-list) · thin `events/show.tsx`.
  - Test: `tests/Feature/HealthSafety/HsEventRegisterTest.php` (payload, worksafe-tab scope, detail-over-list, deep-link shell, gating).
  - Acceptance ✅: register reads as the H&S gold standard; row/right-click opens the modal over the list; `/health-safety/events/{id}` deep-link renders the same modal on a thin shell; `HsEventBackboneTest` (observer/service) un-regressed.
- [x] **Step 2 — Gated close (Item 1 / E-Gap 1, G2).** [no schema] ✅ verified (HsEventClosureTest 6 + register regression green; tsc/eslint/php-lint 0) — `HsEventService::closeEvent()` + `closeBlockers()` gate; `HsEventController@close` + `POST …/events/{event}/close` (gated `hazards.manage`); `close_gate` in payload; Close-event ActionPane (gate checklist + required summary + logged override) + Options-bar button (critical + blocked-reasons title) + row-menu item. Acceptance ✅: incomplete required investigation / unverified action ⇒ blocked (flash.error) unless logged override; clean event closes with a summary; close route gated.
- [x] **Step 3 — WorkSafe notify/acknowledge (Item 2 / E-Gap 2, G1).** [**SCHEMA** — additive, migration `2026_06_18_020000`: `worksafe_notified_at`, `worksafe_method`, `worksafe_acknowledged_at`, `worksafe_site_preserved`; run locally] ✅ verified (HsEventWorksafeTest 6/21; tsc/eslint/php-lint 0; migration ran clean). `recordWorksafeNotification()`/`acknowledgeWorksafe()` (guards: notifiable-only, notify-before-ack, ack-locks); routes `…/worksafe/notify` + `/acknowledge` (gated `hazards.manage`); WorkSafe notify pane (date/method/ref + site-preservation toggle + notify-ASAP/0800/keep-5-yrs) + acknowledge pane; Options-bar + row-menu entries; banner shows method + site-preservation status. **Note:** the `worksafe-register` report is a JSON export feed (not an Inertia page) — the *actionable* surface is the events register's WorkSafe-notifiable tab + the dialog notify/ack action, so no separate report-page change was needed. Acceptance ✅: pending→notified (date/method/ref persisted)→acknowledged; guards + gating enforced.
- [x] **Step 4 — Investigation + corrective-action controllers (Item 3 / E-Gap 3, E+F).** [no schema] ✅ (backend 4a tested over HTTP; UI 4b-i/ii on the proven pane pattern; tsc/eslint 0)
  - 4a Backend ✅ verified (HsEventWorkflowTest 4/27; php-lint 0): `HsInvestigationController` (store/findings/submit/return/complete) + `HsCorrectiveActionController` (store/start/complete/verify/close/return) exposing the existing gated services; 11 routes gated `hazards.manage`; controllers are thin (guards live in the services) and a forbidden transition / SoD violation surfaces as flash.error. Verified over HTTP: full investigation lifecycle auto-advances the event to corrective_action; CA lifecycle enforces verifier ≠ completer and auto-advances to monitoring on the last close.
  - 4b-i Frontend — investigation UI ✅ verified (tsc/eslint 0; register regression 5/63 green): pane infrastructure (`ActivePane`/`PaneRenderer`, replacing the flat action key) + investigation action panes (start → record findings → submit → complete / return) inline in the Investigation section, driven by status; gated row-menu "Start investigation"; `assignable_staff` added to the payload (only when `can.manage`).
  - 4b-ii Frontend — corrective-action UI ✅ (tsc/eslint 0): add-standalone pane + per-action buttons (start · complete · verify-with-SoD-note · close · return) in the Corrective-actions section, driven by status, + gated row-menu "Add corrective action". Field names + URLs match the 4a controllers. ("Seed action from recommendation" is Step 6.)
  - Acceptance: each modal action hits its transition; a forbidden transition surfaces the service's gate error in-UI, not a raw write.
- [x] **Step 5 — Source back-link (Item 4 / E-Gap 4, A2).** [no schema] ✅ verified (HsEventRegisterTest 6/78 incl. resolver assertion; tsc/eslint/php-lint 0). `HsEventController::resolveSource()` maps each source type → `{label, url}`, shared by the list rows + `buildEventDetail()`; the Overview "Originating record" card (already url-aware from Step 1) becomes a working jump and the row menu gains "View originating record". Routes: incident `/incidents/{id}`, safeguarding `/safeguarding/{id}`, fleet `/fleet-assets/incidents?incident={id}`, injury `…/injuries/{id}`, drill `…/drills/{id}`; **hazard + restraint resolve to a label only (no per-record route → no broken link)**; orphan categories (no source) render the dashed "no originating record" card. Acceptance ✅: wired events jump to origin.
- [x] **Step 6 — Seed-from-recommendations (Item 6 / E-Gap 6 remainder, B5).** [no schema] ✅ verified (HsEventWorkflowTest 5/32 incl. seed + duplicate-block; tsc/eslint/php-lint 0). `HsCorrectiveActionController@seedFromRecommendation` + `POST …/investigations/{investigation}/seed-action` (gated `hazards.manage`) exposing `createFromRecommendation`; CA payload carries `hs_investigation_id` + `recommendation_index`; the Investigation pane shows a per-recommendation **"Seed action"** button on a completed investigation (→ "Action raised ✓" once seeded; duplicates blocked server-side). Acceptance ✅: each recommendation is one click from a corrective action.
- [x] **Step 7 — Orphan categories (Item 5 / E-Gap 5, A3).** ✅ verified (HsEventBackboneTest + HsEventRegisterTest: 35 tests / 167 assertions). User chose wiring and asked whether site inspection exists. Audit found a real site inspection module (`/sites/{site}/inspections` + global `/sites/inspections`) with failed `SiteInspectionRecord` rows, so no new tab/module was needed. Wired all three categories to existing source records: `SubstanceExposureRecord` → `exposure` (medical-attention exposures become high/investigation-required; non-medical exposures stay medium), failed `SiteInspectionRecord` → `inspection_failure`, high/critical `FleetWorkOrder` → `equipment_fault`. Source links now resolve back to the parent substance record, site inspections tab, or fleet work-order detail.
- [ ] **Step 8 — Polish, a11y, tests, regression, merge+deploy+verify (H).** Feature tests per item; tsc/eslint/build clean; axe AA; confirm no regression to dashboard/analytics heroes or the source modules' "Open in Health & Safety" jump; merge `--no-ff`; Chrome-verify on .com.
  - **F3 sibling register delivered locally:** `/health-safety/corrective-actions` now matches the shared H&S register chrome, has status tabs + hero filters, opens the parent event detail modal on the Corrective-actions pane, exposes right-click actions, and shows the parent event stage so closed/verified actions visibly connect to events auto-advanced to Monitoring. Verified by `HsCorrectiveActionsRegisterTest` (4 tests / 60 assertions), `php -l app\Http\Controllers\HealthSafety\HsEventController.php`, targeted ESLint, and `npm run types`.
  - **2026-06-19 local verification after Step 7:** `npm run types` ✅; targeted `npx eslint resources/js/pages/health-safety/events/index.tsx` ✅; `npm run build` ✅; `npx vite build --ssr` ✅; `HsEventBackboneTest` + `HsEventRegisterTest` ✅ 35/167; `HsEventClosureTest` + `HsEventWorkflowTest` + `HsEventWorksafeTest` ✅ 17/72. Full `npm run lint` remains blocked by existing generated `.design-drops/**/support.js` and `playwright-report/**` lint errors, not by the changed Step 7 files.

## Decisions log
- **18 Jun 2026** — Audit complete; engine confirmed strong + gated. Build in the MAIN repo on `feat/health-safety-events-redesign`. Foundation (list + read-only modal shell) is Step 1 (backend payload + frontend land together — they're coupled); write-action items 2–6 follow the "backend method → route → modal" discipline, each growing the dialog's Options bar (no stubs — actions appear only when their backend lands). Orphan-category decision deferred to Step 7 (stop-and-ask).

## Commits
- Step 1 (foundation: register + read-only detail modal) — `7ed5e7b7`
- Step 2 (gated event closure) — `fd59ac4c`
- Step 3 (WorkSafe notify/acknowledge + additive migration) — `294b9bc2`
- Step 4a (expose investigation + corrective-action services) — `359dcf09`
- Step 4b-i (investigation workflow UI) — `dfc4200c`
- Step 4b-ii (corrective-action workflow UI) — `4c0e3cb9`
- Step 5 (two-way source back-link) — `c2e8ca88`
- Step 6 (seed corrective actions from recommendations) — `e9c5fcbd`
- Step 7 (wire orphan event categories) — `f0ec89bd`
