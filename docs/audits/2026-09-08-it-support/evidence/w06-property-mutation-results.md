# W06 canonical property edit recovery

Implemented; focused automated verification passed. Desktop browser verification against the new asset build remains pending. This is a bounded W06 slice, not a W06 or release-gate completion claim. Existing operational draft recovery remains disabled while retention is undecided.

## Contract and changed files

- `resources/js/components/it/ticket-property-mutation.tsx`: canonical PATCH carries the original actor and displayed ticket version. Success requires HTTP200, typed `committed`, the exact ticket and viewer, and a valid committed version. HTML, malformed or missing acknowledgements remain unconfirmed. Forbidden/not-found or mismatched viewers conceal and clear the proposal. Session expiry conceals private values and provides recovery controls. Current-ticket review revalidates actor and per-record `can.manage`; accepting that version does not write. Reapply is a distinct action.
- The optional effective `draftsEnabled` mode uses the existing `ticket_edit` generation and revision contract. Initial checking is metadata-only, resume is explicit, saved base versions survive resume, text autosaves only while editable, and canonical mutation requires the exact saved snapshot. Only the matching consumed generation/revision acknowledgement clears that draft. Canonical unknown outcomes freeze draft writes until current ticket review. No new browser content storage or parallel mutation service was added.
- Closing a pending edit panel requires acknowledgement and retains the proposal on that page. Cancelling a wait is explicitly not cancelling the server operation; late responses cannot clear a retained proposal or report success. A new property selection requires explicit replacement of an existing proposal. Normal Inertia GET navigation and browser unload require confirmation for retained proposals. Native Back/Forward recovery belongs to the separately coordinated W06 page/draft lifetime work and is not claimed verified by these tests.
- `resources/js/components/it/ticket-version-conflict.tsx`: optional original-actor/current-permission validation, strict current-record parsing, scope/operation guards, cancellation and bounded wait. Existing legacy callers without the optional actor remain compatible; they are not claimed actor-bound by this change.
- `resources/js/components/it/__tests__/ticket-property-mutation.test.tsx`: meaningful property transport/draft recovery cases, replacing the three earlier Inertia-only property tests in `ticket-version-conflict.test.tsx`. Existing generic version and close/wait cases remain.
- Hook callsites are root-owned in the canonical ticket page and hub. Signature accepts `actorId: number | null | undefined`, optional effective `draftsEnabled`, and optional `onCommitted(ticketId, version)`. `hasPending` lets the page-level pre-blur subcategory guard avoid duplicate prompts; `busy` still disables conflicting controls while the recovery panel is open.

## Actual verification

- First executable focused run: 18 passed /4 failed, all four failures caused by duplicated recovery copy matching the same text selector twice. The duplicate user-facing copy was removed. The preceding sandbox attempt never collected tests because esbuild could not read the config parent; it is not a test result.
- Final focused run at2026-09-09 17:02 local: **25 passed /2 files, 6.04s, exit0** using Vitest with mocked HTTP and no database access. Covers actor/version binding, strict positive acknowledgements, validation/conflict/session/access failures, mismatched review actor/permissions, Escape confirmation, retained proposal and late-response denial, explicit navigation acknowledgement, metadata-only resume, stale saved base review, exact consumed draft ACK, frozen unknown writes, and usable concealed session recovery.
- Scoped ESLint for both implementation files and both test files: **exit0, no errors or warnings**.
- The later controlled-explanation follow-up is verified in the combined waiting/routing/property group: **53 passed /5 files,8.23s, exit0**. Every reason keystroke now belongs to the original property snapshot; configured recovery autosaves it before confirmation, refreshed versions do not replace it, and reopening the reason editor preserves it. The actual canonical change still requires explicit confirmation. See `w06-waiting-routing-recovery-results.md` for the combined evidence.
- Full TypeScript recheck completed with no errors in this slice or its root-owned callsites. The overall command exited1 on three `config.data is unknown` errors in the concurrently edited `ticket-intake-draft.test.tsx`; its owning agent was informed. This is not a passing full-tree check. Rerun after that correction before recording full-tree success.

## Resumption

Sources stable for Build6; do not modify during the coordinated asset build. Finish full-tree TypeScript, then use real desktop browser journeys against the correct new manifest for acknowledged update, stale review/reapply, session/access changes and dirty/unknown closure. Enabled draft browser tests must use the separately reviewed token-owned verification process/schema/storage, never silently enable retention on the current Herd data. Migration000007 remains unapplied to that existing database.

## Shared browser-memory follow-up, 2026-09-09 18:00 local

The canonical property editor now receives optional `recoveryTicket:{id,lock_version}` from an authorized detail page. This exposes a metadata-only recovery entry point even when no new edit has begun, including broad `ticket_edit` proposals left by narrower forms. The page stops displaying a pending proposal immediately when that permitted record disappears or changes. `submit()` returns whether it captured the proposal in pending/replacement state, allowing the raw subcategory editor to transfer ownership without losing its text.

Unsaved changes and each controlled explanation keystroke now use the shared actor/context-bound RAM handoff. Explicit resume follows current server authorization, keeps the original base, and never reapplies by itself. Unknown canonical submissions require current-ticket review before an explicit write. Scoped browser work clears only after its exact committed acknowledgement or a deliberate local discard/replacement. New immediate property writes wait for their record scope to render so an older browser copy cannot be skipped. The full reload/application-close limitation is stated in the navigation warning.

Final combined result: **62 passed /5files,8.59s,exit0**; scoped eight-file ESLint **exit0**. The new tests exercise native component unmount/remount, controlled reason recovery and edit, no replay of uncertain submissions, revoked access concealment and immediate concealment when the page loses its permitted record. Full TypeScript run25929 had no source implementation errors but two fixture errors; this slice's required `draft:null` fixture was corrected and the other owner was notified. Root owns final full-tree verification and real desktop browser acceptance.

Root's Build6 desktop browser then proved exact unsent subcategory recovery after native Back and current-access confirmation, and found the adopted proposal lacked explicit cancellation. The review dialog now offers **Discard local proposal** with destructive confirmation for known unsent/reviewed state. Cancel retains it; confirm removes only owned RAM/local pending values and explicitly leaves any saved server draft separate. Unconfirmed canonical or draft operations cannot use this shortcut. Final follow-up group: **63tests/5files,8.69s,exit0**, start18:13:51 local; changed property source/test ESLint **exit0**. The new test proves no ticket write and no surviving browser copy after discard/remount. This last button requires the next asset build and browser verification; the earlier Build6 proof does not verify it.
