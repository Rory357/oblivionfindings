# W14 handoff polling and access-transition desktop recheck

Run in progress. No pending scenario is treated as verified.

Owned token60a4a9d5168bef72, fingerprintfdcb42e843bf5f3822dbe19a050deab759c0f7973350ff4330b120a1fc4e8566. Bootstrap85002 terminal0; server44972. Normal technician sign-in, actor3. Identity confirms exact checkout/schema, array mail, sync queue and normal CSRF. Manifest6d52fa0a916c344ad03451df19a4392782671cdbaa9cc9e78d07238c38d781c2, actual DOM app-B9HhmhJS.js. Build67919 terminal0/3m43s; UI24/3files/8.07s, TypeScript86799terminal0, lint0 with existing epoch-ref warning. Owned in-app tab20; natural1063x856, no resize; user tab2 untouched.

Pending: keep a dirty list-origin handoff open across the30-second poll interval; confirm no unsolicited discard prompt and retained fields; verify actual save and keyboard return/reopen. Then deliberately revoke/restore only the original technician fixture's existing alert-management grant, and age only its current owned database session to exercise actual expiry. Complete normal-login recovery and reconcile exact command/link records. The guarded helper hash matches its reviewed version; no transition has yet run in this environment.

## Polling and saved-work observations

The dirty list-origin alert7→ticket22 handoff remained on step2 for an explicit35-second dwell. No discard prompt appeared; the selected ticket and technical reason remained intact. Keyboard Continue reached review and Link selected ticket returned IT handoff saved, focused Open IT ticket, and retained the operational alert. Back to alert returned focus to Prepare IT handoff; Return reopened a truthful This alert already has IT work view with only Open linked ticket, preventing a duplicate handoff.

Read-only reconciliation confirms one actor3 source link and one committed receipt (UUID e66d5017-2aba-44a2-b33b-040c55ce38ea) for alert7→ticket22; alert7 remains Open; ticket count23 unchanged. No other handoff fixture has a link/receipt.

Additional keyboard gap: closing the whole alert workspace back to its worklist placed focus on the document body rather than the originating Continue response button. This is distinct from the corrected handoff→alert focus. Implement an owning-worklist return target/fallback after finishing the access-transition checks; verify it in current rebuilt assets before marking the list-origin keyboard criterion complete. No production change for this newly observed focus gap has yet been made.

## Explicit approval dependency

Automatic approval review rejected the exact CLI-only role revocation before the process started: it requires user approval for the particular permission, role and disposable scope. The separate session-expiry invocation was also rejected before execution, citing the need for explicit session-alteration approval and its proximity to the refused role change. Neither command ran; no role grant/session timestamp changed. These scenarios remain unverified and must not be replaced by a workaround or claimed from earlier sign-out/backend tests.

An asynchronous question requests approval to temporarily remove/restore controlRoom.alerts.manage from w06-browser-tech/w06-tech@demo.test and expire its one test session in oblivion_it_draft_browser_60a4a9d5168bef72. No answer yet. Keep that dependency pending; elapsed time is not consent. The current owned environment/tab20 is retained for these exact checks. Alert9 has only an unsaved draft for ticket23; no command was sent. The independent focus correction can proceed in source/tests.

## Focus correction implemented, browser verification pending

The shared worklist row now identifies its canonical opener and its stable owning section. A small Control Room hook reuses the existing overlay focus-return helper, preserving the opener across nested handoff dialog replacement and returning to the retained worklist if a row disappears. Both the alerts list and dashboard callers pass the close-focus callback through the existing AlertWorkspaceDialog/WizardShell. Direct-route callers retain their existing behaviour. No design files or component styling rules changed.

Focused worklist/workspace/handoff/dashboard tests40passed/5files/8.50s terminal0, scoped lint terminal0. TypeScript9070 completed terminal0. Seven final focus-source hashes are captured separately. No focus build or browser verification yet: the current owned runtime continues serving the verified polling build app-B9HhmhJS.js. Do not equate these source/test changes with browser-verified focus behaviour.

## Run closed during continuation

CUA reports tab20 no longer belongs to the session and its inventory contains only the preserved user tab2. The server was independently confirmed live before routine cleanup. Final read-only handoff records still show only alert7→ticket22,23tickets and no alert9 command/link. Cleanup1848terminal0 and independent postflight0 confirm the exact schema/root absent. The rejected role/session transitions were never executed, so cleanup is not evidence for either scenario. Approval remains pending; no substitute permission/session mutation was made. Independent focus build26602 is now running for a fresh browser run.
