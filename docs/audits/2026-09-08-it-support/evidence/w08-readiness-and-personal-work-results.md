# W08 current task evidence and canonical personal work

Status: implemented source with focused backend evidence; current-assets browser verification pending. This is not complete W08 acceptance.

The canonical task graph now evaluates exact completion generations, prerequisite chains and same-ticket approval bindings. A reopened/recompleted prerequisite invalidates dependent completion evidence without rewriting the dependent task's saved status/history. Invalid required work blocks manual settlement. Missing historical provenance remains explicitly unknown; the user's legacy policy question is still pending, and no blanket old-work invalidation has been activated.

Private history reads are actor/ticket/task/nonce-bound, paginated at 20 entries, and carry the current aggregate version. They perform no legacy capture or evidence repair. Current work access is required before projection; participant-only and out-of-Site requests are denied. The shared private response middleware suppresses debug details and adds no-store headers for history, and now also for typed approval errors before their controller executes.

Changed root sources: `ItWorkTaskGraphEvaluator`, `ItWorkTaskReadinessService`, `ItWorkTaskHistoryController`, `ItTicketContextPresenter`, `ItWorkTransitionService`, history route and `ProtectItDraftResponses`. W00's canonical completion model/service/migration000011 provide the stored generations; W01's canonical approval service/commands/migration000012 provide decision evidence. Their detailed results remain in `w08-task-history-results.md` and `w08-approval-command-contract.md`.

The existing All tasks/My Day aggregator now includes `ItWorkTaskProvider`, using `ItWorkAccessService::applyWorkScope` before row loading. It projects the same task ID, existing assignee, due date and ticket link (`?tab=tasks#task-ID`). It does not create another task record or assignment endpoint, and does not copy private completion notes/evidence into the feed. Completed work with invalid prerequisite evidence remains actionable. The provider computes one readiness graph per represented ticket. Cross-ticket scale/performance acceptance is still required; no arbitrary truncated total or fabricated count was introduced. Pending approval assignment/cover still needs its canonical named owner before personal approval projection is complete.

## Actual checks

- Pure graph Unit file: 11 passed, 37 assertions, 0.30s (`w08-task-graph-unit-initial.txt`). Includes A→B→C generations, cycles, unknown legacy provenance, invalid current pointers and a 1,000-task chain.
- First task Feature batch: diagnostic events prove 66 passed and one fixture error, 67 finished, 730 summed assertions. Root readiness seven cases/72 assertions and transition 13 cases/95 assertions passed. The synthetic history fixture attempted a now-forbidden model deletion of an immutable approval; only that fixture was corrected after exact schema absence was confirmed. This first run was not green.
- Corrected History + approval first batch: 49 passed/one fixture failure/435 assertions. Corrected history 12/145 and existing approval 22/106 passed. The approval failure injector had removed real model hooks; the fixture now retains them with an armed listener. The failed snapshot is retained.
- Combined approval command/approval/provider/provider-architecture/readiness/local-draft batch: **75 passed, zero failed/errored, 75 finished, 782 summed assertions** from `it_562d1a5af1fc495c.diagnostic.jsonl`. Wrapper log `w08-approval-provider-feature-tests.txt` records `focused_tests_finished` exit0 and all postflight guards true, including exact schema absence. The agent process handle was no longer present when root resumed; the completed log and diagnostic, not the missing handle alone, prove the result. Pest's human summary was buffered away, so no console duration is claimed.
- Root approval transport contract: 16 frontend tests/1 file/1.13s and strict scoped ESLint passed. Original request receipt status remains distinct from current approval state; exact actor/UUID/operation/approval/decision and version are checked. This contract and the new approval command hook are not yet wired into the visible approval controls or private RAM recovery.
- Queued reply acknowledgement wording: 30 frontend tests/1 file/1.17s passed (`w07-queued-reply-contract-tests.txt`). It describes the notification as having been queued and directs users to current delivery updates.
- Protected design hash comparison: all 10 files unchanged (`w08-lifecycle-design-invariants.json`). No design source was edited.
- Empty-ticket capability and task-write regression: **32 passed, zero failed/errored, 357 assertions**, session77401 exit0. The eight readiness cases plus 19 command and five task cases ran in `it_b6afd6b2983847b4`; the wrapper independently confirmed exact schema absence (`w08-task-capabilities-feature-tests.txt`).
- Affected task lifecycle/host/approval frontend regression: **175 passed across 11 files, 46.06s**, session78945 exit0 (`w08-lifecycle-local-takeover-ui-tests.txt`). Full TypeScript passed before the final two hook corrections.
- Final approval identity isolation/history cleanup corrections: **51 passed across two files, 3.41s**, strict three-file ESLint and formatting exit0 (`w08-final-command-focus-tests.txt`, `w08-final-command-focus-eslint.txt`, `w08-final-command-focus-format.txt`). Command callbacks now isolate the full actor/ticket/operation/approval/UUID identity.
- Standalone task-history concurrency: **one passed, zero failed/errored, 69 assertions**, session31238 exit0. Metadata-only events in `it_5440d1b77e374b5b.diagnostic.jsonl` and wrapper exit0 plus exact schema absence prove the result (`w08-task-history-concurrency-tests.txt`). The human Pest summary was buffered away; no duration is claimed.
- Build11: **exit0, 4m02s**, session57204. Entry `assets/app-BbtxMpeT.js`, manifest SHA256 `7fc35035b9e44550f1fe80ae7c88589c99fc1e3ed826a28aabbd81011980bc58` (`w08-desktop-build11.txt`). Current-build browser acceptance remains pending.
- Standalone approval concurrency: **three passed, zero failed/errored, 84 assertions**, session83524 exit0. Metadata-only events in `it_e0d75b5cfa4e4749.diagnostic.jsonl`, recorded test exit0 and independent exact schema-absence postflight prove the result (`w08-approval-command-concurrency-tests.txt`). Covers simultaneous duplicate/opposed/cancel/settle commands and acknowledgement recovery after real commit.
- Final current-source full TypeScript: session96115 **exit0** (`w08-build11-final-types.txt`).

## Current checkpoint and next action

All three parallel workers stopped after reporting usage-limit errors; root is continuing locally. Completed Feature and task-history concurrency schemas were removed. No browser preview is live; Build11 is ready but cannot yet certify browser behaviour. No real communications, provider configuration, production operations or working-Herd migration occurred.

Root added an authoritative `task_work` capability projection for empty tickets and explicit missing-history guards before create/update/reorder touch new columns. Those checks and the affected frontend regression passed as recorded above. Both standalone concurrency checks and final full TypeScript passed. The fresh token-owned Build11 preview is being created for history, cancellation/restoration, personal task links, restricted roles and the earlier compiler/focus/attachment fixes. Approval expiry/reminders/eligible cover, visible command/RAM integration and full W08 acceptance remain open.
