# W05 desktop browser verification — partial

9 September2026, Build4 then Build5 at `https://oblivionfindings.test/it`. Correct checkout and assets confirmed from the DOM. Synthetic restricted technician230; primary viewport1440×900 throughout, no resizing. Existing working database was preserved and all writes below targeted labelled synthetic tickets only. No messages were sent.

## List, navigation and actions

- The default list initially showed15 of17 visible tickets; its default includes settled records. Participant-only tickets12/13 had no management selection control. Manageable records exposed their permitted selection/actions.
- Search `ITSupportW04` returned7 of7. Cards/Table changes retained the search and URL. Keyboard Enter on ticket26's semantic table link opened its canonical detail page.
- Actual browser Back restored `/it?list_view=table&q=ITSupportW04&tab=tickets`, the search field, Table choice and scrollY8. An earlier Alt+ArrowLeft key attempt did not navigate and is not counted as Back evidence.
- Ctrl-click on the ticket26 link opened its canonical detail in a new tab while the original filtered list stayed unchanged. The verification tab was closed afterward.
- Ticket26's kebab and actual table-row right-click exposed the same permitted actions: open, quick peek, start work, reassign, wait, resolve, copy reference and copy link. Keyboard End focused Copy link and Escape closed the menu. Clipboard content and these mutation dialogs were not exercised in this slice.

## Actual partial bulk result and restoration

- Selected only synthetic20 and23. The visible selection bar showed2 selected before the explicit Set status→In progress action. Result reported1 updated,0 unchanged,1 not applied. Ticket20 became In progress; resolved23 stayed Resolved. Persistent per-item text explained that selected record2's current requirements prevent the action and offered Review current list.
- Review current list refreshed without retrying the rejected item. Selected20 alone and explicitly set Open. Result reported1 updated and0 not applied;20 returned Open/SLA on track. Ticket23 remained Resolved/SLA met. The two legitimate changes advanced ticket20's audit/version history; no direct database restoration was used.
- Ticket21's real deadline elapsed during this long session, changing At risk to Breached. The header reflected3 breached/0 at risk. Its deadline was not altered to manufacture a result.

## Desktop readability correction

- The old IT header had `overflow:hidden` and internal scrollTop104 due to decorative overflow; its rail sat104px above the header's actual bottom. The IT consumer now opts into `overflow:clip`.
- Build5 DOM verified scrollTop0 and equal header/rail bottom437.25. Screenshot inspection confirmed the approved rail seam and the long ticket card title wrapping to2 lines, height37.5 with normal whitespace. Shared design sources remain untouched. The header guide explicitly permits the dense two-row IT meter layout.
- Build5 app `app-lNwuFfLJ.js`; manifest SHA256 `e811de7efc0ccb37e7e73f483b20eca1696e29b9a36d30d3fa2b237e618b8950`.

## Saved filters, sorting and pagination follow-up

Build5, same restricted actor230, approximately16:43–16:55 NZ:

- Searched `ITSupportW04`, chose Reference sort, switched Descending→Ascending and verified actual row identities20,21,22,23,24,25,26. Saved the personal filter `W05 synthetic reference-order check`; it appeared after the normal save and survived hard refresh.
- Cleared the search through keyboard Control+A/Backspace, verified `q` disappeared from the URL, then applied the saved filter. It restored the actual search input, ascending seven-record result and canonical `saved_filter=1` URL. The automation API's `fill('')` left the DOM value unchanged; this is not counted as an application defect or a successful clear.
- Opened the advanced filter panel and selected Open tickets only. The checkbox tool reported its immediate checked-state assertion before the async navigation settled. The authoritative subsequent DOM showed checked=true, `open_only=1`,6-of6 and records20,21,22,24,25,26; resolved23 was excluded. Reapplying the saved filter removed that override and returned the original7-of7.
- Saved-filter deletion first offered an explicit confirmation. Keep filter retained the entry. A second explicit Delete filter removed only this personal fixture; the UI showed no personal filters and returned to the complete17-ticket list. No ticket content was changed by this journey.
- Next page showed2-of17 with24 and23 and URL `tickets_page=2`. Switching Table→Cards preserved page2 and the same two identities. Actual browser Back restored Table, page2 and2-of17, confirmed by the checked layout radio, rendered table and URL.

## Remaining acceptance

Live permission-loss recovery and broader combinations/date-boundary filters still need browser journeys. Quick peek is being rebuilt and is not verified from the menu being present. The temporary Knowledge/Reports rail destinations remain until their dedicated W21/W17 landings replace the canonical redirects, as required by the plan; do not remove the only functional content prematurely. W07 must still implement latest-public-speaker Awaiting IT semantics. No whole W05 verification claim is made.
