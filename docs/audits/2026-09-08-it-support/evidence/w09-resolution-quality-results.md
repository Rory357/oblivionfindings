# W09 resolution quality — in progress

10 September 2026. Not browser-verified; Build21 compilation completed. Production/Herd schema unchanged. W09/E08 and the Goal remain open.

## Implemented source, pending full acceptance

- `ItTicketResolutionInput` shares outcome/note/verification rules with HTTP, direct interaction writers and partial public-resolution drafts. Outcomes are explicitly selected: restored, workaround, fulfilled or guidance; no silent restored default. New nullable ticket verification migration is source-only, leaving historical values unknown.
- `ItTicketInteractionService`, `ItTransitionInput`, `ItWorkTransitionService`, `ItTicket`, `ResolveTicketRequest`, `ItProvisioningController::resolveTicket`: normalized evidence persists transactionally, verification is included in the public reply, exact acknowledgement includes saved evidence, resolution/reopen events preserve prior evidence. Audit failure rolls back the transaction. The generic show payload exposes private resolution metadata only to current work managers; requesters receive the deliberately public conversation.
- `resolve-ticket-dialog.tsx` reuses approved outcome tiles/fields, carries all values through draft save/resume, cancellation and account/session recovery, compares exact saved evidence before success, and shows recorded evidence during uncertain-result review without inferring success. Draft-from-resolution carries the public check forward; reviewed canonical article linkage remains W21 work.
- `it-ticket-draft-contract.ts` and backend draft payload allowlists extended together. Legacy note-only drafts retain their text and require an explicit outcome/check before resolution.

## Actual checks

- New Feature5470: **11 passed / 87 assertions, exit0**. Diagnostic token `it_06c65c99efeb4abb`; exact disposable schema absent on final postflight. Covers four explicit outcomes, missing/invalid values, direct-writer validation, audit rollback/retry, reopen history, and private metadata denial. `w09-resolution-quality-feature-tests.txt` and token JSONL retain evidence; human output was buffered, counts read from Passed/Finished events.
- Final focused UI: **85 passed / 3files / 8.73s, exit0**, `w09-resolution-quality-ui-tests-final.txt`. Includes ordinary/memory recovery, strict acknowledgement mismatch, legacy incomplete drafts and recorded-evidence review. Earlier runs:23failed/56passed because test labels included spaces absent in Testing Library's accessible names; then2failed/77passed from an old incomplete saved fixture and expected payload; corrected explicitly. Intermediate84passed predates recorded-evidence comparison. Logs retained, counts overlap.
- Scoped PHP Pint0. Latest three-file ESLint0 (`w09-resolution-quality-review-eslint.txt`). Full TypeScript37668 exited0 after the recorded-resolution comparison; earlier7193/70261 checks predate that change and are not substituted for the final result.
- Existing lifecycle/approval/tasks/actor/draft/SLA fixtures updated to provide explicit synthetic verification. Three real-worker fixtures updated likewise. These input changes are not a concurrency pass.

## Active work and remaining acceptance

**Latest13:37NZ supersedes historical active-process notes below:** canonical follow-up checks passed: readiness8/98, ticket concurrency1/285, task concurrency3/131, history concurrency1/69, approval concurrency4/135; all individual exit0/exact schema absent. Resolve+Show UI55/2files/24.77s0, lint/types0; Build22 exit0 and completed desktop browser evidence in `w09-browser-build22-results.md`. Actual resolution/recovery/public lifecycle passed and owned environment removed. Browser found a pristine settled property-draft notice; narrow shared recovery fix red1/13passed then final68/3files/10.80s0, ESLint0. **TypeScript52909 and Build23 session52740 active**, no browser token23 yet. No new PHP/runtime migration changes. DP02 reopened SLA clock policy question pending. Full package remains open.

Regression94672 finished **108passed/1failed/109finished/1,087assertions,exit1**. Exact `it_b94cf81de1434e3c` schema absent. Failure was the old exact public-comment expectation at ItBrowserActorBindingTest.php:58; updated to include the explicit verification section. The failed run is retained, not relabelled green.

The generic ticket-settlement gap is now corrected in source, pending current checks: public comment/first-response/projection/audit recording moved from the explicit interaction into the canonical transition service. Incident, Service Request and Security Request settlement require the same explicit evidence and preserve their workflow target. Generic request accepts verification and optional actor binding. Problem/Change/Major Incident settlement keeps its distinct semantics. Existing missing-summary guards and required-work precedence remain intact. Added generic route tests for missing verification, persisted public evidence, correct workflow state and stale retry; the explicit route still creates only one public resolution record.

Canonical regression10108, token `it_d0bc904f1993426e`, log `w09-resolution-quality-canonical-tests.txt`, covered11 Feature files including the new quality and work-transition cases: **135passed/1errored/136finished/1305assertions, Pest2/wrapper1**. Exact schema absent. The task-readiness success retry still supplied obsolete fixed/no verification; corrected to restored/explicit verification. No runtime weakening. **Active session54616** now reruns readiness, then separately TicketCommand, WorkTaskCommand, WorkTaskHistory and TicketApprovalCommand concurrency, sequentially with independent isolated Pest processes and cleanup. No parallel import or PHP/runtime/migration mutation. Each log uses `w09-resolution-quality-<target>-tests.txt`. Build21 log completed3m31s; app-C6g3RoDY.js, manifest93960a484906fb5d60b8d13cd8f1fb9e51d6c1887ecd61b89239799ccdb37642. Preview fingerprint01eb7e58c01fc9ced1d44e1295423a743ddf9f3a16e4bfc915d5cd1e7f1843d2; six helpers/schema unchanged versus20, reviewed nullable migration000014 is the only migration difference. No browser environment yet.

Also pending: current-assets desktop browser proof of new outcome/check fields, public audience, validation, draft recovery and uncertain result; migration/Herd readiness distinction; canonical known-error/article links with access and lock ordering; displayed resolution quality/reporting. W09 close/reopen recovery, related/duplicate/merge and W21 reviewed knowledge remain open. No deployments, provider changes, real communications or design-source edits.

## Next lifecycle slice: revalidated implementation gaps

Latest incremental verification, 13:14 NZ: corrected readiness8passed/98assertions0, exact token`it_b6d0dcfd97cf4b82` absent. Ticket concurrency1passed/285assertions0, exact token`it_e28cfa636f6b4cb8` absent. Task concurrency exited0 with exact token`it_50f9d869618c4937` absent (assertion count still to collect). History/approval remain active in sequence54616.

An additional source slice now displays saved resolution code/explanation/checks to currently authorized work managers in the ticket conversation. `ticket-resolution.ts` shares the existing outcome labels/icons with the Resolve dialog; `ticket-resolution-summary.tsx` reuses current ticket card/button tokens and links to canonical History. Missing historical values say Not recorded, unknown historical codes remain identifiable, requester/account-changed views conceal the entire panel. `show.tsx` binds the existing private backend projection. Two privacy/history tests added in `show.test.tsx`. Focused Resolve+Show **55passed/2files/24.77s0**, scoped ESLint0. Full TypeScript91035 pending. **Build22 session80985 active**; Build21 was compiled but never browser verified and is superseded by this panel change. Frontend frozen until compile settles. No browser token22 yet.

Read-only inspection while isolated checks run confirms these bounded next changes; none is marked implemented here:

- `TicketReopenDialog` currently clears a nonempty reason on close/Escape, uses an Inertia flash fallback for success, and lacks actor binding, uncertain-result handling and requester-capable version review. Replace that body with the approved dialog pattern and retained proposal, explicit discard/focus restoration, typed acknowledgement, timeout/stop/review and separate retry. Keep the existing canonical reopen endpoint and audience rule (actual requester is public even if also IT).
- `ReopenTicketRequest` and `CloseTicketRequest` lack browser actor binding. `ItTicketController::reopen/close` return only ID/version to JSON clients and redirect domain failures. Add explicit operation/status/actor/evidence acknowledgements and JSON error/no-store responses, keeping existing redirect callers compatible.
- `ItWorkTransitionService` allows generic settled-to-active transitions without the explicit reopen reason/comment/history path. Govern actual reopening at the canonical service, preserve type-specific legitimate review transitions, and prevent forged source values from weakening requester authorization. Centralize reopen recording with source/audience metadata and one atomic audit; direct service callers need the same reason bounds. Verify requester window, agent-as-requester public wording, stale/competing closure, prior resolution evidence and SLA history.
- `TicketCloseDialog` has equivalent dirty-loss/flash-fallback gaps; bulk outcomes must keep independent per-ticket refusal and must not silently resubmit already accepted items. Full close/reopen lifecycle stays open until browser and concurrency evidence prove it.
- Existing `ItTicketLinkService` already provides ascending canonical ticket locks and audience-scoped targets. Extend it for known-error/article context with current knowledge access; it currently does not support knowledge targets despite model relationship vocabulary. Do not add another knowledge store. Link mutations need exact version/audit and safe recovery.
- Existing merge only compares requester/requested-for and moves comments/watchers. Full W09 must account for Site/team/sensitivity, file references, tasks/approvals, old conversation pointers, immutable origin history, versioned preview and canonical authorized redirects. The current implementation is not accepted as a completed merge lifecycle.

Formatting correction: read-only Pint63691 with `--dirty --test` unexpectedly included unrelated dirty files and exited1; no source was changed. Correct exact-two-file Pint without `--dirty` exited0 (`w09-resolution-quality-followup-pint-scoped.txt`). The unrelated findings are retained in the first log and are not represented as a clean whole-tree check.
