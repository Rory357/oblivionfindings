# W13 versioned API mutations

11 September 2026. This is bounded W13/E12 implementation and verification. Full identity lifecycle, Operations recovery, E12 and the release gate remain incomplete.

## Implemented

- API resources expose `lock_version`. Transitions require `expected_version`, accept canonical resolution verification evidence and retain server-owned actor/channel provenance.
- PATCH update requires `work:update`, a mutation idempotency key and explicit per-property update grants. It extends the canonical triage service with a distinct `ticket.update` receipt. Supported classification/priority properties use existing policy; raw priority and release require reasons. API raw priority preserves the current impact/urgency assessment, including legacy tickets.
- Related-ticket add/remove requires `work:link`, authorization of both records and both versions. It extends the existing reciprocal relationship command with ServiceApi receipts/provenance, not generic cross-domain link access. Actor locks precede the canonical ticket pair; API Site evidence is current. Browser cancellation/recovery defaults remain intact.
- JSON error handling no longer depends on the caller's Accept header. Existing-request recovery checks current record access before hash conflicts, including all records involved in a changed-route replay. Failed publication rolls back domain changes; proven failed intents can retry their original key.
- The canonical API guide documents these contracts. Existing identities receive no update-field grants automatically. Management UI additions will land with the remaining identity wizard/rotation/transient-credential work; no frontend implementation/build claim in this slice.

## Actual evidence

- Feature60606 / token `it_bac1ad6fe1404b74`: 140 passed, 1 failed; 141 tests / 1518 assertions. All new mutation, direct relationship and provenance scenarios passed. The sole failure was an older browser resolution fixture at `ItTicketVersionTest.php:142` omitting required resolution code/verification evidence before its version test could run. All 14 postflight checks passed and the exact disposable schema was absent.
- Corrected affected-file64846 / token `it_1c17a5ccac3d4889`: 19 tests / 162 assertions passed, terminal 0, all 14 postflight checks and exact schema absence. Successful/stale resolution requests now carry valid evidence; stale conflict and no-duplicate assertions remain intact. All 141 selected scenarios have passing evidence across the broad run and affected-file rerun, not a second combined green run.
- Standalone publication90225 / token `it_0378c8a70b8f476b`: 1 test / 99 assertions passed, terminal 0, all 14 postflight checks and exact schema absence. Actual duplicate create/update/related workers and existing outer-commit acknowledgement-loss recovery passed. Relationship assertions include exactly one version increment on each ticket, two reciprocal links, one API attempt and one canonical command receipt. Evidence: `w13-api-mutation-concurrency.txt` and token diagnostic JSONL.
- Browser bootstrap29515 / run `a5cd92a747204d1e` completed successfully with fingerprint `551e739e831c23cbbc4da403eb0eddc088f3d6d24f77b69ed908aab9b634311f`. Actual local HTTP kernel create/comment/update/link/transition commands and exact replays succeeded; synthetic credentials stayed in memory. Source ticket 8 reached version 9 with Network classification, High priority and In progress status; target ticket 9 was linked once.
- Actual Codex in-app desktop tab39 at `http://127.0.0.1:8766/it/tickets/8` showed the saved classification, manual priority reason, status, API conversation and linked ticket9. The original desktop viewport was retained. The relationship wizard named both ticket references; Escape then keyboard Tab/Return discarded the unsubmitted removal while preserving the link. Reopening, tabbing through to Remove relationship and pressing Return produced a confirmed saved outcome. Reload showed no relationship on source8, and the counterpart9 also showed none. Source history retained creation, approved API reply, reasoned priority override, property update, related add, transition and later browser removal. The screenshot was visually inspected inline; no saved PNG or console-log claim is made.
- Normal sign-out/sign-in as the isolated requester then direct navigation to ticket8 returned a concealed 404 without ticket details. Navigating to its own ticket1 recovered the permitted conversation with requester controls and no technician workspace. Browser routes included `/it/tickets/8?tab=properties`, `?tab=links`, `?tab=history`, `/it/tickets/9?tab=links`, and `/it/tickets/1`. Runtime identity/current assets and isolation were verified in `w13-api-mutation-browser-runtime.json`; manifest `0c4f05f87e230e11d4fd82e56130427511d5c23d97d46b93f051c10c188b13f5`.
- Owned tab39 was closed; user tab3 was preserved. The first cleanup invocation omitted the required original fingerprint and was safely refused before mutation (log retained). Corrected cleanup3419 completed terminal0. Independent read-only `w13-api-mutation-browser-postflight.json` confirms the exact schema and owned runtime directory are absent. No owned server/import/test/build remains active.
- Scoped Pint and diff checks passed; protected ten design sources match their baseline. Source snapshot: `w13-api-mutation-source-hashes.json`.

All test execution used the existing isolated wrapper and disposable local schemas, with mail/notifications/providers isolated. Working database migrations 17–23 remain unapplied. No production/provider operation was performed. Two Terra High workers handled bounded command/test work and review; the parent reviewed, corrected and executed verification.

## Remaining work

The bounded mutation slice is Implemented and locally Verified by the focused Feature, actual concurrency and desktop journeys above. Finish identity issue/rotate/revoke and transient-secret UI, capability controls, scoped Operations/recovery and every remaining W13 acceptance criterion. The broader cross-channel writer graph and complete W27 regression targets still require final release verification; the focused races do not prove all application concurrency.

Two existing Terra High workers completed independent read-only preparation for the next slice while the parent verified the frozen browser runtime. Recommended next contract: a dedicated configuration version (authentication updates `last_used_at`/`updated_at`), current-authority/versioned grant edit/rotation/revoke, and durable nonsecret command outcomes so lost rotation acknowledgement cannot issue a second token or replay a stored secret. Reuse the existing credential service/identity records; preserve execution account/public ID/creator. The UI should use the approved four-step WizardShell, field-grant review including update/link, dirty-leave guard, explicit one-time credential clear/copy-failure states, and current-viewer concealment. No rotation endpoint or identity UI has been implemented in this slice.
