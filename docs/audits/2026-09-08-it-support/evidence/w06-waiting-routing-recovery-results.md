# W06 waiting and routing recovery

Implemented, with focused UI verification. Desktop browser verification against the next coordinated asset build remains pending. No production draft retention, provider configuration, notification delivery or database migration was activated by this slice.

## Changes

- `resources/js/components/it/use-ticket-form-command.tsx` shares the waiting/routing browser command lifecycle. Single updates use the existing canonical PATCH and require HTTP200 plus exact committed ticket/viewer/version. Bulk waiting uses the existing bulk POST, binds the original actor and frozen IDs/versions, and matches completed viewer/action/ordered IDs and actual status counts. Missing or malformed responses cannot close a form or announce success.
- `ticket-waiting-dialog.tsx` preserves its original reason, dependency, next action and displayed version across refreshed props. It no longer sends single edits with Inertia `preserveState:false`. `ticket-routing-dialog.tsx` keeps the same ownership fields and uses the same strict command lifecycle. Manual queue/owner clearing is unavailable because the existing canonical routing policy requires these choices; automatic routing remains an explicit reasoned choice and does not invent an owner.
- Both dialogs support cancellation of the wait without claiming cancellation of the write, late-response denial, original-actor/current-work review, session concealment, access-loss clearing, dirty Escape/close/navigation confirmation and explicit reapply after stale/unknown outcomes. A known closed ticket remains blocked rather than enabling a doomed property write. Normal GET/unload paths are covered; native Back/Forward RAM recovery is the separately coordinated draft-memory adapter integration and is not claimed complete here.
- Partial bulk outcomes remain visible with their waiting evidence. The hub receives rejected outcomes only when the user acknowledges leaving/reviewing; selection pruning therefore cannot silently unmount the form and lose its reason. No bulk draft system was introduced. Legacy non-JSON bulk responses and actor-scoped flash projection remain compatible.
- Effective single `ticket_edit` persistence uses the existing draft hook: metadata-only check, explicit resume, original saved base, exact snapshot save and consumed-generation acknowledgement. Autosave stops during unknown canonical outcomes. A saved draft for different ticket properties cannot be overwritten through this form's manual save control; explicit discard/new or its appropriate editor remains available. Missing retention still disables persistence.
- `BulkTicketActionRequest.php` now uses the existing optional browser-actor intent concern; supplied stale/mismatched actors are denied before writes. `ItTicketController::bulk()` JSON adds `status: completed` and the actual `viewer_user_id`, preserving all per-record outcomes and current canonical service authorization.
- Follow-up: `TicketTriageReasonDialog` accepts optional controlled reason state. The property mutation parent records every explanation keystroke, can autosave its exact draft before confirmation, and preserves that explanation on reopen. Existing uncontrolled bulk callers remain compatible. The ticket itself still changes only on explicit canonical submission.

## Actual checks

- Focused final UI group: **53 passed /5 files, 8.23s, exit0** at2026-09-09 17:31 local: `ticket-form-command`, `ticket-waiting-dialog`, `ticket-routing-dialog`, `ticket-version-conflict`, `ticket-property-mutation`. Existing Inertia callback tests were migrated into real mocked-HTTP lifecycle assertions; their waiting fields, requester copy, original version and routing choices remain covered.
- All ten affected TSX implementation/test files: **ESLint exit0, no errors or warnings**. Three changed PHP files: **syntax exit0** using the existing Herd binary outside sandbox path restrictions.
- Full-tree TypeScript found no errors in this slice; it was not a passing full-tree check because the concurrently edited draft-memory adapter/tests had unresolved union types. The owning agent was informed; rerun after shared-source stabilization.
- Initial isolated bulk run: **12 passed /1 failed,129 assertions,183.15s**. The new denial test compared an unrefreshed factory model with a refreshed database row; the baseline was corrected to include database defaults before comparing the unchanged record. The endpoint returned the required403 in that run.
- The next full isolated retry exited2 without Pest output after passing preflight (`it_6c31f2135fad49cc`); it is not a pass. The visible-output two-case retry (`it_682606e193444ad3`) then **passed2/11 assertions,229.51s, exit0**, proving mismatched original actor denial leaves the complete record/event state unchanged and a legitimate waiting mutation returns the exact viewer/selection result. The wrapper's read-only postflight confirmed this exact schema was removed. `w06-waiting-bulk-actor-tests-retry.txt` preserves the output. The eleven existing bulk cases and the new positive acknowledgement case passed in the initial run; a fully passing thirteen-case group is not claimed from the interrupted retry. Root is investigating the unexplained interrupted run; no further fresh-schema retry or independent cleanup is authorized by this evidence.

## Resumption

The shared RAM adapter is now integrated and this slice is source-stable for Build6. Root owns the main ledger, current desktop browser and release gate. Browser journeys must cover waiting/routing commit, stale review, session/access changes, cancelled waits, partial bulk results and explicit dirty exits against the correct current assets.

## Browser-memory integration follow-up, 2026-09-09 18:00 local

- Single waiting/routing commands continuously hand off the exact unsaved fields, original base and unconfirmed-command state to the shared RAM adapter. Nothing private is written to browser storage. Native component unmount/remount exposes only an opaque recovery notice until current canonical authorization succeeds and the user explicitly resumes. A full reload or closing the application cannot preserve this unsaved RAM; close copy says so.
- Each specialized host supplies an exact field allowlist and pure semantic predicate. Both the latest and pending-save snapshots are checked before atomic adoption; an incompatible waiting status or mixed manual/automatic routing patch remains available for the canonical property editor, without hydrating or consuming it in the wrong form. All service actor/Site checks remain server-owned.
- A restored older base requires explicit current-ticket review. An earlier unconfirmed canonical submission is never replayed automatically. Committed ACKs clear only this mounted editor's owned browser buffer; a definitive422 settles that operation without erasing the retained details. Actor switches and current access denial conceal content. Bulk waiting has no shared single-ticket memory or persistence scope.
- Final combined focused group: **62 passed /5 files,8.59s,exit0**, start18:00:23 local. New host cases include both waiting/routing original-version recovery, uncertain submission after unmount, incompatible state rejection before adoption, actor switch, bulk isolation, controlled property explanation recovery and current-record permission loss. Scoped eight changed implementation/test files: **ESLint exit0**.
- Full TypeScript run25929 found no implementation errors; it reported missing `draft:null` in the new synthetic memory fixture (corrected) and one concurrent owner's `result:unknown` fixture (owner notified). Root will run the final coordinated full-tree check; this report does not claim it passed.
- The former silent PHP exit was diagnosed by the optional metadata recorder as an E_COMPILE_ERROR at a duplicate intake method; see `w06-php-test-diagnostic-results.md`. The interrupted bulk suite remains unverified as a whole until its necessary clean rerun.
- Root subsequently found a missing explicit discard after property proposal adoption in the real Build6 browser. The known-unsent local discard confirmation is now implemented, with final combined **63tests/5files,8.69s,exit0** and scoped property ESLint0. Unknown operations remain held for review. The added button awaits the next browser asset build; see `w06-property-mutation-results.md`.
