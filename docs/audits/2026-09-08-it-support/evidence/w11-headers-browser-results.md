# W11 malformed provider headers — actual browser, 11 September 2026

Partial W11/F03/E10 verification. Whole W11 and the goal remain incomplete.

Runtime `2d84ce70951f4a36`, schema `oblivion_it_draft_browser_2d84ce70951f4a36`, bootstrap24568 terminal0, PHP24024. Reviewed fingerprint `a587e2f90233cc7bc9266e7929c7d6125977a79fbcfbafb6770f89aa152bd988`, MailboxFixtures enabled. Only owned in-app tab21. Normal settings-operator sign-in and Settings → Support Mailbox navigation. Current `/settings/it-mailbox`, actual1235×856 viewport, never resized. DOM app `app-kNtA0mJt.js`, unchanged Build41 manifest `825aa72ccee696cfd66aead2b85c8ffaa722e8d801081fc70a6c848edb87a4d1`.

Microsoft Poll mailbox now activated with Enter: processing0, acknowledgement1, quarantine4; Needs attention and no completed-poll claim after the synthetic first ACK503. Google Poll with Enter: processing0, acknowledgement0, quarantine4 and Last poll completed. Global quarantine count8. Actual screenshot rendered in the task shows the Google status/counts/controls; no disk screenshot path claimed. Reload current state with Enter preserved Microsoft's pending acknowledgement and disabled Poll while the real cooldown still applied. No clock bypass or reconnect used.

The two additional synthetic cases are duplicate References headers and multiple From authors. Actual read-only w11-headers-browser-reconciliation.json confirms54tickets,54 committed create commands/audits and two committed reply commands/audits. Each provider has27new requests, one reply, one duplicate and four quarantines: one each message_id_collision, sender_inactive, duplicate_message_headers and sender_ambiguous. All quarantined body previews are absent. After the real cooldown expired, Reload and Poll activated with Enter completed Microsoft acknowledgement recovery: processing0/acknowledgement0/quarantine4, Last poll completed and no retry delay. Every provider message was read once; Microsoft used34acknowledgement attempts and Google33. No duplicate ticket/comment writes. The preceding seven-file106test run and final48test run verify the malformed-header branches and quarantine ACK retry in isolated backend tests. This browser run does not yet verify a quarantine review/retry interface, attachments, loops/bounces, merged/settled journeys or live providers.

Owned tab21 closed; user tabs1–3 untouched. Exact-token cleanup29713 terminal0 with the original fingerprint and MailboxFixtures. Independent read-only w11-headers-browser-cleanup-postflight.json confirms schema and owned directory absent. No active runtime remains. No console log result was collected; no console-clean claim.
