# W11 message identity and threading — 11 September 2026

Scope: partial W11; F03/F05, B01/B02, E10. The package is not fully implemented or verified.

## Implemented boundary

- `ItEmailMessageIdentifiers` parses bounded RFC Message-ID / In-Reply-To / References fields, normalizes equivalent quoted identifiers, preserves opaque case and rejects malformed or conflicting single Message-ID fields. Full identifiers are not truncated to the old 255-character columns.
- Additive migration `2026_09_11_000017_add_it_inbound_message_identity.php` extends the existing inbound ledger with a database unique identity claim, lookup/content hashes and encrypted full identifiers/references. Legacy rows get lookup hashes only; they are not silently coalesced or declared proven duplicates from truncated previews.
- `InboundEmailIngestor` commits identity reservation and ingestion together, detects reused IDs with changed content, checks current access before returning a duplicate ticket and links separate transport receipts to the original. `ItTicketReferenceResolver` reconciles subject and header candidates using current visibility and canonical merge destinations; ambiguity fails closed.
- Existing Gmail and Graph readers now pass References to the ingestor. No new provider connection, ticket system or production fake.

Changed files: `app/Domain/It/Exceptions/ItInboundHeaderException.php`, `app/Domain/It/Services/ItEmailMessageIdentifiers.php`, `app/Domain/It/InboundEmailIngestor.php`, `app/Domain/It/ItTicketReferenceResolver.php`, `app/Models/ItInboundEmail.php`, `app/Services/GoogleGmailService.php`, `app/Services/MicrosoftGraphService.php`, `app/Http/Requests/It/IngestInboundEmailRequest.php`, migration000017, `tests/Unit/It/ItEmailMessageIdentifiersTest.php`, `tests/Feature/It/ItInboundIdentityTest.php`.

## Actual verification

- Six-file focused run: **67 passed, 351 assertions**, approximately182 seconds from diagnostic execution timestamps. Wrapper session65626 terminal exit0. Evidence: `w11-identity-threading-initial.txt` and `it_65e75920393f492b.diagnostic.jsonl`. Selected files: parser, inbound identity, existing email inbound, mailbox paging, Gmail mail and Graph mail tests. All14 postflight checks true, exact disposable schema absent. Working database untouched.
- Covered persisted identity encryption, equivalence, opaque case, long IDs, changed-subject threading, conflicting references, unknown parent, content collision, access revocation, transport duplicate receipts, unproven legacy identity, malformed headers and outer rollback/retry.
- Scoped Pint and diff whitespace checks passed before this run. No new browser claim: Build41 is the preceding verified UI, with no identity migration applied to its now-removed runtime or to the working database.
- `tests/Concurrency/It/ItInboundIdentityConcurrencyTest.php` and `tests/Support/It/inbound-identity-concurrency-worker.php` force both workers past the absent-identity lookup before either INSERT. Initial run session73123 terminal1/Pest2, token`it_b9a633d90bb14495`, log`w11-identity-concurrency-initial.txt`. All14 postflight checks true and exact schema absent. Diagnostic reports a JSON decoding error after8 assertions: Laravel's worker exception renderer returned a non-JSON result with exit0. The application log for that exact isolated schema/time confirms MySQL1213 during competing identity insertion. This is a failed race, not a passed duplicate check.
- Correction: top-level ingestion transactions have at most3 deadlock retries. Laravel still propagates a nested transaction deadlock to its outer owner; mailbox processing can retry its retained receipt after rollback. Worker exception reporting now explicitly exits1 with only exception class/code. Corrected race **1 passed, 34 assertions, 178 seconds**, session49622 terminal0, token`it_af333a975fdf41fd`, log`w11-identity-concurrency-retry.txt`; all14 postflight checks true and exact schema absent. Both identical and conflicting-content rounds reached both insertion barriers.
- Expanded six-file suite **81 passed, 405 assertions, 176 seconds**; session50853 terminal0, token`it_3aa9a26c44fc44e3`, log`w11-identity-threading-bounds.txt`, all14 postflight checks true and exact schema absent. The fingerprint uses the entire bounded subject; only its display column is shortened. Malformed or oversized fields quarantine before identity reservation without retaining the body. Header parser rejects bare line breaks/controls even when the whole value would otherwise trim to empty. This supersedes the initial67-test functional run; counts overlap and are not additive. Scoped Pint and git diff --check passed; all10 protected design hashes remain unchanged. Exact source snapshot: `w11-identity-source-hashes.json`.

## Remaining work

The direct-ingestion two-worker race is verified. Actual legacy backfill and contention through nested mailbox claim rollback/retry remain separate acceptance work. Extend canonical intake and comment commands for trusted email provenance; the adapter still contains direct ticket/comment writers. Then address settled/merged lifecycle, strict provider header/sender ambiguity, attachments, loops/bounces, quarantine review/retry and retention. Legacy backfill and full browser quarantine recovery acceptance remain unverified. DP07 live provider consent remains external. Production AI disabled; desktop web only, never resize.
