# W06 draft migration and desktop browser isolation — preparation

Prepared only; migration000007 has not been applied to the existing browser database, no verification server has been started, and no runtime environment/configuration was changed by this preparation. Persistent recovery remains disabled with both retention durations unset. The operational retention decision remains separate from synthetic verification values.

## Existing browser database gate

`w06-draft-browser-migration.php` defaults to `--pretend`. PHP syntax validation and the actual read-only preview both exited0. `w06-draft-migration-preview.json` records migration pending, registry998, no unrelated pending migrations, recovery disabled, both retention values unset,26 tickets,22 comments,12 attachments,5 command receipts and9 delivery records. Rows are hashed in memory; no bodies, credentials, addresses or record values are printed. The preview uses `Connection::pretend()` for the reviewed schema-only `up()` while the surrounding transaction is explicitly read-only; no DDL is executed.

Reviewed inputs for this preview:

- Migration SHA256 `3e21b4fa1de519e1afa1e92b88b7099da8a89d884d6c111279dd67b642185fbe`.
- Gate SHA256 `3c38b46671224255fd55aba3a6fe6b9f81b66e51d28f1b674cce3e9a7c7ae31e`.
- Approval fingerprint `f9479fcda662070b201eddfe3390622e388bc76976a2de2d3adc6b47e79bdb3f`.

Future apply mode accepts only `--apply=<exact reviewed fingerprint>`. It rechecks the source, full current protected rows, design baseline and registry; subsequent browser writes invalidate this fingerprint and require a fresh preview. Root must finish/quiesce the current browser mutations before applying. Exact guards require this checkout, local Herd app host, loopback MySQL database `oblivion_findings_codex_test`, uncached configuration, array mail/cache, sync queue, null broadcast and disabled/unconfigured drafts. It fakes notification/mail/bus and prevents stray Laravel HTTP requests for this console process only. The canonical migration cache event has only the guarded array cache available.

Only the exact migration file is passed to the canonical migrator. During migration, only the captured DDL, reads and migration-registry INSERT are allowed. Before evidence is created exclusively; existing apply evidence refuses a blind rerun. After evidence compares all protected legacy rows, attachment legacy columns, empty draft table, six new attachment column defaults, existing registry rows, exact single registry increment to999 and unchanged design/pending migrations. No roles, grants, operational settings or existing records should change. This additive MySQL DDL is not transactional: partial failure is reported for forward inspection; no rollback/down/reset/seed or unrelated migration is attempted.

Applying this schema to the current local database would still leave recovery disabled. It is not required to activate recovery on that server for verification.

## Recommended separate verification environment

A separate ephemeral loopback PHP server and new random schema can verify persistent recovery while preserving the current Herd runtime. Use the same checkout and current built assets; create only synthetic records. This is the recommended next reviewed setup, not an executed server or an approved retention policy.

Concrete preparation sources now exist: `w06-draft-browser-environment.ps1` (Preview/CreateAndStart/StopAndRemove), shared `w06-draft-browser-runtime.php`, fresh-schema `w06-draft-browser-bootstrap.php`, canonical synthetic `w06-draft-browser-fixtures.php`, normal-request `w06-draft-browser-router.php` and exact-schema `w06-draft-browser-teardown.php`. Root reviewed their isolation architecture. The PowerShell parser and all five PHP syntax checks passed. Only read-only `Preview` ran: source fingerprint `ea304bace9a9c1a130bdf3475e4e0080f7f6b3d6f50b8ffae81a54c30743f901` is recorded with exact hashes in `w06-draft-browser-isolation-source-preview.json`; two independent previews matched. Earlier fingerprints were superseded by provider absence checks and the approved per-process upload limits. No CreateAndStart, database creation, fixture mutation, server start/request or cleanup mode has run. Sources are frozen pending Build6; rerun Preview against that completed asset manifest before reviewed creation. Syntax/preview is not runtime/browser verification.

CreateAndStart requires both a supplied fresh16-hex token and the current reviewed fingerprint; occupied port8766 refuses without stopping anything. The process-local key is newly generated and is never persisted/printed. StopAndRemove requires the original owner fingerprint and unchanged helper sources; a later asset build does not strand cleanup. It validates the recorded PID/executable/router/start time, waits for that exact process to exit, drops only the token-owned schema and checks every resolved descendant before native PowerShell removes the exact token directory. Uncertain creation/cleanup keeps evidence and refuses broad retry. This does not activate or alter the current Herd database.

- Allocate a fresh16-hex token, database `oblivion_it_draft_browser_<token>`, and private directory `storage/framework/testing/it-draft-browser-<token>`. Refuse any pre-existing database/directory, symlink or path outside that exact resolved testing parent. Keep the token ownership manifest under the new private directory; never put secrets in browser-visible evidence.
- Use an unused loopback port such as8766 after an immediate listener check. Bind only `127.0.0.1`; never stop an existing process or alter Herd, hosts, `.env`, `public/hot`, shared build output or cached configuration. Start a hidden Windows helper with process-local environment, capture its exact PID/command/source hashes and stop only that process when verification ends.
- Set `APP_ENV=local` and `APP_DEBUG=false` so real auth/CSRF handling remains enabled. `APP_URL=http://127.0.0.1:<port>`, exact fresh `DB_DATABASE`, loopback connection with no database URL/read-write alternate; a newly generated per-environment application key. Read only the existing local test database connection credential source into process memory; do not print it, invent access or copy provider credentials.
- Set `IT_DRAFTS_ENABLED=true`, `IT_DRAFT_RETENTION_DAYS=2`, `IT_DRAFT_TERMINAL_RETENTION_DAYS=3` **only inside this disposable synthetic environment**. These are test fixture values matching focused tests; no existing organisation record is retained under that choice. Record this distinction beside browser evidence.
- Laravel's installed `Application::storagePath()` honours `LARAVEL_STORAGE_PATH`. Point it to the exact token directory; create its required framework/cache/views/sessions/logs and app/private directories. Thus canonical private attachments, logs and compiled views stay isolated without editing application storage configuration. Use unique absolute token-local `APP_CONFIG_CACHE`, `APP_ROUTES_CACHE`, `APP_EVENTS_CACHE`, `APP_SERVICES_CACHE` and `APP_PACKAGES_CACHE` paths. Refuse any existing config/route cache; verify effective storage and disk roots before the first HTTP mutation.
- Use `SESSION_DRIVER=database` in the new schema, a unique `SESSION_COOKIE` containing the token, null `SESSION_DOMAIN` and HTTP-local `SESSION_SECURE_COOKIE=false`. An array session driver cannot preserve real login across requests. This separate cookie/host must not replace the current Herd session.
- Keep `MAIL_MAILER=array`, `QUEUE_CONNECTION=sync`, null broadcast, array cache and telemetry integrations disabled. Blank provider credentials in process-local configuration; the synthetic schema contains no mailbox, OAuth token, calendar subscription or live integration settings. Add a verification-only guard preventing stray Laravel HTTP requests before request dispatch. Do not run scheduler/worker/collector/provider commands. Canonical ticket/outbox behavior still executes against synthetic records and array mail.
- The hidden PHP process alone receives `upload_max_filesize=16M`, `post_max_size=64M`, and `max_file_uploads=20`; runtime startup and health verify these exact values. Canonical application limits remain five files and ten megabytes each. The higher transport ceilings let desktop browser tests reach application-level size/count validation rather than PHP silently truncating files. No `php.ini` or Herd setting changes.
- Bootstrap the fresh schema from the repository's reviewed MySQL schema dump plus the exact current pending migration sources, following the already-tested isolated importer. The dump contains schema and migration registry, not a copy of the browser's users/content; verify this before import. Do not invoke `Tests\TestCase` directly: its shutdown handler would remove the schema before browser verification. A standalone bootstrap must create once, refuse an existing schema and never execute reset/drop/fresh against an inferred target.
- Seed only the canonical RBAC definitions and bounded synthetic approved requester, technician and restricted actors across synthetic Sites; create fresh tickets for requester intake, public reply/internal note recovery, edit/resolve, scope revocation and file lifecycle. Synthetic users may use the existing repository E2E demo password source; do not print it or alter existing accounts. Default role/provider records and actual policy boundaries must be exercised through normal login.
- The verification router must serve only resolved real files inside this checkout's public directory before dispatching normal Laravel requests, rejecting path traversal and foreign hosts. Record checkout path, token environment, effective schema, storage root and built-manifest/route assets via a safe allowlisted health response or console preflight. An HTTP response from another checkout or stale asset is not evidence.

Before execution, root should review the concrete launcher/router/bootstrap/fixture/cleanup sources and their source fingerprints. After execution, root should verify effective runtime identity and guards and then exercise real desktop auth, explicit resume, session expiry/account changes, Back/Forward/drawer navigation, two-editor conflict, immutable upload retry/remove, discard/expiry and canonical commit acknowledgements. Public/internal comment commit remains a W07 dependency; its current redirect must not be presented as confirmed draft consumption.

Teardown must first stop/wait the exact recorded server process and any owned child workers. Then verify the manifest, exact loopback token schema and resolved token directory still match creation evidence. Remove only that disposable schema/directory using one guarded native implementation. No general sibling pruning, production paths or existing app rows are involved. Capture the existing Herd runtime/schema counts and protected state before/after the separate environment to prove preservation. Browser conclusions must distinguish backend verification from still-pending frontend integration and operational activation.

## Alternative considered

Temporarily enabling2/3-day recovery on `oblivion_findings_codex_test` would mix a synthetic retention choice with existing local organisation records and the root's active browser session. It also requires restoring shared configuration and risks sending the wrong browser journey to the wrong data. Do not use that approach while the operational policy is undecided; a separate process/schema/storage/session provides a reviewable boundary.
