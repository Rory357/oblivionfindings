# W14 technical delivery desktop verification — 12 September 2026

Mappings: W14/F06/B04/E13 and W13 Operations audit. W14 and E13 remain In progress. These are local synthetic browser results, not provider or release acceptance.

## First owned run

- Checkout: C:/Users/steph/Herd/oblivionfindings, main ba8339bca plus the subsequently identified access-refresh correction.
- Runtime token33eaee60878c45e8, fingerprint ec85676b883f5da244226ac91d2bd27a97dcc8903e0a44314ed437b229349752; loopback8766, disposable schema oblivion_it_draft_browser_33eaee60878c45e8. Bootstrap65569 terminal0, server45156. Array mail, sync queue, normal CSRF, isolated storage/session, cleared providers and SSR disabled.
- Current DOM script app-3uQjQ9dl.js matched manifest72cb583b2a5ac87d6a8b4cd2fc93ab1f3e77ad0a7097c1a1d5c6af9ae132479c. Runtime identity is saved in w14-technical-browser-runtime-identity.json. Direct browser navigation to the JSON identity endpoint returned ERR_BLOCKED_BY_CLIENT, while normal /login and /it/setup loaded. The identity was read independently through the local HTTP health endpoint; this was not a browser page success.
- In-app tabs13 and14 owned by this run; user tab2 and all Chrome tabs preserved. No browser resize; DOM reported2222×1216. Existing dark theme retained. Dialog screenshot was inspected in tool output; no saved screenshot file is claimed.
- Fixture preparation used actual MonitoringObservationIngestor and Fleet heartbeat/offline paths and their canonical source/IT jobs. A temporary fixture-only ticket-insert exception caused rollback and a persisted failed IT delivery, then was disabled before browser use. No production mock or operational source configuration was used. Before evidence: four failed deliveries, one attempt each, no associated ticket;19 existing fixture tickets.

## Observed journeys

- Technician3 saw Device13 deliveries/3 failures and Fleet5/1. SiteC's Device delivery was excluded. Histories displayed source acknowledgement separately from IT outcome, preserved attempt counts and safe diagnostic classifications.
- Keyboard Enter opened Device delivery8. Request one retry stayed disabled until the explicit checkbox was selected. Keyboard submission recorded a completed IT outcome with attempts2/limit2; focused feedback announced the allowance separately from completion. A fresh review no longer offered another retry.
- Fleet delivery5 opened through the shared actions menu and recovered through the same consent flow. After Escape and the close animation, focus returned to the stable Fleet row. The initial three-second locator wait expired before the review response arrived; later inspection showed a valid loaded review, not an application failure. Subsequent waits explicitly allowed15seconds.
- Two tabs reviewed Device delivery9 before either submitted. First retry succeeded. The second request displayed the stale-review explanation, concealed the prior details and required Refresh delivery outcome. Fresh review showed the existing completed outcome at2/2 with no second retry control.
- Header search "delivery failed" returned only Device delivery10 while retaining total13/one failure. Fleet history showed a clear filtered-empty message with full total5/zero failures. Counts were not replaced by filtered row counts.
- Restricted technician4, with IT management but no source-read grant, saw neither Device/Fleet rows nor counts. Submitting technician3's old open review after normal sign-out/sign-in as actor4 concealed both sources and the dialog; focus moved to it-device-delivery-unavailable. Read-only reconciliation confirmed delivery10 remained failed with one attempt, no ticket and no retry audit. This proves account change/current source access, not timed expiry or same-account permission revocation in the browser; independent-process tests cover the latter backend boundary.
- A bounded25second lock held only synthetic Device delivery10. Browser Request one retry followed by Stop waiting showed the explicit unconfirmed/cancellation explanation. After the lock released, the late result did not overwrite that state. Refresh delivery outcome then showed Technical work created at2/2. Lock67676 terminal0; no record mutations by the lock helper.
- Final reconciliation: four canonical open tickets20–23, all Site1/queue1/team1/assigned technician3/owner3. Each delivery has exactly one retry audit attributed to actor3 and one completion audit; each has two lifetime attempts/limit2. Fixture ticket count19→23, not more. See w14-technical-browser-final.json; intermediate before/retried/stale-access snapshots are retained.
- One inspector query used an incorrect assignment column and failed read-only with PDOException. Corrected to the model's assigned_to_user_id (and owner_user_id); the failed result remains w14-technical-browser-retried.json, successful correction in w14-technical-browser-retried-recheck.json. No production code or records changed to make that query pass.
- Browser console check on tab14 returned no warnings/errors. These checks do not imply full application browser coverage.

## Browser-discovered correction and next verification

Confirmed defect: after access loss, Refresh delivery access used router.reload({preserveState:false}). The installed Inertia implementation forces preserveState:true in reload, retaining the local concealed state even after technician3 signs back in. The real two-account browser sequence reproduced this. Replaced it with a fresh GET of the current URL using preserveState:false/preserveScroll:true/replace:true. The UI regression also asserts that requesting refresh does not expose old rows before a fresh page is received.

First focused selection passed35 tests/3files/5.06s; one operations-suite path was incorrect and therefore not selected. Corrected full selection passed61/4files/5.86s (w14-technical-access-refresh-ui-recheck.txt). Scoped lint terminal0, no diagnostics. Full TypeScript77830 terminal0 with no diagnostics. New build21008 is running; the corrected action's actual browser retest remains pending. Do not treat first-run browser evidence as verification of the unbuilt correction.

Owned tabs13/14 closed. StopAndRemove39346 terminal0 and independent postflight terminal0 prove the exact schema and owned root absent. Working Herd environment unchanged. Next run the corrected access-recovery sequence against current assets. The next fixture adds13 real acknowledged source events with undispatched pending IT intents, so authorized Device history has26 rows and spans two pages. Actual pagination verification, complete W14 source capability coverage, timed expiry and final release acceptance remain open.

## Corrected-build run

- Build21008 terminal0/3m53s, app-DGWKkeTu.js, manifest53ded253117c1391e126e8a20f6ef1894336e6273c58de24e493186b00858ebb. Fresh bootstrap86846 terminal0; token b26e8827895f418d, fingerprint3387b2838c972e0140efb69f79653fb9e91fdc7920d862848e579692f7fa2883, server44560. Runtime identity and actual DOM script matched. The same fake-communication/normal-CSRF/isolated-storage checks passed. Independent Governance source changes were present, including its policy registration in AuthServiceProvider; preserved, not claimed as part of IT verification.
- Owned tabs15/16. DOM reported1063×856, reflecting the current browser pane; no viewport or window-size action was performed. The preceding run was2222×1216. These are naturally observed desktop sizes, not mobile-emulation acceptance.
- Device history displayed26 total/3 failures/13 pending,25 of26 on page1. Keyboard Next page navigated to the canonical page2 URL and showed the remaining single row. Header search from page2 reset pagination to page1 and returned13 pending intents, retaining complete source totals. Searching for delivery failed returned the three failure rows and a correctly separate Fleet history.
- Opened Device delivery8, changed to restricted actor4 in the second tab through normal sign-out/sign-in, then refreshed the old review. Fresh restricted view and old account-bound review concealed all delivery counts/history; the old dialog closed and exposed Refresh delivery access.
- Signed back in as permitted actor3. Keyboard activation of **Refresh delivery access** now restored the three current failure rows and complete26-row totals while preserving the existing search URL. The concealed-state message/button disappeared. This reproduces and verifies the correction that failed in the first browser run; no manual page reload was used to make it pass.
- Reopened delivery8 with the keyboard. Retry remained disabled before consent. One explicit retry completed at2/2, with focused honest allowance/result feedback and no further retry control. Escape returned focus to the stable originating row after the dialog closed. Current dialog screenshot inspected in tool output; no saved screenshot claimed. Console returned no warnings/errors.
- Final read-only evidence w14-technical-access-browser-final.json: exactly one new open ticket20, Site1/queue1/team1/assigned3/owner3, one actor3 retry audit and one system completion audit. Other three failed fixtures remain at one attempt/no ticket/no retry audit. All13 pending history intents remain acknowledged/pending/zero IT attempts/no ticket. Total fixture tickets19→20. Extending the inspector to report pending history changed only its read-only projection; application source and runtime fingerprinted helpers were unchanged during this run.
- Owned tabs15/16 closed. Cleanup64863 terminal0 and independent postflight terminal0 confirm the exact schema/root absent; working Herd environment unchanged. Final18-source/10-design hashes are recorded in w14-technical-browser-final-source-hashes.json. Only the intentional read-only inspector projection changed after build; every application source and protected-design hash matches. No owned runtime, test, build, proxy or row lock remains.

The delivery operations/retry slice now has local automated and desktop lifecycle evidence, including the discovered correction. This does not complete W14/E13: remaining source capability adapters, operational/provider dependencies, timed-session-expiry coverage and final requirement-by-requirement release acceptance remain open. Next dependency-ready implementation is the canonical non-availability monitor fault/recovery adapter described in w14-source-capability-inventory.md.

