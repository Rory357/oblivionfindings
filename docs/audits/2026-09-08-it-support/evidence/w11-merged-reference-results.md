# W11 merged-reference verification

11 September 2026. Scope: the merged-reference portion of W11/E10 using existing canonical merge, reference resolver and conversation commands. Whole W11 remains incomplete.

## Current code and test result

The resolver already follows canonical merge destinations and rechecks viewer access along the chain. The ingestion transaction locks the resolved destination, refuses a destination that merged in the meantime, and rechecks reply authority. This slice adds missing two-provider evidence; no production merge implementation change was required by the completed backend cases.

`tests/Feature/It/ItInboundMergedReferenceTest.php` performs canonical merges and polls synthetic Microsoft/Gmail messages separately. Ten scenarios per provider cover an old subject reference, a changed-subject parent, two merge steps, converging ancestry, source/intermediate/destination audience changes, conflicting subject/ancestry, database refusal of a nonexistent destination, and refusal of a deliberately corrupted legacy cycle. Each injects a first acknowledgement 503, retries after cooldown, and replays the inbox without another reply/ticket. Original receipt ticket IDs remain historical evidence; new reply commands target only the authorized surviving record.

- Final run45099, token `it_454dd0cc35b848d4`: exit 0, **20 tests / 364 assertions / 186 seconds**. `w11-merged-reference-tests-final.txt` and token diagnostic JSONL. All 14 postflight checks passed, including exact disposable-schema absence.
- Initial run38890 / token `it_bd60ae78a0a34b25`: test-only query used an incorrect column; SQL42S22, 0 assertions, Pest exit2. Corrected to the canonical mailbox-scope lookup. All14postflight/schema absence.
- Intermediate run55080 / token `it_f6c4d8ffb3164917`: 8 passed / 144 assertions before the fixture's nonexistent destination correctly hit FK1452. The revised case asserts that protection and an unchanged valid destination; no database constraint was disabled. All14postflight/schema absence.

## Browser preparation

The fingerprinted existing mailbox fixture supports a staged merge journey through `w11-merge-browser-scenario.php`. `before_merge` is allowed only on its pristine, exactly owned synthetic inbox and presents message44 to each provider. `after_merge` requires both replies acknowledged and the original ticket actually merged into the named survivor; it then presents old-subject45, changed-subject46, duplicate47 and unauthorized-sender48. Message45 injects first-ACK503 for each provider. The helper changes only synthetic provider state, never ticket records or database configuration. `observe` is read-only and records canonical ticket/receipt/comment/command/audit evidence plus provider attempts.

New helper syntax and invalid-token rejection were checked. The helper is included in both owned-runtime source-fingerprint lists. Initial runtime `f4ac36e257194bd0` was removed after the staging report selected an incorrect receipt column, before inbox mutation; independent postflight confirmed schema/root absence. The corrected helper reads `it_mailbox_connection_id` and performs report queries before staging.

Current owned runtime is `ba36125e97ac40d8`, PHP35200, fingerprint `b7f8d2ceed678c2dca39916ba846bd19f48c3fd8e46f70f22cfa10ae970e2abd`. Bootstrap77259 exited0. This continuation rechecked the live endpoint and process: exact checkout/schema, array mail, sync queue, CSRF bypass false and fixture providers. Current assets are `app-CE5taJm1.js` (manifest `f2834ff231cc6ce5521e7838ac2dfd1c62a089bb29203bf4bd1e27fd1b2ce0dc`). Previous tab28 was absent; owned tab29 resumed the existing signed-in runtime, preserving user tab3. No resize or provider configuration change.

The settings operator polled original message44 for each provider. Technician then used the actual two-step merge dialog, selected IT-000001, entered an internal reason, reviewed the two moving public messages and retained evidence, checked the acknowledgement and committed IT-000007 into IT-000001. The dialog confirmed the saved result; Open surviving ticket showed both replies and the preserved-original link. `w11-merge-browser-staged-after.json` independently confirms source closed/merged into1 and original receipts still linked to7.

Both post-merge UI polls completed processing, displayed Needs attention and one pending acknowledgement each, and disabled polling for the provider cooldown after the injected first-ACK503. `w11-merge-browser-before-ack-retry.json` proves seven tickets, six email comments/commands/comment audits, two merge events, duplicate suppression and unauthorized-sender quarantine. All six public comments belong to survivor1; original receipts remain historical on7.

After the real cooldown, Reload current state enabled polling. Google and Microsoft retries each changed the displayed state to Last poll completed, zero awaiting acknowledgement and one retained quarantine. `w11-merge-browser-final.json` confirms all ten receipts acknowledged, six comments/commands/comment audits unchanged, source closed and target open. Each provider read messages44–48 once; only45 was acknowledged twice. Duplicate47 produced no extra comment. Unauthorized48 has no ticket link and remains sender_unauthorized. The original two receipt IDs still reference7; later processed/duplicate receipts reference1. No extra ticket was created.

Requester `w06-requester@demo.test` navigated to `/it/tickets/7` and reached `/it/tickets/1?merged_from=7`; the actual desktop accessibility tree contained all six expected replies and no internal-note/work controls. Inline screenshot inspection showed the rendered survivor header/conversation at the existing browser size (no resize; no saved screenshot file claimed). The preserved-original link opened `/it/tickets/7/original`, showing the original report, closed status, read-only conversation and a survivor link. Unrelated requester `w06-other@demo.test` received rendered404 on both survivor1 and original7, then returned to `/it` and saw only their permitted IT-000003. These are bounded role checks, not the whole module acceptance gate.

Owned tab29 was closed, user tab3 preserved. Cleanup2746 exited0; `w11-merge-browser-cleanup.txt` and independent `w11-merge-browser-postflight.json` prove exact schema/root absence. No runtime, build, test or cleanup remains active. No live-provider acceptance is claimed.

## Remaining boundaries

DP04's operational choice for post-window replies is still unanswered. Existing governed reopen behaviour is preserved. W12 must also verify real outbound RFC Message-ID ancestry: the current inbound resolver consults inbound identity records, and a provider's opaque transport ID must not be assumed to be a reply header identity. Full E10 and the module release gate remain open.

Next dependency-ready inspection: complete loop prevention across canonical IT notifications and ingestion. `ItEmailContent::assertHumanMessage` already refuses Auto-Submitted values other than no, null Return-Path and delivery-report MIME. Current application search finds no outbound Auto-Submitted/X-Auto-Response header construction, and `TicketRepliedNotification::toMail` builds a normal MailMessage. `InboundEmailIngestor::isExactConnectedMailboxPrincipal` is an authorization exception for an approved connected principal, not loop detection. Verify framework/custom transport behaviour before choosing the shared insertion point; preserve legitimate human staff replies. This is an identified integration boundary, not a demonstrated live mail loop. No additional test run or production change was made for it in this continuation.
