# W15 canonical catalogue recovery contract — backend verified, UI pending

Confirmed browser defect is recorded in w15-catalogue-actor-binding-browser.md. The actor-bound server prevented a write, but the old Inertia Dialog retained private fields underneath a generic403 overlay. Do not mark that browser criterion passed.

Implemented after exact cleanup: StoreCatalogRequest requires originating account identity; the existing ItCatalogSubmissionService now exposes recovery under the same actor mutex as submission, uses current canonical ticket/provisioning access and returns the existing immutable submission. A committed original result remains recoverable after unpublishing. A missing result returns not_found only for a currently discoverable published item; recovery never triggers fulfilment or notifications. The controller supports typed201 create/200 replay or recovery JSON, private/no-store, with actor/item/request/version/canonical result identity and no private form values. Existing Inertia redirect callers remain supported until the requester wizard adopts the new command transport. Routes and RecoverCatalogSubmissionRequest are in the current source bundle; no new record system or migration was added.

Actual backend result: full catalogue suite46571 terminal0;32 diagnostic Test Passed, no Failed/Errored,32 Finished; all14 postflight checks passed and exact tokenit_dabcd94bb2a24f3b schema absent. Earlier29-case suite remains evidence for the preceding binding-only source, not a second disjoint test count. New cases verify both outcome types, bound JSON identity, no private values, no-store, original result after withdrawal, wrong actor/item denial, missing-key validation and unknown recovery without writes or notifications. Existing replay/current-result-access/field/Site/publication regressions all remain in the full suite.

The new frontend catalogue-submission-contract parser strictly binds account/item/request/original version and canonical result URL/reference. It rejects malformed, cross-account, cross-form, stale-version, external-URL and HTML/flash bodies. Unit23 tests/1file passed1.51s; scoped ESLint and Pint/source whitespace checks passed. Parser is not yet wired into the requester form; no whole-app type/build/browser claim for this subsequent code. Ten source and10 unchanged protected design hashes are captured.

Next implementation constraints:
- Use WizardShell/ReviewCard/ReviewRow/confirmed WizardSuccessPane and existing confirmation patterns. Conceal and clear the old form on account/access denial; expired sessions need explicit same-account recovery. Remove the generic Inertia error overlay by consuming the typed JSON command transport.
- Retain only original actor/item/version/UUID metadata for uncertain outcomes in browser storage. Private fields/files stay in canonical drafts or RAM. Never replace an unknown UUID to make a retry appear successful.
- Add cancellation of an uncommitted identity using the existing ItTicketCommandReceipt operation/metadata and shared actor mutex; do not create a second catalogue ledger or mutate immutable submissions. Cancel-after-commit must return the original result, not undo a ticket or workflow. A not_found read alone is not a cancellation tombstone: an original request may still arrive later. Test submit/cancel races with actual workers.
- Extend canonical ItTicketIntakeService for trusted published catalogue service/work type/approval defaults, requested-for and attachments; current manual createTicket writer and controller post-commit notification sends still require replacement with canonical audit/outbox/storage behavior. Keep lock order consistent: pre-lock involved users deterministically before item locks, rather than introducing item-to-user inversions with publisher/requested-for accounts.
- Finish requested-for permissions, attachment fields, durable draft behavior and complete provisioning fulfilment/failure/retry/cancel/reversal, with original published/template version retained. W15/E14 and the full plan remain incomplete.

No live test job, owned browser runtime or disposable schema remains. Mainf7ad55b5e is the last local checkpoint; this combined actor/recovery work is uncommitted. Preserve independent Governance and Site calendar changes, protected design files, working DB and user tabs. No public push/provider/production AI work was performed.
