# W07 Build9 desktop verification

9 September 2026. Root owns this browser evidence. This record distinguishes source checks, compiled assets and actual browser results. W07 and the full release gate remain open.

## Source gate

- Combined UI:148 tests in9 files passed,9.21s, process16686 exit0 (`w07-build9-combined-ui-tests.txt`). Covers Thread/composer/watchers/delivery/drawer/refresh hooks/ticket page/Setup workspace.
- Full TypeScript: process87944 exit0 (`w07-build9-types-final.txt`). The earlier `w07-build9-types.txt` failure remains preserved; its unsupported Testing Library selector options were corrected.
- Storage caller verification: standalone3/76 plus affected Feature37/334 passed, wrapper0 and exact schema cleanup independently observed. See `w07-attachment-caller-integration.md`. These are separate test groups, not browser proof.
- Protected design files match the recorded baseline (`w07-build9-protected-design-check.json`). Desktop web only; no viewport resizing.

## Build and environment

Build9 process63310 completed exit0 in3m18s (`w07-desktop-build9.txt`). Entry `assets/app-lVvmYFAZ.js`; manifest SHA256 `833efcfb44cd3a07a77c86b7310593603983239359cab7c91947b91ba3d73aa0`; no `public/hot`. Build8 is superseded. Old token12c6cbe82a04eb7d was stopped and its exact schema/storage independently confirmed absent.

Fresh reviewed fingerprint `4ebb2ccd42acb0fec221b4bc5daf00ea808f29d1fc273a7c6542590de8fdfb20`, new token `f55d5818cbf5499b`, expected schema `oblivion_it_draft_browser_f55d5818cbf5499b`. Start process54756 is running; final launch/readiness and actual browser asset checks remain pending. Review is preserved in `w07-browser-build9-environment-preview.json`; launch output in `w07-browser-build9-start-f55d5818cbf5499b.txt`. All six helper hashes and migration000010 match reviewed frozen sources. No simultaneous PHP test import is running.

The fresh environment must use the reviewed existing six-file helper, a new explicit random token and final asset/migration fingerprint. It must retain real CSRF, separate synthetic schema/storage/key/session, array mail, absent/blocked providers and synthetic-only2/3-day draft retention. Herd configuration and retention remain unchanged.

## Pending browser journeys

Execute `w07-build8-browser-checklist.md` on Build9, despite that checklist's historical filename. Record actual actor, ticket, actions and reload proof. Prior Build7 journeys remain historical and do not certify new source.

- Eligible watcher add/cancel/remove, reload, keyboard confirmation, self watching and restricted participant denial.
- Pristine and successive public/internal drafts; honest commit versus notification state; audience separation.
- Draft-preserving bounded delivery refresh, session/account change, recovery and scoped delivery-log navigation.
- Current attachment caller integration through normal protected upload and authorization paths.

The checklist above is historical planning; actual results follow. Scanner engine/outage/quarantine policy, unknown reservation reconciliation and retention remain separate open gates. Cleanup Operations and subsequent title/task changes are outside Build9 asset acceptance.

## Actual desktop results, 9 September 2026

Launch54756 completed exit0: schema `oblivion_it_draft_browser_f55d5818cbf5499b`, private root `storage/framework/testing/it-draft-browser-f55d5818cbf5499b`, PID39548, registry1002 through migration000010. Actual `/it` DOM loaded `assets/app-lVvmYFAZ.js` at the existing1072×856 viewport. No resizing or mobile testing. All actors and records below belong to this disposable isolated environment. Provider-accepted labels were produced by the array transport, not real communications.

- Login initially timed out, with one actual server500/max_execution_time30 in Laravel Database Connection426. Normal reload recovered. Guarded read-only health evidence subsequently measured PDO22.48ms and metadata0.64–1.71ms with no active owned-schema process. No historical cause established and no timeout changed. See `w07-browser-build9-db-health-after-retry.json`.
- Technician3 opened public ticket1 from Overview into its drawer. Empty composer had no false unsaved/resume warning. Keyboard Space/ArrowDown/Enter selected watcher4; Add/Enter opened the approved confirmation. Escape cancelled without membership change and returned focus to Add after the closing animation. Explicit Add watcher persisted; drawer close, full-page handoff and reload retained watcher4.
- Technician3 explicitly committed public A then public B as comments1/2. The next-public-reply action opened an empty focused composer. Delivery Refresh retained the exact unsent B text and did not add another comment. Each committed public reply showed its separate array-transport accepted count1 and explicit acceptance-versus-delivery wording. The canonical IT-authored reply recipient is the requester; adding a watcher does not change that existing policy.
- Normal logout in a second browser tab followed by first-tab Refresh redirected to login and removed ticket/private text from the displayed DOM. Signing back in as3 offered metadata-only Resume; explicit Resume restored B, then explicit submit committed it. This proves the actual redirect journey, not the separately tested inline401 branch.
- Technician3 saved a separate internal draft Q. Normal second-tab account change to restricted technician4 followed by first-tab Refresh showed “Your signed-in account changed”, concealed conversation/private draft DOM and required Reload page. Reload under4 offered only that actor's empty fields, no Q or original actor's Resume.
- Restricted4 explicitly stopped watching; confirmation and full reload showed zero watchers. On sensitive participant ticket2, internal-note/watcher-management controls were absent. Direct unapproved ticket3 returned404. A private-work display defect remains: the redacted participant view still renders a zero required-work meter and Work navigation, implying knowledge it lacks. Root/W08 will remove that projection when work access is withheld.
- Returning as original technician3 to ticket1 required explicit internal Resume. Q was retained with a stale-version blocker after watcher removal; Add note stayed disabled. Review current ticket was read-only, then explicit “Use version, keep draft” retained Q and enabled submission. Explicit Add note committed internal comment3 without an email-delivery block. The “Add another internal note” control was visible but its next-note journey remains pending.
- Keyboard Enter on comment1's Review delivery log reached `/it/setup?tab=operations&delivery_comment_id=1`, showing exactly one filtered canonical record, one attempt and zero retries. New cleanup catalog data appeared through the backend, but its newer frontend renderer was not in Build9 and is not browser verified here.
- Requester1 explicitly navigated to ticket1: public A/B remained visible, comment meter2, no Q/internal/work-management controls and no delivery groups for other authors' public replies. Direct access to comment1's scoped Operations log returned403. An earlier back/reload capture had no comments and no captured URL, so it establishes no full-ticket reload result or defect.
- Requester1 typed/autosaved R (“W07 Build9 requester follow-up. Keep this text when a file is rejected. Final R.”). The documented filechooser API in two bounded calls selected an84-byte inert text fixture named `w07-disallowed.exe`. **Confirmed defect:** the saved-draft path accepted it, stored attachment1 and displayed a protected download link. R survived. The direct composer already rejected that extension. The file was not posted. Root is correcting shared filename/detected-content validation and will retest rejection/correction; oversize and valid-file journeys are still pending.

Visual inspection confirmed the full-ticket long title was cropped by the action/search row at the existing desktop viewport. Root added opt-in title wrapping to shared PageHeader and enabled it only for the ticket workspace; Setup's Home breadcrumb now uses `/dashboard`. These fixes are source-only, scoped Prettier/ESLint0, and need the next build/browser check. No protected design source changed.

## Live resumption state

Server/token/fingerprint above remain live. Primary in-app tab4 is requester1 on ticket1 with saved R and the rejected-policy fixture attachment1 still staged; do not post that file. Secondary tab5 shares the current session. Review actual DOM before continuing. Remove attachment1 through its own draft control, then retest disallowed/oversize/approved files with retained R. Root must append post-fix results and run the guarded read-only conversation evidence helper. Stop/remove only the exact owned environment with the original reviewed fingerprint, then independently prove schema/root absence. Do not reuse removed tokens or alter the six running helper files.

## Final checkpoint superseding live state above

The fixture environment is now **stopped and removed**. Process7604 exit0; original fingerprint used. Independent PHP postflight exit0 confirms exact schema/root absence in `w07-browser-build9-cleanup-postflight.json`. Herd configuration/providers were unchanged. Tab4 now holds historical DOM; do not treat it as a live server or reuse the token.

- Removed staged invalid attachment1 through the normal control; R remained. Corrected server validation rejected a fresh `.exe` selection with the actual extension error and no file link.10MiB+1 selection rejected locally and retained R. Build9 kept an obsolete previous extension error alongside that later size message; source correction clears only obsolete attachment errors and awaits the next build.
- Approved uppercase`w07-approved.TXT`73B staged as attachment2. Explicit Add reply committed R as comment4, no duplicate. Full reload retained public A/B/R, meter3/1file, and the canonical link. Requester2's copied attachment2 URL returned404. Positive file opening via a hidden in-app tab returned `net::ERR_BLOCKED_BY_CLIENT` and left tab6 blank; no successful browser download/preview claimed. Authorized backend download cases passed independently.
- Original technician3 signed in normally and explicitly reopened ticket1. Start a new draft for Internal note produced an empty editable field. Explicit note T committed comment5, with no internal email block. Keyboard Enter on Add another internal note produced an empty, enabled, focused visible textarea; count remained5 and no automatic submit occurred.
- Reviewed read-only helper executed twice, emitting IDs/hashes/statuses only. `w07-browser-build9-conversation-after-requester-reply.json`:4 comments/4 receipts, ticket version8, next party`it`, last public comment4/requester. `w07-browser-build9-conversation-final.json`:5 comments/5 receipts, version9, same next party/last public comment, zero watchers. Only public technician comments1/2 have array-accepted outbox entries, one each. Internal notes did not move Awaiting IT or send notifications; the removed watcher received no new outbox intent.
- A further visible defect was recorded: the untouched ticket-edit draft generation showed a stale-version notice above the hero despite having no proposal. Root's source-only recovery change keeps that empty generation quiet without altering its version/capability checks; the warning appears when work exists. Its focused regression and next-build browser check remain pending.

Build9 verifies the specific interactions above. New title wrapping, redacted-work navigation, cleanup Operations renderer, attachment frontend corrections and W08 task UI remain unbuilt here. Scanner/quarantine/outage policy, retention, positive browser download, W09 settlement lifecycle and final E/release coverage remain open.
