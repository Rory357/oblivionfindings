# W08 immutable task completion and reasoned repair

Latest evidence,10 September07:28 NZ: corrected History Feature12/145 passed in the frozen combined batch; standalone history concurrency1/69 passed with exact owned-schema absence. Build11 desktop verification covered two immutable completion generations, dependency invalidation, explicit reasoned cancellation/restoration and blocked resolution. Current browser-discovered summary/history/corrective-link fixes passed58 UI cases and21/193 affected backend cases; Build12 browser retest remains pending. See `w08-readiness-and-personal-work-results.md` and `w08-browser-build11-results.md`. Earlier pending/failure checkpoints below are retained as provenance, not current status.

10 September 2026. **Implemented in source; behavioral verification pending.** Root confirmed Build10 server/schema/storage teardown and independent absence before releasing this slice. No existing working database or browser helper has been changed. This is the single-organisation canonical task lifecycle, using current approved-Site work access and `it.manage`.

## Source contract

- `database/migrations/2026_09_09_000011_create_it_work_task_completions.php`: append-only completion child, unique task/sequence and composite same-task current pointer; nullable task approval binding. History restricts deletion of its task/approval and a lossy migration rollback. No historical backfill or actor foreign-key nulling. Frozen migration SHA256: `7b7c5e44a638322025943e67be5b69e41488d41ab18788d4c097510c0ff969a1`.
- `app/Models/ItWorkTaskCompletion.php` and narrow `app/Models/ItWorkTask.php`: immutable completion records with `source`, `recorded_at`, actual nullable `completed_at` and original actor scalars, definition, exact prerequisite completion bindings, exact approval row, note/evidence. Current pointer is a compatibility projection, never history replacement.
- `app/Domain/It/Services/ItWorkTaskCompletionService.php`: canonical mutation helper. New completion snapshots actual current definition/evidence. Legacy capture retains only extant evidence/known actor/time and the definition observed at capture; prerequisite and approval provenance remain unknown. Capture does not invent a new completion event or separately advance ticket version. Schema readiness is checked once per helper/connection, including an actionable missing-storage failure.
- `app/Domain/It/Services/ItWorkTaskService.php` and `app/Domain/It/Data/ItWorkTaskInput.php`: same versioned command namespace and receipt flow. Complete records history/pointer/current fields atomically; reopen retains/captures history before clearing current fields. Actual cancellation, restoration, required-flag changes, dependency-set changes and approval-binding changes require a reason. Restore permits cancelled→pending only. Cancellation/restoration have explicit private event types; existing generic update audit action remains compatible and captures before/after bindings. Unchanged patches remain no-ops. No descendant state is rewritten.
- Root-owned `ItWorkTaskReadinessService::guardAction` is called after staged start/dependency changes and after locked legacy prerequisite capture before completion. It owns current/transitive verdicts, warnings and settlement consistency; this slice adds no second evaluator.
- `app/Domain/It/Services/ItWorkTaskCandidateService.php` and `app/Http/Requests/It/ValidateItWorkTaskCandidateRequest.php`: permission-only RAM proof checks current/pending `approval_id` plus every historical `bound_scopes[].approval_ids` list against exact same-ticket approval rows before recovery. Proof returns no private request reason or proposed content.

DTO names agreed with root/W02: task `current_completion_id`, nullable `approval_id`; completion `id`, `task_id`, `sequence`, `source` (`command`/`legacy_snapshot`), `recorded_at`, nullable `completed_at`, nullable `completed_by_user_id`, `recorded_by_user_id`, `task_definition`, `prerequisite_completions` (`[{task_id,completion_id}]` or null legacy), nullable `approval_id`, `completion_note`, `evidence`. Source distinguishes known-empty current bindings from unknown legacy provenance. Root owns scoped/paged history projection and current graph fields; W02 owns the corresponding lifecycle UI.

## Prepared verification

`tests/Feature/It/ItWorkTaskHistoryTest.php` has twelve focused cases: immutable generations/replay; truthful unknown legacy capture and nonmutating GET; one legacy prerequisite capture; reasoned cancellation/restoration/repair; same-ticket exact approval; history write failure rollback/retry; audit failure rollback; restrictive deletion/current-pointer/down guard; missing-schema recovery; actor deletion; approval RAM scope denial; and transitive generation invalidity/recompletion. These are prepared assertions, not reported passes.

`tests/Concurrency/It/ItWorkTaskHistoryConcurrencyTest.php` runs alone outside Feature transaction traits and reuses the existing guarded task worker. It prepares real duplicate completion, duplicate reopen and reopen-versus-settlement races, plus an actual outer postcommit failure during legacy reopen and exact receipt recovery. Parent/worker schema and notification guards remain unchanged. No workers have run at this checkpoint.

PHP syntax and explicit-path Pint passed for the owned source/test files. Local sandbox path resolution initially prevented a PHP invocation; the successful syntax/Pint checks used the approved installed Herd PHP binary. No database tests, providers, full frontend types or build ran for this slice yet.

## Precise resumption and remaining scope

Own source is frozen pending root's serialized PHP slot and root-owned readiness/presenter freeze. First run the new history Feature file with the existing task/command/candidate files through `run-isolated-it-tests.ps1`; wait for actual results and exact-schema absence before any edits. Then run the new standalone history file alone. Preserve failures and fix only after each snapshot settles. Record actual logs/counts/schema postflight here before claiming verification.

Root subsequently froze its runtime and released the Feature slot. The six-file batch started in session `69309`, token `it_797e3800c2bd4c80`: new History, existing Task/TaskCommand/TaskCandidate, root's seven-case Readiness and existing WorkTransition. All wrapper preflight isolation checks passed. Log: [w08-task-history-feature-tests.txt](w08-task-history-feature-tests.txt), diagnostic `it_797e3800c2bd4c80.diagnostic.jsonl`. Actual tests and independent postflight remain pending at this checkpoint. Backend/migrations/test sources are frozen for the entire run; no standalone process is active.

### First Feature result

The batch finished with Pest exit2 (wrapper exit1) and an independently verified absent `oblivion_it_support_test_it_797e3800c2bd4c80` schema. Console test output was buffered away, so the result is taken explicitly from the existing metadata-only PHPUnit diagnostic: **66 Test Passed events, 1 Test Errored event, 67 Test Finished events, 730 summed assertions**. The only error is `LogicException` at `ItTicketApproval.php:49` from `ItWorkTaskHistoryTest.php:237`: the synthetic RAM-history fixture attempted normal model deletion of a rejected approval, now correctly forbidden by W01's immutable approval model. No product assertion failed in this snapshot.

After the entire process/postflight settled, that fixture alone was corrected to a query-builder deletion of its exact synthetic ID, explicitly simulating an unavailable historical selection. The production model's deletion guard is unchanged. W01's independent read-only review found no additional history-loss, identity or transactional defect; that review is not additional test evidence. The corrected history file and standalone concurrency proof remain pending their own guarded runs.

The user decision on blanket legacy settlement blocking remains pending with root. Current completed status is preserved with explicit unknown-provenance warnings; known current-generation mismatch is invalid. No expiry, retention, blanket legacy blocking, descendant rewriting, new approval authority or provider policy is selected here. Approval lifetime/personal work/catalogue integration and current-assets desktop history/cancel/restore acceptance remain required W08/W15 work. W08 is not complete.

### Granular first-batch result (diagnostic mapping)

The metadata tracer hashes `className::methodName`. Matching those hashes to the installed Pest `P\\Tests\\Feature\\It` class naming and `Str::evaluable` method construction gives the following exact case counts, including dataset expansions:

- `ItWorkTaskReadinessTest`: **7 passed, 72 assertions**, diagnostic sequences 353–401; 01:11:43–01:11:48 NZST on 10 September, a **5-second event window**.
- `ItWorkTransitionTest`: **13 passed, 95 assertions**, sequences 405–496; 01:11:48–01:11:56 NZST, an **8-second event window**.
- Existing task cases: `ItWorkTaskTest` **5/73**, `ItWorkTaskCommandTest` **19/195**, `ItWorkTaskCandidateTest` **11/151**, all passed; event windows 4, 14 and 8 seconds respectively.
- `ItWorkTaskHistoryTest`: **11 passed / 1 errored, 144 assertions**, sequences 10–92. Its 178-second event window includes the first schema preparation (168 seconds), so it is not a feature execution-only duration.

These are one-second-resolution diagnostic event windows, not a Pest console timing report; the console summary remained buffered in this run. All six files total **66 passed / 1 errored, 730 assertions** across a 217-second execution event window. Exact owned-schema absence was independently confirmed by the wrapper postflight. The corrected History fixture is assigned to W01's next frozen Feature batch with approval tests; the standalone task-history concurrency test remains prepared and unrun pending the next explicit serial slot release.
