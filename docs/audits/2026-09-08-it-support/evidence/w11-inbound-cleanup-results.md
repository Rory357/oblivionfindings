# W11 independent accepted email-copy recovery

11 September 2026. Backend slice and the updated restricted desktop Operations explanation verified. Full W11, E05/E10 and final release acceptance remain incomplete.

The existing `it:retry-attachment-cleanup` command now recovers accepted receipt-owned temporary copies independently of polling or a surviving mailbox connection. The canonical storage service selects one shared bounded batch across direct rollback intentions and eligible inbound copies; it never doubles the configured limit. Selection prioritizes never-attempted work then older retries, with one attempt per selected file. Existing per-file/receipt locks recheck current state before deletion. Final ticket/comment attachments, quarantined content and unclassified reservations are retained. Explicit inbound selection arguments are bounded and validated.

CLI and the existing Operations UI use the same aggregate evidence service. Accepted pending copies contribute to pending cleanup; a cleanup flag with missing/unaccepted ownership contributes to reconciliation. No filename/path/receipt detail is added to the public DTO. Existing IT management plus audit permission checks remain mandatory for counts. Updated UI copy and the existing recovery runbook explain the expanded scope.

## Actual verification

- Standalone real-commit test session49488, token `it_6df6172b13b54fbd`: terminal0, one test, 53 assertions, 171 seconds. Tests bounded failure/retry fairness across both sources, deleted mailbox connection, canonical accepted final bytes/clean scan retained, quarantined copy retained, invalid-owner cleanup flag reported without deletion and unclassified bytes retained. Uses fake HTTP/storage failures/scanner in an isolated schema, not live providers or a production scanner.
- Regression session31444, token `it_74c89cfc00db45b8`: terminal0, 29 tests, 358 assertions, 202 seconds. Unit command/result/readiness branches and real-MySQL Operations permissions, combined counts, revoked grants, concealed private evidence, missing schema and canonical scheduler recording. Both database runs passed all 14 postflight checks and confirmed their exact schema absent.
- Initial restricted-shell regression launch could not resolve installed Herd PHP, before tests. Explicit installed-runtime execution succeeded; this is not counted as a test failure or a passing run.
- Existing Operations component test: first run9passed/1failed because the old copy expectation included “may still belong”; updated the expectation to the displayed wording. Retry10passed, 2.95 seconds. Both logs retained. No new test merely duplicates the copy implementation.
- Focused Pint test passed (eight PHP files); Prettier check passed (two TSX files); `git diff --check` passed.
- Current assets built successfully, session74895 terminal0, 3m37s. Manifest `36a47bd0d34f8b80b0442bfd43cee08039557dd89a1ed0e168bd4cacdd7ab000`. An initial runtime Preview during build failed because the manifest was temporarily absent; no runtime was created. Fresh post-build Preview succeeded.

## Browser and resumption

Owned runtime bootstrap session91918 terminal0, token `131366fe77e64bc5`, fingerprint `13db20928531e849c4a2d804186c4c7b9f59372dc7bbcaf6b193c3b4ddddb050`, PHP PID12072. Exact loopback8766, disposable schema only, array mail, sync queue, no mailbox provider fixtures; working database untouched. Read-only runtime endpoint evidence is saved in `w11-inbound-cleanup-browser-runtime.json`. Browser navigation to that diagnostic endpoint reported `ERR_BLOCKED_BY_CLIENT`; the normal application route loaded successfully. No browser security setting was altered.

Actual Codex in-app browser tab23, 11 September approximately06:35–06:37 NZST:

- Technician `w06-tech@demo.test` (IT management, no audit grant) signed in through the normal login form and reached `/it/setup?tab=operations`. Cleanup explanation included accepted email copies, retained quarantine and uncertain reservations. “Storage records available” was separate from “No verified check”. Counts were withheld with an explicit access explanation, not replaced by zeros.
- Ctrl+End revealed the complete cleanup section. Inline screenshot was visually inspected; no screenshot file is claimed. DOM read showed1235×856 window,1220px client width and1220px document width: no horizontal overflow, no viewport override/resizing. JS `app-Bjh3zw_6.js`, CSS `app-CE9g3ZyJ.css` match the current build; manifest hash recorded above. Captured warn/error console list was empty at this technician checkpoint (not a claim about later expected403 requests).
- Shift+Tab focused API, Enter navigated to `?list_view=cards&tab=api`; Tab+Enter returned to Operations. ArrowLeft on the Operations tab did not move focus. Existing shared `PageHeaderRail` renders tab roles without arrow-key handling; retain this observation for W27 keyboard review rather than declaring full tab-pattern acceptance.
- Logged out, then signed in as `w06-requester@demo.test`. The intended Setup redirect showed403; direct navigation to the same copied Operations URL also showed403 Forbidden. Navigating to `/it` recovered to the requester’s five synthetic requests, with Setup absent from navigation. No permission grants or business records were changed for this journey.
- Closed owned tab23; the three existing user tabs were preserved. Exact owned teardown session24983 terminal0. Independent `w11-inbound-cleanup-browser-postflight.json` confirms exact schema and private fixture directory absent; no working environment change. No active browser/runtime/test/build/import/cleanup remains.

The browser verified copy, layout, keyboard Tab/Enter and restricted access. Authorized nonzero cleanup counts, storage failure/retry and deleted-connection recovery were verified in the focused backend tests, not this empty browser fixture. Provider attachment browser acceptance remains open.

Remaining W11 includes real-browser provider file ingestion/download/denial/recovery, quarantine review/retry/retention and scanner readiness, actual crash/uncertain-commit and contention, settled/merged policies and full E10 review. Review inbound cleanup audit-failure fairness: its transaction currently throws after an audit error, rolling back attempt timing and aborting the remaining batch; missing physical bytes are still idempotently recoverable, but this differs from direct-intent failure timing and requires a focused fault test. Mechanical accepted-copy recovery does not prove those criteria. Production AI disabled; migrations17/18/19 unapplied to working DB; no real communications/provider configuration/deploy.
