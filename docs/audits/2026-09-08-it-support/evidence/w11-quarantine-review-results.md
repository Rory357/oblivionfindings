# W11 quarantine review and retry — 11 September 2026

Status: implemented; focused backend/UI and the browser journeys described below verified. This is a bounded F03/B01, F05/B02, E10 recovery slice, with E05 file protection and W26 runbook integration. It does not close W11 or the release gate. No active test, build, browser tab, runtime, server, import or cleanup remains.

## Implementation

- Migration000020 adds review version and pending retry time to the canonical `it_inbound_emails` ledger. The working database is not migrated. No duplicate quarantine, ticket, file or credential store is added.
- `ItInboundQuarantineReview`, its settings controller and routes use the existing `integrations.manage_secrets` permission and current provider/account/mailbox boundary. The bounded 25-record cursor list exposes decision metadata only; no sender/body/subject/ticket/provider IDs or file URLs. Copied records from another mailbox are denied.
- Retry requires a current connection and record version, no active lease or provider cooldown, confirmed prior acknowledgement, a proven original identity/content fingerprint, and an eligible sender/access failure. Unsafe files and other security decisions cannot be manually released. A required audit and persisted pending state precede the “requested” response.
- `InboundEmailIngestor` retries the same immutable receipt through the canonical intake/reply commands. It rechecks current access, refuses altered identity/content and retains original evidence. Unchanged sender denial stays quarantined. Required outcome-audit failure rolls back canonical work and leaves the retry recoverable. Original received time is preserved.
- The typed settings review panel uses existing Card, Alert, Button, StatusBadge and ConfirmDialog primitives. It supports loading, empty state, pagination, confirmation cancellation, pending/unknown outcome, stopping the browser wait, fresh-read recovery and access loss. A changed connection clears its prior review list. No model execution, live provider configuration or retention deletion is enabled.
- `docs/runbooks/it-mailbox-quarantine-recovery.md` describes the lifecycle and explicit DP07 boundaries. The existing read-only browser reconciliation helper records review versions and audit events without message contents.

## Actual focused results

- Backend92694, token`it_61c130cd79004a0f`: terminal0,72tests619assertions213s across new quarantine review, existing inbound identity, canonical lifecycle and mailbox settings tests. All14 postflight checks passed and exact disposable schema was absent. `w11-quarantine-review-tests.txt` and the token diagnostic JSONL are authoritative.
- UI initial/final pre-browser:15passed (two files),4.03s/3.81s. Initial sandbox toolchain resolution failure is preserved separately in `w11-quarantine-review-ui-tests.txt`; permitted toolchain runs are separate logs.
- Browser discovered Cancel returned focus to BODY. Fixed the panel’s `onCloseAutoFocus` to return to the originating retry button, or its status/content when submission disables the button. Added a focused regression: all15 tests passed in3.72s (`w11-quarantine-focus-ui-tests.txt`). Browser recheck on the rebuilt assets confirmed focus returned to “Request retry for inbound record 42”.
- First build23581 terminal0,3m22s. Final build81031 terminal0,3m16s; manifest914586e1e6900cc46fdefa469e5088eb16ee2a0417b6cbdcfb456462895920ac, app-CTl5PeJJ.js/app-CE9g3ZyJ.css. Final focused Pint, Prettier and diff checks passed. `w11-quarantine-review-checkpoint.cjs` verified13source hashes, all10protected design files unchanged, backend/postflight results and browser ledger/file/cleanup assertions; snapshot `w11-quarantine-review-source-hashes.json`.

## Actual browser results

Owned runtime7574d29be8994d0d, PHP3604, bootstrap36909 terminal0; initial fingerprint1a8fdcb5a81e72d7d40f9556b083ec7c2fa1e64998e0dab49f0d5c5f81ec0a34. Correct checkout/current first manifest, isolated schema/storage, array mail/sync queue, no CSRF testing bypass and synthetic provider/scanner fixtures were verified. Owned tab26; three user tabs untouched. Desktop1235×856, no resize, no horizontal overflow.

Normal settings login and both 43-message initial polls completed their recorded outcomes. Quarantine lists showed10/provider and refused retry while the connection cooldown applied. One premature keyboard poll attempt correctly found its button disabled; after the actual delay and reload Microsoft scanner/ACK recovery completed. Record42 retry confirmation used the approved modal with initial focus on Cancel. Keyboard cancellation submitted no retry (read-only `w11-quarantine-browser-after-cancel.json`); its focus restoration defect triggered the fix above.

The reviewed asset refresh preserved the same owned records, keys, sessions and synthetic providers; final fingerprintc7211e9002be45ca9af65399f4e9df0080c330c617b041f946a101a5c63e1f24. Rebuilt DOM loaded app-CTl5PeJJ.js. At1235×856 (client/scroll width1220with scrollbar), keyboard cancellation returned focus to the original record42 button, with no resize or horizontal overflow. The confirmation and final review layout were inspected using actual inline browser screenshots; no saved screenshot file is claimed.

Microsoft42 and Gmail85 each moved to “Retry requested” only after the server acknowledged the persisted request. The existing poll then retrieved the original message again. Reload showed both still quarantined for `sender_unknown`; no account approval or role/site grant was made. Provider switching concealed the previous provider’s record list. Both unsafe-file records remained without a release control. Requester sign-in followed normal logout/login; an initial textbox wait found the public sign-out landing page, then its observed login route was used. Direct `/settings/it-mailbox` navigation as `w06-requester@demo.test` returned403 Forbidden.

Read-only final reconciliation: records42/85 each review version2, pending marker cleared and provider acknowledgement recorded; exactly two request audits and two completion audits. Both provider message42 reads and acknowledgement attempts equal2. Canonical totals remain the expected58tickets/4email replies, all86receipts acknowledged (31processed/2duplicate/10quarantined per provider). Fourteen file records/eight remaining private objects, all existing hashes match; both retried source files remain ready/clean/private with their original content hashes, and quarantined body previews remain empty. Actual changed-access success, stale request, audit failure, altered message and pagination cases are backend tests; interrupted client request/access-loss concealment are UI tests, not claimed as separate browser fault injections.

Owned tab26 closed; this agent closed no original user tabs. Final inventory contained original tab3. Cleanup74255 terminal0 stopped PHP3604 and removed only the exact owned schema/root. Independent `w11-quarantine-browser-postflight.json` confirms both absent. No active handles remain. Working database migrations17–20 remain unapplied; no real provider/scanner settings, communications, production deletion or deployment occurred.

## Remaining acceptance

Actual provider/scanner authorisation, operational retention/key custody/restore ownership (DP07), lost-provider-message recovery, true worker crash/uncertain commit/concurrent-claim stress, governed related work for settled/outside-window replies (DP04), and complete E10/release review remain open. Successful browser download completion remains unverified from the preceding file slice; this work does not change that evidence. W27’s previously observed shared rail arrow-key gap remains open. Full W00–W27 goal is incomplete.
