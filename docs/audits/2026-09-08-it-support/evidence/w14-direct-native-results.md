# W14 direct native technical intake — 12 September 2026

Mappings: W14 / F06 / B04 / E13. Implementation is in progress; this is not full package or browser acceptance.

## Current implementation

- The existing Control Room processor records a bounded operational routing decision on the canonical Signal. Configured info/low/medium native IT faults go directly to IT; high/critical and missing operational assessment retain Control Room coordination. Sender severity alone cannot bypass coordination. Matching rules now have an explicit ID tie-breaker after priority.
- `MonitoringWorkRouting` verifies the Monitor and immutable start/current observations through `MonitoringAvailabilityEpisode`. Source labels alone are insufficient. Recovery already observed before source delivery produces direct technical verification work without a stale operational alarm.
- The existing Device outbox freezes that decision alongside source identity. Delayed delivery preserves the accepted decision across policy edits and rejects mutated stored routing or changed canonical Device/Site scope. Suppressed source signals receive an explicit ignored IT outcome.
- The existing listener creates direct tickets through the canonical priority matrix and routing service, persists canonical Device links and frozen evidence, and retains failure/retry/audit behavior. Evidence replay for a settled native episode records history without reopening it or creating another ticket. A later distinct episode can create new work.
- Migration000027 makes the existing evidence alert FK nullable while preserving its restrictions and immutable history. Rollback refuses to discard direct evidence. It is applied only to wrapper-owned disposable schemas; working migrations17–27 remain unapplied.
- The evidence presenter requires current ticket, Device and Site access for direct evidence; existing alert evidence retains Control Room permission checks. The existing card presents direct source evidence explicitly instead of inventing an alert. No production AI, provider or communication changes.

## Verification in progress

- UI: 2 tests passed, 1 file, 26.85 seconds; runner51624 terminal0. `w14-direct-native-ui-tests.txt`.
- PHP formatting, full TypeScript runner40769 and scoped ESLint runner14930 passed, terminal0.
- Initial backend runner31970/token `it_09fd119d53a94ec5`: **71 passed, 6 failed, 544 assertions, 196.47s**, terminal1; all14 postflight checks and exact schema absence passed. These failures exposed a real typed-rule defect: SecurityDevicesSignalSeeder persisted a type FK without a code, and scopeForSignalType treated every null code as a wildcard. An unrelated critical rule therefore outranked the configured offline rule. The matcher now resolves a retained type FK to its actual code and reserves wildcard behavior for rules with neither identifier; new seeder rows include both identifiers. Existing rule configuration is not rewritten. Added regression covers retained FK-only rows and an explicit configured wildcard.
- Recheck runner72278/token `it_0dcbeb0625b94d82`: **77 passed, 1 failed, 606 assertions, 195.42s**, terminal1/all14 checks/schema absent. All new direct cases passed. The remaining dependency-recovery fixture expected operational alerts under the default medium policy; it now explicitly configures high severity to preserve that test's operational-alert purpose. Matching also now rejects contradictory code/FK identifiers rather than allowing either to override the other; a new regression covers this conservative fallback.
- Final runner44150/token `it_b1a4a311de984711`: **79 passed, 614 assertions, 194.28s, terminal0**. All14 postflight checks and exact schema absence passed. `w14-direct-native-final-tests.txt`.
- Expanded real-worker runner32538/token `it_d9cf3acdd083494a`: **1 passed, 272 assertions, 191.49s, terminal0**. Direct and urgent branches both cover competing duplicate deliveries, interruption before and after commit, competing explicit retries, and two distinct source outboxes for one episode. All14 postflight checks/schema absence passed; `w14-direct-native-worker-cleanup.json` independently confirms no owned barrier remains. `w14-direct-native-workers.txt`.
- Build87731 passed **3m40s, terminal0**; app-BnOtWrxV.js, manifest a69df113659426996552d1b29389292d2ca87025d44632e35ed4dff80cd24fa8. `w14-direct-native-build.txt`.
- Browser harness has opt-in MonitoringFixtures with guarded direct, recovered, urgent and other-Site canonical work through real ingestion and fake queues/notifications/providers. First bootstrap47122/run703b38b1a99f4d16 failed because the general RbacSeeder did not seed the required Device permission. Read-only metadata confirmed the exact missing key and successful migrations; the fixture transaction rolled back. Original fingerprint cleanup19075 passed, and independent postflight proved schema/root absent. Evidence: browser-inspection.json, browser-failed-cleanup.txt, browser-failed-postflight.json (all prefixed w14-direct-native-).
- Corrected browser-only fixture invokes existing SecurityDevicesPermissionsSeeder and grants the intended synthetic roles Device module entry plus view. Bootstrap32417/runcb57a07bc03846b1 completed terminal0. Real desktop direct/recovered/urgent/source-link/other-Site/cover/requester/keyboard/denial-recovery journeys passed against app-BnOtWrxV.js. The JSON diagnostic route alone was blocked in-browser; read-only HTTP identity confirms exact isolation/assets. See `w14-direct-native-browser-results.md`. Owned tab closed, cleanup92562 terminal0 and independent postflight confirms schema/root absent. External Chrome untouched.
- Browser-discovered copy corrections are implemented and browser verified: automatic source label, explicit recorded Device-health distinction and neutral linked-record footer. Existing evidence/context7 tests and workspace29 tests passed, full TypeScript/scoped ESLint passed. All10 protected design hashes match. Build4357 passed3m24s; fresh owned rund8c24b736921a0ef verified current app-Co7HIszW.js through normal sign-in, direct ticket evidence and retained human-requester attribution. Owned tab closed; cleanup31714/postflight remains the next environmental step. Full details in browser-results.md.

## Changed source files

MonitoringAvailabilityEpisode, new MonitoringWorkRouting, SignalProcessingService, SignalRule, SecurityDevicesSignalSeeder, ItMonitoringDeliveryService, ItTicketLinkService, CreateOrUpdateMonitoringTicket, MonitoringIncidentEvidenceService, MonitoringIncidentEvidencePresenter, migration000027, MonitoringAvailabilityEpisodeTest, DependencySuppressionTest, monitoring-incident-evidence-card.tsx and its new test. The existing ItMonitoringDeliveryConcurrencyTest now verifies both direct and urgent delivery, including two distinct outboxes for one episode. Browser-only harness changes: w06-draft-browser-environment.ps1, runtime/bootstrap PHP, and new w14-monitoring-browser-fixture.php. These extend canonical services and records.

## Next

Both browser runs are now clean: final cleanup31714 terminal0 and independent postflight confirms exact schema/root absence. Next implement Fleet-to-IT capability and operator create/link handoff, delivery failure/retry controls and all remaining mapped lifecycle/release acceptance. The direct/native regression and browser slice above is locally verified; full W14/E13 are incomplete. Native Control Room recovery retains existing auto-resolution behavior; assess the complete plan's recovery-verification criterion during the handoff review. No full release claim.
