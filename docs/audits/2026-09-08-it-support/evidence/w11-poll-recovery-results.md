# W11 mailbox failures and polling recovery — implementation checkpoint

11 September 2026 NZ. Single organisation, existing approved-account permissions and canonical mailbox/ingress records. No production provider call, new connection configuration or real message. F05/B02/E10 initial transport/recovery scope; whole W11 remains in progress.

First boundary run76883 completed exit1 with49 passed/1 failed/201 assertions according to diagnostic events. Console test summary was buffered; no crash or PHP fatal recorded. Failure location ItGraphMailTest.php96: two overlapping Http::fake registrations in one existing test left its first403 response active, invalidating the second malformed-response assertion. Split them into independent cases. Graph-only rerun25819 exit0,4 passed (exact assertions/time in w11-provider-failure-run-summary.json). Both wrappers finished all14 postflight checks and their exact schemas were absent. These runs overlap; do not sum them as independent coverage. Later recovery implementation below requires its own rerun.

Current source adds migration2026_09_10_000015 for configuration_version, last_poll_attempt_at, last_poll_failure_code, consecutive_poll_failures, next_poll_at, poll_claim_token and poll_claim_expires_at on ItMailboxConnection. No working-database migration was applied. Historical successful timestamps are not fabricated. Claim token is hidden, credentials retain existing encrypted casts.

ItMailboxPollState serializes claim acquisition through the canonical row, denies overlap, permits expiry recovery, records safe failure/backoff and separates last attempt from successful poll. Retry starts at60s and doubles to3600s maximum for ordinary temporary failures; provider Retry-After can extend it. Invalid provider intervals above one year are rejected, preventing date overflow rather than clipping them into a misleading short delay. Expired/revoked authorization waits for approved reconnect. Error-status connections are eligible again when their persisted retry time permits.

ItMailboxPollingToken rechecks current claim/version/expiry before reading credentials and serializes refreshed-token persistence with reconfiguration. Poll performs local ingestion inside the same connection ownership transaction, then acknowledges via the guarded token after commit. A stale worker cannot publish completion/failure, write refreshed credentials or continue after disconnect. Claim duration20minutes exceeds the current bounded25-message detail/read batch; renewal occurs before each ingestion. Complete paging and per-message acknowledgement ledger remain open, so no unbounded-run guarantee is claimed.

OAuth reconnect and mailbox address changes increment configuration_version and clear prior ownership/success timestamps; disconnect removes the canonical connection under lock. These mutations now write canonical integrity-sensitive audits without secret/address payloads. OAuth failure responses no longer echo raw exception messages. Poll-now only acknowledges a request and explicitly describes skipped connections, not successful ingestion. Full address-access verification, form concurrency/recovery and settings UI remain open.

Current verification: formatting and diffcheck exit0. Seven-file isolated run76423 tokenit_4cd2edcfd0464a10 terminal0:76 passed,343 assertions,192s from diagnostic event metadata. All14 pre/postflight checks passed and exact schema was absent. Tests cover typed failures/refresh validation, persisted cooldown, expired ownership, invalidated credentials, acknowledgement retry without duplicate ticket, disconnect during provider read, reconnect/configuration audits and existing provider/connection behaviour. All10 protected design files unchanged;21 source/test hashes recorded. Actual independent-worker run66060/tokenit_f8082a930d9945d2 also terminal0:1 test,30 assertions,182s; exact schema absent and all14 postflight checks passed. Competing claims admitted one owner; refresh against the actual disconnect controller did not recreate a removed connection. Ready/attempt barriers, running-worker assertions and timestamps establish overlap behind the held row lock. Both workers and their token-prefixed barrier files are cleaned in finally. No live runtime/test process remains.

Next: complete paging/acknowledgement checkpoint, identity/threading through existing intake/reply commands, quarantine/attachments/loop handling and proper settings controls. Frontend should reuse approved PageHeader, connected rail, StatusBadge, shared date formatting and compact ConfirmDialog; provider detail should use the available settings width. Project safe failure vocabulary for legacy errors rather than exposing historically stored raw provider messages. Further OAuth callback expiry/offline-token validation belongs with the remaining configuration lifecycle. No W11 browser or DP07 live-provider acceptance claimed.
