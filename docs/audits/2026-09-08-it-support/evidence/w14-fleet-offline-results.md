# W14 Fleet offline publication — local results

11 September 2026. Mapping: W14 / F06 / B04 / E13. This slice is implemented and locally tested; the package and release gate remain incomplete.

Changed: `app/Jobs/DetectFleetOfflineDevices.php`, `app/Services/Fleet/FleetSignalService.php`, `app/Events/FleetSignalEmitted.php`, `tests/Feature/Fleet/FleetOfflineSignalAtomicityTest.php`. No migrations. Fresh candidates are rechecked under a row lock; snapshot, canonical source and outbox commit together. Episode keys survive retries. Queue dispatch and the domain event wait for the enclosing commit.

Final guarded run: **12 passed / 77 assertions**, terminal 0, token `it_1b2520ca4de24107`, all 14 isolation postflight checks passed, exact disposable schema absent. [Raw results](w14-fleet-offline-focused-final.txt). Six new cases cover repeat sweeps, outbox failure and retry, outer rollback/commit, a heartbeat arriving after the initial scan, strict cutoff and a later episode. Six existing SafetySignalDeliveryRecovery cases also pass. Pint passed; [final source hashes](w14-fleet-offline-final-source-hashes.json).

Earlier failures are retained: [first run](w14-fleet-offline-feature.txt) exposed EventFake::forget being a no-op; a scoped injection flag fixed the test. [Broader recheck](w14-fleet-offline-feature-recheck.txt) recorded 29 passed / 6 failed / 18 pending / 1,444 assertions. One new test used a stale snapshot instance; refreshing it fixed the fixture and the final focused run passes. Five FleetTelemetryIngest failures remain unresolved: geofence signal, trip start/stop, consented location update, consent fixture validity, and missing Control Room signal after late consent withdrawal. No baseline attribution or full telemetry pass is claimed.

Separate-worker verification subsequently passed **1 test / 24 assertions**, terminal 0, token `it_0b9163d578b744bd`, all 14 postflight checks passed and exact schema absent. [Raw concurrency results](w14-fleet-offline-concurrency.txt). Both real workers wait after selecting the same online snapshot: a parent heartbeat makes both skip, while a stale episode produces exactly one signal/outbox and one queue/domain-event publication at transaction level zero. Workers and exact owned barrier files are cleaned in finally blocks. No RefreshDatabase transaction or fake event dispatcher conceals the commit boundary. Queue transport is deliberately faked and external communications disabled.

Additional files: `tests/Concurrency/It/FleetOfflinePublicationConcurrencyTest.php` and `tests/Support/It/fleet-offline-concurrency-worker.php`; Pint passed. [Six final source hashes](w14-fleet-offline-concurrency-source-hashes.json).

Telemetry follow-up corrected five obsolete consent fixture blocks using `tests/Support/AuthoritativeConsentFixture.php`, explicitly asserting initial validity and binding missing client Sites. Two delayed-delivery cases now provide a canonical Fleet source and call the actual outbox handler through the container while the test transaction remains open. Production consent checks were not changed. [Complete telemetry recheck](w14-telemetry-fixtures.txt): 39 passed / 2 failed / 3,156 assertions, terminal 1, all 14 postflight checks/schema absence passed. All five earlier failures cleared and the previously pending cases ran. The two remaining failures were exact lock-order contracts that omitted DeviceFieldOwnershipService's existing recheck of the already locked device. Updated expectations retain exact order and additionally prove both locks address the same device.

The detector now rejects invalid timeout configuration before any state/source change. Ten negative cases include missing, zero, negative, booleans, fractional, empty, malformed, overflow and array values; an environment integer string preserves its configured cutoff. No new operational timeout or severity was selected.

**Final combined verification: 64 passed / 3,321 assertions**, terminal 0, token `it_14c28734f5e548da`. All 14 isolation postflight checks passed and the exact schema is absent. [Raw final results](w14-fleet-publication-final.txt), [Pint](w14-fleet-publication-final-style.txt), [seven final source hashes](w14-fleet-publication-final-source-hashes.json). This covers all FleetOfflineSignalAtomicityTest, FleetTelemetryIngestTest and SafetySignalDeliveryRecoveryTest cases. The earlier separate-worker result preceded timeout validation; the positive-cutoff publication path is otherwise unchanged. No active test process or owned preview remains.

Fleet recovery publication, durable IT outcomes, source privacy and browser handoff acceptance remain outstanding. Existing working database and providers were not modified. Full W14 and the release gate remain incomplete.

## Configured Fleet source availability

The next confirmed prerequisite defect was an inactive configured source being accepted by Fleet ingestion. `SignalProcessingService::ingestFromFleetSignal` now locks and validates the existing `queclink_fleet` source inside the canonical outbox transaction, before signal projection or alert creation. Missing/inactive source raises the existing SafetySignalUnroutable outcome. The canonical failure report and explicit recovery operation are reused; no new queue or provider configuration is created.

Two new SafetySignalDeliveryRecoveryTest cases verify missing/inactive configuration, durable failure with no downstream signal/alert, failure visibility, exclusion from automatic retries, and one successful publication after explicit fixture repair/retry. Production source configuration is untouched.

**Latest affected regression: 49 passed / 3,239 assertions**, terminal 0, token `it_e55c4f6308594bbe`; all 14 isolation postflight checks passed and exact schema absent. Covers the complete SafetySignalDeliveryRecoveryTest and FleetTelemetryIngestTest files. [Raw results](w14-fleet-source-availability.txt), [Pint](w14-fleet-source-availability-style.txt), [nine current source hashes](w14-fleet-source-availability-hashes.json). Full W14 remains incomplete; the next adapter's unresolved episode, re-pairing, Site and out-of-order constraints are in ../w14-source-capability-inventory.md.
