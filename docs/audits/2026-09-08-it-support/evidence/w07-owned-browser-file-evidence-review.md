# Owned browser reply/file evidence helper — prepared, not executed

Prepared `w07-owned-browser-file-evidence.php` for the root's explicitly reviewed synthetic browser token. The historical hardcoded conversation helper is unchanged. The new helper requires exactly two CLI arguments: the 16-hex token and its 64-hex approval fingerprint. Root supplied the current candidate token `b972632227dd4b91` and fingerprint `ec720fd147c4e1c28335d6d1a9ae65e28a83190dae8c4e89ee4844832dc639c9`; neither is hardcoded into this generic helper.

Read-only source review:

- The guard prefix is byte-identical to the current `w08-owned-browser-task-evidence.php` after normalizing line endings: exact checkout, token syntax, resolved non-link owned root/files, owner database/token/root/fingerprint, ready state, synthetic-only marker and current asset manifest. The existing test credentials are read without being emitted; PDO connects only to the derived loopback synthetic schema and verifies `DATABASE()`.
- Every database statement is a bounded SELECT. Each record query permits fewer than 1,000 rows and fails closed at the limit. There is no Laravel/autoload/bootstrap, transaction, session-setting statement, filesystem mutation, provider call or file-content read. Observations are explicitly sequential, not claimed atomic. Session/system timezone and TIMESTAMP epochs prevent local SQL rendering from being mistaken for UTC storage.
- Ticket 1 is required. Comments are restricted to that ticket; only `browser` / `ticket.comment` receipts are returned, with opaque original identity, canonical references and optional consumed-draft revision. A missing stored `state` key is retained as missing: committed comments need not populate it.
- Attachment rows are restricted to the ticket, its comments or its own canonical draft slots. `it_ticket` and `it_ticket_comment` match the registered morph map; `App\Models\ItTicketDraft` is the current unmapped canonical draft class. Metadata includes database-recorded size, MIME, uploader/reference IDs, SHA256 of the original filename, draft storage/cleanup fields and the associated direct storage intent's state/revision. No original filename, storage path, file contents, encrypted draft content or content fingerprint is emitted. No scan status is invented; the current attachment model has no implemented scan-state column. A consumed staged file can legitimately have null draft state and no direct-storage intent.
- Reply outbox rows are limited to ticket 1 and `ticket_replied`. Counts distinguish all stored attempts from current leaf attempts using the existing retry chain. No subjects, recipient addresses, notification bodies/context, provider IDs or raw error messages are emitted. An accepted status is the canonical stored transport result, not proof of real-provider delivery.
- Column checks used the current attachment/comment/delivery models and migrations 000007/000008/000010. Missing required schema fails safely rather than falling back or modifying it.

Verification performed: PHP `-l` exited 0 with no syntax errors; static guard-prefix comparison returned true; whitespace check was clean. The first sandboxed attempt could not resolve the Herd executable, so syntax checking was repeated with the authorized parse-only invocation. The helper itself has **not** run and no database was accessed during preparation. Recorded sizes do not prove physical byte integrity or download access; those remain separate browser evidence.

Frozen helper SHA256: `42b0b9c4abcd7905e3031ddd58749f657710a1c85329309ff05b9731151ad80f`.

Resumption: root reviews and executes once against the exact current owned environment before teardown, then records its actual result. Do not reuse an old token after cleanup or claim the expected 88-byte/76-byte rows were observed by this preparation.
