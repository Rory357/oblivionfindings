# Knowledge full document and media slice — 13 September 2026

Implementation task: `01a09836-ba6c-72f2-8473-a91fa040721d`; saved checkout on main, starting HEAD `527aff2ac76d467019b30da4513531aad0fdcbc7`. The full goal remains active. W21/W22 are not complete or browser Verified.

## Latest focused editor browser and activation checkpoint

### Actual upload ceiling corrected and verified

The later host-only PHP activation supersedes the failed20MiB check below. The reviewed candidate appends `[HOST=oblivionfindings.test]` with upload_max_filesize20M/post_max_size64M to PHP8.4's loaded ini, preserving every original byte. Original SHA256 `535248604c8aebf4c22beede834e21f54ac7ca122bbefd44174e13a65a0fcffa`; applied SHA256 `53ee3464fc0df597b70d8e98e690e49a4238116b33a44d2d9e8b1eb078a45da8`. Private encrypted DPAPI backup roundtrip verified before write; PHP8.4 alone refreshed through Herd's existing restart endpoint, response success. Separate CGI probes checked this host20M/64M and an unrelated host2M/8M. This uses PHP's documented [per-host configuration sections](https://www.php.net/manual/en/ini.sections.php); the loaded version-specific file and HTTP restart requirement match [Herd PHP settings guidance](https://herd.laravel.com/docs/windows/technology/php-settings). No global upload default, Nginx configuration, database reset or provider configuration changed.

Actual HTTPS ordinary author234, current `app-Bfnoeiwx.js`, unchanged1280x720 viewport: uploaded harmless `knowledge-20MiB.pdf` successfully through real ClamAV as file2 in unpublished synthetic draft10. Editing remained in Files with the new version visible. A20MiB+1 selection was rejected and left the existing two files intact, but PHP returned generic upload-error wording; source follow-up adds immediate accessible size guidance before submission. Word file1 opened its bounded text preview. PDF file2 displayed an explicit unsupported-browser fallback, and Download original produced a real download event. Embedded PDF rendering is not claimed. Console error list empty for this retry tab. Tab1 closed; all6814 source/build/app-config baseline entries unchanged. See `knowledge-herd-upload-postflight-20260913.json`. The deliberate synthetic draft and files remain unpublished and retained; no existing operational article was edited or removed.

The user now wants a full Visio-style interactive mockup before integration. Separate task `01a09932-577b-7e90-95f9-fb43eebecda5` owns the artifact-only design, official Visio feature inventory and user review. Production integration requires the user's later approval and shared ownership coordination. The present builder does not satisfy full Visio parity.

After browser cleanup, oversized-file preflight was added to the Knowledge picker: accessible specific20MB guidance, no POST for rejected selection, and error recovery when a valid replacement is chosen, including exact20MiB. Focused editor11tests passed5.90s; later combined navigation/editor24tests across3files passed7.86s. Scoped lint and formatting pass. These source changes await the next coordinated build/browser window; they are not part of the earlier actual-host asset. Shared register navigation was separately consolidated in response to the user's report of two entries for the same register. Knowledge source is frozen for Ticket's next reserved window after all owners checkpoint.

### Earlier activation and browser evidence

Final build22455 completed successfully in5m22s: `app-Bfnoeiwx.js`, manifest `9cabac05b4a75659821f563cee704e278b92b886689f162493b035928f495697`, public/hot absent. Actual `https://oblivionfindings.test` ordinary existing author234 created unpublished synthetic draft10, saved two labelled diagram shapes and a connector, exercised dirty-diagram upload protection and save-and-continue to Files, and uploaded `w22-synthetic.docx` as file1 through real ClamAV. A20MiB PDF failed at PHP startup with post_max_size8M, before routing or database writes; no PDF file was attached. Public/.user.ini does not apply to Herd's external `server.php` front controller. Maximum-size support is therefore not yet accepted on this host. Tab3 closed; all6,814 source/config/build baseline entries remain unchanged. Vendor has the subsequent navigation slot; upload configuration investigation is read-only until that slot ends. The existing nine articles remain untouched; the additional draft10 is deliberate synthetic browser data and remains unpublished.

Local activation completed after exact runtime cleanup and the separately authorized Overview integration. Config gate23f04f9478594a08d8e133000e5d20f17b97f226e89b51c0ad78b4e4ac5f9022 returned0, encrypted backup roundtrip verified. Only IT_INBOUND_MALWARE_SCANNER_BINARY/IT_INBOUND_MALWARE_SCANNER_DATABASE and public/.user.ini20M/64M were changed. Exact pending000002/media and000003/relationship repair applied through gateb6305106b159e75f1bee9fb67bc5454a9fcb2df34ae6c8b2e233b37cdafc25ce, terminal0/invariants_verified=true. Registry1045→1047; all9articles' original-column digest remained c3545f4324e844422049ba200d71d7357179f390c416e289a8d86231b385fb3a. Users236/sites26/tickets26/interactions2 and existing revision/working-copy digests stayed identical; unrelated registry preserved. No revision000001 rerun, reset, seed, down migration, or production action. Private encrypted row/schema snapshot roundtrip verified. This proves preservation for the applied local change; it does not relabel the corrected but unrerun Feature JSON-key-order assertion as a new suite pass.

Incoming main86dee9068 preserves all217othertrackeddirtyfiles and unrelated untracked work. The approved Operations/header changes to DESIGN.md, PAGE_HEADER_STYLE_GUIDE.md and PageHeader are individually recorded in the Overview local-sync protected-change-report. The original11-file Knowledge design baseline is retained. A private reviewed manifest checks the9unchanged guides, the2explicitly approved new guide hashes, and exact integrated PageHeader hash; its hash participates in the migration fingerprint. Post-integration UI28tests/5files passed7.21s, including editor/files, upload recovery, sidebar/register navigation and PageHeader rail. Typecheck11888 returned only3pre-existing My Day test errors, none in the changed IT surface. Finalbuild22455 is running; actual Herd ordinary-login upload/builder proof is next.

Build16313, asset `app-ByR3FTp2.js`, manifest `13ba4a42bdaded9f113d70fc78094a56c0b7da59d48e1db8a55dece2baf818a9` was observed on `http://127.0.0.1:8766/it/knowledge/12` in the intended checkout. Ordinary synthetic author login, unchanged normal1280x720 browser. Read Diagrams → Build diagram opened the Diagram builder directly. Created two labelled shapes and one labelled connector, moved a shape with ArrowRight, verified Files disabled uploads while changes were unsaved, then saved and continued in the Files editor. DOCX and PDF each uploaded and retained editing mode. Word text preview opened; PDF fallback and a real original-file download event passed. Return preserved the Files section; reopening Diagrams visually confirmed the saved shapes and connector. Console errors:0. This focused pass does not claim PDF embedded rendering, `.doc` rendering, new recovery cases, size boundaries, or whole-package acceptance.

Tab2 closed before Vendor exact cleanup42424 terminal0. Root independent postflight also terminal0: exact schema and owned root `2f1870aa4c75e732` absent. Independent source audit found16 ticket/work source changes during the runtime from the newly user-authorized ticket production task. Knowledge/Vendor source files and11protecteddesign hashes were unchanged. Browser claims remain asset-bound with the backend drift disclosed; no clean whole-source freeze claim. The next build must incorporate these changes and the separately published Overview commit86dee9068.

The three harmless fixture input files were copied to this task's private artifact root before cleanup. Verified ClamAV files are now copied into ignored `storage/app/private/it-runtime/clamav-1.5.4`; binary SHA256 `F368912F61DDEA8B302ACF9114891EC57F8A45B0DE77F076B1D19C960BC6F7CF`. Scanner environment, per-site upload settings, pending Knowledge000002/media and000003/relationship repair remain unapplied at this checkpoint. The historical setup/browser statements below are superseded by this section and the fresh inventory above.

## Substantive source delivered

Latest local-host correction (fresh inventory, 13 September): revision000001 is already registered (id1041/batch19) with its earlier three-column shape; related_records is absent. Media000002 is pending. Earlier statements below that both were unapplied are historical and superseded. Added forward-only000003 to fill the missing nullable relationship column; it preserves existing rows and is a no-op on updated schemas. New legacy-shape regression awaits W15's affected-only group. Actual local activation is authorized after guarded browser cleanup, via exact reviewed additive gate and private recoverable snapshot; no application DB mutations yet.

Real scanner preparation: official ClamAV1.5.4 Windows portable archive SHA256 `0D9E0228B2674137EA1A2853566C98A0278AD52AB2582C3D6DBD75373848C395`, verified good signature with official Talos key `5BADCA2665EF59DCF8A23D8B707F0DB480836771`. FreshClam verified main63/daily28121/bytecode339; real scan of harmless synthetic PDF returned0 and input hash stayed identical. No app configuration activation, external file upload or system antivirus change. Optional database_path in shared scanner/config passes13unit tests/78assertions (2PHPUnit notices). Installation/signature procedure follows [official ClamAV guidance](https://docs.clamav.net/manual/Installing.html) and [signature database guidance](https://docs.clamav.net/manual/Usage/SignatureManagement.html). Portable files currently live in this task's private visualization artifact directory; runtime location/configuration and actual-host browser proof remain next.

Current direct-user follow-up: full-page editing now includes Files & versions, with Word/PDF selection, separate document/file forms, and Save changes and continue to files when text/diagrams are dirty. File changes cannot silently overwrite unsaved document edits. Successful upload/retry stays in the Files editor. Read Diagrams has a direct Build diagram action and editor navigation labels Diagram builder. Missing storage setup is explicitly explained for both. Focused23tests across4files passed at15:23:15 (6.35s), covering these flows and shared register navigation; scoped lint/diff passed. Current-build browser verification remains queued after Vendor96773 and reserved W15 regression/build. The user's real Herd Knowledge/8 still has the unapplied Knowledge migrations; source success is not working-host availability.

- Canonical `/it/knowledge/{article}` full document page with approved reading, separately saved proposal, full-page editing, structured content, related records, ownership/review, file versions and diagram sections. Existing lifecycle/history/compare/publish/restore services remain authoritative. Older query links redirect to the full page. Native and ordinary library title links preserve filter/page/list-view context. The old editor's duplicate success toast is removed.
- Visual diagram authoring: rectangle/ellipse/diamond, text labels, drag and keyboard movement, named connectors, removal and full labels. Bounded inert JSON is saved with the document; source and file changes receive immutable encrypted media-draft snapshots. Publication retains the previous approved rendering until review. Restore of a pre-media snapshot clears later media in the proposal.
- Private Word `.doc`/`.docx` and PDF upload, replacement series/version identity, permitted history, protected open/original/inline-PDF routes. Word preview parses bounded OOXML into text with no active HTML/entities/external resources; PDF uses an embedded viewer and visible original fallback; unsupported Word preview explicitly offers the original. Uploads reserve owned storage first, require the shared malware scanner and audit, then add a file to the canonical versioned draft. Unavailable scanning leaves the document unchanged and bytes inaccessible. No real scanner/provider configured.
- Additive migration `2026_09_13_000002_add_it_knowledge_document_media.php` adds nullable diagram/file references and private `it_kb_files` metadata. It is tested in disposable schemas only. Revision migration000001 and media000002 remain unapplied to the working DB. Working `oblivion_findings_codex_test` was never reset.

## Actual checks

- Initial backend run96112: 16 passed,2 failed,331 assertions,262.42s. The failures were an unnecessary empty-media restore snapshot and a strict test assertion on MySQL JSON key order. Both corrected. All14 postflight checks passed; exact schema `oblivion_it_support_test_it_3408a575a8c841df` absent.
- Affected backend rerun97607:5 passed,87 assertions,275.61s, terminal0. Restored publication, diagram source/history/publication, private PDF versions/direct denial, Word preview/legacy fallback, scanner-unavailable denial. All14 postflight checks passed; exact schema `oblivion_it_support_test_it_68f5b55e9fac490e` absent. Counts overlap; do not sum them into a new suite count.
- Initial component group:5 passed across3 files (diagram/editor/revision). Full-page + diagram group:6 passed across2 files. Existing library regression initially10 passed/5 failed because its old modal expectations changed; updated the five assertions to check canonical full-page navigation while retaining legacy reader/access tests.
- Final library/full-page/diagram group:21 passed across3 files,6.13s. Ordinary diagram buttons do not accidentally submit the document; full content survives section changes; proposal/publication are separate; dirty Cancel retains the draft; canonical read/edit links carry bounded library context.
- Scoped Pint, PHP syntax (including shared routes/bootstrap and owned browser fixture helpers), final ESLint with zero warnings, and touched-file diff whitespace checks passed. First typecheck found two new missing TierTwoTabs renderer props, corrected; subsequent typecheck had no IT errors and only three independent My Day test `ByRoleOptions.exact` errors. Initial Governance type errors disappeared with independent work; none was edited here. A final typecheck may be active; see current resumption for its handle.
- No current-build browser verification claimed yet. Browser fixture preparation generates exact harmless Word/PDF bytes inside a new owned root; its synthetic scanner accepts only those exact hashes, never arbitrary uploads. Existing unchanged-dimensions ordinary-login/current-build guards and exact cleanup remain required.

## Coordination and scope

User-requested parallel tasks supersede the sequential-only instruction. Knowledge owns shared integration and aggregate progress. Vendor task `01a09846-8439-7ea2-a70b-4dc93d6304a1` owns W23/W24; Provisioning task `01a09850-626a-7c93-953c-33f81ff1a41a` owns W15. Read `parallel-session-ownership-20260913.md`. Do not implement their packages concurrently here.

Shared integrations applied at Vendor's request: require `routes/vendor-vault.php`, schedule `vendors:check-renewals` daily without overlap, canonical `/vendors/{id}` Knowledge href, and dontFlash value/totp_secret/verification_code/rotation_evidence. Knowledge vendor projection still uses canonical access query and only id/company_name/site_id/service_type. Contract fields/files are never projected here. Contract access is Finance plus organisation and site/house management under approved-site/record scope, separate from vendor access and house-staff credential reveal.

Transferred `routes/sites.php` credential/vault gates to Vendor. Transferred W15 canonical ticket-draft extension points after freeze to Provisioning, preserving existing W06 invariants. No parent or mockup task may mutate production source. All implementers must explicitly freeze before a shared build/browser window and wait for exact cleanup/postflight before resuming.

## Remaining acceptance

Real browser full-page read/edit/diagram save-reopen-review-publication, Word/PDF upload/open/replacement/history, native filter/page return, stale/session recovery and denial must still be verified. New media storage needs bounded abandoned-upload reconciliation/retention, complete failure recovery and file-history scale/paging evidence. The `.doc` upload test uses a synthetic uploaded-file MIME fixture; it does not prove rendering or a real binary `.doc` browser upload. Working-DB migration approval/readiness, full Knowledge navigation/review/resolution reuse and every broader W00–W27/E01–E23 gate remain open. No production AI, provider changes, real communications, push or deployment occurred.

## Pre-browser boundary follow-up

- File-reference validation now requires an already accessible current/historical reference or access to every Site in the file's original scope. A ready same-document file ID alone is insufficient. Added an atomic guessed-history regression and reran it with the private PDF upload/replacement case; see `w22-document-file-scope-20260913.log` for actual completion, not assumed here.
- Corrected the full-page test fixture to provide the complete KbRow shape. Final typecheck output contains only three existing My Day ByRoleOptions.exact errors and no IT errors.
- All 11 protected design files match the captured baseline hashes.
- Vendor and Provisioning explicitly froze source/build/helper edits. Vendor requested an exact private SiteCredentialController helper rename to authorizeCredential to avoid shadowing inherited AuthorizesRequests::authorize; applied before build, with PHP syntax passing. Canonical SiteCredentialAccess::authorize remains unchanged.
- Vite current-checkout build is running; no browser pass is claimed by this entry. SiteProfileAdminPresenter::vendorsCredentials transferred to Vendor only after cleanup/release. This Knowledge browser window does not validate Vendor/Sites dialogs.

## Actual current-build browser pass

Build77139 exited0:5221 modules,4m7s,app-DdJkxbPG.js; manifest c916a55ef3d6bcd3e43bddcf3b4eaa9ed7c4084b49198f94441babbd1911556d. Backend52059 exited0:2passed/36assertions/306.41s, all14postflight and exact schema oblivion_it_support_test_it_d2729ce4ca554f4a absent. Browser launcher64710 exited0 for token9d79226425444831/fingerprint6ec62400dba0daef3f271c198ba78aa7207d22e2d18b5c7b23870629696e2e43, server18864. In-app tab1, ordinary login, unchanged1280x720; fixture landing redirected to My Day, Knowledge opened through ordinary navigation. No source/build/helper edits during runtime; all14 scoped source hashes and asset manifest identical before cleanup.

Verified on http://127.0.0.1:8766:
- Filtered library page2 native href /it/knowledge/22?library=q%3Dsynthetic%26page%3D2 opens a full reading page with complete content. Back to Knowledge returns to the same filtered page2 (25–27 of27).
- Published article1 full-page editor starts from the proposal. Typed text survives section changes. Rectangle/ellipse labels, connector, keyboard movement and actual drag work. Dirty Cancel offers to retain editing. Saved text+diagram persist on reload and reopening. One article-save success toast observed. Approved publication remains unchanged until review. First close-spaced diagram had crowded connector text; dragging separated shapes and rendered the full connector label clearly.
- PDF picker/upload creates file1/version1. In-app browser displays the explicit PDF fallback (embedded rendering NOT verified); original PDF download event succeeds. Replacement creates file2/version2 and retains file1 under Other saved file versions. Word .docx upload creates file3 and safe Word text preview renders the generated synthetic instructions with original-download controls. No real .doc browser upload claimed.
- Author sends the proposal for review; editing is then unavailable. Separate reviewer signs in normally, sees approved content until choosing the proposal, compares diagram/files against publication, confirms approval, and obtains Article published. Full reload shows the new published text, diagram and both current files, without a proposal toggle. Revision history includes all saved media versions. Browser console errors recorded:0.

Browser follow-ups observed (not fixed during runtime): discard-dialog generic description says saved settings; file preview/back/history drops earlier library filter context. An immediate next-page click while a search debounce was still settling returned to page1; after search settled page2 worked. Session/back recovery, authenticated Site-denial browser journeys, maximum-file-size and full binary .doc preview remain unverified. Backend access checks are evidence only for their covered routes/cases.

Owned tab1 closed; no in-app tabs remain. Exact cleanup19066 is running; do not claim schema/root absence or release source until independent postflight succeeds.

## Cleanup and next coherent slice

### Subsequent implementation and grouped verification

Operational boundary for the current media slice: preserve `it_kb_articles`, encrypted working copies/revisions, the revision-file index and private `it_kb_files` bytes together in an approved backup/restore. Keep the existing encryption-key recovery process; never print keys or document bodies into diagnostic evidence. Ready versions and archived drafts are retained. Removing an unfinished upload changes its state to abandoned without deleting bytes or the ownership/audit record. There is no scheduled purge or automatic malware-check bypass. An operator should reconcile missing bytes/checksums and references using an isolated restore before any separately approved repair. The current migrations are still unapplied to the working database; their destructive down paths are not a safe rollback after documents have been created. Prefer disabling access or a forward repair while preserving records. Full W26 restore/retention acceptance remains pending actual isolated restore evidence.

- Final affected fourth group47234 terminal0/Pest0:4/4passed (calendar adapters, audit outcome projection, Knowledge interrupted recovery and unavailable scanner). All14postflight checks true; exact schema `oblivion_it_support_test_it_d59bba1de98349b4` absent. All21 current Knowledge revision workspace cases are now covered by passing third/fourth groups. Final affected UI12passed/3files/6.16s, saved in `w22-document-recovery-ui-20260913.log`; full tsc95473 terminal1 contains only the three independent My Day exact-option errors. All11 protected design hashes unchanged. Source is frozen for the imminent Vendor-owned shared build/browser; Knowledge will take a serialized browser handover before Vendor exact teardown/postflight. No later browser outcome claimed here.

- Vendor corrective second group: full vault security and Knowledge revision workspace,29passed, terminal0/Pest0; all14postflight true, exact schema `oblivion_it_support_test_it_29b69c6749c44ef7` absent. The first Vendor group did not reach Knowledge and is not evidence of its pass.
- Vendor third group17783:62 owned Vendor/legacy cases plus19 Knowledge cases passed;1 Knowledge fixture errored before the final scanner case. Exact schema `oblivion_it_support_test_it_b879603c39f7401a` absent and all14postflight true. The fixture used Eloquent replicate, which omits required `created_at`; explicit creation time added. Affected-only recovery/scanner cases are queued with the new Vendor calendar privacy case.
- Draft removal now archives drafts with file/revision history and allows restoration without deleting private bytes or immutable snapshots. Dedicated archive/restore and scan-unavailable→resume→same-file replay cases passed within the third group.
- Unfinished uploads retain an actor-owned reservation, UUID/content identity, original scope and expected document version. Storage, checking and final attachment serialize under the parent lock. Retry rechecks current actor, every original Site, current document state/version, replacement membership and stored checksum. Scanner outages preserve a private retryable reservation; damaged bytes fail closed. Dismissal is idempotent and retains the private record/bytes. No retention purge is enabled or claimed. The author-facing list pages20 rows after scope filtering, supports retry/status check and removal, and rejects mismatched response identities. Stale uploads require reviewing the current draft and starting an explicit new upload; retries do not silently rebase.
- URL search now remains visible on the dedicated Knowledge page. Library page controls pause until the new search settles. File previews and history retain library context. Duplicate delete/save toasts and generic discard copy were corrected.
- UI group28passed/5files/6.97s covers search, file history, diagrams, unfinished uploads and removal. Later editor recovery group14passed/2files/4.94s covers fresh editor context, retained typing, stale expected versions and wrong-account concealment. Scoped lint/Pint/syntax/diff checks passed. Typecheck63431 caught transient Vendor/W15 integration errors now corrected by owners;47283 caught a transient misplaced editable prop in KnowledgeRevisionDialog, since corrected to KnowledgeUnfinishedUploads. Another coherent typecheck remains pending. Existing three My Day test errors are independent.
- Full-page editing now rechecks current actor/document/options before showing the editor and on focus/return. A bounded three-document,30-minute in-memory buffer offers explicit Resume/Discard after navigation; it retains the original version for conflict review and clears after save, deliberate discard or denied access. No unsaved text is written to local/session storage. Reload/sign-out recovery still requires a saved server draft; this is not durable offline autosave. Full read-only browser Back/session-denial acceptance remains open.
- Current source has not yet had a new production build or browser pass. The earlier browser pass above covers its recorded build only. Working DB and protected design sources remain untouched; all whole-package and full-goal acceptance stays open.

Cleanup19066 exited0; independent postflight exited0, exact schema/root absent. Vendor and Provisioning source freeze released. Navigation/copy follow-ups implemented after cleanup, scoped lint/Pint/syntax pass. Shared Vendor integration connected with canonical masked credential reference fields, custom endpoint/type picker support, response-protection middleware, renewal TaskAggregator provider and00:05 worker-local scheduler. All own integrations await grouped backend/full-build validation; owner implementations remain in progress.

Knowledge file history now uses immutable revision-file reference indexing in still-unapplied migration000002, scope-before-limit20-row paging, a current-actor JSON endpoint and load-older retry UI. No encrypted historical bodies loaded to list files. New regression includes25 files with a hidden newest historical file and a current file, asserting20+3 accessible historical rows and no snapshot selection. Added credential metadata privacy/Site/retired checks and native file-return context assertion. PHP/migrations/tests frozen pending Vendor-owned combined disposable run of the full Knowledge revision workspace file and its two Vendor feature files. Current follow-up UI22passed/1failed; the sole failure is W15's old provisioning filter/layout test fixture against its new page props, transferred to Provisioning owner; Knowledge history/new wrong-account UI cases passed. Final follow-up build/browser still pending; earlier browser evidence is not automatically evidence for later changes.
