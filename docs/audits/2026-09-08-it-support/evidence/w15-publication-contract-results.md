# W15 immutable catalogue publication and recovery

Implemented, initial verification in progress; not a release or browser acceptance claim.

The canonical catalogue now has immutable publication-version records. Draft updates increment the service contract version and require an optimistic item token; they leave the published pointer unchanged. Publication explicitly snapshots the draft. Withdrawal preserves prior versions and existing submissions. Discovery/search and intake use the published contract, including its original internal audience rule and approval requirement, rather than unsaved/unpublished draft fields.

Submissions store the complete selected contract and publication ID with an input fingerprint. Actor-row serialization protects the global requester idempotency key across different items. Exact replays can recover after withdrawal or draft changes, but only while the current canonical result remains visible; different input with the same key is rejected. Legacy submissions retain unknown full-contract evidence as null and use their recorded schema/values for conservative replay matching. No historical contract or human review was fabricated.

Migration000035 adds only canonical catalogue version/pointer/snapshot fields, with no tenant boundary. Existing published records are captured as legacy_current; imports are labelled imported_current. The migration is applied only by the disposable test harness. The working database is untouched. Preserve separate Governance migrations32–34.

UI distinguishes current draft and published versions, permits review/publication of revised live items, shows approval/audience/outcome in review, disables duplicate publication while pending, and displays stale-version feedback. Full Rory wizard conversion, preview, draft recovery and real browser lifecycle remain outstanding. The source still has a publication DomainException→flash/onSuccess close issue to fix before browser acceptance.

Initial TypeScript81594terminal0 and scoped lint0 passed. Focused publication/navigation UI10passed/2files/4.92s terminal0. PHP82793/token it_5f16bfc623ba4547 is running22 catalogue/workflow cases; wait for its result and all14postflight/schema absence before editing loaded PHP. No other test/build/browser server/schema is live.

Before final verification: preserve seeded existing records (current updateOrCreate overwrites them), ensure imported item/publication creation is atomic, and test a failed publication retains review. Extend synthetic release fixture cleanup for the new immutable version dependency where relevant; do not delete historical operational evidence. Add real independent publication/submission races and migration/backfill coverage. Complete the remaining W15 audiences/Sites, requested-for, attachments, versioned provisioning workflow and approval/failure/reversal criteria.

## Initial result and final failure-path selection

Initial PHP82793/token it_5f16bfc623ba4547 completed terminal0:22passed/22finished/377assertions; all14postflight checks passed and schema absent. This proves the initial publication/recovery contract within those scenarios, not the entire package.

Follow-up implementation now wraps canonical item insertion plus imported publication in one connection transaction; the existing seeder uses firstOrCreate by its canonical slug so reruns preserve authored records. Publication DomainExceptions are returned as validation feedback, keeping the review open instead of reaching the successful-submit callback. Added actual HTTP failure, seed-rerun and injected publication-storage rollback tests. UI now renders all publication errors; final UI11passed/2files/5.15s terminal0. Pint passed.

Final backend run21191/token it_e55abe5c09fe4c1a and TypeScript41363 are running. No build/browser server is live. Fifteen final source hashes are captured in w15-publication-contract-source-hashes.json. Do not edit loaded PHP until terminal result and exact cleanup; no final-backend/browser pass claim yet. Next: inspect results, then complete the catalogue editor with the existing WizardShell (request details, field builder, review), canonical dirty/cancellation/recovery controls and actual desktop acceptance. The protected Popup guide requires this multi-section entity editor to use the shared wizard. Keep simple publish/withdraw confirmations within the approved simple-dialog pattern. No design file was changed.

## Interrupted run and verified recovery

Run21191 ended terminal1 because its PHP process25428 exited -1 before any test completed and without a shutdown diagnostic. The read-only postflight correctly reported its schema remained. No qualifying PHP Application crash event was returned; the cause is unconfirmed. This is not a test pass or an assertion failure. The exact residual schema had835tables and zero active connections; the PHP process and matching mysql importer were absent. The reviewed exact-schema helper preview461220cd4c6bda5f0172b1facd12c250cc4cb6135c129fabd89d8a798ecec8f0 preceded cleanup96262terminal0. A separate read-only postflight confirms schema/table/connection absence. No working database or other schema was removed.

Only after that terminal result and cleanup, recheck71921/token it_c57c2b174d93402f ran the same focused selection. It completed terminal0, with all14postflight checks and exact schema absence. See the preserved diagnostic stream and interrupted-recheck log for actual counts. The earlier TypeScript41363 completed terminal0. Current free-memory observation was6834MB of31622MB; it does not establish the prior exit's cause.

Catalogue editor is now composed with WizardShell, type tiles, request-details/field-builder/review steps, computed completeness, confirmed-save success pane, discard guard and keyboard step/error focus. Simple publication/withdrawal dialogs use the guide's480px sizing and header icons. Initial wizard UI11passed/3failed: the new test selectors incorrectly inserted spaces in shared rail/tile accessible names. Corrected selectors and a React render-ref lint issue; UI14passed/2files/5.36s and scoped lint0. The first wizard TypeScript run40606 found only unsupported exact options in new Testing Library queries; those test options are corrected. Final TypeScript72609 and asset build97056 are active, not yet verified. The optional fingerprinted CatalogueFixtures browser setup is implemented and syntax-checked; no browser run has started. No design file changed.
