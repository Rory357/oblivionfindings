# W11 attachment payloads and provider read paths — 11 September 2026

Previous goal turn: progress (bounded native response transfer and both persisted provider retry journeys verified). Full W11/goal remains incomplete.

Implemented ItEmailAttachment typed unscanned evidence and ItEmailAttachments parser. Gmail traverses bounded MIME structure, separates named text files/inline images from the outer report, validates the complete batch before fetching, preserves external attachment IDs and rejects duplicate IDs, conflicting inline/external sources, unsafe names, unsupported extensions/forwarded entities, excess files and sizes. Graph validates only fileAttachment objects, declared metadata and exact identity/name/type/inline/size on detail reads. Empty inline-image labels get deterministic safe display filenames; original provider labels remain separate for identity checks. No supplied filename becomes a disk path. Actual detected MIME validation and scanning still belong to the pending canonical storage stage; declared MIME is not a clean-file verdict.

Shared ItEmailBase64 now supplies both body and file decoders with strict complete encoding and size checks. Existing body's1MiB part bound remains; attachments preserve the canonical5files x10MiB. Unit cases decode a full10MiB file for each provider and validate50MiB batch metadata. The file HTTP ceiling is30MiB to allow standard base64 plus worst-case JSON escaped slashes; Gmail full-message ceiling70MiB covers ordinary base64url full-file batches. Aggregate MIME/header overhead still needs review in the final staging path; limits must not silently reduce the approved file allowance.

Existing GoogleGmailService/MicrosoftGraphService readMessage now accept a trusted includeAttachments flag (defaultfalse), returning the same normalized report plus typed file descriptors from that read. Gmail raises its receive limit only for the file-aware read. Graph always checks its attachment collection, since hasAttachments omits inline files; follows bounded pages only on the exact approved mailbox/message attachment path and rejects cycles/excess/partial traversal. Both services expose readAttachmentContents with the existing bounded HTTP boundary, exact encoded message/file URLs, transport failures left retryable and no storage/public exposure. Graph retains ImmutableId preference. These paths are implemented but NOT YET invoked by the production poller: private staging/scanning/canonical consumption must be wired first. No provider attachment ingestion or browser file acceptance is claimed yet.

Files: app/Domain/It/Data/ItEmailAttachment.php; services ItEmailAttachments/ItEmailBase64/ItEmailContent; existing GoogleGmailService/MicrosoftGraphService; new Unit/It/ItEmailAttachmentsTest and Feature/It/ItEmailAttachmentProviderTest. First unit run tokenit_36c0d4dd7ee1437f terminal1/Pest2:26passed/one error59assertions, stopped at incorrectly shaped associative Pest dataset arguments; fixed to pass each change as one array. Final unit retry tokenit_71a45af357d54fe1 terminal0:97tests133assertions, under one second at diagnostic timestamp precision, all14postflight/schemaabsence. Combined provider regression25326 terminal0:88tests239assertions172s, tokenit_4efa466856204f7e exact schema absent/all14postflight; logw11-attachment-provider-tests.txt. Counts overlap; do not add them into185unique cases. Eight source hashes/test totals and all ten unchanged protected design hashes recorded by w11-attachment-payload-checkpoint.cjs in w11-attachment-payload-source-hashes.json. Pint/diffcheckpassed. NO active test/browser/runtime/build/import/cleanup. This verifies provider read methods, not production poller attachment ingestion, storage/scanning or browser downloads.

Next: connect these typed reads to private durable staging and the shared MalwareScanner, then pass clean UploadedFiles through canonical commands with ItAttachmentWriteContext. Add file digests to inbound identity while preserving old no-file fingerprints; handle duplicate/unauthorized/quarantined mail without exposing files, retry/failure, outer rollback, process crash and uncertain commit. IT scan configuration/model metadata absent. ScannerUnavailable must be explicit and live scanner verification remains DP07. Finish actual two-provider file/browser download-denial/retry journeys before marking W11/E10 verified. Production AI disabled; no real provider traffic/communications/configuration/deploy; working DB unchanged, protected design files immutable, never resize browser.

Primary references read: Gmail MessagePartBody documents byte size independent of encoding and data/attachmentId split; Graph fileAttachment documents contentBytes, inline status, file ID and displayed name. See [Gmail attachment resource](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages.attachments) and [Graph fileAttachment](https://learn.microsoft.com/en-us/graph/api/resources/fileattachment?view=graph-rest-1.0).
