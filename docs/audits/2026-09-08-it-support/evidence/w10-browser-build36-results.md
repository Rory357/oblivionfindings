# W10 local SSO browser verification — Build36

Environment: exact shared Build36 synthetic runtime107ee8b81e2c4322, isolated schema, real CSRF, array mail, provider configuration cleared. Fixture adds separate settings.access.manage actor; ordinary actors expressly denied that grant. Production provider settings/consent are not in scope. Actual provider sign-in remains DP07 external.

Actual first check: restricted technician normal login → direct /settings/sso renders403 Forbidden, no settings or credentials visible. Local authorized save/refresh, validation, secrets and conflict checks still pending.

Normal synthetic settings login succeeded. Microsoft invalid directory UUID rejected; focused error summary and nonsensitive domain/ID retained. Screenshot exposed confirmed UI defect: Recovery places ul/actions directly inside Alert's two-column grid, so validation text enters zero-width icon column and wraps word by word. Fix required; this layout is NOT verified compliant. No shared design file changes needed.

Corrected synthetic Microsoft UUIDs/domain, both sign-in toggles off: save reported actual application version1 with provider consent/sign-in still unverified. Hard refresh persisted values/version1. Local Check accurately reported missing readable secret. Google disabled synthetic client/domain plus explicit replacement using clearly labelled non-provider test sentinel: saveversion1, replacement input cleared, present/concealed status, no secret returned in rendered page. Ordinary domain change with Keep current secret saved next; readback pending. No OAuth/provider requests or real credentials used.

Environment limitation: transient dynamic import index-BFN3gNz8.js failure after forbidden page→desk rendered blank. File existed at exactBuild36, bounded local GET200; explicit page reload recovered. Preserve error as observed, not an SSO pass or confirmed missing asset.

Google ordinary keep saveversion2 and hard refresh preserved concealed secret/domain. Remove without acknowledgement rejected and secret remained; deliberate reselect Remove + checked acknowledgement savedversion3/no readable secret, both sign-ins remained off. Two normal authorized tabs loaded provisioning0; first disabled staff creation and saved1. Second stale portal-change save rejected with focused conflict/disabled fields. Review current saved settings displayedversion1 and actual stafffalse/linktrue/portaltrue without resubmission. Use saved values→disableportal→save succeeded; independent refreshed tab confirmed stafffalse/linktrue/portalfalse. No new account created or access granted. Normal logout in10→stale provisioning save9 concealed all provider/mapping forms with focused session guidance and sign-in/reload links, no success message.

Tabs9/10 closed. Cleanup23202 terminal0; independentpostflight0 proves exactBuild36 schema/directory absent. UI correction applies AlertDescription around error list/recovery actions in canonical settings form, group mapping feedback and page access-loss state. Shared Alert/design files unchanged. Provisioning conflict now describes account-creation settings, not nonexistent secrets. Existing3suite tests/types/lint41787 live; first sandboxattempt1 could not load vitest config due filesystem denial, no tests ran; authorized rerun recorded separately. Build3745667 live. Corrected layout requires fresh browser verification and is not yet Verified.

Corrected SSO automated run41787 terminal0:24tests/3files/3.49s, fulltypes0, scopedlint0. Build3745667 terminal0/3m28s, app-CBSIGKxN.js; all10 protected files unchanged. New runtime39284 startup live token46072a8ced874baf/fingerprint4ffc77e58804c5637f530d2cf75622d7e8f8b7712790f56d11aaff4426690ba9. No corrected-layout browser pass yet.

Build37 import took longer than previous runtime. Read-only exact-schema probe22:42:52NZST found827 tables and active query waiting for handler commit1s; no SQL/private values emitted and no reset/competing import. Evidence w10-build37-import-progress.json. Launcher39284 still active at that observation.

Additional focused source observation while Build37 imports: shared Button has no default type; Recovery buttons nested in SSO forms should explicitly set type=button (review/use saved/keep fields) to avoid implicit native submit events even though the current failure/pending guards prevented extra saves in actual conflict checks. Apply after frozen Build37 browser acceptance and verify through existing request-count regressions; do not silently attribute a later source change to Build37.

Second exact-schema progress observation22:47:49NZST:835tables (upfrom827), active preparing-for-alter1s, latest migration2026_08_30_000200_create_facility_signal_delivery_outbox. No schema reset, competing import or production operation. Evidence w10-build37-import-progress-second.json.
