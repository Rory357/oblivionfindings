# W11 attachment transaction ownership — 11 September 2026

Previous goal turn: progress. It completed actual full-body/automatic-message browser evidence and exact isolated-runtime cleanup. Full objective remains active.

Confirmed dependency defect: independent direct-file reservations outlive the business transaction. Inner canonical commands could return, then a later inbound-ledger write roll back the outer mailbox transaction. The inner catch never ran, leaving the reservation in reserved state and private bytes behind. Existing cleanup correctly excludes unknown/reserved intents from automatic retry, so simply sweeping every reserved row would be unsafe.

Implemented: explicit single-use ItAttachmentWriteContext belongs to the mailbox transaction. Canonical creation and comment commands record their reservations before private byte copies. ItMailboxPollState owns its transaction; ItMailboxInbox passes that owner through InboundEmailIngestor to the existing commands. Failed outer callbacks request existing durable cleanup only after proven root rollback on the original PDO. A completed callback followed by failed commit acknowledgement, a surviving parent transaction, changed connection or unavailable evidence preserves bytes/intents. No new storage system, payload field, notification, migration or operational retention choice.

Changed files: new app/Domain/It/Services/ItAttachmentWriteContext.php; existing ItMailboxInbox, ItMailboxPollState, ItTicketIntakeService, ItTicketInteractionService and InboundEmailIngestor. New standalone tests/Concurrency/It/ItInboundAttachmentTransactionTest.php exercises actual mailbox claim plus creation/reply with private UploadedFiles, outer rollback, retry, after-commit failure, exact replay and stale context rejection. It uses real commits, not worker concurrency; provider HTTP is forbidden. Scoped Pint and diff check passed.

Standalone verification passed: session14687 terminal0, one test/29assertions,175seconds from runner start to finish including isolated setup. Tokenit_89049c80d61042b0 exact schema absent and all14postflightchecks true. Logw11-attachment-transaction-tests.txt and diagnosticJSONL. This proves actual outer rollback, successful retry and preservation/replay after the real commit followed by a thrown callback. It does not simulate network-level PDO commit acknowledgement loss or worker crash. Regression suite68848 terminal0:53tests570assertions187seconds across four existing canonical inbound/poll/header/content Feature files. Tokenit_c14c88b649444330 schema absent; all14postflightchecks true. No active test/browser/runtime/build/import/cleanup. Both test counts/timings and seven source hashes recorded by w11-attachment-context-checkpoint.cjs in w11-attachment-context-source-hashes.json; ten protected design hashes unchanged, diffcheck/Pintpassed. No new browser journey for this backend ownership foundation; the provider-file browser criteria remain open. This is not a claim of provider attachment ingestion, scanner readiness, crash recovery, or full W11/E10 verification.

Next implementation: use the existing shared app/Services/Files/MalwareScanner adapter (only Consents currently supplies settings; IT scanner configuration is absent). Current attachment model/storage already supply private download authorization, five files and10MiB each, detected MIME plus extension allowlist. Provider file retrieval still absent. Gmail detail provides MIME file parts with inline/base64url body or message-bound attachmentId; Graph requires bounded attachment collection/detail handling, including explicit non-file/reference attachment outcomes. Bound received HTTP bytes before JSON decode, reject unsafe metadata/content, stage privately with durable recovery and scan before exposing a file. Keep scanner-unavailable and infection outcomes explicit. Include actual file digests in inbound replay identity without changing existing no-file fingerprints. Outer rollback context now provides the necessary canonical cleanup integration; crash/uncertain-commit reconciliation remains separate unfinished work.

Primary references inspected for next work: [Guzzle request options](https://docs.guzzlephp.org/en/stable/request-options.html), [Gmail attachment resource](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages.attachments), [Graph fileAttachment resource](https://learn.microsoft.com/en-us/graph/api/resources/fileattachment?view=graph-rest-1.0). No live provider traffic. Desktop web only, no resize. DESIGN.md/design_styles immutable; working database not reset and migrations17/18 not applied to it.
