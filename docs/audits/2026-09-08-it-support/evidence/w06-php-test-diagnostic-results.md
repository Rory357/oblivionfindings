# W06 isolated test interruption diagnosis

The instrumented requester-options run proves a PHP **E_COMPILE_ERROR (64)** while loading `app/Domain/It/Services/ItTicketIntakeService.php:116`. Root inspected that location and found a duplicate `requesterOption` declaration; its source owner removed the duplicate and is verifying it. This is a source compilation failure. No Windows native-crash or resource-exhaustion evidence was found. Prior silent runs did not record their fatal location individually and are not retroactively claimed to have passed.

- Recorder evidence: `it_1cce77999e634ab3.diagnostic.jsonl`, sequence9 at05:54:21UTC. Peak allocated memory85,983,232bytes, final error64/file/line, output-buffer depth2, no buffered PHPUnit internal-error marker. The recorder never captured exception messages, SQL, payloads, session contents or environment credentials.
- Optional `-Diagnostics` in `run-isolated-it-tests.ps1` prepends `isolated-test-diagnostic.php` only for a guarded exact token. It preserves the same test/schema/sink boundaries and1024M configured memory. The initial recorder namespace mistake (`Tracer` versus the installed `Tracer\Tracer`) failed before a schema was created; root corrected it. Exact token03000e7f535d409e postflight was absent. This preparation failure is not an application test result.
- Read-only inspection at05:44:54UTC proved the four earlier named schemas existed with837tables each and no active connections:1c92ebd123104476,6c31f2135fad49cc,1e0980617e1440c7,f545a50617604042. The first requester-options output had only a preflight, so its `absent=true` did not establish post-test cleanup. The diagnostic compile failure added1cce77999e634ab3 as a fifth residue. Original0a4c4293ec6f47f3 is absent.
- `tests/TestCase.php` registers best-effort shutdown cleanup after the runner's shutdown handler and suppresses cleanup exceptions. Those callbacks did not remove the five exact schemas in these interrupted runs. Numeric-PID stale cleanup deliberately does not match the `it_<hex>` tokens. The source error must be fixed before rerunning; simply launching more fresh schemas would repeat the gap.
- `w06-test-residual-cleanup.php` is prepared for source review, with immutable exact-name allowlist, same loopback test credentials, source/state fingerprint, no active connections, and a repeated exact-state check before each bounded drop. Default invocation only inspects metadata. Preview fingerprint:93ef2c9d235a3b48665cda74d717c523edc04e43090ef923f292b9b71c0e84fc. No cleanup has been executed at this checkpoint; append actual apply/postflight only after review and confirming no tests are running.

The complete thirteen-case bulk suite and the interrupted combined draft group still require their necessary clean reruns after the source fix. The independently completed two-case bulk actor/ACK run remains valid (2tests/11assertions, exact isolated schema absent).

## Correction and cleanup completion

Root reports corrected requester/local/persisted draft group **47tests/469assertions,233.90s,exit0**, exact token454facc536d74784 postflight absent. After that process ended, root reviewed and authorized the exact cleanup fingerprint. Apply57677 completed **exit0**, removing only the five initially existing allowlisted schemas; the originally absent0a4 name remained absent. `w06-test-residual-cleanup-results.txt` records all before/after states. A separate PDO information_schema query then independently confirmed all six exact names absent, recorded in `w06-test-residual-cleanup-independent-postflight.txt`. The application database was never a cleanup target. The interrupted thirteen-case bulk suite still needs its own clean group rerun if required by final regression; it is not converted into a pass by the47-test group.
