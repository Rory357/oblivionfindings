# W11 bounded provider receive bodies — 11 September 2026

Previous goal turn: progress. Attachment transaction ownership was implemented and verified by real commits plus existing inbound regressions. Full W11/goal remains incomplete.

Confirmed gap: MailboxProviderHttp bounded connection/total time and redirects, but native Guzzle handlers could buffer an unrestricted response before the MIME parser checked content sizes. Implemented MailboxResponseBody middleware with a fresh memory-only sink per request, default8MiB and trusted explicit ceilings up to80MiB. It rejects oversized declared Content-Length before body acceptance and caps decoded writes even with no length, chunked transfer or gzip. It rejects truncated unencoded declared bodies. A custom/fake handler returning another stream gets bounded copying before adapter JSON decode. Ordinary transport exceptions are sanitized without retaining provider URLs, bodies or token-bearing causes. Response size failures use the existing durable poll retry state and a truthful response_too_large message.

Files: new app/Services/Integration/MailboxResponseBody.php; existing MailboxProviderHttp and Exceptions/MailboxProviderFailure; new tests/Unit/It/MailboxResponseBodyTest.php plus one-shot loopback tests/Support/It/mailbox-response-server.php; existing ItMailboxProviderFailureTest/ItMailboxPollTest now cover oversized responses and persisted recovery for both providers. No production mock, external network/provider request, migration or frontend build.

Initial native test run terminal1/Pest2,4passed/one error/9assertions, tokenit_ef4a86ec12ec42e0, all14postflight/schemaabsence. It exposed double-copying because Guzzle MockHandler writes a sink but returns its original body. Corrected with a fresh bounded copy when a handler returns a different stream. Rerun93630 terminal0/tokenit_723d2bcbe4cc4659:17tests80assertions9s, all14postflight/schemaabsence, including12 real CurlHandler/StreamHandler transfers from synthetic ephemeral loopback servers. Each server handles one bounded request and exits; test finally stops it if necessary. No application bootstrap or database in the loopback worker. Final combined regression19214 terminal0:101tests705assertions188s, tokenit_980a3a5360e14b78, logw11-response-bounds-regression-tests.txt, all14postflight and exact schema absent. This combined count includes the17native/unit cases, not an additional17. Both actual PollItMailboxJob adapter journeys retain a pending discovery receipt, record response_too_large without a successful poll or acknowledgement, then after the retry interval create one ticket and acknowledge once. Seven-source snapshot and diagnostic totals in w11-response-bounds-source-hashes.json; all ten protected design hashes unchanged and Pint/diffcheckpassed. NO active test/server/runtime/build/import/cleanup. New file/browser acceptance remains pending.

Limits: this receive sink bounds decoded body memory and announced lengths; it does not separately count raw compressed wire bytes. Transport timeout remains20s. The current8MiB default is for existing message/metadata reads. Future full-message/file adapters must select limits derived from canonical five-files/10MiB rules (base64/JSON overhead included), then enforce individual and aggregate file bounds; do not call attachment support complete with a smaller accidental transport ceiling. File descriptors, downloads/staging, scanner integration, canonical file ingestion, crash recovery and browser file journeys remain unfinished. Private responses never spill from this sink into untracked OS temporary files.

Reference: Guzzle request-options documentation (sink/on_headers) and the installed Guzzle7 native CurlFactory/StreamHandler/MockHandler implementations were read before implementation. Native handler tests, not the documentation alone, provide the transport evidence. No new browser acceptance claimed for this backend foundation. All protected DESIGN/styles remain read-only; working database preserved.
