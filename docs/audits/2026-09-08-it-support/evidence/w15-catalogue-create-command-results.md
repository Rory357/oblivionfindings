# W15 catalogue create recovery — implementation checkpoint

12 September 2026. Implemented; backend and UI checks passed within the stated scope. Current-asset browser acceptance is pending. W15 and the full plan remain incomplete.

Extended the existing ItSetupCommandService/ItSetupCommandReceipt for catalogue-items instead of introducing another command system. Migration000036 adds a canonical catalogue FK to the existing receipt. The actor is freshly authorized and locked, the original command identity binds the input and result, and replay returns the original record. Field and choice ordering participate in the fingerprint. Recovery rechecks current manager authorization and rejects deleted results. Cancellation either prevents a future create or returns a committed result without undoing it. Audit/record/receipt writes share one transaction. Rollback of the new column refuses to discard catalogue command history.

SaveItCatalogItemRequest binds the original browser actor for current forms. Existing form compatibility is retained. The setup controller serves the JSON command route only for explicit command-bearing requests; ordinary direct service calls retain canonical catalogue behaviour. No new tenant/organisation boundary or provider execution was added.

The catalogue wizard now uses the existing useSetupCreateCommand and SetupCreateRecovery components. A failed or timed-out wait retains the command identity and freezes the original payload for exact retry. Reload stores only an actor-scoped UUID marker, never private form values. Check saved result, Cancel wait, Retry exact create and confirmed Cancel earlier create reuse established behaviour. Confirmed persistence alone shows success; returning to catalogue reloads current records. Changing the signed-in actor remounts the catalogue editor; expired/denied command states conceal its form. Full unsaved draft recovery and edit-command uncertain outcome handling remain open; this checkpoint does not claim them complete.

Backend28103/token it_dee0bea1439142b1 terminal0. The standard summary was not emitted by this run; diagnostic JSONL records26 Test Passed events, no Failed/Errored/Skipped events, ExecutionFinished and TestRunner Finished. All14 independent postflight checks pass and the exact schema is absent. Do not invent an assertion count. Meaningful new HTTP coverage includes exact replay, actor mismatch/denial, changed ordered choices, original outcome recovery, committed cancellation, pre-create cancellation, deleted result denial and rollback/retry after an injected audit failure. Existing teams/queues/services command tests passed in the same run. Pint passed.

Initial UI19passed/2files/4.20s; after reusing the approved recovery panel, final UI19passed/2files/5.01s. A lost-response integration test proves recovery issues one create and one recover request, binds the original UUID/actor, clears its marker only after confirmation and never shows premature success. Scoped lint passes. Initial TypeScript28199 found one test-only unknown-data access; corrected with the mock's explicit response shape. Final TypeScript92763 is running. Build is being started; inspect the current process before starting another.

Final TypeScript92763terminal0, scoped lint0 and build64720terminal0/4m18s. Current app-BaXy6juF.js, manifestf43a2704bedf2e94473f80fe4f3e689674c3a852c369b0e3773a2c3ffcecd608.26 source hashes refreshed; protected design unchanged. The guarded inspector can hold only the exact synthetic technician row for25seconds without mutations; PHP syntax passed, contention mode has not yet executed.

Fresh CatalogueFixtures environment is starting for token0f67ad218cb954e3/fingerprint2acd099dec2db597ac72c429e75df75e2f666dc0d5f7fa0d413e94ee1f3d46a8. Next inspect its exact process and identity, verify normal JSON create plus duplicate-safe recovery/cancellation and account/error controls through the real desktop UI, inspect receipts, then exact cleanup. Continue full W15 requester intake/preview/Sites/audiences/requested-for/attachments and provisioning templates/approval/evidence/lifecycle rather than treating this checkpoint as completion. The working database was not migrated; migrations35/36 are isolated-only.

Further focused revalidation for the next requester slice: ItServiceCatalogue closes on Inertia success without a persistent receipt/result view. My requests uses only myTickets; routes expose provisioning mutations but no canonical GET detail page. A catalogue equipment request therefore needs an authorized public projection/detail link and requester tracking through the existing provisioning record. Do not create another request/ticket store to fill this gap. This is confirmed source evidence, not yet implemented. Private technician notes and internal submitted fields must not be copied into that public view.
