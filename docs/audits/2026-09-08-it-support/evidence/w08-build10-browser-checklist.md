# W08 task foundation / Build10 desktop checklist

Prepared 9 September 2026. This is planned verification, not results. Root owns the in-app browser at its current size; no resizing. W08 remains incomplete after these foundation journeys: immutable completion history, dependency-generation consequences, approval lifetime/cover and personal-work projection are later slices.

Use a fresh reviewed `w06-draft-browser-environment.ps1` token only after final source checks/build. Verify the owner/readiness manifest, exact current entry asset, real CSRF and synthetic actor grants before any mutation. No simultaneous schema import, no Herd database reset, no real notifications/providers. The four existing synthetic tickets and approved Site fixtures are sufficient; create task data through the normal UI.

1. Desktop anatomy: inspect a real ticket screenshot, wrapped title, full-width Messages/Work sections, approved profile navigation and task dialog. Verify the pristine empty generation emits no misleading recovery notice. Record actual viewport without changing it.
2. Task creation: keyboard Add task; blank Continue focuses title. Enter clearly labelled synthetic task A, retain through Escape/Keep-and-close, explicit Resume. Choose current eligible technician, due time, required/evidence flags, explicit Review then Add. Only a validated ACK may show the completion pane; reload proves one persisted task.
3. Task dependencies: create B depending on A; cycle and foreign/unavailable selections must be rejected without losing entered work. Try completion before A is complete and record the real blocker. Starting before a prerequisite remains a known later evaluator gap unless current implementation proves otherwise.
4. Task completion: missing evidence is rejected with a usable focused error; meaningful evidence and note explicitly complete A. Reload verifies actor/time/evidence. Complete B only after A. Do not claim immutable completion history from current mutable fields.
5. Reopen/order: reason required for reopening; cancel the dialog once and verify no mutation, then explicitly reopen. Reorder with keyboard controls and explicit review; reload verifies order. Record the expected version and opaque receipt/event counts from guarded read-only evidence.
6. Two-editor conflict: use another in-app tab at the same desktop size, alter a different task field normally, then submit the old editor. Review must show every current editable value, preserve the dirty proposal and adopt only explicitly; applying is a separate action. Verify current assignment, due, flags, dependencies and register order where relevant.
7. Recovery: normal native navigation retains only actor/context-bound RAM, then fresh authorization and explicit Resume restore the latest proposal. Controlled command uncertainty/cancel and capacity rejection are focused test evidence until a real browser journey is executed; do not fabricate those outcomes.
8. Privacy/session: restricted participant on sensitive ticket has no work meter/nav/private form; copied unapproved ticket is denied. Through ordinary logout/account change, retained editors conceal on session loss and purge on another actor/current permission loss. New actor sees only its own authorized recovery inventory.
9. W07 correction checks: disallowed extension and oversize file keep reply text/prior files and show the current error; valid correction clears obsolete errors. Approved upload explicitly posts once and survives reload. Try the documented browser download event/media API; a blocked new-tab navigation is not successful browser download. Copied file URL remains denied to unrelated requester.
10. Setup Operations: real cleanup freshness distinguishes never-run/unmeasured from healthy; ordinary IT manager sees health without privileged global intent counts. No scheduler/provider activation is implied by opening this page.

Save actual DOM, screenshots where available, committed state observations, errors, recovery actions and test/environment identities in a separate results note. Stop only the exact owned token with its original fingerprint and independently confirm exact schema/directory absence. Every unexecuted item stays open.
