# W15 catalogue wizard — implementation and verification

Implemented, automated checks passed; real browser acceptance is starting, not complete.

The existing catalogue editor uses Rory's canonical WizardShell with request-details/type tiles, the existing field builder, and review cards. Add/edit share the same steps. Completeness is calculated from actual fields. Client validation and server errors return to the relevant step; focus moves to the current labelled form. Dirty close/navigation uses the existing ConfirmDialog, background partial polling does not open unsolicited discard prompts, and inputs/submission/navigation are guarded while saving. A success pane appears only after the server's success flash; an error or unconfirmed response stays actionable. This does not yet establish durable uncertain-save recovery/cancellation, which remains required W15 work.

Publish/withdraw use the guide's480px simple-dialog sizing, header icons, visible validation and current version tokens. The existing published service remains independent of the draft. No DESIGN.md/design_styles file was edited; all10 baseline hashes match. Backend publication/provisioning recheck71921 passed25tests392assertions, terminal0/all14postflight/schema absent. See w15-publication-contract-results.md for exact earlier failure and cleanup evidence.

Initial wizard UI11pass/3selectorfail and render-ref lint error were corrected. The failing selectors inserted spaces absent from the existing shared rail/tile accessible names. Draft baseline is now React state. Final UI14passed/2files/5.36s, scoped lintterminal0. First wizard type run40606 found seven test-only unsupported exact query options; corrected test options preserve default exact string matching. Final TypeScript72609terminal0. Build97056terminal0/4m26s; app-C4-eA06x.js, manifest8fe5fbaa7e4951ddb82a6eb66a451f13d60d766051c650b06a2b7aae5e821893. Nineteen final source and ten protected-design hashes are recorded separately.

The existing isolated harness now accepts optional CatalogueFixtures, fingerprints its helper and flag, and publishes ready.json only after all four canonical fixtures are created. Cases: approval-required provisioning, public service, unpublished draft, and revised internal draft with the original public version still published. Fixture authoring uses the canonical management services. PHP fixture/bootstrap/runtime syntax checks passed. No browser assertion yet.

Owned bootstrap67674/token9482be3c7961a5d0/fingerprintabd2c0b0e73705f4053794faf3a4ee82ad5cfa2211a7eef6eff40b6b1a751c61 is running. No parallel DB/test/build/lock. Do not edit fingerprinted helpers/migrations/assets. Next verify identity and real desktop author/requester journeys in a fresh owned in-app tab, preserve user tab2 and never resize. Reconcile actual writes and clean the exact runtime after acceptance. W15 and the complete W00–W27/E01–E23 release gate remain open.
