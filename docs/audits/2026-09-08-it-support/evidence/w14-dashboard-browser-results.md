# W14 native Monitoring dashboard desktop verification

Run in progress; this file records observed cases only. W14/E13 and the full release gate remain open.

Owned token d716ac905348a2f9, fingerprint511157badfc1a14fea63663543468e62da94ba43ddc953dcd75322895d82182c. CreateAndStart70194 completed terminal0; owned serverPID46040. Read-only identity confirms this checkout, exact disposable schema, array mail, sync queue, normal CSRF and manifest e3e6553f6071fe1b0ed608079d23254d158ce6628949b95b43957fe152bd5373. Browser DOM confirms app-CUgUJHwk.js. Owned in-app tab19 only; user tab2 and Chrome tabs untouched. Natural viewport1063x856; never resized.

Normal password sign-in as the dedicated synthetic w14-monitoring account landed on My Day, followed by the actual Monitoring navigation link. The Monitoring page showed24 permitted checks and21 active findings from these fixtures. Other-Site device4/ticket11 and device13/ticket16 were absent, as were their Site options. Availability direct8, recovered9, urgent10 and urgent-recovered12 links matched the fixture records. Technical direct13, recovered14 and urgent15 links were also present. Pending technical delivery12 had no invented ticket link.

Keyboard Apply search for condition reduced the list to four technical checks. Direct13 had an independent IT link without a Control Room alert. Recovered14 was Healthy and explicitly retained the technician-closure-pending ticket. Urgent15 had both CR-2026-0003 and IT-000015. The screenshot was inspected inline in dark mode: visible cards, filters, focus ring and linked-work rows fit the natural desktop viewport. No screenshot file was saved.

Return on the direct13 link opened the canonical ticket: Open, System original report, failed-technical-check wording and assigned technician/Site A. Returning through Monitoring and pressing Return on CR-2026-0003 opened the actual read-only alert workspace. Its Linked records section showed the same IT-000015 and canonical device11, with the operational response still Open. Escape exited the workspace. This proves the urgent link target, not a fabricated operational transition.

Two selector waits expired before slower local navigation completed; inspecting the completed DOM recovered them. One used Original report instead of the actual System — original report label. No duplicate submission or application mutation was performed. Browser error/warning log at the first navigation was empty; final log and cleanup remain pending.

Next: source-only account concealment and direct denial/recovery; optionally close the outstanding worklist-origin handoff entry criterion in this same owned runtime. Then exact-fingerprint teardown, independent schema/root absence and source/design hash recheck.

## Restricted access observed

Normal logout/login as w14-monitoring_source_only showed the same four permitted technical checks, no IT ticket href/reference and no Control Room alert href. Other-Site checks stayed absent. Direct navigation to the known ticket13 returned the actual403 page. Returning to Monitoring succeeded without elevated permissions. The full monitoring actor also opened alert7 from the worklist in read-only mode; its IT handoff controls were absent because that fixture has no alert-management grant. No grant was added to force that action. The original technician fixture already has the appropriate handoff grant and is used for the following checks.

Prepared a guarded CLI-only evidence helper for the original technician in the exact owned schema. It can revoke/restore only that fixture's existing controlRoom.alerts.manage grant or age exactly one of its database sessions. Expiry exercises Laravel's actual DatabaseSessionHandler last_activity comparison; it does not change application time, session policy, cookies or production accounts. No access transition has yet been executed. The helper is separate from the fingerprinted bootstrap sources and receives its own source hash before use.

## Worklist-origin defect and cleanup

The original technician opened alert7 using Continue response, then Linked records → Prepare IT handoff. The actual list-origin wizard selected existing ticket22 and reached its technical-reason step. However, the discard confirmation repeatedly reopened after Cancel. Revalidation identified the underlying worklist's30-second router.reload({only:['alerts','stats']}) entering the handoff's global navigation guard. This is a confirmed UI defect; prior direct-route browser evidence did not cover this caller. Two retries of Cancel/Continue were obstructed by that polling. The draft was deliberately discarded; no handoff was submitted.

Read-only final reconciliation shows all handoff fixture links/receipts empty and23 tickets, unchanged. Final browser warning/error log was empty. Owned tab19 closed; cleanup35750 terminal0; independent postflight terminal0 proves exact schema/root absence. No permission or session transition helper was executed. User tab2, working DB and protected design were preserved.

Implemented correction: while a draft or unconfirmed handoff needs guarding, same-URL asynchronous GET partial reloads are prevented without opening a leave dialog. Actual navigation/full reload/write still requires the existing explicit review. This pauses the worklist background refresh rather than permitting it to replace the active form. The current Inertia implementation marks reloads async and preserves state; no library behaviour was guessed. Focused UI/command/contract run24passed/3files/8.07s terminal0; lint terminal0 with the existing epoch-ref warning. Full TypeScript86799 is running. Rebuilt browser acceptance, list-origin save/return and actual permission/session transitions remain pending.
