# IT implementation handover — 13 September 2026

## Start here

The user explicitly requested a fresh session once the current browser check and cleanup finished. Both are now complete. Continue implementation in this SAME saved checkout as sole implementer. No worktree, subagents, reset, revert or unrelated staging. Preserve independent My Day, Governance, Sites and shared edits. The parent task will stop editing after dispatching the new task.

Read root AGENTS.md, applicable instructions/design, `docs/architecture/single-tenant-application.md`, the full `implementation-plan.md`, `audit.md` and `implementation-progress.md`. The latest CURRENT entry governs runtime state; older package paragraphs and checkpoint histories include obsolete live-process instructions. Do not replay completed tests merely to reconstruct history.

## Latest user priorities and product requirements

The user feels too little visible functionality has been built and too much time spent testing. Deliver substantive implementation before grouped relevant checks; rerun affected failures only. Start implementing in the first turn after the necessary targeted source inspection rather than writing another audit or launching another verification-only cycle.

1. Every Knowledge document needs its own complete, directly addressable full page, with full-page reading and editing as the primary experience. Bring content, owner/audience/review date, related records, files, diagrams and revisions together. The current modal reader is partial and does not satisfy this. Reuse existing article identity, content-on-demand endpoints and review/version services. Maintain legacy links and preserve the library filters/page when returning.
2. Word `.doc`/`.docx` and PDF upload, open, original download, replacement versions and history must be implemented for Knowledge. Protected ticket/catalogue attachments do not satisfy this. Prefer in-app preview with an explicit original-file fallback where preview is unsupported; never render a blank success. An optional earlier question about Word Open has not been answered; do not reask or stall, use that reasonable default. Enforce direct-file access with current role/site/document policy.
3. Create and edit diagrams INSIDE Knowledge, save/reopen editable source and retain versions. Default to visual shapes/connectors/text authoring and a readable published rendering. Imported images or a code-only placeholder do not deliver the requested diagram authoring. Inspect existing installed facilities before choosing an implementation; apply document draft/review/publication and protected file rules.
4. W23 vendors and W24 credentials are the next dedicated workstream after this Knowledge slice, not an indefinite backlog. Vendor/contact/renewal and shared credential foundations exist, but full dedicated implementation passes have not started. Vendors cover canonical suppliers/contacts, licences, warranties/contracts, owners/dates/renewals and follow-ups. Credentials must reuse the SAME vault/credential ID in Sites and IT, with masked lists, granular permissions, required step-up including SSO-only users, audited reveal/copy, concealment, lifecycle, compatibility and recovery. Include all-approved-Sites or this-Site access and explicit permitted house-staff reveal/copy; do not invent real people or credentials.
5. Retain the earlier priorities: separate Provisioning, Knowledge and Reports destinations, ticket-focused Service Desk, modern Problems/Changes/Major Incidents, structured systems/assets/software/vendors/relationships/runbooks/revisions/review ownership. Some source and browser slices exist; none imply complete packages.

The full authorized objective remains ALL W00–W27, all16 audit findings, all36 backlog items, E01–E23 and every release criterion. Resume the goal, do not narrow it to this handover. W15 attachment/draft/joiner-mover-leaver completion, W19 multiple technicians/schedules and the remainder still apply. AI W25 is preparation only; production model execution stays disabled. This handover does not certify completion. Current ledger: one verified baseline,19 in progress,8 planned;27 nonbaseline packages open. Do not present these counts as an effort percentage.

## Implemented and tested source to reuse

- Dedicated navigation and refreshed specialist lists/details already exist in this dirty checkout. Recent fixes: `specialist-workspace-header.tsx` and `specialist-record-header.tsx` overflow clipping; `ItMajorIncidentService::postUpdate` prevents internal command notes from moving audience deadlines.
- Knowledge revisions: separate working proposal, immutable reviewed revisions, owner/review dates, compare/publish/restore, typed structured content and canonical relationships. `ItKbRevisionService` normalizes date-only ISO projections without rewriting historical ciphertext. All broader acceptance remains open.
- `ItKnowledgeWorkspace` now selects metadata for24 library rows, omits body/structured content and working-copy ciphertext from list queries, and fetches complete selected reader/editor/history content separately. `content_loaded` distinguishes full content from metadata.
- `resources/js/pages/it/index.tsx` prioritizes full selected content, loads metadata rows through authorized navigation and handles reader-close/immediate-pagination without reopening. `it-wizards.tsx` hydrates the editor once from fresh context using Inertia `form.setDefaults`/`setData`, retaining later typing through context refresh.
- Publication confirmation now keeps its identity and disabled Saving state until the mutation completes; pending/failure/retry UI regression passes.

Two small observed defects are deliberately carried into the requested full-page implementation, not silently marked fixed: native title hrefs drop the library page/filter context (`index.tsx` hrefFor near3420/3460), and `KbArticleDialog.afterSave` duplicates the shared flash toast (`it-wizards.tsx` near3270). Resolve them as part of the new document navigation/editor.

## Last verification; avoid rerunning unchanged checks

Read `evidence/w21-demand-loading-20260913.md` and `evidence/w21-correction-desktop-browser-20260913.md` for precise limits. Latest correction UI group27 passed; demand-loading group passed23 across initial and affected reruns, with the strengthened15-case page suite passing separately. Scoped lint/format/Pint passed. Final repository typecheck has no IT errors and three independent My Day `ByRoleOptions.exact` errors in `resources/js/components/today-retirement.test.tsx`; preserve that independent work.

Guarded backend session88177 EXIT0: Major Incident7 plus Knowledge Revision Workspace9 =16cases,251assertions,230.23s. All14 pre/postflight checks passed and exact disposable schema was absent. Build69748 EXIT0:5217modules,3m55s, `app-CcDgQVmz.js`, manifest `60291dcf135455e785804f9f9387c141e0d36fa501b5ce675a88f8f86d439b85`.

Actual browser verified all three specialist headers in Cards/Table, internal-note deadline unchanged, on-demand full article loading, close/immediate pagination, full fresh proposal editing with retained content and correct calendar date, separate reviewer comparison, stable publish confirmation during Saving, successful publication and reopened approved content. Console warn/error empty. Native title return-context and duplicate toast defects remain as above. No Word/PDF, diagram-authoring or full-page-document claim.

## No live owned runtime

Browser token `3060db40e95144d9`, fingerprint `3877985f66e687cdf139ece4bd14b056f1904f87dcfe0e74454f53e31f0d816f`, port8766 is CLEANED. Owned tab2 closed. StopAndRemove88758 EXIT0, server settled, exact schema/directory removed; independent read-only postflight EXIT0 confirms both absent.96 source/asset hashes remained unchanged throughout browser verification. Do not recreate or clean that old token. Preserve unrelated user tab1 at `http://127.0.0.1:8767/it?tab=catalog`.

Working database `oblivion_findings_codex_test` must NEVER be reset. New revision migration `2026_09_13_000001_add_it_knowledge_revision_workspaces.php` was tested only in disposable schemas and remains unapplied to the working DB. Backend tests use only the existing guarded isolated runner; wait for independently running test work before another database-intensive batch. Browser uses in-app browser, unchanged dimensions, ordinary login/current build, fresh guarded token and exact cleanup/postflight. Never edit source/build/helpers while the owned browser runtime exists.

PHP: `C:/Users/steph/.config/herd/bin/php84/php.exe`; Node: `C:/Users/steph/.hermes/node/node.exe`. Vitest/build/native PHP may need the existing reviewed escalation. Prefer focused grouped checks. Do not run unsafe default Playwright global setup against the working database. Single operating organisation, approved Sites, canonical records and role/record privacy boundaries; no new tenant architecture. Preserve DESIGN.md/design_styles. No deploy, real communications, live provider config, production AI, destructive production action or GitHub push/retry. Local commits were authorized, but do not stage unrelated work.
