# Fleet & Asset Incidents redesign — build PROGRESS tracker

> Loop checkpoint doc. Branch `feat/fleet-incidents-redesign`. Design drop: `.design-drops/fleet-incidents-redesign/` (`Fleet Incidents.dc.html` + `HANDOFF.md`). Canonical specs: `FLEET_INCIDENTS_LIFECYCLE_PLAN.md` (data/lifecycle/cross-module) + `docs/FLEET_INCIDENTS_GAP_ANALYSIS.md` (A–H checklist SSOT). Near-twin of the shipped Incidents (`docs/incidents-redesign/PROGRESS.md`) + Safeguarding (`docs/safeguarding-redesign/PROGRESS.md`) redesigns — reuse those patterns 1:1.

**Region:** NZ only (Waka Kotahi, Land Transport Act 1998 **s22** 24h Police/TCR, NZ Police, ACC, WorkSafe/HSWA, Ngā Paerewa). **Platform:** web-only. **Scope:** H&S incident slice now; register/telematics pieces = `PREP-LATER` (UI+schema now, wired later).

## Decisions log
- **2026-06-18** — Build autonomously in `/loop` (user dropped the design drop + started the loop). Feature branch in the main repo (NOT a worktree) so backend tests exercise the edited code + migrations run against local DB. Migration policy: run local autonomously.
- **2026-06-18 — Severity vocab (Gap F4):** KEEP `FleetIncident`'s `minor/moderate/major/critical` (established vocab + data + UI + validation); map correctly at every cross-module boundary (critical/major→high, moderate→medium, minor→low). Chose "map in UI/service" over a destructive migrate. **Fixes the latent observer bug** where `severity in ['high','critical']` never matched `major`, so major incidents never raised a Control Room alert.
- **2026-06-18 — Attachments:** dedicated `FleetIncidentAttachment` table mirroring `SafeguardingAttachment` (matches the H&S family; reuses shared `file-dropzone.tsx`).
- **2026-06-18 — Follow-ups:** mirror the `IncidentFollowup` pattern (confirm reuse-vs-new after reading it in Step 1).

## Steps
- [x] **Step 1 — Backend schema + model + observer fix** (B1–B6) — DONE (commit pending). Migration `expand_fleet_incidents_capture` (70 cols across §3.1–3.12 + near-miss, all nullable/defaulted, short VARCHAR + `fi_*` indexes); `fleet_incident_attachments` + `fleet_incident_followups` tables/models; `client_incidents.fleet_incident_id` reverse FK + `ClientIncident::fleetIncident()`. `FleetIncident` model: full fillable/casts/relations (attachments/followups/clientIncidents/assignedTo/supervisor/linkedHsEvent), enum constants, `mapSeverityToHs()` (minor→low/moderate→medium/major→high/critical→critical), s22 helpers (`requiresPoliceReport`/`isPoliceReportDue`/`policeReportDueAt`/`policeReportHoursRemaining`), `isOffRoad`/`reference`/`isHighSeverity`, tab scopes. **Fixed latent observer bug** (`severity in ['high','critical']` never matched `major` → no CR alert + HsEvent recorded as `low`; now routes through `hsSeverity()`/`isHighSeverity()`). Migrations ran clean locally; boot-checked mapping/s22/classifier/schema/scopes.
- [x] **Step 2 — Controller + routes + services** — DONE (commit pending). `IncidentController` rewritten: full `captureRules()` validation (create=required core / update=sometimes); `applyRegulatory()` computes s22 `police_report_due_at` (occurred+24h) + `is_notifiable` via reused `NotifiableEventClassifier`; `store()` snapshots rego/category + sets `fleet_incident_id` on cascaded client incidents (Gap F1); content-negotiated `show()` (`wantsJson`→`buildDetailPayload` for the modal, else Inertia thin page) — see [[reference_inertia_axios_405]]; tab-scoped `index()` (Site/Vehicle/Driver/type/severity/search/date filters, `stats`+`tabCounts`, row `flags` incl. live police countdown, `formOptions` for the wizard, CSV export w/ new cols). Endpoints + routes: `update`, `updateStatus` (closure gate: resolution_notes required + advisory warnings), `addFollowup`/`completeFollowup`, `uploadAttachment`/`downloadAttachment`/`destroyAttachment` (mirror Safeguarding evidence ctrl), `logPoliceReport`(TCR)/`logClaim`/`markOffRoad`/`backInService` — all `RespondsToInertiaOrJson`. 15 routes register; lint+boot clean; `applyRegulatory`/`captureRules` reflection-verified. **Full HTTP feature tests deferred to Step 7 (need factories — local DB unseeded).**
- [x] **Step 3 — List page** (`index.tsx`) — DONE (commit pending). Rebuilt on `hs-hero-kit` (medallion Truck, eyebrow pill, 2 clusters: This period + Needs attention w/ live tones), `TabStrip` (9 tabs w/ counts), footer band (HeroSegmented period + Site/Vehicle/Driver `EntityFilter onDark` + Severity segmented + search + CSV + clear), `ShiftContextMenu` right-click rows (status-appropriate, copy-PRN pattern) + row→detail modal via the house `only:['detail']` partial-reload pattern, badges incl. **live s22 police countdown** (`PoliceCountdownPill`). Controller `index()` reworked to lazy closures + `detail`/`report` props (so a detail-open doesn't recompute the list). Mirrors the shipped `pages/incidents/index.tsx` (string paths, not Ziggy). **Built genuine (not fake) `FleetIncidentDialog` (read-only detail w/ s22+WorkSafe banners, all sections, linked records, evidence) + `FleetIncidentReportDialog` (functional core-capture form → store)** — both restructured in Steps 4–5. **`tsc --noEmit` = 0 errors project-wide.**
- [x] **Step 4 — Detail modal** (`FleetIncidentDialog`) — DONE (commit pending). Rebuilt on `WizardShell` read-only chrome: 10-section rail (Overview w/ StageTracker + s22 countdown + WorkSafe banners · Vehicle/asset w/ PREP-LATER snapshot note · People incl. testing · Scene & conditions w/ coords + Google/live-map links — no embedded Leaflet · Damage & recovery/VOR · Police & regulatory · Insurance & cost · Photos & documents via shared `AttachmentUploader` + gallery w/ download/delete · Investigation & follow-ups w/ complete action · Linked records both-shown), footerStart severity/status/VOR chips, footerEnd Options bar. **6 in-place action panes** (status+closure-gate, follow-up, Police-report/TCR, claim, off-road, back-in-service) all `useForm`→Step-2 endpoints→`back()` refresh — **covers most of Step 6's workflow sub-modals too**. `tsc` 0 errors. Thin `show.tsx` fallback = Step 7.
- [x] **Step 5 — Report wizard** (modal, 3 branches) — DONE (commit pending). `FleetIncidentReportDialog` rebuilt on `WizardShell` multi-step: **vehicle 6-step** (Vehicle/driver+TilePicker type · What-happened+scene Segmenteds · People+third-party mini-form+witness repeater · Damage+ChipMulti areas+VOR · Police/regulatory [injury toggle→s22 InfoCard + injury TilePicker + TCR; ACC; breath/drug] · Review+insurance) · **asset 4-step** (Asset details · What happened · Condition & cost · Review) · **near-miss 4-step** (Vehicle · What happened · Could-have-happened [potential severity + contributing factors ChipMulti] · Review). Per-step `stepValid` gating + `pct` Ring + Back/Next/Submit; `WizardSuccessPane` (Open incident / Report another / Done). Photos **deferred to detail** (no record id at report time — matches H&S wizard; InfoCards point to the Photos tab). `form.transform` casts numerics + arrays → `store`. **Backend:** `store()` now `back()->with('created_fleet_incident_id')` + added that key to `HandleInertiaRequests` shared flash (mirrors created_client/site/concern). `tsc` 0 errors. ⚠️shared-file edit flagged: HandleInertiaRequests (+1 flash key, additive).
- [x] **Step 6 — Cross-module + telematics storyboard** — DONE (commit pending). Workflow sub-modals already shipped in Step 4. **(1) Telematics PREP-LATER storyboard** (`fleet-telematics-storyboard.tsx`): 4-step stepped modal (Sensor→Triage→Confirm→Draft) mirroring the client-incident fall-detected flow + the `sensor-triage-dialog` visual; launched from a dashed "Telematics preview · PREP" button in the list hero; design-only (telemetry is passive). **(2) Reverse client-incident link (Gap F1):** the shipped client-incident detail now surfaces its originating fleet incident — `app/Http/Controllers/IncidentController.php::buildIncidentDetail()` eager-loads `fleetIncident:id,incident_type` + adds a `fleet_incident` block (`reference()`/`incident_type`); `incident-detail-dialog.tsx` adds the `fleet_incident` type + a Truck `LinkedRow`→`/fleet-assets/incidents?incident={id}` + guard. `tsc` 0 errors. ⚠️**cross-module edits (additive):** Incidents `IncidentController` (+payload block, +eager-load) and `incident-detail-dialog.tsx` (+type, +linked row).
- [x] **Step 7 — Tests + retire + ship** — DONE (commit pending; merge next). `tests/Feature/FleetAssets/FleetIncidentTest.php` (14 tests/69 assert): store+s22+WorkSafe derivation, observer Gap-F4 fix (major→HsEvent `high`+CR alert; minor→`low`+none), transport cascade w/ direct FK + SafeguardingAlert, closure gate, followups, attachment upload, police-report/TCR, VOR round-trip, claim, worklist scopes, index render + detail JSON, permission 403. **Found+fixed via tests:** cascade `SafeguardingAlert` silently failed two enum constraints (`severity` major∉enum, `alert_type` transport_incident∉enum) → now maps severity + uses `requires_monitoring`. Aligned `FleetIncidentFactory` severity to model vocab + corrected 2 HS tests that encoded the old low/medium/high vocab. **Regression-safe: 203 tests green** (FleetIncident 14 + HS observer 49 + client IncidentController 140). Retired `create.tsx` (create()→redirect) + `show.tsx` (show() non-JSON→redirect to list `?incident={id}`, modal-over-list). tsc 0, vite build clean.

## Build log
- **Commit `bbf4e390`** — branch + docs (gap-analysis SSOT + this tracker).
- **Commit `44e23b1d`** — Step 1 backend (4 migrations + 3 models + ClientIncident reverse FK + observer Gap-F4 fix). Migrations applied to local DB; boot-checked.
- **Commit `2844c0f7`** — tracker anchor.
- **Commit `9a57d5bc`** — Step 2 controller + routes (15 routes; logic reflection-verified).
- **Commit `6733c484`** — Step 3 list page + index closures/detail prop + dialog stubs (tsc 0 errors).
- **Commit `fe8361c6`** — Step 4 detail modal on WizardShell (10 sections + Options bar + 6 action panes; tsc 0 errors).
- **Commit `e6da3274`** — Step 5 report wizard (3 branches) + store back()/flash + HandleInertiaRequests flash key (tsc 0 errors).
- **Commit `ca32cb5e`** — Step 6 telematics storyboard + reverse client-incident link (cross-module; tsc 0 errors).
- **2026-06-18** — ✅ full `vite build` clean (3m41s). tsc 0 + build 0 across the whole branch. Only Step 7 (tests + retire pages + merge/deploy/verify) remains.

## NEXT (resume here) — Step 7: tests + retire + ship (FINAL)
**Test-infra facts (mapped):** PHPUnit class style (`class X extends Tests\TestCase { use RefreshDatabase; }`); test DB = **MySQL** `oblivion_findings_codex_test_{PID}` loaded from `database/schema/mysql-schema.sql` **+ pending migrations run automatically** (so my new fleet_incidents cols + tables ARE in the test DB); `hs_events`/`control_room_alerts`/`notifications` tables present (observer side-effects work synchronously, QUEUE=sync, MAIL=array). **Auth pattern:** `$this->seed(\Database\Seeders\RbacSeeder::class);` then `$u = User::factory()->create(['role'=>'admin','approved_at'=>now()]); $u->roles()->attach(\App\Models\Role::where('name','admin')->first());` → `actingAs($u)` passes `fleet.incidents.manage|fleet.manage`. **Factories EXIST:** FleetIncident, Asset (`->vehicle()`,`->forSite($site)`; NO `registration_number` by default), User (no `role` default — pass it), Client (no `site_id` default), Site, FleetVehicleBooking. **MISSING:** `FleetResidentTransportFactory` → create it or use raw `FleetResidentTransport::create([...])` (cols: `booking_id`,`resident_id`,...). ⚠️**`FleetIncidentFactory` severity uses `low/medium/high/critical`** — stale vs model `SEVERITIES`=minor/moderate/major/critical → **align the factory** (then run full fleet suite to confirm no breakage). `store()` fans out `FleetIncidentReportedNotification` to managers → `Notification::fake()` in store tests if noisy. Run: `php84\php.exe artisan test --filter=...` (NON-parallel — [[reference_testing]]).
1. **Feature tests** (`tests/Feature/FleetAssets/FleetIncidentTest.php`). Cover: store (vehicle) creates + computes s22 `police_report_due_at`(occurred+24h when injury) + `is_notifiable`; observer fix (`severity=major` → linked HsEvent severity `high` + a control_room_alert row); transport cascade sets `client_incidents.fleet_incident_id` (+ SafeguardingAlert for major/critical); updateStatus closure gate (close w/o resolution_notes → `assertSessionHasErrors`); addFollowup/completeFollowup; uploadAttachment (`UploadedFile::fake()`); logPoliceReport(TCR)/logClaim/markOffRoad+backInService; index `tab` scoping (police_report_due) + JSON `show` payload (`->getJson` has flags). Permission-denied path (non-admin → 403) optional.
2. ✅ **`vite build` DONE** — `npm run build` clean (3m 41s, no wayfinder OOM). (eslint warnings-only, matches sibling pattern — OK.)
3. **Retire legacy pages:** `create.tsx` (create() already redirects) + `show.tsx` → thin deep-link shell rendering `FleetIncidentDialog` (the controller `show()` already returns the JSON payload / Inertia thin page — make `show.tsx` mount the dialog standalone, or redirect to `?incident={id}`). Delete dead page components.
4. **Permissions:** no new permissions (reuses `fleet.viewAny|assets.viewAny` + `fleet.incidents.manage|fleet.manage`) — confirm, so no PermissionsSeeder run needed on deploy ([[reference_deploy_seeders]]).
5. **Merge** `feat/fleet-incidents-redesign`→main (`--no-ff`) → push → deploy webhook (~5-8min) → **Chrome-verify on .com** as Demo Admin (list/hero/tabs/ctx-menu/detail-modal+sections/report-wizard 3 branches/telematics storyboard; 0 app console errors). ⚠️busy main — re-check before merge. ⚠️run `*PermissionsSeeder` only if perms added (they aren't).
Two pieces (the workflow sub-modals are already done in Step 4):
1. **Telematics crash-detection → confirm → draft storyboard (Gap F2, PREP-LATER).** A 4-step storyboard modal (sensor hard-event → Control Room paged → operator confirm/dismiss → drafts a pre-filled FleetIncident), mirroring the client-incident `fall_detected` flow. Design-only/PREP-LATER — launch it from the list hero (a small "Telematics preview" affordance) or the detail header. Build as a self-contained component `resources/js/components/fleet/fleet-telematics-storyboard.tsx`; no backend wiring (telemetry is passive). Check the prototype's storyboard + `INCIDENTS_DESIGN_PROMPT.md` fall_detected flow for the visual.
2. **Reverse client-incident link (Gap F1 cross-module).** The originating fleet incident should surface on the client-incident detail. Edit the shipped `incident-detail-dialog.tsx` Linked-records section + its controller payload (`IncidentController`/`buildDetailPayload` for client incidents — find it) to include `fleet_incident_id`→a link to `/fleet-assets/incidents?incident={id}`. The FK + `ClientIncident::fleetIncident()` already exist (Step 1). ⚠️shared/cross-module edit to the Incidents module — additive, flag in PROGRESS.
Then Step 7 (feature tests w/ factories + full `vite build` + retire `create.tsx`+`show.tsx`→thin page + merge/deploy/Chrome-verify on .com).
Replace `resources/js/components/fleet/fleet-incident-report-dialog.tsx` (currently a functional core-capture form) with the full **branched `WizardShell` wizard** mirroring the shipped `resources/js/components/incidents/incident-report-dialog.tsx`. Branches: **vehicle 6-step** (Vehicle/driver/booking · What happened+scene · People · Damage & drivability+photos · Police & regulatory [injury→s22 + WorkSafe auto-surface] · Insurance & review) · **asset 4-step** (Asset details · What happened+photos · Condition & cost · Review) · **near-miss 4-step** (Vehicle & driver · What happened+photos · Could-have-happened [potential severity + hazard] · Review). Use the wizard primitives (`Field`/`SelectInput`/`Segmented`/`ChipMulti`/`TilePicker`/`Ring`) + `WizardSuccessPane` (Report another / Done / Open incident) + `AttachmentUploader` for photo capture. Posts to `store` (already accepts the full capture set). `formOptions` already passed in. The mode is set by the launcher (`ReportMode`). **Use a subagent** to extract `incident-report-dialog.tsx` + `WizardShell` multi-step (stepIndex/onStepClick/pct/success) + wizard primitives API before writing.
Then Step 6 (telematics PREP-LATER storyboard launched from list/detail + client-incident reverse surfacing of the originating fleet incident — Gap F1 cross-module edit to `incident-detail-dialog.tsx` + its controller payload), Step 7 (feature tests w/ factories + full `vite build` + retire `show.tsx`→thin page + merge/deploy/Chrome-verify).
**Note:** Step 4 already built the workflow sub-modals, so Step 6 is just the telematics storyboard + the reverse client-incident link.
Rebuild `resources/js/components/fleet/fleet-incident-dialog.tsx` on the **`WizardShell` read-only chrome** (rail = 10 sections, footer = Options action bar) mirroring `resources/js/components/emar/prn-detail-dialog.tsx` (`PrnDetailDialog`) + the shipped `resources/js/components/incidents/incident-detail-dialog.tsx`. Sections: Overview (stage tracker Reported→Investigating→Resolved→Closed + s22 countdown + injury/WorkSafe banners) · Vehicle/asset (PREP-LATER snapshot note; equipment variant) · People (driver/passengers-residents/third parties/witnesses) · Scene & conditions (map placeholder reusing fleet-assets/map) · Damage & recovery (VOR) · Police & regulatory (countdown + TCR) · Insurance & cost · Photos & documents (reuse shared `components/ui/file-dropzone.tsx` [[reference_premium_file_dropzone]]) · Investigation & follow-ups · Linked records. Options bar → Step 6 workflow sub-modals (status/followup/upload/police/claim/VOR), each posting to the Step-2 endpoints then `onChanged()`. The detail payload (`FleetIncidentDetail`) + endpoints already exist. **Use a subagent** to extract `PrnDetailDialog`/`IncidentDetailDialog` + `WizardShell` read-only API + `file-dropzone` API before writing.
Then Step 5 (report wizard on WizardShell, 3 branches — replace the core-capture form), Step 6 (workflow sub-modals + telematics PREP-LATER storyboard), Step 7 (feature tests w/ factories + full `vite build` + retire `show.tsx`→thin page + merge/deploy/Chrome-verify).
Rebuild `resources/js/pages/fleet-assets/incidents/index.tsx` to the gold standard. Props now provided by the controller: `incidents{data,links,meta}` (rows w/ `reference`+`flags`), `tab`, `tabCounts`, `stats`, `filters`, `formOptions{assets,users,sites,types,severities,injury_severities,damage_classifications}`, `can.manage`.
- Hero on `hs-hero-kit` (`resources/js/pages/health-safety/components/hs-hero-kit.tsx`): `HeroShell`+`HeroMedallion`(Truck)+`HeroStatusPill`; `HeroCluster` ×2 — **This period** (Reported/Investigating/Resolved/Closed from `stats`, each →tab) + **Needs attention** (police_due/worksafe_notifiable/off_road/open_claims/injury_acc). Primary action **＋ Report incident** → launcher (3 choices) [wizard = Step 5].
- `TabStrip` (`@/components/rostering`): All·Open·Under investigation·Police report due·Injury/ACC·Insurance & claims·Off-road (VOR)·Near misses·Closed (counts from `tabCounts`). Footer band: `HeroSegmented` date-range + Site/Vehicle/Driver `EntityFilter onDark` + type/severity + search pill + CSV export.
- Rows: right-click `ShiftContextMenu` (copy PRN `openRowCtx` — `pages/emar/PrnRecords.tsx:193`) + row→detail modal (Step 4 dialog; stub the open handler now). Badges from `flags` (police-due **live countdown**, injury/ACC, VOR, claim, alert-linked, attachments). Tab/filter changes via `router.get` partial reload (`only:[incidents,stats,tabCounts,tab,filters]`).
**Use a subagent** to extract `hs-hero-kit` exports + the shipped Incidents list (`resources/js/pages/.../incidents/index` or equivalent) + PRN ctx-menu pattern into a precise prop/usage spec, to spare context before writing the page. Then Step 4 (detail modal `FleetIncidentDialog`), Step 5 (wizard), Step 6 (workflow modals + telematics storyboard), Step 7 (tests+ship).
