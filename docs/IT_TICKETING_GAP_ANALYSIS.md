# IT & Provisioning → Full IT Ticketing System — Gap Analysis & Build Checklist

> Seeded by pass 0 of the loop (2026-07-07). Loop protocol: take the **first unchecked item**,
> implement it as one small verifiable slice, verify, tick with a one-line note, commit, stop.
> Schema only from §P of the loop prompt; anything beyond → `IT_TICKETING_QUESTIONS.md` (repo root).

## Verify commands

- `php artisan test` (Pest — scoped: `php artisan test tests/Feature/It`) — ⚠️ junctioned-vendor worktree: PHP tests autoload the PARENT's app/; verify in parent or copy vendor + dump-autoload first.
- `npm run build` · `npm run types` · `npm run lint`
- `npm run test` (vitest) · `npm run visual:test` (Playwright, axe available)

## Audit findings (pass 0 — all confirmed against the code)

1. **Self-service is impossible.** `routes/web.php:144–154`: the outer group is `permission:it.view` (gates even `GET /it`) and `it.tickets.store` sits inside the nested `permission:it.manage` group. `it.view`/`it.manage` are granted to admin/provider_manager/hr only (RbacSeeder L417–418, grants L628; grant migration `2026_07_02_100002`). Support workers 403 on everything — confirmed by the existing test ("the /it hub is gated on it.view").
2. **No ticket detail surface.** No show route, no thread, no comments, no history. `resolveTicket` (controller L252–269) takes no resolution note; nothing records what fixed it. All actions live in row context menus.
3. **No SLA anything.** `it_tickets` has no due/response columns (migration `2026_07_02_100001`), no breach state, no escalation, no scheduled command. `routes/console.php` is where commands schedule (`app(Schedule::class)` blocks, ~L70+).
4. **`stats()` is thin but correct** (controller L353–376): requests pending/in_progress all-time + done_30d, tickets open + urgent. No unassigned, no SLA states, no awaiting-reply, no per-view counts. Needs a real summary payload.
5. **Queues are unpaginated** — `requestRows`/`ticketRows` (L276–350) do `get()->map` full-table arrays. Needs server-side pagination + summary endpoint for hero counts.
6. **Hero is hand-rolled** — `it/index.tsx:267` `bg-gradient-to-br from-primary/90 via-primary to-primary/80`, static (non-link) chips. Not the golden band kit (`people-hero.tsx` shape).
7. **Ticket feature gaps:** flat 4 categories, no subcategory, no attachments, no watchers, no reopen, no auto-close, no CSAT, no merge, no saved views, no bulk, no reports, no KB, no email-in. Variant maps at `it/index.tsx:84–104` are semantic maps *feeding* `StatusBadge` — acceptable pattern, keep/extend.
8. **Provisioning gaps:** no priority, no due date; `notes` is a single final-outcome field overwritten at fulfil.
9. **Notifications:** only `ItProvisioningCancelledNotification` (app/Domain/Hr/Notifications) exists. Requesters hear nothing on create/assign/resolve. No ticket notifications at all.
10. **Tests:** 4 happy-path tests in `tests/Feature/It/ItProvisioningTest.php` (gate, checklist bridge, fulfil→task-complete, ticket lifecycle). No policy/authz matrix, no SLA, no comment coverage. These 4 stay green forever.
11. **Attachment infra answer (§P.4):** NO generic polymorphic attachment table exists — every module has its own model (`HsAttachment`, `PrivacyAttachment`, `ClientIncidentAttachment`, `ClinicalAttachment`, …). The shared piece is the controller concern `app/Http/Controllers/Concerns/ServesPrivateAttachments.php` (private disk, mime allowlist, CSP sandbox — closes stored-XSS) + frontend `components/ui/file-dropzone.tsx`. **Decision: build `it_attachments` (morphs: ticket|comment|kb_article) per §P.4 and serve downloads via `ServesPrivateAttachments`.**
12. **Cross-loop bridge (sacred):** `OnboardingService::createItProvisioningRequests()` (L524–568) — IT-category tasks, equipment excluded (asset path), idempotent per task, Schema::hasTable-guarded, never blocks checklist creation. `fulfil` completes the linked task via `completeTask()` in a DB transaction (controller L114–137); `cancel` annotates the task + notifies the checklist creator best-effort (L163–183).
13. **Tenancy pattern:** `ResolvesHrTenant` trait (`resolveHrTenantIdForUser`, `assertHrTenantAccess`, `rejectForeignTenantRecipient`) + `forTenant` scopes on both models. Keep on every new query/mutation.
14. **No FormRequests in the module** — all inline `$request->validate()`. New mutations get FormRequests per the non-negotiables.

## Build order (master checklist)

- [x] 1. §A audit pass → seed this gap doc (pass 0). *(2026-07-07: findings above; attachment decision recorded — docs/IT_TICKETING_GAP_ANALYSIS.md)*
- [x] 2. §P.9 + §B permissions: `it.request` migration/seeder, sidebar gate, route regrouping (`it.tickets.store` out of manage), `ItTicketPolicy` + authz tests. *(2026-07-07: `2026_07_07_100001` grant migration + RbacSeeder augment (staff roles only — client/next_of_kin excluded, see IT_TICKETING_QUESTIONS.md §1); routes regrouped `it.request|it.view`; ItTicketPolicy (view/comment/reopen/update/resolve/close/delete); controller capability-split payloads + `myTicketRows` + triage-fields-agent-only on store; `can.it.request` in HandleInertiaRequests (cache v4); sidebar OR-gate; minimal My-tickets tab + requester hero chips + assignee field hidden for requesters (full §B UI lands in item 5). Existing gate test updated to the new spec (worker 200 + requester payload; roleless 403); 10/10 Pest, types/lint/build clean.)*
- [x] 3. §P.1–3, 5, 8, 10 migrations + model/relationship/constant updates (+ factories) — schema lands early, in one pass, tested. *(2026-07-07: `2026_07_07_100002_extend_it_ticketing_schema` — it_tickets +17 cols (reference/subcategory/source/asset+prov FKs/SLA clocks/lifecycle/CSAT, tenant-unique reference, 2 composite indexes, backfill source=agent + per-tenant IT-%06d refs), it_ticket_comments, it_ticket_events (morphs, created_at-only), it_ticket_watchers (unique pivot), provisioning +priority/due_date; ItTicket rebuilt (STATUSES+waiting, SOURCES/SLA_STATES consts, relations comments/events/watchers/asset/provisioningRequest, HasFactory), ItTicketComment+publicOnly, ItTicketEvent+static record(), provisioning events/linkedTickets, morphMap aliases it_ticket/it_provisioning_request; ItTicketFactory+ItTicketCommentFactory. 15/15 IT Pest green. §P.6 SLA→item 8, §P.7 KB→item 14, §P.4 attachments→item 6.)*
- [x] 4. Reference generation + backfill; controller pagination + the new server summary payload + saved-view params. *(2026-07-07: ItTicket `creating` hook + `nextReference()` (max-based per tenant) + `createWithReference()` bounded retry on the tenant-unique index; storeTicket stamps source/reference + `created` event, flash carries the ref; queues → `paginate(15)` with named page params (`tickets_page`/`requests_page`) + `withQueryString`+`through`; saved-view param `view` (all_open/unassigned/mine/breaching/breached/awaiting_reply(v1 proxy=no first response)/waiting/recently_resolved) via `applyTicketView`; `stats` → full `summary` (tickets counts+by_status+per-view, provisioning incl. overdue/pending_over_7d, per-user `my`; requesters get `my` only); it/index.tsx consumes paginators + LaravelPagination + summary-fed hero chips/badges, rows show reference. 19/19 IT Pest, types/lint/build clean. Backfill of pre-existing rows shipped with item 3's migration.)*
- [x] 5. §B My tickets tab + Raise-a-ticket quick modal + created-receipt notification. *(2026-07-07: `RaiseTicketDialog` (it-wizards.tsx) — single-step review-free, plain-language category tiles + urgency Segmented→priority, "+ Add more details" disclosure, success pane shows the stamped reference via new `flash.it_ticket` (HandleInertiaRequests); `TicketCreatedNotification` (app/Notifications/It, database+mail, reference+title ONLY — description never leaves the app) receipt→requester + urgent_alert→it.manage agents minus actor (`usersWithItManage` mirrors HrNotificationService query, org-or-NULL tenant scope); My-tickets rows gain StatusDots progress + "Waiting on you" warning badge; requester hero CTA + tab CTA open the quick modal. Deferred to their own items (would be dead UI now): attachment drop→6, KB search/suggestions→14, CSAT slot→15. 22/22 IT Pest; types/lint/build clean.)*
- [x] 6. §E ticket workspace: show route/policy payloads → `TicketThread` (comments API, internal notes) → properties rail (assign/status/priority/category/asset/watchers) → timeline → drawer. *(split 2026-07-07; all four sub-items shipped — headline #2 done end-to-end.)*
  - [x] 6a. Workspace backend: `it.tickets.show` + capability payloads (internal notes stripped server-side), comments store (FormRequest, internal gated `it.manage`, first public agent reply stamps `first_responded_at`, requester reply on `waiting` resumes the clock + flips `in_progress`), watch/unwatch routes, events on update (status/priority/assigned) + `TicketAssignedNotification`/`TicketRepliedNotification`, authz tests. *(2026-07-07: ItTicketController show/storeComment/watch/unwatch (+BuildsItOptions trait shared with provisioning ctrl); StoreTicketCommentRequest = first module FormRequest; ItTicket startWaiting/stopWaiting bank sla_paused_minutes; updateTicket rewritten — subcategory validated, waiting transitions via helpers, status/priority/assigned events + assignee notification (never self); replied notifications: agent-public→requester, requester→assignee+watchers minus actor, internal→nobody; lean v0 show page (header/thread/composer with Reply⇄Internal + Ctrl+Enter) so the route is real — full rail/timeline = 6b. 28/28 IT Pest; types/lint/build clean. NOTE: parent node_modules churned mid-pass → worktree now has its OWN npm ci'd node_modules (memory updated).)*
  - [x] 6b. Detail page `/it/tickets/{ticket}`: shared `TicketThread` (thread + composer Reply⇄Internal, Ctrl+Enter), compact header band (reference copy, badges), properties rail (status/priority/category/subcategory, assign + assign-to-me, watchers, linked asset/provisioning, stamps), timeline toggle. *(2026-07-07: extracted `components/it/ticket-thread.tsx` (Conversation⇄Activity lanes, humanised event lines, compact mode for the 6c drawer); show.tsx rebuilt — copy-on-click reference chip, SLA chip (renders once item 8 stamps due dates; tone by sla_state, always text+icon), Assign-to-me + Watch/Unwatch header actions, agent rail (status open/in_progress/waiting selects — resolve/close arrive item 7, priority/category/subcategory-with-save, assignee, linked-asset picker over canonical assets register via new agent-only `assetOptions` payload), read-only rail for requesters/settled tickets, provisioning chip, watcher chips, en-NZ absolute stamps, copy-link. Backend: updateTicket accepts category+asset_id. 29/29 IT Pest; types/lint/build clean.)*
  - [x] 6c. Quick-peek drawer sharing `TicketThread` + condensed rail; row click → drawer, "Open full page"; My-tickets + queue wiring. *(2026-07-08: `components/it/ticket-drawer.tsx` (Sheet, condensed header meta, skeleton, Open-full-page) fetching the SAME it.tickets.show payload over a JSON branch — one route, identical policy/stripping for page + drawer; TicketThread gains `onPosted` refetch so drawer replies stay fresh; queue + My-tickets rows: click→peek, Ctrl/⌘-click + double-click→full page, hover tint, ⋯ stopPropagation; agent row context menu gains Open + Quick peek. Tests: JSON branch parity (agent full thread, requester stripped, stranger 403). 30/30 IT Pest; types/lint/build clean.)*
  - [x] 6d. Attachments (§P.4): `it_attachments` morphs table/model, upload (ticket/comment) + authorized download via `ServesPrivateAttachments`, dropzone in composer + raise modal disclosure. *(2026-07-08: `2026_07_08_100001` it_attachments (morphs, private disk, no per-row disk col — constant), ItAttachment (ALLOWED_MIMES stored-XSS allowlist, 10MB, max 5), `StoresItAttachments` concern = one write-path riding the ticket/comment request (no free-standing upload endpoint), morph alias it_ticket_comment; `it.attachments.download` streams via ServesPrivateAttachments with thread-audience authz (internal-note files agent-only, owner-or-agent otherwise, orphans 404); payloads carry chips (ticket + per-comment); TicketThread AttachmentChips + composer Attach+StagedFileCard (forceFormData), RaiseTicketDialog FileDropzone in the disclosure; drawer passes through. Tests: private-disk store + payload chip, svg refused, download matrix (owner/agent/stranger/internal), requester payload strips internal attachment with its note. 33/33 IT Pest; types/lint/build/pint clean.)*
- [x] 7. §N3 Resolve modal + close/reopen routes + auto-close command + `TicketResolved`/`TicketReopened` notifications. *(2026-07-08: resolveTicket rewritten on `ResolveTicketRequest` (note REQUIRED → final public reply, notify_requester toggle, first-response backstop, sla_state→met when inside a stamped resolution target incl. paused minutes, waiting banked, `resolved` event); `it.tickets.close` (closed_at + event, from any non-closed) + `it.tickets.reopen` (requester-reachable, policy 7-day window, resets stamps + sla_state→ok, reopened_count++, event, assignee notification); `it:close-resolved --days=7` daily 07:10 NZ (chunked, system `closed` event via auto_close); TicketResolved (requester w/ CSAT-nudge deep-link + watcher variants, never actor) + TicketReopened (assignee) notifications, reference+title only; `ResolveTicketDialog` (exported — used by queue ctx menu 'Resolve…' incl. waiting tickets, and workspace header Resolve/Close/Reopen buttons via can.reopen); queue menu Close/Reopen now hit the dedicated routes. "Draft KB from this" shortcut deferred to item 14 (no KB yet = dead UI). Legacy resolve test updated to carry a note. 39/39 IT Pest; types/lint/build/pint clean.)*
- [x] 8. §G SLA: policies table/seeder/editor → stamping + waiting-pause → `it:check-sla` + notifications + queue/hero chips. *(split 2026-07-08; waiting-pause maths shipped early with items 6a/7; all three sub-items landed 2026-07-08 — the SLA engine is complete end-to-end: policy table + code-default fallback, creation-anchored stamping with priority re-target, waiting pause banking, hourly watchdog with escalation ladder, live chips on queue/hero/workspace, admin target editor.)*
  - [x] 8a. `it_sla_policies` (§P.6) + `ItSlaPolicy` model with §G defaults as code fallback + seeder; due-date stamping on create + priority-change restamp (anchored at created_at); due dates in queue row payloads; tests. *(2026-07-08: `2026_07_08_100002` table (tenant+priority unique); `ItSlaPolicy::DEFAULTS` + `minutesFor()` row-or-default so deploys-without-seeders still stamp; `ItSlaPolicySeeder` materialises editable rows; `ItTicket::stampSlaDueDates()` anchored at created_at — storeTicket stamps on create, updateTicket restamps on priority change (re-target, not restart); queue rows carry first_response/resolution/first_responded ISO stamps (feeds 8c chips); TicketRow TS updated. Tests: defaults normal+urgent, tenant override, 2h-late priority bump keeps creation anchor, seeder rows + factories-stay-unstamped. 44/44 IT Pest; types/lint/build/pint clean.)*
  - [x] 8b. `it:check-sla` hourly: ok→at_risk (≤25% window left, paused minutes honoured)→breached transitions + `TicketSlaNotification` (assignee at-risk; assignee+managers breach; unassigned urgent >30m → admins) — idempotent, one notification per transition guarded by `it_ticket_events`; tests with time travel. *(2026-07-08: `CheckItSlaStates` (`it:check-sla`, hourly NZ in routes/console.php) re-derives sla_state fresh each run from the nearest live clock — first-response until first public reply, resolution shifted by banked + live waiting pause; entering at_risk/breached persists state + `sla_at_risk`/`sla_breached` event + `TicketSlaNotification` (at_risk→assignee only; breached→assignee+all it.manage deduped; unassigned at-risk notifies nobody by design); relaxations (pause growth, priority restamp) save silently — state change IS the idempotency guard, and re-entry after a genuine relax is a new episode that correctly re-pages. Stateless unassigned-urgent>30m escalation→admins guarded by `sla_escalated` event, re-armed only by reopen. `ItStaffDirectory` (App\Domain\It) extracted from the controller's private usersWithItManage so command+controller share one recipient query (+admins()). 6 new time-travel tests (at-risk once, direct-to-breach fan-out, banked+live pause hold, escalation matrix, unstamped/met left alone, breach relaxation); 50/50 IT Pest. Backend-only pass — no frontend touched; queue/hero/workspace SLA chips = 8c.)*
  - [x] 8c. Surfaces: queue SLA chip (due-in/breached, text+icon), hero Breaching-soon/Breached chips, workspace live countdown (minute tick), SLA policy editor modal (`PUT it.sla.update`, manage+admin) mounted on the tickets toolbar until Overview/Reports exist; business-hours-calendar note → IT_TICKETING_QUESTIONS.md. *(2026-07-08: shared `components/it/sla-chip.tsx` (SlaChip + useMinuteTick) — live clock picks response-until-first-public-reply then resolution, tone strictly from the server sla_state verdict (pause-aware), "Clock paused — with requester" on waiting, hidden when met/settled, icon+text always; queue gains an SLA column (grid retempered), workspace header chip swapped from static absolute-time to the live one; hero gains Breaching-soon/Breached chips as real links to `?tab=tickets&view=breaching|breached` (first stat-as-link chips — full golden-band = item 10); `SlaPolicyDialog` (WizardShell single-step, per-priority minutes grid with humanised "= 4 h" hints, Custom/Default badges, reset-to-defaults) on the tickets toolbar for admins — `PUT /it/sla-policies` (`it.sla.update`) via `UpdateSlaPoliciesRequest` (authorize = it.manage + admin role; gte cross-field rule), controller `updateSlaPolicies` upserts all four tenant rows, `slaPolicies` grid + `can.edit_sla` served admin-only; §2 business-hours note added to IT_TICKETING_QUESTIONS.md. Tests: admin retune→new ticket stamps 30/120, manage-without-admin + worker 403, resolution<response 422. 53/53 IT Pest; types/lint(0 errors)/build/pint clean. Incidental (own commit `c31a7b01`): fixed the 2 pre-existing react-compiler lint errors (hr/assets/show, hr/leave/balances) + stray prefer-const autofix so the repo lint gate is genuinely green.)*
- [x] 9. §F2 queue rebuild: standard table, toolbar, saved views, bulk, pagination. *(split 2026-07-08 — backend lands first, then the standard-table frontend; SLA column chip already shipped with 8c; all three sub-items landed 2026-07-08 — the queue is now the /hr/people-standard table: saved-view chips w/ counts, search + status/priority/category/SLA/assignee/date toolbar, sortable headers, checkbox bulk-select → action bar (assign/priority/status/close), SLA column, LaravelPagination.)*
  - [x] 9a. Queue backend: `it.tickets.bulk` (assign/priority/status/close — FormRequest, tenant-scoped, per-change events, restamp on priority, waiting-pause honoured, never un-settle a resolved/closed ticket, never self-notify) + toolbar query params (escaped LIKE search over reference/title/requester, category + SLA-state + created date-range filters, sortable columns keeping the triage default) with Pest tests. *(2026-07-08: `BulkTicketActionRequest` (authorize=it.manage, ids≤50, status limited to OPEN_STATUSES so bulk can never bare-set resolved/closed — settling is close-here or the note-bearing resolve modal); `ItTicketController::bulk` + four private helpers, tenant-scoped fetch (foreign ids silently drop → "unchanged"), one event row per real change with `via=bulk`, priority restamps the SLA clock, waiting↔status uses start/stopWaiting to bank the pause, close banks-then-closes, assignee notified once per ticket but never the actor; foreign-tenant assignee guard mirrors `rejectForeignTenantRecipient` (HrEmployeeProfile.tenant_id, not the wrong organization_id). `ItProvisioningController` index filters gained `q`/`ticket_category`/`sla`/`from`/`to`/`sort`/`dir`; `applyTicketSearch` (LIKE-escaped, requester via whereHas), `applyTicketSort` (semantic priority/status ordering, keeps the open-first-urgent-first triage default, stable id tiebreak), strict `cleanDate`. Route `it.tickets.bulk` (manage group). 6 bulk Pest tests (assign+idempotent no-op, self-assign silent, priority restamp, waiting-start + resolved-skip, close-with-pause-bank, agent-only + tenant scope); 59/59 IT Pest (625 assertions); pint clean on the 2 new files. Frontend consumes it in 9b.)*
  - [x] 9b. Queue frontend — navigate & filter: saved-view chips with per-view counts (`summary.tickets.views`, default persisted `it.ticketsView`), debounced search, category/SLA/date-range toolbar, sortable column headers. *(2026-07-08: 8 saved-view chips (all_open→recently_resolved incl. Breached so the hero deep-link lands highlighted) with live counts + active pill; clicking persists `it.ticketsView` to localStorage and an agent opening Tickets with no `view` set lands on their remembered one (a `?view=` deep-link always wins). Refactored `applyFilter` onto a shared `navigate(patch)` (filter change drops the page cursor → page 1); added `applyView`, `applySort` (first click desc, re-click toggles asc⇄desc — chevrons via new `SortHeader` on Ticket/Priority/Status/Age→created), debounced (350ms) free-text search box with clear-`X`, category + SLA-state `FilterSelect`s, and a `DateRange` (two native pickers, cross-bounded, feeding `from`/`to`). Filter-aware empty state ("No tickets match" + Clear-filters button via extended `EmptyState`). `Filters` TS widened (ticket_category/sla/q/from/to/sort/dir); TICKET_STATUSES gains `waiting`. Pure frontend — IT Pest stays 59/59; `npm run types`/eslint(index.tsx)/`npm run build` all clean. Checkbox bulk-select → 9c.)*
  - [x] 9c. Queue frontend — bulk act: checkbox column + select-all, sticky action bar (assign/priority/status/close via `it.tickets.bulk`) with `alert-dialog` confirm on close and N-selected count; clears selection + toasts on success. *(2026-07-08: agent-only 36px checkbox track (grid widened via `ticketGridCols`) with a per-page select-all header checkbox (true/indeterminate/false) and per-row checkboxes (`stopPropagation` so they don't open the drawer; selected rows tint `bg-primary/5`); a sticky action bar surfaces at `selected.size > 0` — "N selected", Assign-to…/Set-priority…/Set-status… Radix Selects (placeholder-menu pattern, Unassign sentinel) that dispatch immediately, a Close button gated behind an `alert-dialog` ("Close N tickets?"), and Clear. `runBulk` POSTs `{ids:[...selected], action, …}` to `it.tickets.bulk` (preserveState/Scroll), surfaces the flash as a toast and empties the selection; selection also auto-drops whenever the visible page changes (keyed on the row-id list) so it stays per-view. Pure frontend — IT Pest stays 59/59; `npm run types`/eslint(index.tsx)/`npm run build` clean.)*
- [x] 10. §C hero (golden band + summary chips + right-rail toggle). *(split 2026-07-08 — shell first, then the inline-SVG right rail; both landed 2026-07-08. `/it` now leads with the People-standard command band: deep-linking stat chips, "Needs you" attention row, quick actions, requester compact variant, and an agent-only right rail that toggles the live-queue status donut ⇄ 30-day SLA-compliance ring (localStorage-persisted).)*
  - [x] 10a. Golden-band hero shell cloned from `people-hero.tsx` (new `components/it/it-hero.tsx`): brand-gradient `HERO_STYLE`, stat-as-link chips deep-linking to filtered queues/views, "needs you" amber chips (unassigned urgent / SLA at risk / awaiting your reply / provisioning >7d), quick actions (Raise — everyone; Log & triage — agents; KB/Reports deferred until those tabs exist), requester compact variant (My open / Awaiting my reply / Resolved 30d + Raise). Pure frontend; replaces the hand-rolled `primary/90→80` gradient. *(2026-07-08: `ItHero` cloned from the People gold standard — `HERO_STYLE` color-mix brand gradient (re-themes per tenant) + `--it-amber` var; 7 agent stat chips (Open/Unassigned/Breaching soon(amber)/Breached(amber)/Awaiting reply/Pending provisioning/Resolved·30d) each a link-button navigating via `router.get` into the matching `?tab=…&view=…` queue; "Needs you" row surfaces only non-zero attention chips (unassigned-urgent→urgent+unassigned queue, SLA at risk, awaiting your reply, provisioning pending >7d); quick actions Log & triage (agents, solid) + Raise (solid for requesters) wired to the existing modals; requester compact variant = My open/Awaiting my reply(amber)/Resolved·30d. `index.tsx` drops the hand-rolled gradient + now-unused `Link` import. Right-rail donut⇄ring = 10b. IT Pest untouched at 59/59; `npm run types`/eslint(index+it-hero)/`npm run build` clean.)*
  - [x] 10b. Right rail: inline-SVG donut (tickets by status) ⇄ ring (30-day SLA compliance %) toggle persisted `localStorage it.heroRight`; needs the `met_30d` count added to the server summary (+ compliance derivation) with a test. *(2026-07-08: `ItHero` restructured to the People flex layout (left column + agent-only right rail); `StatusDonut` = inline-SVG arcs over the live-queue statuses (open/in_progress/waiting, settled tickets excluded so the mix reflects current workload, centre = open total) with a token-coloured legend; `ComplianceRing` = met_30d/resolved_30d ring (tone green≥90/amber≥70/critical below, "X of Y settled within SLA this month" caption, "no tickets resolved" empty state for young tenants); `Status`⇄`SLA` `RailTab` toggle persisted to `it.heroRight`. Backend: `summary()` gains a `met_30d` SUM (settled-in-30d AND sla_state='met') appended as the last placeholder + `met_30d` in the tickets payload; `Summary`/`ItHeroSummary` TS widened. Test: 2 settled (1 met/1 breached) + an out-of-window met → resolved_30d=2, met_30d=1. Full IT suite 60/60 (638 assertions); `npm run types`/eslint/`npm run build` clean.)*
- [x] 11. §F1 Overview tab. *(split 2026-07-08 — KPIs + needs-attention lanes (11a) then the activity feed (11b); both landed 2026-07-08. Agents open on a command board: KPI row + four needs-attention lanes + a recent-activity feed, everything deep-linking into the filtered queue / quick-peek drawer.)*
  - [x] 11a. Overview tab (new agent default): KPI row (Open / Unassigned / Breaching soon / Breached / Avg first response 30d — CSAT deferred to item 15, no data yet) + "Needs attention" lanes (SLA at-risk/breached list, Awaiting agent reply list, Aging >7d list, Unassigned-by-priority chips), every item deep-linking into the filtered queue. Backend `overview()` payload (avg-first-response + the four lanes, tenant-scoped, capped) + tests; new `components/it/it-overview.tsx`; tab shell gains Overview as the agents' default. *(2026-07-08: `ItProvisioningController::overview()` — `AVG(TIMESTAMPDIFF(MINUTE,created_at,first_responded_at))` over 30d replies + four capped(6) tenant-scoped lanes (SLA at-risk/breached ordered breached-then-due, awaiting-reply oldest-first, aging open>7d, unassigned-by-priority SUMs), Schema-guarded empty board, wired into agentProps only; `ItOverview` board — 5 KPI stat-cards (Breaching amber / Breached critical, avg-first-response `fmtMins`) over four LaneCards whose rows quick-peek the ticket (drawer) and whose "View all →" jumps to the saved view, unassigned-by-priority chips deep-link `view=unassigned&ticket_priority=`; tab shell reordered Overview·Tickets·Provisioning with Overview the agents' default (`useHrTab('overview')`). Test: breached→sla_lane[0], urgent→unassigned.urgent=1, 60-min reply→avg=60, requester payload `missing('overview')`. Full IT suite 61/61 (666 assertions); `npm run types`/eslint/`npm run build` clean. Activity feed → 11b.)*
  - [x] 11b. Overview recent-activity feed from `it_ticket_events` (humanised lines — created/assigned/status/priority/sla/resolved/reopened — actor + relative time, deep-link to the subject ticket); backend feed slice + test. *(2026-07-08: `ItProvisioningController::recentActivity()` — latest 8 ticket-subject events tenant-scoped, morph `subject` eager-loaded (deleted ticket drops out via `filter`+`values`), actor name + type + payload + subject reference + relative time, added to the `overview` payload (+ empty-board guard); `ItOverview` gains an `ActivityFeed` card — `ACTIVITY_ICON` per type + `activityVerb` payload-aware humaniser (status/priority read `payload.to`, sla/system rows show "System"), each row quick-peeks its ticket, taught empty state. Test: raising a ticket via the write path surfaces `recent_activity[0]` = created / actor=hr / ref IT-000001. Full IT suite 62/62 (684 assertions); `npm run types`/eslint/`npm run build` clean.)*
- [x] 12. §H provisioning uplift (priority/due, manual wizard, linked tickets, bulk, events). *(split 2026-07-08; 12a–12d all landed 2026-07-08 — §H COMPLETE: activity events + priority/due surfacing with overdue tint (12a), manual New-provisioning-request wizard (12b), raise-linked-ticket convert/link with reciprocal chips (12c), and bulk assign/fulfil + CSV export (12d). The onboarding→provisioning bridge stayed sacred throughout — the 4 original Pest tests plus the fulfil→task-complete transaction are green. Next: item 13 (§N2 agent Log & triage wizard rebuild).)*
  - [x] 12a. Provisioning activity events + row priority/due surfacing: record `it_ticket_events` (polymorphic) on assign/fulfil/cancel; provisioning queue rows render a priority badge + due date with an overdue tint (pending/in_progress past `due_date`). Backend event rows + frontend row uplift + tests. *(schema priority/due already landed item 3; onboarding due-date inference is minor → note/defer.)* *(2026-07-08: `assign`/`fulfil`/`cancel` now `ItTicketEvent::record($provisioning, 'assigned'|'fulfilled'|'cancelled', …)` — the polymorphic events table already served provisioning subjects, so the timeline is complete without new schema (fulfilled recorded inside the fulfil transaction; cancelled carries the reason payload). Provisioning queue re-tempered to 7 cols — Employee · Item · Assignee · **Priority** (badge) · Status (+ raised/done subline) · **Due** (en-NZ `formatDue`) · ⋯ — with a critical overdue tint on the row + "Overdue" flag when a pending/in-progress request is past `due_date` (lexical `todayISO` compare). Test: assign→in_progress+assigned event, fulfil→fulfilled event, cancel→cancelled event w/ reason payload; the 4 sacred provisioning tests stay green. Full IT suite 63/63 (692 assertions); `npm run types`/eslint/`npm run build` clean. Manual wizard → 12b.)*
  - [x] 12b. Manual "New provisioning request" wizard (§N5b): `POST /it/provisioning` `it.provisioning.store` (FormRequest + `created` event, tenant-scoped) + a WizardShell modal (employee `PeoplePicker`, type tiles, item, assignee, due, notes) + hero/tab "New request" action. Tests. *(2026-07-08: `StoreProvisioningRequestRequest` (authorize=it.manage) + `ItProvisioningController::storeProvisioning` — tenant asserted on the chosen employee profile AND foreign-tenant assignee guard (mirrors assign/fulfil), assigned→in_progress else pending, `created` event; `employeeOptions()` (active tenant profiles, Schema-guarded) added to agent props. `NewProvisioningRequestDialog` — 2-step WizardShell (employee `SelectInput` — the module's wizard-kit picker, not a raw PeoplePicker; type tiles account/access/equipment/other; item → assignee/priority/due `type=date`/notes), success pane; new-request `ItModal` case + `EmployeeOption` type wired through the dispatcher; "New request" button on the provisioning toolbar (agents). `POST /it/provisioning` route in the manage group. Test: assigned→in_progress + created event + priority/assignee persisted, unassigned→pending, worker 403. Full IT suite 64/64 (701 assertions); `npm run types`/eslint/`npm run build`/pint(new FormRequest) clean. Linked tickets → 12c.)*
  - [x] 12c. Convert/link — "Raise linked ticket" from a request (ticket pre-filled + `provisioning_request_id` set) + reciprocal chips (request shows its linked ticket, ticket shows its source request). Tests. *(2026-07-08: `storeTicket` accepts an agent-only `provisioning_request_id` (nullable, `Rule::exists` tenant-scoped; dropped for self-service requesters like the assignee field) → persisted on the ticket + recorded in the `created` event payload; `requestPage` eager-loads `linkedTickets` and exposes `linked_ticket` {id,reference} + `linked_ticket_count`. Frontend: `CreateTicketWizard` gains an optional `provisioning` context — pre-fills the title ("Issue with {item}"), sends the id, shows a "Linked to provisioning request" `InfoCard`; the `ticket` `ItModal` carries `{provisioning?}`. Provisioning row context menu gains "Raise linked ticket" (any status — a fulfilled item can still arrive broken) + "Open {ref}", the `⋯` menu now shows for all agents, and the row renders a clickable linked-ticket chip (reference + "+N") that quick-peeks the ticket. The ticket workspace already carried the reciprocal provisioning chip (6b). Test: agent link round-trips both ways; a requester's `provisioning_request_id` is ignored. Full IT suite 65/65 (706 assertions); `npm run types`/eslint/`npm run build` clean. Bulk assign/fulfil + CSV → 12d.)*
  - [x] 12d. Bulk assign/fulfil on the provisioning queue + CSV export. *(split 2026-07-08: bulk actions (12d-i), then CSV export (12d-ii); both landed 2026-07-08 — the provisioning queue now carries the tickets-queue command layer: checkbox bulk assign/fulfil (bridge-safe) + a filter-aware, injection-guarded CSV export.)*
    - [x] 12d-i. Provisioning bulk assign/fulfil: `it.provisioning.bulk` (FormRequest, tenant-scoped fetch drops foreign ids, foreign-tenant assignee guard, per-change `it_ticket_events` with `via=bulk`, fulfil completes the linked onboarding task through the bridge + skips settled/blocked, never re-settle) + checkbox bulk-select action bar on the queue (shared `useRowSelection`). Tests. *(2026-07-08: `BulkProvisioningActionRequest` (authorize=it.manage, ids≤50, action assign|fulfil, assign requires a real recipient — no bulk "unassign" for provisioning); `ItProvisioningController::bulkProvisioning` + `bulkAssignProvisioning`/`bulkFulfilProvisioning` helpers mirror the tickets bulk: tenant-scoped `forTenant` fetch (foreign ids silently drop → "unchanged"), settled (done/cancelled) skipped, `assigned`/`fulfilled` event per real change with `via=bulk`, foreign-tenant assignee guard mirrors assign/storeProvisioning. Fulfil rides the SACRED bridge — each in its own `DB::transaction` calling `OnboardingService::completeTask` (dependency/rollup fire), a blocked task's `\LogicException` caught per-request so one can't sink the batch. Route `it.provisioning.bulk` sits above `{provisioning}` so `bulk` is never bound as an id. Frontend: extracted a shared `useRowSelection(pageIds)` hook (per-page selection, clears on page change) and migrated BOTH queues to it (`ticketSel`/`reqSel`); provisioning queue gains the agent-only 36px checkbox track + select-all header + per-row checkboxes (selected tint wins over overdue), a sticky action bar (Assign-to Select dispatches immediately; Fulfil behind an `alert-dialog` "Fulfil N requests?" since it completes onboarding tasks) via `runProvisioningBulk`→`it.provisioning.bulk`, toasts + clears on success. 3 Pest tests (assign pending→in_progress+event & skip settled & idempotent re-run; fulfil→done+`fulfilled` event + linked onboarding task completed + skip cancelled; agent-only 403 + foreign-tenant id dropped). Full IT suite 68/68 (736 assertions); types/eslint/build/pint clean. CSV export → 12d-ii.)*
    - [x] 12d-ii. Provisioning CSV export: filter-aware `it.provisioning.export` streamed download (CSV-injection guarded via `SanitizesCsvOutput`) + toolbar button. Test. *(2026-07-08: `ItProvisioningController::exportProvisioning` — `GET /it/provisioning/export` (`it.provisioning.export`, gated `permission:it.view` in the outer group so read-only agents can export but requesters 403), re-parses the queue's status/type/assignee filters, tenant-scoped `forTenant` fetch of ALL matching rows (not just the page) in the queue's pending→in_progress→done order, streamed via `response()->streamDownload` with every cell through the base Controller's `putCsv()` (the `SanitizesCsvOutput` formula-injection guard — `=/+/-/@`-leading cells get an apostrophe). 12 columns (Employee·Role·Item·Type·Status·Priority·Assignee·Due·Source·External ref·Raised·Fulfilled), enums humanised, timestamps in `app.worker_timezone`. Frontend: `Export CSV` button (`Button asChild`→`<a href>` carrying the active filters via `provisioningExportUrl()`) on the provisioning toolbar for all agents. 2 Pest tests (headers + content + injection-guard + tenant scope + requester 403; status-filter honoured). Full IT suite 70/70 (748 assertions); types/eslint/build clean. No new PHP files → no pint. §H provisioning uplift COMPLETE.)*
- [x] 13. §N2 agent Log & triage wizard rebuild (SLA preview, on-behalf-of, asset link). *(split 2026-07-08; 13a (backend contract) + 13b (3-step wizard UI) both landed 2026-07-08 — the agent Log & triage modal now logs on behalf of a colleague, sets subcategory/asset/watchers, shows a live SLA preview, and ends on Open-ticket/Log-another. Next: item 14 (§I Knowledge tab + KB modal + deflection + helpful votes) — needs the §P.7 `it_kb_articles` schema.)*
  - [x] 13a. Backend: extend `storeTicket` for the agent triage fields — on-behalf-of `requester_user_id` (foreign-tenant guarded, receipt goes to the requester not the actor), `subcategory`, `asset_id`, `watchers[]` (all agent-only, dropped for self-service); expose `slaPolicies` targets to all agents (SLA preview source) + `assetOptions` in the index agent props (shared `BuildsItOptions::assetOptions()`). Pest tests (on-behalf + full triage; requester ignores triage fields). *(2026-07-08: `storeTicket` now validates + applies `subcategory`/`requester_user_id`/`asset_id`/`watchers[]` alongside the existing assignee/provisioning fields, ALL gated behind `$isAgent` — a self-service requester's triage fields are dropped (requester stays themselves, no assignee/asset/watchers/subcategory). On-behalf-of: the `created` event carries `on_behalf_of`, watchers attach via `syncWithoutDetaching`, and the "we've got it" receipt is routed to the REQUESTER (the colleague), never the acting agent. `BuildsItOptions::assetOptions()` extracted from the workspace `show` (both controllers now share it — no parallel IT register) and added to the index agent props; `slaPolicies` grid now served to every agent (the wizard's live SLA preview source) with editing still gated by `can.edit_sla`. 2 Pest tests (agent on-behalf + full triage incl. watcher attach + receipt-to-colleague-not-agent; requester triage fields ignored); the existing authz tests stay green. Full IT suite 72/72 (766 assertions); pint(new test)/types clean; no frontend touched. Wizard UI → 13b.)*
  - [x] 13b. Frontend: rebuild `CreateTicketWizard` into the 3-step Details → Triage → Review wizard — requester `SelectInput` (on-behalf-of), category+subcategory, description, attachment · priority with live SLA preview ("resolution due …"), assignee, asset link, watchers · review; success pane (open ticket / log another). *(2026-07-08: `CreateTicketWizard` rebuilt on the same WizardShell — **Details** gains a Requester `SelectInput` ("Me — logging for myself" default → on-behalf-of), a Subcategory input, and a `FileDropzone`/`StagedFileCard` attachment field (forceFormData); **Triage** keeps the priority tiles and gains a live SLA-preview `InfoCard` ("First response due Thu 2:00 pm, resolution by …", projected client-side from `slaPolicies[priority]` so it updates as priority changes — no re-fetch), the asset-link `SelectInput` over the new `assetOptions` (name · tag), and a Watchers add-picker→removable-chips control; **Review** surfaces requester/subcategory/files + SLA-due/asset/watcher-count. Success pane now offers **Open {reference}** (`router.visit`) / **Log another** (`form.reset` + back to step 0) / Done, reading the stamped ref from `flash.it_ticket`. Threaded `AssetOption` + `assetOptions`/`slaPolicies` through the `ItWizard` dispatcher and `index.tsx` Props. Sentinel `UNASSIGNED` used for the Me/Unassigned/No-asset placeholders; `X`-icon chips. Pure frontend — IT Pest stays 72/72; `npm run types`/eslint(both files)/`npm run build` clean. §N2 Log & triage rebuild COMPLETE.)*
- [x] 14. §I Knowledge tab + KB modal + deflection + helpful votes. *(COMPLETE 2026-07-08 — 14a schema+CRUD → 14b Knowledge tab + KB modal → 14c requester browse/search + helpful votes → 14d deflection at raise/resolve/compose. §I shipped end-to-end. Next: item 15 (§K CSAT).)*
  - [x] 14a. §P.7 `it_kb_articles` schema (migration + `ItKbArticle` model + factory, reuses ticket categories, `draft|published`, tenant-unique slug, view/helpful counters) + agent KB CRUD (`ItKbController` store/update(+publish toggle)/destroy, FormRequests, tenant-scoped, `it.manage`) + routes `it.kb.store|update|destroy`. Pest tests. *(2026-07-08: `2026_07_08_100003_create_it_kb_articles` (tenant-unique slug index + `(tenant,status)` index, `author_user_id` nullOnDelete, view/helpful counters); `ItKbArticle` (CATEGORIES = ItTicket::CATEGORIES, STATUSES draft|published, `forTenant`/`published` scopes, `helpfulPercent()`, `uniqueSlug()` with -2/-3 collision suffix) + factory (+`published()` state). `ItKbController` store/update/destroy on `StoreKbArticleRequest`+`UpdateKbArticleRequest` (the update `sometimes`-rules serve both the full editor and a status-only Publish toggle); slug generated at create and kept stable across edits; tenancy asserted via `assertHrTenantAccess` (cross-tenant → 404, never leaks existence). Routes `it.kb.store|update|destroy` in the manage group. 3 Pest tests (create + tenant-unique slug dedup; edit + publish-toggle + slug-stable; agent-only + cross-tenant 404 + delete). Full IT suite 75/75; pint clean on the 6 new files; no frontend touched. Knowledge tab UI → 14b.)*
  - [x] 14b. Knowledge tab (agents): card/table (Title · Category · Status · Views · Helpful% · Updated · ⋯) + KB article modal (§N6: Basics → Content markdown+preview → Review, Save & add another) + row context menu; server `kbArticles` payload + tab shell + "New KB article" hero/toolbar action. *(2026-07-08: `ItProvisioningController::kbArticles()` (Schema-guarded, whole catalogue incl. drafts newest-edited first, carries body for edit-prefill, `helpfulPercent`) added to agent props; payload test (agents get `kbArticles`, requesters `missing`). `KbArticleDialog` (it-wizards.tsx) — 3-step WizardShell Basics (title/category tiles/status) → Content (markdown Textarea + live `KbPreview` — a safe block-level renderer for #/##/### headings, -/* bullets, paragraphs; no dangerouslySetInnerHTML) → Review; create posts `it.kb.store` with Save-&-add-another (`form.reset`→step 0), edit patches `it.kb.update`; new `KbRow` type + `{type:'kb', article?}` `ItModal` case wired through the dispatcher. `index.tsx`: Knowledge tab (agents, count badge) with a Title/Category/Status/Views/Helpful%/Updated/⋯ table, "New KB article" button + taught empty state, row context menu (Edit → modal · Publish/Unpublish → `it.kb.update` status patch · Delete → `alert-dialog` → `router.delete`). Pure-additive backend read → IT suite 76/76 (811 assertions); `npm run types`/eslint(both files)/`npm run build` clean. Requester browse/search + votes → 14c.)*
  - [x] 14c. Requester browse/search of **published** articles + article view with view_count increment + "Was this helpful?" votes (`it.kb.helpful`, `helpful_yes`/`helpful_no`, one-vote localStorage guard). Tests. *(2026-07-08: `ItKbController::view` (`it.kb.view`) + `helpful` (`it.kb.helpful` on `KbHelpfulRequest`) — both requester-reachable (outer `it.request|it.view` group), shared `assertPublishedInTenant` guard (draft/cross-tenant → 404, never leaks); `increment()` tallies `view_count`/`helpful_yes`/`helpful_no`. `ItProvisioningController::kbPublished()` → base payload for **pure requesters** (`$canRequest && !$isAgent`) — published only, with body for the reader. Frontend: requester-only Knowledge tab (browse); search + category-filter (client-side `filteredKb`) over article cards; clicking opens a `Dialog` reader that POSTs `it.kb.view` (partial reload of just `kbPublished`), renders the body with the exported `KbPreview`, and offers "Was this helpful?" Yes/No → `it.kb.helpful` with a `localStorage it.kb.voted` one-vote guard. 3 Pest tests (pure-requester browse = published-only + agents get the catalogue instead; read+vote tallies climb; draft browse 404s). Full IT suite 79/79 (846 assertions); pint(new request)/types/eslint(both files)/build clean. Deflection at raise/resolve/compose → 14d.)*
  - [x] 14d. Deflection: Raise-a-ticket modal searches published titles as the requester types; ticket workspace composer suggests articles; Resolve modal "Draft KB article from this resolution" pre-fills the KB modal. *(split 2026-07-08; all three sub-items landed 2026-07-08: requester raise-time deflection (14d-i) → draft-from-resolution (14d-ii) → agent workspace composer suggest (14d-iii). KB now deflects at every touchpoint the brief names.)*
    - [x] 14d-i. Raise-a-ticket modal deflection: as the requester types the title, matching **published** titles surface as "These might fix it now" chips; clicking one closes the raise modal and opens the KB reader (counting the view). Threads `kbPublished` → RaiseTicketDialog + an `onOpenArticle` callback. *(2026-07-08: `KbSuggestion` type + `RaiseTicketDialog` gains `kbSuggestions` + `onOpenArticle` — once the title is ≥3 chars, up to 3 published titles containing it surface in a "These might fix it now" card; clicking a suggestion fires `onOpenArticle(id)`. `ItWizard` threads both through to the `raise` case; `index.tsx` passes `kbSuggestions={kbPublished}` (KbPublishedRow[]→KbSuggestion structurally) and an `onOpenArticle` that closes the raise modal and calls the existing `openArticle` (opens the reader + counts the view via `it.kb.view`). Pure frontend — the requester `kbPublished` payload already exists (14c); IT suite unchanged 79/79; `npm run types`/eslint(both files)/`npm run build` clean. Agent draft-from-resolution + workspace suggest → 14d-ii.)*
    - [x] 14d-ii. Resolve modal "Draft KB article from this resolution" — the resolve success pane offers a button that opens the KB create modal pre-filled (title = ticket title, body = the resolution note); adds a `draft` variant to the `kb` modal + `onDraftKb` through the dispatcher. *(2026-07-08: new `KbDraft` type + `{type:'kb', draft?}` modal variant; `KbArticleDialog` pre-fills the create form from `draft` (title/body/category) while staying in create mode (no `article` → still posts `it.kb.store`); `ResolveTicketDialog` success pane gains a "Draft KB article" button (shown only when `onDraftKb` is wired) that calls `onDraftKb({title: ticket.title, body: resolution note})`; `ItWizard` threads `onDraftKb` to the resolve case; `index.tsx` passes `onDraftKb={(draft) => setModal({type:'kb', draft})}` so the button swaps the resolve modal for a pre-filled KB create modal. Pure frontend — no backend (the KB create path already exists); IT suite unchanged 79/79; `npm run types`/eslint(both files)/`npm run build` clean. Workspace composer "Suggest from Knowledge" → 14d-iii.)*
    - [x] 14d-iii. Ticket workspace composer "Suggest from Knowledge" — as an agent types a reply, matching published articles surface for insertion (needs the workspace show payload to carry published KB for agents). *(2026-07-08: shared `BuildsItOptions::kbSuggestions()` (published-only, tenant-scoped, Schema-guarded, lean id/title/category — never body/client detail); `ItTicketController::showPayload` adds a `kbSuggestions` key gated on `$isAgent` (requesters get `[]` — they already met the KB at raise time (14d-i), so their payload stays lean). Frontend: `TicketThread` composer — as the agent types, ≥3-letter body tokens are matched against published titles (score = tokens found in the title) and the top-3 surface as "Suggest from Knowledge" `BookOpen` chips; clicking one inserts a plain-text `Related guide: "…" — search it in the Knowledge tab.` reference into the reply (no dead link — requesters find it via the Knowledge browse tab; dedup-guarded so re-clicks don't stack) + toast. New exported `ThreadKbHint` type threaded through `show.tsx` (Inertia prop) and `ticket-drawer.tsx` (axios `PeekPayload`). 1 Pest test (agent workspace payload carries published-only + no body; requester on their own ticket gets 0; the JSON/drawer branch mirrors the agent list). Full IT suite 80/80 (872 assertions); `npm run types`/eslint(3 files)/`npm run build` clean; no new PHP files → no pint. §I deflection + item 14 COMPLETE.)*
- [x] 15. §K CSAT (prompt, store, rail display). *(COMPLETE 2026-07-08 — 15a backend (policy/route/FormRequest/action + payloads + 5 Pest) → 15b frontend (My-tickets prompt card + workspace rail + 5-star confetti). Requesters rate a resolved ticket in two clicks; the score reads back in both surfaces and to agents; editable until the ticket closes. Next: item 16 (§L Reports).)*
  - [x] 15a. Backend: `ItTicketPolicy::csat` (requester-owned + status resolved = editable-until-closed) + `it.tickets.csat` route (requester-reachable, outer group) + `SubmitCsatRequest` (score 1–5, optional comment) + `ItTicketController::csat` (stamps `csat_submitted_at` on first submit, records one `csat_submitted` event, editable while resolved). `showPayload` gains `ticket.csat` + `can.rate`; `myTicketRows` gains `csat_score`/`can_rate`. Pest. *(2026-07-08: `ItTicketPolicy::csat` = `owns() && status==='resolved'` — carries requester-only, not-before-resolved AND editable-until-closed in one predicate (a close flips status → policy denies → 403, no separate guard). Route `it.tickets.csat` sits by `reopen` in the `it.request|it.view` group; `SubmitCsatRequest` authorizes via the policy (so agent/stranger/open/closed all 403 before validation) + validates `score` required int between:1,5, `comment` nullable max:1000. `ItTicketController::csat` maps `score`→`csat_score`, `comment`→`csat_comment`, stamps `csat_submitted_at` + records the `csat_submitted` event only on the FIRST rating (a re-rate updates the score silently — one-shot event, editable score). `showPayload` ticket gains `csat` (score/comment/submitted_at, null until rated) + `can.rate` (`$isRequester && resolved`); `myTicketRows` gains `can_rate`/`csat_score`. 5 Pest tests (rate + single event; re-rate updates score but not stamp/event; agent+stranger+open+closed all 403; score 1–5 validation; workspace `can.rate`+`ticket.csat` and My-tickets `can_rate`/`csat_score` prompt→result). Full IT suite 85/85 (953 assertions); pint clean on the 2 new files; no frontend touched. My-tickets card + workspace rail + confetti → 15b.)*
  - [x] 15b. Frontend: My-tickets CSAT prompt card (1–5 stars + optional comment → `it.tickets.csat`, confetti on a 5) + workspace rail CSAT display (score after submission, prompt while rateable). *(2026-07-08: shared `components/it/csat.tsx` — `CsatStars` (read-only gold-star row, `--status-warning` via inline var, `role=img`+aria-label) + `CsatRater` (keyboard-operable `radiogroup` of 5 star buttons → pick + Submit = two clicks per §K; optional comment `Textarea` max 1000; POSTs `it.tickets.csat` with `preserveScroll`, toast + `fireConfetti()` on a perfect five; a re-rate pre-fills the existing score/comment). My-tickets tab (`index.tsx`): a "How did IT do?" prompt card above the table lists resolved-and-unrated tickets, each with an inline `CsatRater` (the card empties as they rate); rated rows show a "You rated ★★★★☆" `CsatStars` chip under the status dots. Workspace rail (`show.tsx`): requesters (`can.rate`) get the rater pre-filled from `ticket.csat`; everyone else with a submitted score sees the read-only stars + comment ("Satisfaction"). Added `can_rate`/`csat_score` to `MyTicketRow`, `csat` + `can.rate` to the `TicketPayload`. Pure frontend — 15a already serves the payloads; IT suite unchanged 85/85; `npm run types`/eslint(3 files)/`npm run build` clean. §K CSAT COMPLETE.)*
- [x] 16. §L Reports tab (server aggregates + recharts + export). *(COMPLETE 2026-07-08 — 16a `it.reports.data` aggregates endpoint + 16b Reports tab UI (recharts area/donuts + range picker + empty states) + 16c per-card CSV export. Agents get a full server-computed analytics tab. Next: item 17 (§O right-click everywhere + default-tab persistence).)*
  - [x] 16a. Backend: `ItReportsController@data` at `GET /it/reports/data` (`it.reports.data`, agent `it.view`, `from`/`to` range params clamped ≤365d) computing all §L aggregates — created-vs-resolved daily trend, open by priority/category, SLA compliance %, avg first-response/resolution mins, CSAT avg + response rate, top requesters, agent workload, provisioning throughput (raised/fulfilled/avg-days) — tenant-scoped + Schema-guarded. Pest asserts the numbers vs seeded fixtures + requester 403 + empty-tenant shape. *(2026-07-08: new `ItReportsController` (single-purpose, `ResolvesHrTenant`) — `range()` parses/clamps `from`/`to` (default last 30d, span capped at 365d so a hostile `?from=1900` can't table-scan); `report()` splits into readable per-metric queries reusing the module idiom (`forTenant`, `selectRaw` SUM/AVG/`TIMESTAMPDIFF`, `OPEN_STATUSES`). KPIs = point-in-time state (open/unassigned/breaching/breached) + range flow (avg first-response/resolution mins, `sla_compliance` = met/(met+breached) of resolved-in-range, `csat_avg` + `csat_response_rate` over resolved-in-range); `trend` = zero-filled daily created/resolved (UTC buckets, noted as v2 to NZ-day); `openBy` = canonical-order donuts over open tickets (trusted enum column, not user input); `topRequesters`/`agentWorkload` capped grouped lists; `provisioning` raised/fulfilled/avg-days via the bridge's `fulfilled_at`. Route in the outer group gated `permission:it.view` (like the CSV export — read-only agents in, requesters 403). Every table `Schema::hasTable`-guarded → empty tenant returns a zeroed report, never 500. 2 Pest tests (requester 403 + young-tenant zeroed shape incl. 30-day zero-filled trend; full fixture aggregation — every KPI/distribution/trend-sum/people/provisioning number asserted). Full IT suite 87/87 (985 assertions); pint clean on the 2 new files; no frontend touched. Reports tab UI → 16b.)*
  - [x] 16b. Frontend: Reports tab (agents) — fetch the endpoint (axios), KPI tiles + recharts (created/resolved area, priority/category donuts, SLA/CSAT) + date-range picker + taught empty states + skeletons. *(2026-07-08: new `components/it/it-reports.tsx` `<ItReports>` — fetches `it.reports.data` via axios (7/30/90-day range picker drives `from`/`to`), renders an 8-tile KPI row (open/unassigned, SLA at-risk+breached with tone, avg first-response/resolution, SLA compliance % with success/warning/critical tone, CSAT avg + response rate, provisioning raised/fulfilled+avg-days), a created-vs-resolved recharts `AreaChart` (primary/success tokens + custom legend), open-by-priority + open-by-category recharts donuts (`PieChart` — semantic priority tokens + `--chart-*` category palette, per-slice legend with %), and hand-rolled Top-requesters + Agent-workload bar lists; skeletons while loading + a taught empty state for a young tenant. All colours are design tokens (no hex — `var(--status-*)`/`var(--chart-*)`/`var(--primary)`, `text-status-*` @theme utilities). Wired into `index.tsx`: a `BarChart3` Reports tab (agents) + `{can.view && tab==='reports' && <ItReports/>}` panel. Pure frontend — 16a serves the data; IT suite unchanged 87/87; `npm run types`/eslint(2 files)/`npm run build` clean. Per-card CSV export → 16c.)*
  - [x] 16c. CSV export per card (filter-aware, injection-guarded) + per-card download buttons. *(2026-07-08: `ItReportsController::export` at `GET /it/reports/export` (`it.reports.export`, agent `it.view`) — 6 cards (summary/trend/by_priority/by_category/top_requesters/agent_workload) each mapped to `[filename, headers, rows]` via `exportCard()`, reusing the 16a private aggregate methods; streamed via `response()->streamDownload` with every cell through the base Controller's `putCsv()` (the `SanitizesCsvOutput` formula-injection guard), Schema-guarded, unknown card → trend fallback. Frontend: `it-reports.tsx` `Card` gains an `exportHref` (a "CSV" download link in each card header) + a top-level "Export" summary button, both carrying the active date window (`exportUrl(card)`). 1 Pest test (requester 403; top-requesters stream = text/csv with a `=cmd|calc` requester-name cell apostrophe-neutralised; by_priority `High,1`; summary `Open,1`; unknown-card 200) — the header assertion was corrected once for CSV-quoting of space-containing cells. Full IT suite 90/90; `npm run types`/eslint/`npm run build` clean. §L Reports COMPLETE.)*
- [x] 17. §O right-click everywhere + default-tab persistence. *(COMPLETE 2026-07-08 — the tickets/provisioning/KB row menus already landed with items 9/12/14; 17a added the requester My-tickets row menu + copy-ref/link round-out; 17b added the tab-strip "Set as default view" action + `it.defaultTab` persistence with a star decoration. Next: item 18 (§S delight + axe + final DoD sweep).)*
  - [x] 17a. My-tickets row right-click (Open · Reply · Reopen ≤7d · Copy reference) + add Copy reference/Copy link to the agent ticket menu and Copy link to the provisioning menu, per §O. *(2026-07-08: shared `copyText(text, what)` helper (clipboard + toast); new `myTicketMenu(MyTicketRow)` — Open · Reply (both → the workspace where the composer lives) · Reopen (only when `resolved`; `it.tickets.reopen` enforces the ≤7d window server-side, toasting the flash error if past it) · Copy reference — wired via `onContextMenu` on the My-tickets rows, the one requester-facing menu that was still missing. Rounded out the agent menus per §O: `ticketMenu` gains a divider + Copy reference + Copy link (`/it/tickets/{id}`); `requestMenu` gains a Copy link scoped to the linked ticket (provisioning rows have no own URL, so no dead link). Icons `Copy`/`Link2`/`MessageSquare` added. Pure frontend — IT suite unchanged 90/90; `npm run types`/eslint/`npm run build` clean. Tab-strip default-view persistence → 17b.)*
  - [x] 17b. Tab-strip "Set as default view" → persist `it.defaultTab` (localStorage) via `HrTabs` `onItemContextMenu` + a star `decorations`; `useHrTab` initial tab reads the stored default. *(2026-07-08: on mount a validated `it.defaultTab` (guarded against the user's capabilities — `my-tickets` needs `it.request`, the agent tabs need `it.view` — so a stale/foreign id is ignored) seeds `useHrTab`'s default; a `?tab=` deep link still wins because `useHrTab` reads the query param first. `tabMenu(id, e)` wired to `HrTabs onItemContextMenu` opens a `ctx.open` menu — "Set as default view" writes localStorage + `setDefaultTab` + toast, "Open" switches — and `tabDecorations` stars the pinned tab (a gold `--status-warning` `Star` via `HrTabs decorations`). Pure frontend — IT suite unchanged 90/90; `npm run types`/eslint/`npm run build` clean. §O COMPLETE.)*
- [ ] 18. §S delight + axe pass + screenshot diff of every tab/modal/workspace vs gold standards; final DoD sweep. *(split 2026-07-08: most §S delight already landed across the loop — WizardStepPane/success panes, 5-star CSAT confetti (15b), skeletons (reports/drawer), optimistic toasts, Ctrl+Enter send, live SLA countdowns (sla-chip), plain-language requester copy, taught empty states. Remaining slices: 18a Reports "X of Y within SLA" microcopy; 18b confetti on clearing the breach queue; 18c static a11y/WCAG review (axe Playwright can't run against the junction worktree — Herd serves the parent — so record the check + any fixes); 18d final DoD sweep (full `php artisan test` + build/types/lint + walk every DoD bullet + confirm gap doc fully ticked).)*
  - [x] 18a. Reports "X of Y within SLA this month" microcopy (expose `sla_met`/`sla_measured` in the reports KPIs + Pest; render the microcopy under the SLA-compliance tile). *(2026-07-08: `ItReportsController::kpis` now returns the raw `sla_met`/`sla_measured` counts alongside `sla_compliance` (+ zeros in `emptyKpis`); the `it-reports.tsx` SLA-compliance tile sub-line reads "X of Y within SLA" when there's a measured cohort, else falls back to "N resolved". +2 Pest assertions (sla_met=1, sla_measured=2 of the 1-met+1-breached fixture). Full IT suite 90/90; `npm run types`/eslint/`npm run build` clean. Breach-clear confetti → 18b.)*
  - [x] 18b. Confetti on clearing the breach queue (a breach-count >0→0 transition fires `fireConfetti` + a "Breach queue cleared" toast, guarded so it celebrates once). *(2026-07-08: a `useEffect` gated on `can.view` compares `summary.tickets.breached` against a `sessionStorage it.lastBreached` snapshot; a `>0 → 0` transition (the breach queue emptying across a reload) fires `fireConfetti()` + a "Breach queue cleared — every SLA back on track." toast, then re-stores the count so it celebrates exactly once (tab switches don't reload the summary → no spurious fires; requesters are excluded). Pure frontend — IT suite unchanged 90/90; `npm run types`/eslint/`npm run build` clean. Static a11y review → 18c.)*
  - [ ] 18c. Static a11y/WCAG review of the new IT surfaces (colour-with-text, focus-visible, `motion-reduce`, aria on icon-only controls) + any quick fixes; record the axe-can't-run limitation in `IT_TICKETING_QUESTIONS.md`.
  - [ ] 18d. Final DoD sweep — full `php artisan test` + `npm run build`/`types`/`lint` green; walk every Definition-of-Done bullet; confirm the gap doc is fully ticked and the questions file holds the deferred stretch decisions.

## Decisions / questions

- **§P.4 attachments:** no clean generic infra → build `it_attachments` with `attachable` morphs, serve via `ServesPrivateAttachments` (decision recorded in finding 11; within pre-approved schema).
- Deferred decisions live in `IT_TICKETING_QUESTIONS.md` (repo root) — created when the first one arises.
