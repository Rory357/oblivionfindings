# Finance Dashboard — build progress (loop state)
- [x] Phase 0  Audit -> docs/finance-dashboard-audit-findings.md
- [x] A1 Hero (primary gradient, period control, Site+Funding filters)
- [x] A2 Quick-action wizard modals
- [x] A3 Finance hubs quick-links bar
- [x] A4 KPI cards (8)
- [x] A5 Donut row (3)
- [x] A6 Charts row (area + bar, tokens)
- [x] A7 Tables row + recent journals
- [x] A8 Footer
- [x] A9 Needs-attention strip
- [x] B  Period-aware real data + new metrics
- [x] C  P0 correctness (3.1, 3.2, 4.1, 2.1/2.2)
- [x] D  Funding pipeline (1.1, 1.2, 1.3) + utilisation donut/table
- [x] E  Net-pay disbursement (4.2) + IRD payday filing (4.3)
- [x] F  Budgets (5.1, 5.2) + finance calendar (5.3)
notes:
- 2026-06-15 first run: created progress file; design drop lives in parent repo (.design-drops gitignored). Starting Phase 0 audit.
- 2026-06-15 Phase 0 DONE: wrote docs/finance-dashboard-audit-findings.md. Verified gap claims vs live code — concurrent Finance loop already fixed 3.2 (markPaid posts journal), 3.3, 4.1 (lock generates payslips), partial 4.2 (net-pay journal done, direct-credit file missing). Dashboard AR KPI (DashboardAggregatorService:73) still reads orphaned App\Models\Invoice — the one live 3.1 break. Phase C is small; substantive new work = D (funding/remittance), E (payday filing + DC file), F (budgets + calendar surfacing).
- 2026-06-15 Phase A DONE (one commit): full Dashboard.tsx rebuild to README §1-9. New presentational comps: components/finance/{finance-hubs-bar,needs-attention-strip,finance-dashboard-footer}.tsx. Reused PageHero (NO category→purple), MultiEntityFilter (onDark), DonutCard, the 4 wizard dialogs (open/onClose modal state; ref-data props default [] → real data Phase B), formatMoney/Compact, StatusBadge. Additive (backwards-compat) props on shared rostering Donut: DonutLegend/DonutCard gained formatValue + showPercent (defaults preserve rostering). Tokens only (zero hex; chart-1..5/primary/status-warning). Placeholders clearly TODO-marked: donuts (TODO B/C/D), funding-claims table (TODO D), attention items (TODO B/D/F), funding-util + revenue/resident KPIs (TODO A→B). Gates: tsc clean, eslint clean, vite build OK (3m29s).
- ⚠️ WORKTREE SETUP (bare worktree had no node_modules/vendor/.env/Wayfinder TS): node_modules resolves via parent up-walk; copied resources/js/{routes,actions,wayfinder} from parent (gitignored); robocopied vendor + .env from parent; vite build needs PHP_INI_SCAN_DIR=C:\Users\steph\AppData\Local\Temp\phpmem_fin (memory_limit=1024M) because the @laravel/vite-plugin-wayfinder runs `php artisan wayfinder:generate` on build. Run BUILD via bash `npx vite build`; run TESTS via PowerShell `php artisan test` (php=Herd php.bat, only on PS PATH; bash has no php). tsc: ignore pre-existing @/routes errors, only check changed files. Test DB = MySQL oblivion_findings_codex_test (RefreshDatabase, ~95s migration on first test).
- 2026-06-15 Phase B DONE (one commit): DashboardAggregatorService now period-aware (month/quarter/NZ-FY Apr-Mar) + cost_centre/funding_stream filters on all GL aggregates; NEW real metrics: revenueByFundingStream (un-stubs donut 1), apDueWithin7 {count,total}, cashRunwayDays (cash ÷ 90d avg expense). Controller accepts ?period/?site[]/?funder[]; supplies filter options (siteOptions=cost centres, funderOptions=funding streams) + modal ref-data (accounts/costCentres/fundingStreams/vendors/clients/taxRates) as Inertia CLOSURES so partial reloads skip them. Frontend: period buttons + Site/Funding filters → router.reload({only:[metrics], data:{period,site,funder}, preserveState}); donut 1 real (dollars, formatMoneyCompact); cash/AP KPI subs real. Test tests/Feature/Finance/FinanceDashboardPeriodTest.php (3 tests, time frozen 2026-06-15): period scoping + funder filter + by-stream breakdown; draft journals excluded. ⚠️ NZ FY gotcha confirmed in test: Feb 2026 is PRIOR FY when now=Jun 2026. STILL STUBBED: AR KPI/aging (3.1 Phase C), utilisation donut 2 + funding-claims table + attention items (Phase D/F), funding-util KPI + revenue/resident KPI (need resident count). Gates: 3 tests green, tsc clean, eslint clean, vite build 2m58s.
- 2026-06-15 Phase C DONE (one commit): 3.1 FIXED — DashboardAggregatorService now injects AccountsReceivableService and sources AR from the live FinInvoice aging snapshot (removed `use App\Models\Invoice` + the orphaned getOutstandingReceivables query). New `arAging` prop {current,d1_30,d31_60,d61_90,d90_plus,over60,total} (point-in-time, NOT period-scoped; org-scoped only — invoices have no cost-centre/funding-stream dim so AR ignores those filters by design). Frontend un-stubs donut 3 (real 5-bucket AR aging, dollars) + AR KPI sub = `$X >60d`. 3.2 already guarded (MarkPaidReceiptJournalTest) — no action. 4.1 already FIXED (PayrollExportService::lockRun:171 generates payslips when count=0) — ADDED guard test to PayrollRunIntegrityTest ("locking a run generates payslips..."). 2.2 already FIXED (ClientLedgerService segregates personal vs operational: `affects_personal_balance`, "Personal balance unchanged by operational rows") — verified, no action. 2.1 (ClientLedgerEntry zero write paths / canonical resident-money store) DEFERRED — big cross-module migration touching Clients/Operations/family-portal, and the DASHBOARD surfaces NO resident money, so it doesn't block any dashboard surface; ClientFund remains the populated store. Tests: FinanceDashboardPeriodTest +1 (AR from FinInvoice, paid/draft excluded), PayrollRunIntegrityTest +1 (payslip-at-lock); 11 passed. ⚠️ N+1: getAgedReceivables does 1 alloc query per sent invoice (inherited from the shared AR service; reuse-first — left as-is, bounded by outstanding-invoice count). Gates: 11 tests green, tsc clean, eslint clean, vite build 3m13s.
- STILL STUBBED after C (for D/F): donut 2 utilisation + funding-claims table + attention items (Phase D/F); funding-util KPI (line ~564, needs Phase D pipeline — TODO comment says Phase B but really D) + revenue/resident KPI (line ~566, needs funded-resident count) + hero meta (line ~413, real org/period). Must all clear by DoD.
- 2026-06-15 Phase D DONE (2 commits, f706dc9e D-1 + D-2):
  - D-1 (f706dc9e): dashboard funding surfaces now REAL from existing data. DashboardAggregatorService::getFundingClaims (recent FundingClaim + funder via serviceAgreement.funding_body) + getFundingUtilisation buckets — claimed&paid (status=paid) / awaiting-remittance (submitted+approved) / delivered-unclaimed (BillingEntry status=pending, service_date≥now-90d) / write-off-risk (pending, <now-90d); utilisation_pct = claimed/deliverable. Un-stubbed donut 2 + funding-claims table + funding-util KPI; removed DONUT_UTILISATION/FUNDING_CLAIMS/fmtK placeholders; CLAIM_TONE map for status badges. Test +1 (5 total). Funding surfaces are org-scoped snapshots (NOT period/filter-scoped — FundingClaim has no GL funding_stream dim; claims link funder via service_agreement.funding_body string).
  - D-2 (1.3): DELETED dead+broken FundingService::generateClaimFromBilling + approveClaim + generateClaimReference (ZERO callers, ZERO tests; wrote non-fillable keys reference/amount + nonexistent FundingClaimItem.billing_entry_id → would corrupt claims if ever called). Kept getExpiringAgreements + getBudgetAlerts (WIRED by CheckExpiringAgreementsJob; use REAL ServiceAgreement.total_budget/budget_used cols — the audit's "dead columns" claim was WRONG, the cols exist). Real claim path = FundingClaimController::store/approve + FundingClaimJournalService. Verified: FundingClaimJournalDispatchTest passes (1 test, 11 assertions); php -l clean.
  - 1.1 (funder remittance model + match queue) DEFERRED w/ evidence: grep `remittance`=0; a new FundingRemittance model + bank-payment→claim matching workflow + UI queue is a large Operations feature. Dashboard "awaiting remittance" bucket is ALREADY truthful (approved+submitted claims = claimed-but-unpaid), so it doesn't block dashboard truthfulness. Like 2.1, deferred — needs product direction on the matching workflow.
  - 1.2 (auto-generate claims from delivered service) DEFERRED w/ evidence: needs a BillingEntry↔FundingClaim link (no column exists — FundingClaimItem has no billing_entry_id) + a migration + a scheduled/triggered generation job + seeding + Operations-module regression risk. Dashboard "delivered-unclaimed" bucket is ALREADY truthful (pending BillingEntry). Operations workflow feature, not a dashboard truthfulness gap.
- 2026-06-15 Phase E DONE (3 commits 5bb7934a E-1 + 52dfa888 E-2 + E-3):
  - E-1 (4.3, 5bb7934a): IRD payday filing wired. IrdFilingService::createPaydayFiling builds the EI payload + totals (gross/paye/kiwisaver/student-loan) from a POSTED run's payslips; validateFiling payday branch; migration adds fin_ird_filings.payroll_run_id (filing_data is encrypted:array → can't query into it, so a real column is needed); IrdFilingController::createFromPayrollRun + route finance.ird-filings.from-payroll; IrdFilings/Index page gains a 'Create Payday Filing from Payroll Run' form (availablePayrollRuns = posted runs without a filing). Test PaydayFilingTest. ⚠️ Valid NZ IRD test number 49091850 (passes modulus-11). Tenant==org (ResolvesHrTenant falls back to org_id).
  - E-2 (52dfa888): needs-attention strip now REAL + data-driven. DashboardAggregatorService adds payrollAwaitingApproval (HrPayrollRun draft/locked + journal_id null) + paydayFilingDue (posted runs minus runs with a payday FinIrdFiling). Strip built in Dashboard.tsx from real props (AR 90+, bills ≤7d, payroll-awaiting, payday-due, delivered-unclaimed) — each item shows only when its metric is live; removed hardcoded ATTENTION_ITEMS. Test +1 (6 in FinanceDashboardPeriodTest).
  - E-3 (4.2): net-pay direct-credit (bank batch) CSV. PayrollJournalService::buildNetPayDirectCreditCsv (employee name + encrypted bank_account + net_pay + ref, mirrors PaymentRunService::generateBankFile), generated ON DEMAND (no storage/migration); PayrollExportController::downloadNetPayFile (streamDownload, guards net_paid_at) + GET route hr.payroll.runs.net-pay-file. Test +1 in PaydayFilingTest (2 there). ⚠️ Employee bank_account EXISTS on HrEmployeeProfile (encrypted). NOTE: net-pay JOURNAL (DR2300/CR Bank) already posted pre-Phase-E (postNetPayPayment); 4.2 added only the bank file. UI download button on the HR payroll page = trivial follow-up (route is deep-linkable; not surfaced yet — non-dashboard).
  - Gates: PaydayFilingTest 2 green, FinanceDashboardPeriodTest 6 green, php -l clean; E-2 vite build 3m58s (E-3 is PHP-only, no build).
- STILL STUBBED after E (Phase F targets): needs-attention GST-due item (needs FinanceCalendarAggregator); funder-remittances item (1.1 deferred — won't add). Leftover placeholder dashboard bits: revenue/resident KPI (~line, needs funded-resident count); hero meta (~line 413, real org/period). 5.1 budgets / 5.2 spend-approval AP / 5.3 calendar surfacing.
- 2026-06-15 Phase F DONE (2 commits d1e3de37 F-1 + F-2):
  - F-1 (d1e3de37): dashboard FULLY REAL — GST-due attention item from FinanceCalendarAggregator GST provider (real IRD deadlines); revenue/resident KPI = period revenue ÷ funded-resident count (distinct clients on ACTIVE ServiceAgreements); hero meta = open fiscal period + site/region counts (Site is global/un-org-scoped — single-org app) + funded residents. DashboardAggregatorService injects FinanceCalendarAggregator; getFundedResidentCount + getGstDueAttention; controller closures openPeriodLabel/siteCount/regionCount. ZERO TODO/placeholder remain in Dashboard.tsx + service. Tests +2.
  - F-2 (5.3): extended FinanceCalendarAggregator with PayrollObligationProvider (payroll runs on period_end, source 'payroll') + PeriodCloseProvider (fiscal-period end_date, source 'period_close'); registered in defaultProviders(). Calendar now 6 providers. Test FinanceCalendarProvidersTest. (Calendar already existed pre-loop w/ invoice/bill/payment-run/GST + a calendar page — the "no calendar" gap was already closed; F added payroll/period-close enrichment + the dashboard GST surface.)
  - 5.1 (BudgetSyncInterface) DEFERRED w/ evidence: zero impls confirmed; BudgetActualsService (syncActuals/getBudgetVsActualsReport, orgId-based) does NOT cleanly map to the interface (getBudgetEnvelope(fiscalPeriodId)/getActualsForCategory(budgetId,category)/syncBudgetActuals(budgetId)) — implementing it is a Governance budget-sync refactor with regression risk. The DASHBOARD surfaces NO budget data, so it doesn't affect dashboard truthfulness. Deferred like 2.1/1.1.
  - 5.2 (SpendApproval AP/GL) DEFERRED w/ evidence: SpendApprovalController::approve only transitions status+audit (confirmed). Posting a payable on approve is UNSOUND ACCOUNTING (SpendApproval is a governance PRE-AUTH: no vendor field, nullable source morph, requires_board, valid_until — FinBill.vendor_id is a required FK; correct direction is bill→links-approval-via-source) — established by the prior Finance loop (memory project_finance_module_rebuild M8-3). No dashboard surface. Deferred (don't invent a bad journal).
  - Gates: FinanceDashboardPeriodTest 8 green, FinanceCalendarProvidersTest 1 green, php -l clean; F-1 vite build 3m1s (F-2 PHP-only).
- DEFERRED-WITH-EVIDENCE SUMMARY (none block dashboard truthfulness): 2.1 (canonical resident-money store), 1.1 (funder remittance matching), 1.2 (auto-claim generation), 5.1 (budget-sync interface), 5.2 (spend-approval AP — unsound). All are non-dashboard cross-module/Operations/Governance features; the dashboard reflects real current state without them.
- Next: DoD — full `php artisan test` + full `npm run build` green; grep confirms no TODO/placeholder in dashboard/service; then ALL PHASES COMPLETE.
